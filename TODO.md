# FUSION TODO

Validation rules and hard constraints live in [CLAUDE.md](CLAUDE.md); do not restate them here.

## Phase 1: rebrand fork + CI

- [x] **CLI brand sweep DONE 2026-08-14** (fusion-core `ffe3acaa7`): scriptName, eleven command descriptions and three error messages that told the user to run `opencode models` / `opencode auth login`. The piped ASCII wordmark is now derived from the same glyphs the TTY path draws instead of upstream's second hardcoded copy, which is why piped output kept the old name long after the logo was rebranded. Left untouched as functional identifiers: `OPENCODE_SERVER_USERNAME`, the `opencode.local` mDNS domain, the GitHub integration's bot name / workflow filename / `/opencode` trigger, and the config paths `~/.config/opencode` and `~/.local/share/opencode` (worth leaving for compatibility)
- [ ] **`fusion upgrade` would replace a FUSION install with upstream opencode** [user decision]. It fetches `api.github.com/repos/anomalyco/opencode/releases/latest`, so a student who runs it silently loses FUSION. Relabelling it was refused because that would make the help text lie, and redirecting it at the FUSION releases is a FUNCTIONAL change, which the hard rule says to stop and discuss. Its description now says plainly that it upgrades the underlying opencode and not FUSION. Options: leave it honest-but-useless, point it at the FUSION releases (functional patch, grows the rebase surface), or hide the command
- [ ] Remaining brand surfaces: desktop/web icons need an actual FUSION graphic (nature-figure skill or designer)
- [ ] TUI logo v2: current block-glyph version verified rendering as "FUSion" (user: 效果一般, acceptable for now); revisit in the visual design pass together with the icons
- [ ] Build + release pipeline for FUSION binaries (adapt upstream release workflow; bun installed locally)
- [x] 2026-08-11: **Domain is `vibeinscience.com`** (user), live and serving the illustrated project site (architecture, benchmarks, case studies, corpus citation map). Linked from README. **Site staleness RESOLVED.** The three complaints recorded here (17 skills, roadmap stopping at Phase 3, an advertised peer-reviewed paper) were all fixed in the site repo on 2026-08-11 and 08-12; a 2026-08-13 audit found the page already carrying 26 skills, `kb-search`/`literature-wiki`/`research-profile` by name, Phase 4 marked in progress, and no publication claim. Only one number had gone stale, the in-corpus citation-edge counter, corrected 727,841 to 703,430 after the graph rebuild (fusion-web 9957dea). **Note for future audits: the site lives in the `fusion-web` repo** (`~/Desktop/code/fusion-web`, plain static `index.html`, GitHub Pages), NOT in `DAGA`, which also carries a `CNAME` for the same domain and is a separate parody site; grepping for the domain finds DAGA first and misleads

## Phase 2: skill pack

Scope (user directive 2026-07-09): **every excellent open-source nuclear-physics code gets its own skill**, across reactions, statistical/fission, R-matrix/astro, structure, and (scoped) transport/data. Full living roadmap with openness-verification flags and wave ordering: [skills-catalog.md](skills-catalog.md).

Strategy (user 2026-07-20): community codes first, self-consistent benchmarks (reproduce each code's own documented reference values, not cross-code matching), one at a time (build+benchmark cannot be parallelized). Private-code boundary: only published + publicly-open codes (excludes THOx, smoothie, transfer, STARS). Full roadmap + status: [skills-catalog.md](skills-catalog.md).

**PRIORITY, user ruling 2026-08-11: the critical path is Phase 4, OPEN SOURCE FIRST.** Release the platform, collect real user feedback, then decide what to build next. Twenty-one skills is already broad coverage (the heavy-ion row alone holds four), so **skill-building stays paused as ordinary long-term maintenance**, resumed a few at a time when a specific code is actually wanted, never as a race to cover the field. **A skill's pass criterion is reproducing the PUBLISHED work** (a figure, a table, a number from the code's own paper), not the from-source-build certification ceremony; skills whose benchmark reproduces only the code's OWN test suite (Thermal-FIST ctest, SkyNet self-comparison) are weaker and should gain a published anchor when the code's paper permits. See CLAUDE.md Key decisions 2026-07-24 and 2026-08-11.

- [ ] Port the existing ~30 research skills (writing, review, literature, figures) to FUSION format; strip Claude-Code-only mechanics per skill. **Started 2026-07-23**: `skills/exfor-data/` is the first one embedded (see the Key decision of that date for why research skills carry no build or benchmark step). **2026-08-12: `skills/kb-search/` added as the second**, the query surface over the shipped kb-wiki (papers/topics grep recipes, citations.tsv and relations.tsv awk filters, filename convention, trust boundaries). It is a NEW skill, not a port of `literature-corpus`: that skill is bound to the full-text `corpus.db`, which the hard rule forbids shipping, so only the kb-wiki layers have a public query surface. Every recipe in it was run against the shipped base before it was written down. **Same day, `skills/literature-wiki/` and `skills/research-profile/` embedded** (user ruling): byte-identical `cp -RL` copies from the already-public github.com/jinleiphys/research_LLM_wiki repo at c336c55 (the sanitized v2.1 cross-harness versions with hand-written AGENTS.md, which `build_agents_md.py` now detects and leaves alone). That repo stays the source of truth; re-sync with `cp -RL` and `diff -r` must be empty, same discipline as the fresco global/FUSION pair. These ship the METHOD (schema, vocabulary, templates, lint) and define the mount point only; the user's actual wikis stay private per the hard rule. ~26 remain
- [x] 2026-08-13: TALYS follow-up CLOSED. It never dominates an `install.sh` bundle, because provisioning is lazy and the bundle pre-builds no code at all (CLAUDE.md key decision); measured cost when a user does ask for it is 5 min 19 s and 11.2 GB. See the Phase 4 entry below for the full result and for the structure-database completeness guard it produced
- [x] **RESOLVED 2026-07-23: all four subfield areas have at least one completed skill** (reactions / structure / fission-statistical / astro-R-matrix). Reactions (FRESCO, COLOSS, CCFULL, pikoe, NLAT, CNOK, SIDES, SWANLOP) and structure (GSM, KSHELL, NuclearToolkit.jl) were satisfied on 2026-07-21; **fission/statistical** closed 2026-07-22 with CGMF at tier 1 as a dedicated code, so TALYS does not have to double-count; **astro/R-matrix** closed with AZURE2 (2026-07-22, tier 2) and deepened by SkyNet (2026-07-23, tier 1 with a documented macOS caveat). Build order is therefore free to follow research-line relevance; no external date forces the ordering
- [x] 2026-07-22: **GEF DROPPED, not a FUSION skill.** Openness was cleared on 2026-07-21 by the literal rule (GPL-3.0, anonymous download, FreeBASIC source ships, paper verified), but the user tightened the rule on 2026-07-22 (see CLAUDE.md Key decision): "truly open" requires the source to BUILD on the distribution's target platform, and FreeBASIC has no native Apple-Silicon toolchain (no `fbc`, no Homebrew formula, x86-only upstream runnable on ARM only under Rosetta), so a Mac student can only run the vendor x86/Linux binary. Running GEF on heliumx for the user's own work is still fine; it just cannot ship. The fission/statistical area does NOT re-open: **TALYS already carries fission at tier 1** (`n-Th232-fis-wkb`, 128/128 bit-identical, plus `n-Pu239-fy-gef` fission yields). RESOLVED 2026-07-22 (user: find an open-source fission code): **CGMF** fills the fission/statistical row as a distinct dedicated code, so TALYS need not double-count. CGMF is BSD-3, C++/CMake, builds clean on Apple Silicon (the tightened rule GEF failed), CPC 269, 108087 (2021) CrossRef-verified, 252Cf(sf) runs end to end. **Skill SHIPPED 2026-07-22, `skills/cgmf/`, TIER 1** (fission/statistical row now filled by a distinct code). CGMF is deterministic (event i seeded from i) and the repo ships byte-exact .reference history files, so the benchmark reproduces the distributed output EXACTLY, the first fission code in the series to reach tier 1: 252Cf(sf) and n_th+235U 40-event histories bit-exact vs the shipped reference, on macOS/ARM (stable across Release and Debug builds; the reference's origin platform is undocumented, so this is a reproduction of the LANL-shipped file, not a proven cross-platform result, corrected after the Codex pass). Physics: nu-bar_tot(252Cf sf) = 3.82 per the manual at 1e6 events, this build IS that code (bit-exact) so it converges there; low-N deterministic scatter 3.80/3.78/3.72 at 40/500/3000. 11 files: SKILL.md, 4 references (input/output/failure/verification from the source and manual), install/run/verify/selftest/check scripts. **TWO Codex passes DONE, 10 defects total, all fixed:** pass 1 found 6 (nonzero-exit and truncated-history holes in run_cgmf.sh, broken yields-mode detection, install fast-path skipping its probe, a cross-platform overclaim, 3 doc errors); the re-verification pass confirmed those and found 4 more (unvalidated numeric args reaching atoi/arithmetic, yields count using -lt not equality, three guards untested by selftest, stale docs). selftest grown 7->19, and every guard confirmed to flip when disabled; that check itself caught a masked-test bug (the wrong_einc stub was too short and was rejected by the fragment-count guard, not the Einc guard). CGMF is the ninth per-code skill and the first MC code at tier 1. The verified GEF facts (nu-bar 3.8207 for 252Cf(SF); the ctl/done.ctl and Fitpar.dat state-contamination traps; the pkill self-kill) are preserved in devlog-archive.md
- [x] **AZURE2 skill SHIPPED 2026-07-22 (eighth per-code skill), tier 2.** `skills/azure2/` has SKILL.md, `references/azr-format.md` (derived from the parser source, since the repo ships no format docs), `references/failure-modes.md`, `scripts/{install,run,verify}_azure2.sh`, and the `16O_pg_17F` benchmark constructed from PRC 81, 045805 Table V. **Verified**: all 9 published parameters exact (L1); S(90 keV) = 7.6080 keV b against the published 8.07, i.e. **-5.7%** (L2b); and, the strongest check because it does not depend on the paper's own number, **chi^2/N = 1.53 against measured Rolfs (1973) data with nothing fitted** (L2c). The -5.7% was bounded not waved away: channel radius (0.4%), resonance parameters (0.02 keV b), li=3 pathway (0.07%), nuclear masses, lab-vs-CM, GSL Coulomb and the evaluated Ex were each tested and none closes it; the dominant sensitivity is the proton separation energy, unstated in the paper, worth 13% per 3 keV. Two Codex adversarial passes run: the first could not falsify the deck but found 6 defects (2 false-success paths in calibrate_widths.py, an rm -rf symlink escape, 3 documentation overstatements), all fixed and re-tested. NB the `codex:codex-rescue` plugin path was silently dead (broker never started, 0-byte log, no pid) and reported "still running" for an hour; drive `codex exec` directly until that is fixed
- [x] 2026-07-22: **AZURE2 second case DONE, the 6.79 MeV transition of 14N(p,g)15O** (`examples/14N_pg_15O_679/`). S_6.79(0) = 1.257 keV b vs the published 1.30 (-3.2%), inside the 0.1 keV b data-selection sensitivity the paper's own Table IV caption states for this number. Deliberately partial: the full 7-transition case is not reconstructable (unpublished amplitude signs against a destructive-interference S(0); see the 2026-07-22 Key decision), 6.79 is the tractable exception because its resonance term is "added incoherently". Four physics checks pass (dummy inert 15/20/30 MeV, radius flat to 1.6% over 5.0-6.0 fm, ANC scaling within 2% of C^2, resonance worth 0.6% which bounds the unstated M1-vs-E2 choice). Three Codex passes total on this skill: pass 3 confirmed no physical defect in the deck (dummy set complete, each removal costs 1/3 strength) and found 3 harness defects, all fixed. Notable: verify_14N checked only an output number while the resonance widths (0.6% of S) could be edited freely, so a check_14N_inputs.py now asserts the fields directly; and the selftest's "verify must fail on a corrupted deck" section was itself testing the clean tree, the THIRD wrong-assertion bug in this skill, now catches a stubbed verifier (25 cases)
- [ ] **Apply the cross-build check to the already-shipped skills.** `skills/pikoe/scripts/xbuild_check.sh` + `xbuild_compare.py` are generic apart from the deck list. Run them for FRESCO, COLOSS, CCFULL, NLAT and record the agreement in each `verification.md`. This retro-fits the 2026-07-21 build-integrity standard onto skills that shipped under the old reference-output-only framing
- [x] **Wave 1b COMPLETE (2026-07-22).** The community optical-potential code list (2026-07-20): ~~pikoe~~, ~~NLAT~~ (2026-07-21), ~~CNOK~~, ~~SIDES~~, ~~SWANLOP~~ (2026-07-22) all shipped. The pair SIDES + SWANLOP (Arellano/Blanchon/Dupuis, nonlocal-OMP scattering, siblings of COLOSS's Perey-Buck path) done together: SIDES tier 2 (no shipped reference, cross-build ~12 figs + optical theorem), SWANLOP tier 1 (reproduces shipped zz.*.REF). NB the original version of this line listed CNOK twice, once carrying NLAT's description; corrected 2026-07-22
- [x] **SMASH SHIPPED 2026-07-23, seventeenth per-code skill, TIER 1, first of the heavy-ion row.** `skills/smash/`, SMASH-3.3 (pinned `d1a1c6cf`), zero source patches on macOS/ARM and Linux/x86-64. Reproduces SMASH's own 104-case ctest suite, 104/104 on the first attempt on both platforms, plus a seeded Au+Au anchor whose baryon number (788) and charge (316) are exact per block. selftest 103/103 on both platforms. Details and evidence in `skills/smash/references/verification.md`.

  **FIVE adversarial rounds.** Round 1: 19 defects, 7 blockers. Round 2: 2 new blockers, both caused by round 1's fixes. Round 3: 6 residuals, 4 caused by round 2's fixes. Round 4: 2 defects, both caused by round 3's fixes. **Round 5: no new false pass**, which was the stopping condition (not a round count, but a round that comes back without a new defect of the same shape). Round 5's only finding was a false REJECT, `Randomseed: +123`, valid YAML that raw SMASH runs with; fixed.

  **Severity decayed monotonically**: two blockers, then four silent false passes, then two input-validation boundary defects, then one false reject. Round 5 verified the negative result by measurement rather than assertion: int64 boundary from both ends, both quote styles, `in_int64` with a shell-metacharacter payload (argv not eval), `python3` absent and replaced by python2 (fails CLOSED), and **every shipped config under `input/` run through the wrapper**, which is what the fail-closed branch most risked breaking. None falsely rejected.

  **The transferable lessons, for the rest of this row:** (i) nothing in five rounds was found by inspection, including immediately after writing the code; what worked, in order of yield, was running the harness on a SECOND MACHINE, the flip test, and an adversarial reader allowed to run the real code. (ii) The one fix that retired a whole class instead of a case was replacing a fail-open branch with fail-closed: a validation whose "cannot read this" path is `skip` is one unexpected spelling away from not existing. (iii) A guard validated against one configuration has been demonstrated, not tested. (iv) Running on Linux exposed selftest fixtures that were FABRICATING their input while passing cleanly on macOS.

  **Documented gap, not a defect:** a config using `Minimum_Nonempty_Ensembles` instead of `Nevents` is accepted with no exact event-count check, because the event count is not fixed in advance. Stated in the docs rather than hidden.
- [x] **GiBUU SHIPPED 2026-07-24, eighteenth per-code skill, TIER 2, second of the heavy-ion row.** `skills/gibuu/`, GPL-2.0, Buss et al. Phys. Rept. 512, 1-124 (2012) CrossRef-verified, pinned to release 2025 patch 5 by tarball SHA-256 + version.txt (weaker than a git pin, stated as such: hepforge tarballs, svn closed since 2018). Builds UNPATCHED on macOS/ARM gfortran 15.2 and Linux/x86-64 gfortran 13.3. Tier 2 because GiBUU ships NO reference output; build integrity carried by cross-build reproduction (all 8 output files bit-identical at a fixed seed, 5 seed-driven files + 3 lookup tables), physics NOT claimed. Covers neutrino- and lepton-nucleus FSI that SMASH does not. One Codex adversarial pass: 1 blocker + 8 lesser, ALL fixed. The blocker was the same shape as the SMASH ones (seed readback grepped the first SEED= line anywhere, but GiBUU reads the first &initRandom NAMELIST; an empty first block clock-seeded while the wrapper reported success). selftest 37->50, every new guard flipped, VERIFY OK on both platforms. Full write-up: `skills/gibuu/references/verification.md` incl. the "what the adversarial pass found" section.

  **Dual-platform-from-the-start paid off again**, concretely: the -lbz2 conditional retry exists only for Linux and could never be exercised on macOS; and the native-exe fast-path guard rejected the REAL Linux build because GiBUU.x is a symlink and GNU `file` does not follow symlinks by default while macOS `file` does (fixed with `file -bL`). Both were caught only by running on the second machine.

- [ ] **Heavy-ion + EOS rows, remaining candidates.** SMASH DONE (2026-07-23), GiBUU DONE (2026-07-24), **Thermal-FIST DONE (2026-07-24, nineteenth per-code skill, tier 1, first HRG/EoS code, fills the HRG branch of the EOS row; five Codex rounds)**, **vHLLE DONE (2026-07-24, twentieth per-code skill, tier 2 with an analytic Gubser benchmark, the relativistic viscous-hydro branch; VERIFY OK both platforms; one Codex pass, 6 fixed; see skills-catalog.md and skills/vhlle/references/verification.md)**. The hydro chain (MUSIC / TRENTO / iEBE) is only meaningful as a chain, so it stays a later block decision. Candidate table with live-verified repo/license/DOI in [skills-catalog.md](skills-catalog.md).
- [x] ~~Retrofit the from-source-certification hardening across the skill family.~~ **DROPPED 2026-07-24 (user ruling): the from-source-build certification is NOT the axis a skill passes on.** A skill's pass criterion is reproducing the published work (a figure, a table, a number from the code's own paper), not an anti-spoof rebuild ceremony. The certification (`VERIFY OK` vs `PASSED-NOT-CERTIFIED`) stays where it already exists (Thermal-FIST, vHLLE) as a minor build-integrity detail but is not backported and is not to be presented as the pass gate. See CLAUDE.md Key decisions 2026-07-24.
- [x] **Sky3D DONE 2026-07-23, sixteenth per-code skill, tier 1 with a documented macOS stability caveat.** Details in skills-catalog.md and skills/sky3d/references/verification.md. Two Codex adversarial passes, 29 defects total, all fixed; selftest 69 cases; the collision reference is honestly NOT a benchmark (its binary input is not distributed). One thing left open and documented rather than solved: an intermittent SIGBUS at startup on macOS (1 in 25 runs, 0 in 25 on Linux, stack exhaustion refuted, cause unknown). Superseded note, kept for the record: **Sky3D CLEARED 2026-07-23, build it** (TDHF, `manybody/sky3d`, Fortran 90 + OpenMP + FFTW + LAPACK, CPC 185, 2195 (2014) + CPC 229, 211 (2018) both CrossRef-verified). The most research-relevant code in the new section: TDHF fusion and dissipation touch the superheavy white paper and the Line A CF-suppression work. It carries **no open-source license** (no LICENSE file, nothing in the README, no source header, and the CPC program summary's `Licensing provisions: none` means the CPC non-profit use license), but the boundary rule was amended the same day to test obtainability rather than commercial terms, so it is in. **Requirement carried into the skill: SKILL.md must state plainly that Sky3D is CPC non-profit and not open source, and point a commercial user at the authors.** Do not let that note get lost in the build
- [ ] **Finish the EOS-side verification**: CompOSE, SROEOS, HFBTHO, HFODD were surveyed but none was located on GitHub in the 2026-07-23 pass, so each still needs its distribution channel, license and paper confirmed live before it can be listed as a candidate. Also finish the CrossRef check on MUSIC, TRENTO and iEBE, whose repos and licenses are verified but whose papers are not
- [ ] Look at **JITR** (Beyer, Python R-matrix built for calibration/UQ, github.com/beykyle/jitr) before deciding on a skill: it is the closest external code to Line D / DREAM / the emulator work, so it may be more useful as something to read and compare against than as a skill
- [ ] Verify publications for JITR and YAHFC, and licenses for the Alex Brown bundle, before any of them get a skill (open repo alone does not satisfy the hard rule)
- [ ] Decide FRESCO upstream: the skill builds I-Thompson/fresco, but LLNL/Frescox is the actively maintained fork [user decision]
- [ ] Theo4Exp (Seville/Krakow/Milano platform) is registration-gated, so it fails the publicly-obtainable rule as written; flagged not deleted, since it is Moro-adjacent [user decision]
- [ ] GSM follow-ups: benchmark a GSM-CC reaction case (Chapter 9), noting the book's Ex X/XI numbers are superseded by GSM-2.0
- [ ] Wave 2 community heavyweights: ~~KSHELL~~ (done 2026-07-22, first shell-model/structure code after GSM), ~~SkyNet~~ (done 2026-07-23, nucleosynthesis reaction network, astro; BSD-3, HDF5/GSL/Boost, dense LAPACK). (AZURE2 done 2026-07-22; GEF dropped 2026-07-22, not buildable on target platform; GEMINI++ dropped 2026-07-21, not publicly obtainable). ~~NuclearToolkit.jl~~ (done 2026-07-23, MIT, Julia, JOSS 2022; first Julia-package skill, adds ab-initio IMSRG + chiral EFT coverage). Remaining Wave-2 clean-license option: imsrg (GPL-2.0, C++, deepens the ab-initio row)
- [ ] Eligible Lei codes when convenient: SLAM.jl, PINN-ECS, inhomoR (COLOSS done)
- [ ] Each per-code skill meets the quality bar in skills-catalog.md (install from public source, verified deck examples, run/parse, benchmark to N digits with a CLEAN-ROOM build test, failure modes) before it ships. Since 2026-07-20 this also requires a **Codex adversarial pass** and **live citation verification**, and the skill must declare its benchmark tier (see CLAUDE.md Key decisions). Since 2026-07-24 the benchmark should, where possible, be **anchored to a specific PUBLISHED result** (a figure/table/number from the code's paper), not only the code's own test suite, because reproducing the published work is what makes a skill trustworthy to someone who did not build it
- [x] ~~**pikoe follow-up**: email the authors for the missing reference output.~~ **CANCELLED 2026-07-21 (user), and the reasoning generalizes to every skill.** The author's reference output is produced by the same source, so it can only certify **build integrity**, never physics: a genuine physics bug is present in their reference too, and matching it proves nothing. Build integrity is better certified by cross-platform reproduction, which tests more configurations. **Measured on pikoe:** macOS ARM64 gfortran 15.2.0 vs Linux x86_64 gfortran 13.3.0, at `-O2`, `-O0`, and `-finit-real=snan -finit-integer=-99999`, gave **bit-identical output across all six builds** (5642 numbers per comparison, TDXnorm + TDXinv + QDXinv). `-O0` vs `-O2` agreement rules out optimization-sensitive UB; snan-init agreement plus a comparator that rejects non-finite values rules out any uninitialized variable reaching the output. Combined with the existing figure anchoring against the CPC paper, pikoe's verification is complete without the authors. Harness at `scratchpad/xbuild.sh` + `xcmp.py`, worth promoting into the skills
- [x] **Cold-start the auto-install path on the other skills: DONE 2026-08-13 for 19 of 20** (TALYS deferred, see below). Run on heliumx (Ubuntu 24.04 x86-64, full compiler toolchain and no GSL/FFTW/HDF5/Boost/Eigen/Julia, i.e. a student's fresh machine), each skill into a throwaway root via its own path env override, so no working install was touched. **Six portability defects found and fixed** (commits 0454f8ba1d, e82e950e43): sky3d's probe passed `mrest=0`, an integer division by zero that is SIGFPE on x86-64 and silently harmless on Apple Silicon; COLOSS's Makefile hardcoded Homebrew LAPACK and `-lc++`; SWANLOP's makefile required `/bin/csh`; pikoe, nlat and azure2 used the BSD-only `mktemp -t <prefix>`; gsm gave brew-only advice for missing MPI. Result after fixes: 13 install-and-verify clean on the bare box, 4 stop correctly with an actionable missing-dependency message. nlat completes too, `VERIFY OK` in 6500 s (1 h 48 min), local case 14 files and nonlocal case 11 files, zero failures; its first two attempts died on a flaky Mendeley download (caught and reported by its own guard) and then on the harness's 5400 s ceiling, against a runtime the skill's own verification.md documents as 1 h 26 min on one core, so neither was a skill defect. **All 20 skills are now cold-start verified**, TALYS included. Two lessons in the devlog worth carrying: a documented cross-build of the CODE is not a test of the SKILL's scripts (pikoe had one and its verify script still failed on Linux), and a make command-line variable propagates into sub-makes (the first COLOSS fix broke both platforms that way). GitHub is unreachable from heliumx, so the run needs a reverse proxy tunnel from the laptop, recorded in the devlog
- [x] **Cold-start TALYS: DONE 2026-08-13, and it is cheap.** The deferral above was reasoned from a wrong premise: git transfers the compressed pack, which GitHub reports as 2.03 GB, not the 11 GB working tree, and the script already shallow-clones. Measured end to end on heliumx through the proxy tunnel (4.38 MB/s): **install 5 min 19 s, 11.2 GB on disk**, then the `n-Th232-fis-wkb` benchmark in 55 s. It also corrected a real overclaim: the skill's byte-for-byte tables were measured on macOS/ARM and said so nowhere, while the same case on Linux x86-64 agrees to **~5.4 significant figures** (1290 physical observables, worst relative difference 3.89e-06). Both the SKILL.md table and verification.md now scope the claim to its platform and give the Linux number, so a Linux user does not read "not bit-identical" as a broken build. One harness note: TALYS is the only skill whose `verify_*.sh` REQUIRES a sample-case argument, so a no-argument call exits 2 with a usage listing
- [x] **TALYS opt-in question CLOSED 2026-08-13: no change, and the rule generalises.** The decision was already made by the architecture: `run_talys.sh` calls `install_talys.sh` at run time, so a user who never runs a statistical-model calculation never downloads a byte, and TALYS is opt-in by construction. `install.sh` will never pre-build a code (see the CLAUDE.md key decision), which retires the question for every future skill instead of re-litigating it. **The disk question was hiding the real risk**, now fixed: `have_structure()` checked that two directories existed, which cannot detect the state its own comment described, since a complete database is 16 top-level directories and 47,537 files and reclaiming disk by deleting subdirectories leaves the top-level ones standing. It now compares the file count against the clone's own git index (0.06 s, follows the release rather than a hardcoded number) and refuses to hand back a binary path at all when the database is short. Flip-tested in isolation: complete tree passes, one hidden subdirectory reports 46,044 of 47,537 with a repair command and exits 4 with empty stdout, restored tree passes again

- [ ] **pikoe defect found by the cross-build test: `.mod` files poison a recompile.** `install_pikoe.sh` passes `-J "$SRCDIR"`, writing the module files into the source directory. A later rebuild with a different gfortran then dies on "Cannot read module file ... created by a different version of GNU Fortran", which names neither the real cause nor the fix. Clear `$SRCDIR/*.mod` before building, and add the symptom to `references/failure-modes.md`. This is a real user-facing bug (it fires on any compiler upgrade), not a test artifact

## Phase 3: knowledge base ([kb-design.md](kb-design.md); PhySH taxonomy + pre-generated md wiki + semantic relations)

- [x] **`relations.tsv` size RESOLVED 2026-08-13 by dropping the background rows: 87.8 MB -> 34.7 MB**, back under GitHub's 50 MB soft warning with headroom for future passes (it had reached 88 MB and every re-typing pass grew it toward the 100 MB HARD limit, which fails a push outright). The `evidence` column was kept, because the 2026-08-12 Codex pass established that a dispute label must be read with its evidence sentence, so stripping it would have removed the thing that makes a `contrasts` row usable. Dropping background instead removes only rows that say nothing `citations.tsv` does not already say: **an edge with no relations row now MEANS background**. The one hazard this created is closed: 24,814 of 54,039 citing papers had ALL-background rows and would have looked unclassified to `cmd_full`, re-running at real cost, so the resume record moved to `kb-wiki/relations-classified.txt` (0.65 MB) and `cmd_full` reads it (unioned with the tsv's first column for pre-ledger checkouts) and appends to it per paper. Flip-tested: hiding the ledger reports 24,814 to go, restoring it reports 0. Note the reframing in the old entry was wrong on one point, worth keeping: relations.tsv is NOT merely a build artifact, `kb-search` queries it directly for who-disputes-X, so dropping it from the distribution was never really an option
- [ ] Abstract-only pages for the ~1,655 corpus papers without fulltext (lighter template, separate small batch)
- [ ] Widen neighbor whitelist to cut the 36.4% unclassified rate (hadron structure, heavy-ion subconcepts; sampled unclassified = mix of true out-of-scope and concept gaps)
- [ ] Tier-B citation edges (author-year heuristic, 90,246 edges after the 2026-08-13 rebuild) still deserve a systematic false-positive audit. Partly addressed that day: an uncorroborated several-person match now yields NO edge instead of an arbitrary largest-group pick, which repaired the known `Jin15` collision and dropped the `Lei15` ones, at a cost of about 16k Tier-B edges. What was never done is a SAMPLED audit measuring the residual false-positive rate, and the guard cannot catch a wrong single-person match (a surname truncation such as `Lei15` written for Leidemann). Hand-verified exceptions go in `kb-wiki/edge-blacklist.tsv`
- [ ] Hook monthly re-run (digest new papers + kb_classify + build_wiki_layers + kb_citegraph + inject + kb_relations) into the corpus-update launchd job
- [x] Distribution decision for kb-wiki: user decided 2026-07-10, pages live directly in the main repo (night-1 19,202 pages pushed in a1c4357; final ~250 MB, revisit only if GitHub complains)
- [x] **Citation-graph data defects, measured by the 2026-08-12 Codex pass on kb-search: ALL FIVE CLOSED 2026-08-13.** Re-typing and contrasts recheck completed (run at standard price on user instruction rather than waiting for the off-peak window; total spend across both phases roughly $10): the 4,559 re-opened papers re-typed with context (13.0M in / 2.3M out), then all 17,408 contrasts rows re-verified with the dispute-only prompt (7.5M in / 0.9M out): **only 6,000 survived, 11,408 retyped** (uses 6,433, compares 2,631, background 2,344), a measured two-thirds overfire rate. relations.tsv and citations.tsv are 1:1 again at 703,430 rows; final type counts in skills/kb-search/SKILL.md. Recheck verdicts preserved in kb-wiki/recheck-contrasts.tsv. Mechanically repaired the same day (see devlog): (ii) the 4,093 dangling edges are dropped from both TSVs (723,748 edges remain); (iii) the `[TARGET]` evidence placeholder is normalized to `[the cited paper]`; and the repair surfaced and fixed two pipeline bugs Codex had only grazed: `inject_citations.py` never mapped old-style slash ids to underscore filenames, so **all 17,570 old-style pages carried an empty "None detected" citations section** and every old-style link/title/year in the sections was broken (now rebuilt corpus-wide, injections proven idempotent incl. under PYTHONHASHSEED=random), and `kb_relations.py`'s default tsv path pointed at the 221 KB pilot `relations-sample.tsv` rather than the real file (now defaults to `relations.tsv`; bare `inject` runs used to silently rewrite pages from the sample). CLOSED 2026-08-12 (same-day continuation): (i) collision guard written into kb_citegraph.py (self/collaborator corroboration wins, uncorroborated several-person ties yield no edge instead of max()), full KINGSTON rebuild in 11 min, guard calibration in the build output (1511.03214 → 1510.02602 present, soliton edge absent; Leidemann edges dropped by the guard itself, `edge-blacklist.tsv` kept as belt and braces), INSPIRE re-merged, page-set filter reapplied: 703,430 edges, zero out-of-page-set rows, 1,538,412 page links checked with zero missing targets. (iv)+(v) armed as `scripts/run_relations_repair.sh` in the DeepSeek off-peak window: 4,796 citing papers re-opened (3,181 no-context tail + 1,648 edge-set-changed, 4,559 with surviving edges), then a focused `recheck-contrasts` pass over all 15,797 contrasts rows (smoke-tested: the known overfire flips to compares), then one Related-work injection. Check `kb-wiki/relations-repair-*.log` after the window(s); when ALL DONE, refresh the counts in skills/kb-search/SKILL.md and commit the re-typed relations.tsv + pages
- [x] 2026-08-11: **Licensing decision for the public artifact RESOLVED (user ruling): kb-wiki ships AS-IS**, abstracts included, alongside the digests. Backed by `kb-wiki/README.md` (provenance table, the basis for distribution, an explicit warning that the digests are machine-generated and can be wrong, and a no-questions-asked author removal path) and by the amended corpus-redistribution hard rule, which still forbids shipping raw `.tex` or the full-text `corpus.db`. Repo licence is MIT; `LICENSE` disclaims the three things it cannot cover (the driven codes, the opencode fork, the summarised papers)
- [ ] Optional later: MCP server exposing kb_search/kb_browse (demoted from load-bearing to sugar, per 2026-07-09 revision)

## Phase 4: onboarding + distribution (wizard design: [onboarding-design.md](onboarding-design.md))

- [x] **Gitee mirror question CLOSED 2026-08-14 (user): not needed.** The 2026-08-13 measurement on heliumx (github.com 0 of 6 connections, clone dead after 132 s, release download timed out) is real but does NOT describe the intended audience: researchers and graduate students in China run a proxy as standard equipment and reach GitHub directly. The proxy-less case that remains is a LAB SERVER or compute node, which is a real scenario for this group since heavy work runs remotely, so both READMEs now carry one line about it pointing at `http_proxy`/`https_proxy` for the two download steps. Lesson worth keeping: heliumx is not a model of a student's machine, and a measurement on one box is not a claim about a population.
- [ ] `fusion init` wizard v1 (CLI): model+key test, PhySH area picker, kb-wiki slice mount, personal-wiki seeding from user's arXiv ids, skill recommendation with benchmark-on-install
- [ ] concept-skill-map.yaml (PhySH concept -> skills-catalog entries)
- [ ] Monthly personal digest loop (filter corpus updates by user concepts, greet on launch)
- [ ] install.sh: opencode binary + skill pack + kb-wiki + default config in one shot. Constrained by the 2026-08-13 rule: it never pre-builds a nuclear-physics code, since provisioning is lazy and codes arrive on first use. **Reconsider whether it is worth writing at all**: the documented quick start is already three readable commands, verified working end to end on 2026-08-14 from an empty directory on another machine, and collapsing them into one opaque script cuts against a project whose pitch is that you can see what it does
- [ ] Default model config for CN users (domestic providers) and international users
- [ ] Student-facing docs (zh + en); wizard closing demo doubles as the tutorial
- [x] 2026-08-14: **the documented onboarding path verified end to end on a machine that is not the author's** (heliumx, from an empty directory): clone 58.7 s, CLI downloaded and extracted, `--version` reports 0.1.0, skills discovered from inside the clone with no configuration, and a Chinese question ("帮我算 d+58Ni 在 21.6 MeV 的弹性散射") raised the first-run setup offer AND reached for the FRESCO skill in the same turn, reading its examples and namelist reference. This was the platform's central claim and it had never been checked off the machine it was built on
- [ ] **Pilot with 2-3 group students; collect failures into devlog. This is now the top item and the one thing nobody else can do.** The technical gates are clear: every code installs cold, the onboarding path works on another machine, the repo size is honest. What is untested is whether the first-run experience makes sense to someone who did not build it, which no amount of self-testing settles. Fix the `--help` branding leak first, since it is the most likely first command a student types
- [ ] v2+: TUI popup wizard (plugin slots), ORCID/INSPIRE author lookup, group mode (advisor-curated shared config)

## Papers to download (user fetches from the office; added 2026-07-20)

Needed to write the Wave 1b per-code skills. Each code's SOURCE is already
obtainable (Mendeley Data / CPC Library), but the CPC paper is what documents the
input format, the keyword semantics, and the author's own test cases with
reference values, which is what a benchmark has to reproduce. ScienceDirect
blocks automated fetching (HTTP 403), hence this list.

All DOIs below were verified live against CrossRef on 2026-07-20; author, title,
journal, volume, page and year are as returned by the CrossRef record.

| # | code | paper | DOI |
|---|---|---|---|
| 1 | **pikoe** | Ogata, Yoshida, Chazono, *pikoe: A computer program for distorted-wave impulse approximation calculation for proton induced nucleon knockout reactions*, Comput. Phys. Commun. **297**, 109058 (2024) | `10.1016/j.cpc.2023.109058` |
| 2 | **NLAT** | Titus, Ross, Nunes, *Transfer reaction code with nonlocal interactions*, Comput. Phys. Commun. **207**, 499-517 (2016) | `10.1016/j.cpc.2016.06.022` |
| 3 | **CNOK** | Sun, Wang, *CNOK: A C++ Glauber model code for single-nucleon knockout reactions*, Comput. Phys. Commun. **288**, 108726 (2023) | `10.1016/j.cpc.2023.108726` |
| 4 | **SWANLOP** | Arellano, Blanchon, *SWANLOP: Scattering waves off nonlocal optical potentials in the presence of Coulomb interaction*, Comput. Phys. Commun. **259**, 107543 (2021) | `10.1016/j.cpc.2020.107543` |
| 5 | **SIDES** | Blanchon, Dupuis, Arellano, *SIDES: Nucleon-nucleus elastic scattering code for nonlocal potentials*, Comput. Phys. Commun. **254**, 107340 (2020) | `10.1016/j.cpc.2020.107340` |

Priority order is the Wave 1b build order: 1 first (pikoe is next up), then 2, 3,
then 4 and 5 together (SWANLOP and SIDES are siblings from the same group).

**pikoe packaging note (found 2026-07-20; user resolved the same day: proceed).**
The user's ruling: the skill's goal is to get the INPUT right, so an input manual
plus 5 real decks is sufficient to build it, and the missing reference output
does not block. Consequence to state honestly in the skill: pikoe's benchmark
tier is lower than FRESCO/COLOSS/CCFULL/GSM/TALYS. Those reproduce documented
reference numbers; pikoe can only be verified as builds + runs + produces
physically sensible output, plus internal consistency between its own samples
(sample1 normal vs sample4 inverse kinematics are the same reaction, and sample2
vs sample3 are the same observable at 392 vs 100 MeV). Upgrade path once the CPC
paper arrives: compare against its published figures. Details of what is missing:

The source is fetched
and fine (`pikoe1.1.f90`, 2025-03-18, from the author's RCNP page via the
attach-plugin URL; the plain `.zip`/`.f90` URLs 403/404, use
`index.php?plugin=attach&refer=files&openfile=pikoe1.1.zip`). It ships a real
input manual (`input_man.txt`) and 5 sample cases. **But it ships no reference
output.** `readme.txt` documents a `tbl_*.dat` and a `*.outlist` in every sample
directory; neither is in the archive. Verified against both releases: v1.1 has 22
entries, v1.0 has 13, and in both the `sampleN/` directories contain only the
`.cnt` input. So the FUSION standard benchmark (reproduce the code's OWN
documented reference values) cannot be run as things stand. Three options:
  (a) **Ask the authors.** Kazuki Yoshida is a co-author and is the person who
      invited Lei to QFS-RB 2026, so this is a one-line email and by far the
      cleanest fix. It also likely helps the authors, since the packaging defect
      is theirs and they may not know.
  (b) Use the CPC paper's figures as a weak qualitative check. Fails the
      N-significant-figures bar; not really a benchmark.
  (c) Cross-check against another DWIA code. The catalog strategy explicitly
      warns against this (convention archaeology, the COLOSS cautionary case).
(a) is still worth doing opportunistically, since the packaging defect is the
authors' and they likely do not know, but it is no longer blocking.

**All five PDFs delivered 2026-07-21** and read in full (structured extractions
of the program summaries, formalism, test cases and input formats). The pikoe
paper was used the same day to build the pikoe skill, where it turned out to be
worth more than expected: its five figures correspond exactly to the five shipped
sample decks and carry numeric axes, so it supplies the quantitative benchmark
that the distribution's missing reference output cannot.

Still open: ingesting all five into the literature-wiki. The ingest is paused at
the vocabulary-approval step (proposed: methods `dwia`, `johnson-tandy-adwa`,
`nonlocal-iteration-scheme`, `nonlocal-matrix-inversion-scheme`,
`t-rho-rho-folding`; observables `residue-momentum-distribution`,
`triple-differential-cs`; system `16c-12c`; five code entities plus lead-author
entities). Two findings from the reading worth filing when it resumes: SIDES
defines itself against NLAT by name ("without resorting to any ad-hoc seed as
required in iterative methods [4,13]", where [4] is NLAT), which is a real
methodological debate page; and NLAT's headline "4% accuracy" and "Ed = 10 to 70
MeV" appear only in its abstract, never derived in the body.

- [ ] **The `codex:codex-rescue` plugin path is unreliable; drive `codex exec` directly.** On 2026-07-22 it reported "still running" for an hour while the broker had never started (0-byte `broker.log`, no `broker.pid`, no live process); the Codex CLI itself was fine. Cross-AI validation is mandatory before a skill ships, so a silently dead delegation path is a real risk to the workflow, not a cosmetic bug. Until fixed, launch `codex exec --sandbox workspace-write --cd <repo> "<prompt>"` in the background and poll the pid. The plugin was updated on 2026-07-22; re-test before trusting it again

## Repo hygiene

- [ ] **Concurrent-session hazard: the standing rule stays, the 2026-07-20 instance is closed.** That day another Claude session had `scripts/kb_citegraph.py`, `scripts/kb_citemap.py`, `scripts/citegraph_template.html` and a `fusion-web/` citation-graph visualization uncommitted in this same tree. As of 2026-07-23 those are resolved: `kb_citegraph.py` is committed (367a2c35c) and `fusion-web/` is no longer present, so the working tree holds only this session's work. The rule itself does not expire: **never `git commit -a` here**, always stage explicit paths, and check `git status` before and after. Same failure mode the profile records for 2026-07-14, when a parallel session silently dropped another's edits

## Wiki ingest queue

- TALYS code paper: Koning, Hilaire, Goriely, *TALYS: modeling of nuclear reactions*, Eur. Phys. J. A **59**, 131 (2023), DOI `10.1140/epja/s10050-023-01034-3`. Wiki precheck on 2026-07-20 returned **Related-only**: TALYS is named in 11 pages (methods/hauser-feshbach, entities/koning-aj, entities/ripl-library, and others) but has no source page of its own, so every mention is currently unanchored. Open access on SpringerLink, so no download trip needed.

## Completed

- [x] 2026-08-11: **install-script audit DONE, all 20 `install_*.sh` checked for the FRESCO "check one artifact, install several" shape; 2 defects found and fixed.** (1) **TALYS**: the fast path was `[ -x "$BIN" ]` alone, so the script's own structure-database check, sitting below it, was unreachable whenever the binary existed; reproduced with a stub binary and no `structure/` ("talys already built", exit 0). Worse than the FRESCO case because the database is 8.6 GB of the 11 GB install, exactly what a user deletes to reclaim disk, and because a PARTIALLY missing database does not abort but falls back to Duflo-Zuker masses and still prints "successful calculation". Fixed with a single `have_structure()` predicate shared by the fast path and the post-clone check, so the two cannot drift. (2) **Thermal-FIST**: `fast_path_ok` certified the install on `cpc1HRGTDep` while run and verify also dispatch to `cpc2chi2`, `cpc3chi2NEQ` and `cpc4mcHRG`; lower severity since the run wrapper dies with a clear message on its own missing binary, so it was a misleading green install rather than a wrong number. Both fixes flip-tested (hide exactly one artifact, confirm exactly that guard fires, restore) with a no-false-reject case on a complete tree; Thermal-FIST selftest 50/50 unchanged. **The other 18 are clean, and for a reusable reason**: either the skill provisions its dependencies ABOVE the fast-path branch (GiBUU's input database, SMASH's Eigen/Pythia/GSL) so the check cannot be skipped, or its fast path PROBES with a real calculation (CGMF, KSHELL, SIDES, SWANLOP, SkyNet, CNOK, vHLLE) which touches every artifact at once. Hand-enumerating artifacts, which is what both fixes here do, is the weakest of the three and drifts as soon as a skill grows a new output. (3) **AZURE2**, fixed the same day on the user's call: it was the last skill whose fast path was a bare file-exists with no probe and no stamp (Minuit2 is statically linked so the lone binary IS the whole runtime install, but a half-written cached binary was waved through). Its existing post-build probe is now a function called from both paths. Flip-tested against a throwaway `AZURE2_ROOT` after the first attempt's assertion proved wrong (rebuilding and then returning a path is the CORRECT response to a failed probe, so "must not return a path" tested nothing); verify VERIFY OK with both anchors unmoved and selftest 25/25 afterwards. Regression caveat: TALYS is not cached on the laptop, so its complete-tree fast path was regression-tested against a synthetic tree, not a real 11 GB install

- [x] 2026-07-30: **SFRESCO added as a COMPANION skill to FRESCO, not a new per-code row.** `skills/sfresco/` (22 files: SKILL.md, 5 references, 4 example decks + README, `run_sfresco.sh`, `potmap.py`, `fitreport.py`) extends the existing FRESCO row from forward calculation to parameter estimation via the MINUIT chi-squared search front end that ships inside the FRESCO distribution (its binary contains FRESCO). It consumes no new subfield area and does not restart the paused treadmill. **It clears the 2026-07-24 pass criterion outright**: *FRESCO: getting started* section 4 boxes 7 and 8 publish a complete worked fit AND its answer, so `examples/p-cd-manual.*` is a genuine L2 case, reproducing every printed digit from a 15%-displaced start (V = 52.5280 vs 52.53, r0 = 1.17958 vs 1.179, W = 3.46041 vs 3.46, W_d = 7.42937 vs 7.43, chi2/N = 2.1910 vs 2.19). **Lesson: a code whose own documentation publishes a worked example WITH numbers is the cheapest possible published anchor**, and the FRESCO family is unusual in having one. **Platform defect found and fixed in the same pass:** `install_fresco.sh` checked only for `fresco` while installing `fresco` and `sfresco` together, so any machine with a hand-built or older `fresco` (this laptop) got "already installed" and no `sfresco` at all, unreachable short of `--force`; detection now requires BOTH artifacts. Cold start then verified for the first time from an empty bin dir (clone, build, closure fit passes), which also surfaced that a fresh build is FRES 3.5 while `~/bin` holds 3.4, and that 3.5 accepts `type=9` (ANC) data 3.4 rejects. Three bounded Codex passes corrected two documented facts (`pline` also counts `&step` records for TYPE 12-17; the CC-iteration 10000 penalty is unconditional while only the bound-state and R-matrix ones are gated by `number_calls>5`) plus 14 script bugs; a fourth UNBOUNDED pass hung 28 minutes with no output and was killed, the same bounded-prompt lesson as the opticalfisher appeal audit, now observed twice

- [x] 2026-07-23: **First research skill embedded, plus the fresco skill unified and given an OMP generator.** Three pieces, none of them a per-code skill, so the install/build/benchmark bar does not apply. (1) **`skills/exfor-data/`** (SKILL.md, `references/exfor-format.md`, `scripts/exfor.py`): retrieval and parsing of measured data from the IAEA EXFOR database, the partner every reaction skill needs to compare a calculation against experiment. Its integrity check is that parsed values trace back to the raw entry. Two traps it encodes: the interactive search servlet **cannot be scripted** (it answers `Define Search Criteria!` to every GET and POST, the form being JavaScript-built and session-bound), so the workflow is find the accession number elsewhere then pull through `X4sGetEntry`; and the data blocks are **fixed width** (6 fields of 11 characters, wrapping past 6 columns) where a blank field means "not measured", so whitespace splitting silently shifts every later column. Also handles `DATA-ERR` being sometimes `PER-CENT` and sometimes absolute, and `COMMON` vs `DATA` counting their header lines differently. Built to answer "there is no measurement at that energy" as a first-class result; verified live on n+90Zr, where nothing exists at 50 MeV and the nearest sets are 24 MeV (enriched, full angular range) and 55 MeV (natural Zr, forward angles). (2) **`skills/fresco/scripts/omp.py`**: emits KD02 and CH89 global nucleon optical potentials as ready-to-paste `&POT` blocks, pure standard-library Python so no Fortran toolchain is needed. A generator rather than model-written parameters because three handoff failures into FRESCO are silent (deck runs, prints a plausible but wrong cross section): FRESCO builds radii as `R = r0*(Ap^1/3 + At^1/3)` while KD02/CH89 are defined on `R = r0*At^1/3`, so without `ap=0` every radius is ~22% too large; `W_d` must land in `p4` of the `type=2` line, since `p1` makes it a real surface well and the absorption quietly drops; and a `type=0` line is required even for neutrons because it declares the convention. `--selftest` pins 39 values against the reference Fortran at 2e-7, and that residual belongs to the Fortran, not the Python: **every unsuffixed real literal in that kd02.f is single precision** (`59.30` enters as 59.2999992370605), proven by neutron `V` deviating despite containing no cube root and by `-fdefault-real-8` restoring a 16-digit match, while the `d0`-suffixed ch89.f reproduces exactly. End to end the generated deck gives sigma_R = 1301.64017 mb for n+90Zr at 50 MeV, identical to a hand-built deck. (3) **fresco skill unified**: the 2026-07-14 FUSION-only auto-install split is withdrawn (user ruling), the global skill and the FUSION copy are now byte-identical with `diff -r` empty, so a deck authored in one place runs unchanged in the other. **Codex adversarial pass on all of it: 3 defects, all confirmed and fixed** (the precision misdiagnosis above, initially blamed on a single-precision cube root; `omp.py` accepting impossible nuclides so `--target 90,40` silently returned Z=90 A=40 parameters when the user meant 90Zr; and `exfor.py` documenting a header-count consistency check it never performed, so a truncated wrapped record vanished silently, whose fix then exposed the COMMON-vs-DATA header counting difference after 33 false warnings on real entries)

- [x] 2026-07-23: **Fifteenth per-code skill: NuclearToolkit.jl** (skills/nucleartoolkit/, Sota Yoshida, JOSS 7(79), 4694 (2022), DOI 10.21105/joss.04694, MIT, github.com/SotaYoshida/NuclearToolkit.jl, CrossRef-verified). **First Julia-package skill** and the first ab-initio code: chiral-EFT interaction generation + HFMBPT + (VS-)IMSRG + valence shell model, one package. Complements KSHELL (phenomenological shell model) and adds the IMSRG/chiral-EFT capability relevant to Line D (Furnstahl/EFT_Fisher_info). **TIER 1**: reproduces the package's shipped `@test` references. L1 (fast, self-contained) the CKpot Be-8 shell-model spectrum, 10 eigenvalues to the shipped tolerance |dE| < 1e-3 (measured max 4.1e-4, g.s. -31.1194 vs -31.119); L2 the full `Pkg.test` 30/30 in ~3.5 min (chiral EFT -> HFMBPT He-4 [1.493,-5.805,0.395] -> IMSRG g.s. -4.05225276 to 1e-6 -> shell model). **No source patches** (Julia is cross-platform); install is a pinned `Pkg.add` (v0.5.2) + precompile into an **ISOLATED depot + project** so the user's global Julia env is never touched (every julia call sets JULIA_DEPOT_PATH). run is parameterized (nucleus + shipped interaction ckpot/usdb + n_eigen), validated ckpot Be8 (-31.12) and usdb O18 (-11.93). Codex adversarial pass (gpt, exec): 6 findings (5 major, 1 minor), all fixed (run and verify-L1 trusted the eigenvalues over the Julia exit code, so a crash after printing partial output could PASS, and run did not check the requested count; verify-L2 accepted Total>=25 instead of the pinned 30, so a skipped 25/25 could falsely certify tier 1; install extracted the installed version but never compared it to the pin; NTK_PIN was interpolated into Julia source without semver validation; selftest L2 guards were not isolated). Codex confirmed CLEAN: depot isolation (every julia call sets JULIA_DEPOT_PATH), run argument safety (allowlisted interaction, alphanumeric nucleus, integer n_eigen, values passed as Julia ARGS not interpolated), and the honest L1 tolerance (1e-3 is the upstream 3-decimal gate, not a 1e-6 energy claim). selftest 20 cases (argument validation needs no Julia; content guards use a keyword-keyed stub julia), guards isolated. 10 files: SKILL.md, 4 references, install/run/verify/selftest scripts, examples/README (no patch file, unlike the compiled skills). **FUSION now has 15 per-code skills** (FRESCO, COLOSS, CCFULL, GSM, TALYS, pikoe, NLAT, AZURE2, CGMF, CNOK, SIDES, SWANLOP, KSHELL, SkyNet, NuclearToolkit.jl)

- [x] 2026-07-23: **Fourteenth per-code skill: SkyNet** (skills/skynet/, Lippuner + Roberts, ApJS 233, 18 (2017), arXiv:1706.06198, BSD-3, bitbucket.org/jlippuner/skynet, CrossRef-verified). Modular nuclear reaction network library (r-process, rp-process/X-ray burst, alpha network, NSE) with self-heating Helmholtz EOS + Coulomb screening; C++11 + Fortran, CMake, HDF5(C++)/GSL/Boost, dense LAPACK solver (Accelerate on macOS). Second astro code after AZURE2; the astro/nucleosynthesis-network row. **TIER 1 with a documented macOS caveat**: reproduces the shipped CTest self-comparison suite **19/19 on Linux** (heliumx, gfortran/g++ 13.3, conda hdf5/gsl/boost) and **17/19 on macOS** (clang17/gfortran15.2, Homebrew deps). The two macOS exceptions are StopWatch (wall-clock 1% self-test, environment-flaky) and the NSE test's THIRD block (full-network Saha at T9=3, abundances over ~200 decades), libm-limited: Apple exp/log give 7.0e-3 vs a glibc-calibrated 3.5e-5 gate (identical under -O3 and -ffp-contract=off, so not FMA/UB; the same patched source passes 19/19 on Linux, so it is a platform numerical difference not a build fault). Anchors: X(ni56)=1.7794E-02 from the analytic alpha network (identical 5 figs both platforms), NSE Saha block < 1e-10 both, NSE xray-burst block = 0.000777272 identical both. **Five portability patches** for Apple clang/libc++/modern Boost/modern CMake (scripts/skynet_macos_portability.patch, against pinned commit e37ae9c): a constexpr N_A^(2/3) literal (std::pow not constexpr on clang), a __GLIBC__ guard on FPE trapping (feenableexcept is glibc-only; clang defines __GNUC__), a Boost.System component drop (header-only since 1.69, no boost_system target in Boost >= 1.85), a dylib target_link_libraries (macOS forbids undefined symbols in a dylib), one test include for exp10; plus a `-include string` flag (libc++ lacks libstdc++'s transitive <string>) and `-DCMAKE_POLICY_VERSION_MINIMUM=3.5` (CMake >= 4 refuses the declared 3.1). The install prefix avoids a case-insensitive collision with the shipped INSTALL file on APFS. **Codex adversarial pass (gpt, exec): 12 findings (3 blocker, 7 major, 2 minor), ALL fixed** (verify trusted a crashed/absent ctest; NSE block 1 could be missing/nan and pass; run turned any nonzero nse exit into success on all platforms; run alpha overclaimed reference reproduction; ctest allowance was name-only so an abort/timeout could hide; block-3 macOS bound too loose; install fast path had no build-identity stamp; rm-safety unguarded against a bad SKYNET_ROOT_DIR; pinned commit only warned; probe polluted cwd; selftest under-isolated; the constexpr literal was correct for the double exponent 2.0/3.0, comment sharpened). 11 files: SKILL.md, 4 references, install/run/verify/selftest scripts + the patch, examples/README. selftest 20 cases (macOS) / 19 (Linux), guards flip-audited. **FUSION now has 14 per-code skills** (FRESCO, COLOSS, CCFULL, GSM, TALYS, pikoe, NLAT, AZURE2, CGMF, CNOK, SIDES, SWANLOP, KSHELL, SkyNet)

- [x] 2026-07-22: **Thirteenth per-code skill: KSHELL** (skills/kshell/, Shimizu + Mizusaki + Utsuno + Tsunoda, CPC 244, 372 (2019), original arXiv:1310.5431, CrossRef-verified; GPL-3.0 declared in the README). M-scheme large-scale shell model (thick-restart block Lanczos), Fortran 90 + OpenMP + LAPACK/BLAS + Python 3.10+; the FIRST Wave 2 heavyweight and the second structure code after GSM. **TIER 2** (ships no reference eigenvalue): 20Ne USDA (2 valence p + 2 valence n above the 16O sd-shell core) gives the spectrum g.s. -40.46689 MeV (0+), 2+ at Ex 1.696 (exp 1.63), 4+ at 4.091, the K=0 rotational band; **identical to all 5 printed decimals across macOS gfortran 15.2 and Linux gfortran 13.3 (heliumx)**. Built from the maintained **GaffaSnobb fork** (Python 3.10+ tooling; the jorgenem mirror is Python 2 and unusable on a modern Mac); build needs `-fallow-argument-mismatch` (gfortran 10+ rejects the code's rank mismatches) and `-framework Accelerate` (not -llapack, which Homebrew's lapack does not satisfy without -L) on macOS. User boundary ruling: proceed despite an apparent no-license (public-clonable + published, skill clones from upstream); the Codex pass then found the README declares GPLv3, so it passes the open-source boundary cleanly (lesson: grep the README/paper for a license, not just the LICENSE file / GitHub's detector). Non-interactive run bypasses kshell_ui.py: gen_partition.py builds the .ptn, kshell.exe reads the &input namelist. Codex adversarial pass (gpt-5.5 xhigh): 9 findings, 8 acted on (Lanczos non-convergence ignored when a summary exists, now caught via the "NOT converged in Lanczos method" marker; state indices not required to be 1..n; empty .ptn unchecked in verify; install rm -rf clobbering an existing tree before clone, now temp-clone-then-move; plus the license-is-GPLv3, 25-not-24-interactions, Python-3.10+, and 5-row-table doc fixes). selftest grown to 16 cases, guards isolated and flip-proven. **FUSION now has 13 per-code skills** (FRESCO, COLOSS, CCFULL, GSM, TALYS, pikoe, NLAT, AZURE2, CGMF, CNOK, SIDES, SWANLOP, KSHELL)

- [x] 2026-07-22: **Eleventh and twelfth per-code skills: SIDES + SWANLOP** (the Wave 1b nonlocal-OMP pair, Arellano/Blanchon/Dupuis, both from Mendeley Data / CPC Program Library, CrossRef-verified). **SIDES** (skills/sides/, CPC 254, 107340 (2020)): Fortran 90 integro-differential Schrodinger solver for nonlocal optical potentials. **TIER 2** (ships no reference output): shipped n+40Ca 20 MeV TPM gives (reaction, elastic, total) = (1115.717600, 769.200182, 1884.917782) mb, agreeing to ~12 sig figs across macOS gfortran 15.2 and Linux gfortran 13.3 (heliumx), with the neutron optical theorem TOTAL=ELASTIC+REACTION to machine precision; verify is a regression gate (1e-9) against the pin plus the optical theorem, honestly not a shipped-reference reproduction. Build overrides the Makefile's ../sides target (points outside the package) with make exe=sides.x, and skips `make clean` (which rm's ../sides). **SWANLOP** (skills/swanlop/, CPC 259, 107543 (2021)): Fortran scattering-wave code, sibling of SIDES. **TIER 1**: the distribution ships zz.{main,xaq,dsdt}.REF for the quick-start (p+208Pb 30.3 MeV TPM) and this build reproduces all three line-for-line modulo the per-run timestamp, reaction xsec 1.66084 b; install fetches only the 8 MB code tarball, not the 530 MB potential-table supplement. Each got a Codex adversarial pass (gpt-5.5 xhigh): SIDES 8 defects fixed (relative-deck-read-after-cd, unsupported proton runs now handled as reaction-only per the source, a `make clean` deleting ../sides, gate 1e-6->1e-9, single-row verify, doc fixes); SWANLOP 8 defects fixed (an over-broad `Date:|Time:|UTC|CPU` strip that hid appended bogus data lines, tightened to two anchored timestamp patterns; run counting non-numeric lines as observables + not requiring zz.dsdt; docs claiming zz.main reproduction while verify skipped it, now verified; run not copying fort.2/fort.22 for external-potential decks; and KPOT/temp00/LMAX doc errors against the source: KPOT is 0..4 with 4=VKK not a phantom 5, temp00 sets KPOT=2 despite its title, LMAX<0 auto applies to KPOT 0,1,2). selftests 11 (SIDES) and 10 (SWANLOP), guards isolated and flip-proven. **Wave 1b now complete; FUSION has 12 per-code skills** (FRESCO, COLOSS, CCFULL, GSM, TALYS, pikoe, NLAT, AZURE2, CGMF, CNOK, SIDES, SWANLOP). Both are nonlocal-OMP siblings of COLOSS's Perey-Buck path and cross-checks for the Lei+Ren nonlocality-in-DWBA line

- [x] 2026-07-22: **Tenth per-code skill: CNOK** (skills/cnok/, Sun + Wang, CPC 288, 108726 (2023), GPL-3.0, gitee.com/asiarabbit/cnok, CrossRef-verified). C++ Glauber single-nucleon knockout: stripping + diffractive-dissociation cross sections and parallel momentum distributions of the core. **TIER 1.** Benchmark `./mom 1s11p` (^16C -1n, 1s1/2(x)1/2+, 239 MeV/u on ^12C) reproduces the paper's Sec. 5.5 documented value: stripping 60.086689 vs 60.087 mb **exact**, full triple (60.086689, 18.056073, 78.142761) mb **bit-identical across four builds** (macOS/ARM64/clang17 patched at -O2 and -O0; Linux/x86_64/gcc13.3 unpatched and patched, on heliumx). That the gcc-UNPATCHED build equals the clang-PATCHED build bit-for-bit is what proves the two libc++ portability patches (fabs->std::abs in the complex Romberg/interpolation headers; a __APPLE__-guarded ulong typedef) changed portability, not physics; a third difference is a macOS-only `-Wl,-undefined,dynamic_lookup` link flag (the FCI .dylib references CNOK-library symbols a Linux .so resolves lazily). -O0==-O2 rules out optimization-sensitive UB. The paper's diffractive/total (18.050, 78.136) drift 0.03%/0.009% from the released code, localized to the paper side (predates the final commit) by the cross-build bit-identity, within the paper's own CNOK-vs-MOMDIS spread (0.09%). Needs yaml-cpp (brew on macOS; source-built on Linux). Codex adversarial pass (gpt-5.5, xhigh): **8 defects, all fixed** (a path-traversal `rm -f` in run_cnok.sh from unvalidated NAME/BASEDIR, the SAME rm-safety class the devlog repeatedly warns about; a 1e-4 verify gate that contradicted the "bit-identical" claim, tightened to 5e-7; an Eref-absent hole that silently skipped the substituted-deck guard; a 5e-4 total-consistency tolerance tightened to 1e-5; a `-m` doc claim that only stripping is computed, contradicted by the source which writes stripping+diffractive+total; two doc/comment overstatements). 10 files: SKILL.md, 4 references, install/run/verify/selftest scripts, examples/README. selftest 11 cases; each guard isolated and separately proven to flip when disabled. FUSION now has 10 per-code skills (FRESCO, COLOSS, CCFULL, GSM, TALYS, pikoe, NLAT, AZURE2, CGMF, CNOK). Wave 1b remaining: SWANLOP + SIDES (the nonlocal-OMP pair)

- [x] 2026-07-22: devlog.md brought back under the auto-import budget: 249 lines / 16 KB down to 102 lines / 6.2 KB. The four 2026-07-21 entries were consolidated into one that keeps every distinct mechanism (the six false-success costumes, the three wrong-operand guard bugs, the verification philosophy, and the GEF/pikoe/NLAT specifics); their FULL text was moved verbatim into `devlog-archive.md`, which is not auto-imported, so nothing was lost rather than rewritten

- [x] 2026-07-21: Seventh per-code skill: NLAT (skills/nlat/, Titus + Ross + Nunes, CPC 207, 499 (2016), GPLv3, CPC catalogue AFAY_v1_0). Single-nucleon transfer (d,p)/(d,n)/(p,d)/(n,d) in ADWA with explicitly NONLOCAL optical potentials, solved by iteration instead of the Perey correction factor. **Tier 1**: the distribution ships real reference output and the skill reproduces it. Local case 14 files / 1,863,151 numbers / worst 2.067e-11 (on a 6e-22 magnitude value); nonlocal case 11 files / worst 8.4e-07 on the iterated S-matrices with TransferCS at 1.3e-12, 1 h 26 min. Source is NOT where the paper says: the Queen's University CPC library is retired (HTTP 502) and AFAY_v1_0 lives on Mendeley Data, DOI 10.17632/xnwjvk86bs.1, free; archive byte count and line count both match the paper's own program summary. Codex adversarial pass confirmed **21 defects, all fixed**, three ship blockers (an rm -rf guard testing the reverse relationship to the dangerous one, the SAME class pikoe shipped with hours earlier; an all-NaN output reported as a perfect match; a reference fingerprint hashing ls -l metadata instead of content). Surfaced five upstream defects in NLAT itself, see devlog. FUSION now has 7 per-code skills (FRESCO, COLOSS, CCFULL, GSM, TALYS, pikoe, NLAT)

- [x] 2026-07-21: Sixth per-code skill: pikoe (skills/pikoe/, Ogata + Yoshida + Chazono, CPC 297, 109058 (2024), MIT). Exclusive (p,pN) knockout in DWIA: TDX, QDX, analyzing power, residue momentum distributions, normal and inverse kinematics. install/run/verify/check scripts; the run wrapper recreates the upstream sampleN/ layout with symlinks so the shipped decks are used verbatim. **Benchmark is figure-anchored, not reference-anchored**: upstream ships no reference output (readme documents one per sample directory, archive contains none, in both releases), but the five decks are exactly the paper's five figures, so peaks were compared against numeric axes (TDX 127.03 at 40.5 deg and 128.32 at 61.0 deg vs about 130 and 135; QDX 0.18147 at 185 MeV and 0.17408 at 325 MeV vs about 0.175 and 0.168; LG peak 36.724 vs about 37). Codex adversarial pass confirmed **24 defects, all fixed**, four of them ship blockers (an rm -rf guarding a different path than it deleted; zero-byte tables counted as results; a skipped check printing VERIFY OK; an unreachable diagnostic under set -euo pipefail). FUSION now has 6 per-code skills (FRESCO, COLOSS, CCFULL, GSM, TALYS, pikoe)

- [x] 2026-07-20: Fifth per-code skill: TALYS (skills/talys/, Koning + Hilaire + Goriely, EPJA 59, 131 (2023), MIT). install/run/verify scripts; 5 clean-room sample benchmarks (n-Nb093-14MeV-full, n-Sn120-omp-KD03, n-Th232-fis-wkb, n-Os187-astro-ng, p-Mo100-medical) reproducing 1415 of 1438 distributed reference files byte for byte, the remaining 18 data files to ~6 sig figs on 4633 observables (the precision of TALYS's own output format). Three traps found and handled, all of which produce confident-looking wrong results: locale-collation source glob dropping 13 files without LC_ALL=C; character(len=132) path cap forcing a short install root; and **exit status 0 on fatal error** (the CCFULL false-positive trap in a new guise). Citations verified live against CrossRef + INSPIRE; input reference written from the shipped 890-page manual, not from memory. FUSION now has 5 per-code skills (FRESCO, COLOSS, CCFULL, GSM, TALYS)
- [x] 2026-07-20: Fourth per-code skill: GSM (skills/gsm/, Michel + Ploszajczak, LNP 983 Springer 2021, github.com/GSMUTNSR/book_codes, AFL v3.0). Covers the whole Berggren/Gamow stack: one-body Gamow states, pole and antibound searches, complex-scaling widths, two-body and many-body GSM, GSM-CC. install_gsm.sh clones + unpacks the shipped zips + patches + builds any of 10 targets; run_gsm.sh runs with a clean-room guard; compare_gsm.sh does magnitude-split numeric comparison (a plain diff is wrong here: shipped references are GSM-1.0, sources are GSM-2.0, print format changed). Three clean-room benchmarks vs the book's own exercise outputs: Ch2 Ex XV neutron resonance 11 sig figs / 35 observables, Ch3 Ex XIII Berggren diagonalization in a foreign basis 9 sig figs / 562, Ch5 Ex II 18O many-body 8 sig figs / 2539 (ground state 12 figs). Found + patched an upstream infinite recursion in numlib `finite()` (see devlog). FUSION now has 4 per-code skills (FRESCO, COLOSS, CCFULL, GSM)
- [x] 2026-07-20: Phase 2 per-code skills, two shipped (both clean-room verified from public source). COLOSS (skills/coloss/, CPC 2025, Lei-eligible): builds from public repo (make + bundled C++ Coulomb lib + LAPACK), FRESCO cross-check n+40Ca sigma_R 1157.5 vs 1157.7 mb (4 sig figs) + complex-scaling theta-invariance 5 sig figs; radius convention (target-only At^1/3) documented. CCFULL (skills/ccfull/, Hagino CPC 1999, community): fetch+build from Kyoto page (FORTRAN77, gfortran -std=legacy), 16O+144Sm fusion reproduces reference barrier + sub-barrier excitation function exactly, tail 4-5 sig figs; caught+documented the interactive-stdin quirk. FUSION now has 3 per-code skills (FRESCO, COLOSS, CCFULL)
- [x] 2026-07-20: 108 topic-page Landscape syntheses written by DeepSeek (grounded in each topic's top-15 cited papers' abstracts), 108/108 topic pages, ~217k/40k tokens (pennies), 0 em-dash. Quality spot-checked (breakup, shell-model, astro all accurate)

- [x] 2026-07-20: Semantic layer (L3) COMPLETE. All 54,378 citing papers classified, 727,841 edges typed. Final distribution: background 485,740 (67%, dropped), uses 187,742, compares 20,767, contrasts 17,505 (2.4%), extends 11,361, applies 4,726; ~242k non-background kept (33%). `## Related work` injected into 44,891 pages (bidirectional). Claude QC: 0 fallbacks, contrasts spot-check all genuine disagreements, sample pages sensible. Phase 3 (knowledge base) core fully done: 4 layers live (concepts, citations, semantic relations, all on 61,059 pages). Bug fixed to get here: the last 3,181 recent/backfill papers hung the batch on KINGSTON .tex reads (no timeout on file I/O); fixed with --no-context (skip .tex for backfill papers, classify on titles+abstracts) + 60s API timeout + background-fallback-marks-done (see devlog 2026-07-20)
- [x] 2026-07-15: INSPIRE citation backfill DONE. Fetched references API for all 27,682 zero-outgoing-edge papers (27,630 hit INSPIRE, 99.8%, 52 not indexed), mapped to corpus ids, added 376,503 new in-corpus edges. Coverage: corpus-wide 81% -> 96%, newest-500 papers 27% -> 87% (the recent-preprint gap closed). Merged into citations.tsv (351k -> 728k edges), re-injected citation sections (36,980 pages updated). Verified 2604.11226 gains 8 correct edges. Zero LLM tokens. Tonight's semantic run now covers 54,378 citing papers (was 33,377)
- [x] 2026-07-15: Semantic layer (L3) built and validated. DeepSeek v4-pro on deepseek-semantic.md brief: citation-context extractor + relation classifier (6 types) + bidirectional page injector; 200-paper sample. Claude cross-review: hard gate 1711.07540->1511.03214 = extends verified against raw .tex; 3 contrasts edges independently confirmed real; type distribution healthy (76.6% background discarded, contrasts 2.6%). Fixed one self-flagged overfire (target-as-evidence-against-third-party now correctly `uses` not `contrasts`), re-verified. Merged kb-semantic; full 33,377-paper run armed off-peak
- [x] 2026-07-15: Phase 3 CORE COMPLETE. Full corpus digested (61,059/61,059 pages over 4 off-peak windows, zero API failures, ~$120 total). Cross-link layers built by DeepSeek v4-pro on the deepseek-crosslink.md brief and Claude cross-reviewed: L0 physh-nuclear.yaml (180 concepts, tiered matching, negative filters), L1 classification 38,824 papers tagged + 109 topic pages + index tree, L2 citation graph 351,338 in-corpus edges (calibration edge 1711.07540->1511.03214 verified; Tier A edge spot-checked in raw .tex; 81.2% of papers have >= 1 edge). All 61,059 pages carry concepts frontmatter + In-corpus citations sections. Final QC: 13 defect pages regenerated, 0 em-dash, 0 malformed
- [x] 2026-07-14: Phase 2 first per-code skill landed in-repo: `skills/fresco/` real self-contained copy (establishes skills/ layer) + binary auto-install (install_fresco.sh clones+builds I-Thompson/fresco when ~/bin/PATH lack it, run_fresco.sh auto-wires); gfortran build reproduces B1-elastic sigma_R = 1575.17495 (ref 1575.175). Codex cross-checked; caught the cp -R symlink trap (see devlog 2026-07-14), applied fixes #1/#12/#4/#16/#17
- [x] 2026-07-09: Phase 0 quality gate, all items: opencode 1.17.15 + DeepSeek/Qwen keys + 36 skills symlinked (pre-existing); 3 real-case tests vs Claude references (litsearch exact BibTeX; fresco 4-5 sig figs; prc-writing 10/10 verified citations); user verdict = proceed (phase0/report.md)
- [x] 2026-07-09: Phase 1 fork created: github.com/jinleiphys/fusion-core @ v1.17.16, default branch fusion-brand, dev = pristine upstream mirror
- [x] 2026-07-09: Phase 1 brand assets mapped; TUI logo patched ("FUSion" block glyphs + compact "fu" pulse logo, 3 glyph iterations with user screenshots); MIT notice untouched
- [x] 2026-07-09: Phase 1 CI weekly rebase (fusion-rebase.yml, Mondays 02:00 UTC) verified green on manual dispatch (run 29000283160)
- [x] 2026-07-09: Phase 3 design settled then revised same day: PhySH v2.8.0 (CC0) taxonomy, Nuclear subtree 176 concepts; wiki form changed from DB-rendered + digest-on-touch to pre-generated md (user decision; see devlog)
- [x] 2026-07-09: Phase 3 pilot: 500-paper digestion by DeepSeek via deepseek.md brief (500/500, $1.74, 9.5 min); Claude cross-review passed (structure 0 violations, no fabrication on calibration set, one new finding: raw cite-key leakage, fixed in template v2); merged kb-pilot into main (fa5ee32)
- [x] 2026-07-09: Template v2 (cite-key resolution, numeric-bullet rule, review-paper branch, reference-stripping before truncation) smoke-tested on 1812.11248; full-corpus list 61,059; off-peak launcher armed under caffeinate for tonight 00:30 (~$109 off-peak; user topping up 900 RMB)
