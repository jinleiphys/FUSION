Reading additional input from stdin...
OpenAI Codex v0.145.0
--------
workdir: /Users/jinlei/Desktop/code/FUSION
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: xhigh
reasoning summaries: none
session id: 019f8e0d-7584-7ca1-b1a9-1d6a829edabf
--------
user
RE-VERIFICATION of skills/smash/ at HEAD 842cbabd5. You found 19 defects earlier; they are now fixed. A working build is at ~/.cache/fusion/smash.

PART A: re-test each of your 19 original findings, state FIXED / NOT FIXED / PARTIAL with evidence. They were: (1) negative seeds other than -1 accepted as pinned; (2) initial ctest exit status discarded; (3) mixed failure types (Failed + Timeout) bypass; (4) retry accepted on exit code alone (No tests were found); (5) pre-set SMASH/SMASH_BUILD/SMASH_ROOT bypasses identity checks; (6) SMASH_EXPECTED_TESTS makes 'exactly 104' false; (7) run_smash accepts forged/garbage OSCAR output; (8) baryon_number wrong for 5-8 digit resonances like 12112; (9) any comment containing ' end' counted as an event; (10) conservation only checked on the total, so per-event violations cancel; (11) shipped examples run with wrong auxiliary tables (missing -p/-d); (12) wrapper hard-requires particle_lists.oscar; (13) selftest does not exercise the guards; (14) potentials.cc/random.cc are not the only self-seeded tests; (15) output format list wrong (Root not ROOT, missing formats); (16) collisions_binary.bin does not exist, no SMASH.log; (17) stale event grammar; (18) loose numeric validation; (19) species-min and B/Q overstated.

PART B: attack the NEW code for defects introduced by the fixes. Especially: the new identity check in verify_smash.sh (can it be spoofed? does it break a legitimate user build?), the rewritten baryon_number, the new per-event parsing in check_conservation_smash.py (does it handle real SMASH output with ensembles, Only_Final:No intermediate blocks, and the 'in' event lines?), the new auto-detection of particles.txt/decaymodes.txt (can it stage the WRONG tables?), and the strict OSCAR structural checks in run_smash.sh (do they reject any LEGITIMATE SMASH output, e.g. from a Box modus or with Only_Final:No?).

Run scripts/selftest_smash.sh yourself and report. Do NOT rewrite files. Concise: table for A, numbered list for B. Under 180 lines.
codex
I’m using the `skill-iterate` protocol for this adversarial re-verification. I’ll keep the repository read-only, run the supplied self-test against the cached build, then probe each old guard and the newly rewritten parsing paths.
exec
/bin/zsh -lc "sed -n '1,240p' /Users/jinlei/Desktop/claude_skills/skills/skill-iterate/SKILL.md" in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
---
name: skill-iterate
description: >-
  Iterate and improve skills with the Read, Test, Reflect, Write loop. Use to optimize skill descriptions, fix failing skills, run cc_skill_test, verify evals, check cross-skill consistency, or improve skill quality. Triggers: 迭代skill, 改进skill, 优化skill, skill不好用.
---

# Skill 自动迭代

通过 Claude Code 驱动 skill 的自动迭代改进。本 skill 针对学术写作与研究工作流的 skill 生态进行了专门优化。

## 本 Skill 自带的脚本

`scripts/` 目录下包含两个工具。调用时，先用 Glob 或文件路径定位本 skill 的目录位置，然后用该路径拼接 `scripts/` 下的脚本名。

| 脚本 | 用途 |
|------|------|
| `scripts/cc_skill_test.py` | 结构校验、脚本语法、引用完整性、描述质量、学术专项检查、风格检查、跨 skill 一致性 |
| `scripts/cc_iterate.py` | 迭代 workspace 初始化、快照、diff、历史记录 |

调用示例（假设本 skill 在 `~/Desktop/claude_skills/skills/skill-iterate/`）：

```bash
# 测试单个 skill
python3 ~/Desktop/claude_skills/skills/skill-iterate/scripts/cc_skill_test.py <目标skill路径>

# 测试某个目录下的所有 skill（含跨 skill 一致性检查）
python3 ~/Desktop/claude_skills/skills/skill-iterate/scripts/cc_skill_test.py --all --path <skills目录>

# 初始化迭代 workspace
python3 ~/Desktop/claude_skills/skills/skill-iterate/scripts/cc_iterate.py init <目标skill路径>

# 记录迭代结果（迭代号自动递增，也可用 -n 手动指定）
python3 ~/Desktop/claude_skills/skills/skill-iterate/scripts/cc_iterate.py record <workspace路径> -s pass/fail --notes "修改内容"

# 查看迭代历史
python3 ~/Desktop/claude_skills/skills/skill-iterate/scripts/cc_iterate.py history <workspace路径>
```

## 用户的 Skill 生态

用户的 skill 集中在学术研究工作流，迭代时需要理解各 skill 之间的协作关系：

| 类别 | Skills | 关键特征 |
|------|--------|----------|
| 论文写作 | prl-writing, prc-writing, jpg-writing | 起手 wiki-first: Skill(literature-wiki) 定框架 → Skill(literature-search) 出已核 BibTeX; LaTeX/REVTeX/IOP 格式 |
| 综述与文献 | review-writing, literature-wiki, literature-search | 两层栈: literature-wiki(已读语料, FIRST) → literature-search(新文献发现); rag-review 已废弃(AnythingLLM 仅作手动 fallback) |
| 研究规划 | research-planning | 数学背景, 技术实现, 验证计划, 发表路线图 |
| 学术展示 | slidev-talk | Slidev Markdown 幻灯片, 叙事结构, 会议报告 |
| 教材写作 | book-writing | 中文段落式叙述, Jupyter Book, 双线叙事 |

迭代某个 skill 时，要注意它与同类 skill 的一致性。例如 prl-writing 和 prc-writing 应该共享相同的 INSPIRE-HEP 检索流程。

## 写作风格规范

用户对输出风格有明确偏好，迭代 skill 时必须遵守：

1. **产出物禁用破折号**：用户使用 skill 生成的所有内容（LaTeX 论文、slides、文献综述、教材等）中不能出现破折号。中文不用中文破折号（——），英文用逗号替代 em-dash。SKILL.md 指令文本本身可以使用破折号，但必须包含一条明确的规则告诉 Claude 生成的产出物不能有破折号。
2. **中文用段落式叙述**：参考 book-writing skill 的风格，避免简单的项目符号堆砌。
3. **解释为什么**：用推理引导模型行为，而不是堆砌 MUST/NEVER 大写指令。

## 核心工作流: Read, Test, Reflect, Write

**重要**：在开始迭代前，先用 `cc_iterate.py init` 初始化 workspace，确保历史版本可追溯。每轮修改后立即 `record`，不要等到最后。

### Step 0: INIT（每个 skill 只需一次）

```bash
python3 <skill-iterate路径>/scripts/cc_iterate.py init <目标skill路径>
```

这会创建 `<skill-name>-workspace/` 目录，保存初始版本快照。

### Step 1: READ

根据用户指定的 skill 名称或路径，找到目标 skill 目录。常见位置：
- `/Users/jinlei/Desktop/claude_skills/skills/<name>/`（用户自定义 skill）
- `/Users/jinlei/Desktop/claude_skills/Memento-Skills/builtin/skills/<name>/`（Memento 内置 skill）

读取 SKILL.md 的完整内容，理解 frontmatter、指令正文、scripts/、references/ 的结构。

### Step 2: TEST

运行本 skill 自带的测试脚本：

```bash
python3 $SKILL_DIR/scripts/cc_skill_test.py <目标skill路径>
```

测试脚本包含以下检查维度：

| 维度 | 检查内容 |
|------|----------|
| structure | SKILL.md 存在, frontmatter 格式, name/description 规范 |
| scripts | Python 语法, 安全性（eval/exec） |
| references | SKILL.md 中引用的文件是否存在 |
| description | 触发关键词, 长度合理性 |
| style | 破折号使用, 写作风格 |
| academic | 论文结构, 文献检索, LaTeX, RAG 集成, 叙事结构 |
| evals | 测试用例是否存在和格式正确 |

### Step 3: REFLECT

分析测试结果，按优先级排序问题：

| 优先级 | 问题类型 | 修复方向 |
|--------|----------|----------|
| P0 | FAIL（结构/语法错误） | 立即修复 |
| P1 | 学术专项检查 WARN | 补充缺失的学术要素（文献检索、论文结构、LaTeX） |
| P2 | 风格检查 WARN | 消除破折号，改善叙述风格 |
| P3 | 引用/描述 WARN | 修正引用路径，优化描述 |

对于学术 skill，还需要检查：
- 每个 writing skill 是否起手就发真实的 `Skill(skill="literature-wiki", ...)` 调用（wiki-first 框架），再走 `Skill(skill="literature-search", ...)` 出已核 BibTeX；两者都必须是真实工具调用而非 prose
- literature-search 是否有完整的 INSPIRE-HEP / arXiv / CrossRef 检索-核验流程
- 跨 skill 的文献检索口径是否一致（统一 wiki-first 两层栈；rag-review 已废弃，不应再被任何 writing skill 引用为活跃依赖）
- 展示 skill 是否有从论文到幻灯片的转化流程

### Step 4: WRITE

根据分析结果修改 skill，遵循原则：
- 从反馈中泛化，不针对特定测试用例过拟合
- 保持 prompt 精简，删除没有实际作用的指令
- 重复出现的辅助代码提取到 scripts/
- SKILL.md 正文控制在 500 行以内
- 用推理和解释引导模型行为

### Step 5: RECORD + VERIFY

修改后，先用 `cc_iterate.py record` 保存本轮快照（迭代号自动递增），再重新运行测试确认修复有效。

```bash
python3 <skill-iterate路径>/scripts/cc_iterate.py record <workspace路径> -s pass --notes "本轮修改摘要"
python3 <skill-iterate路径>/scripts/cc_skill_test.py <目标skill路径>
```

如果测试失败，用 `-s fail` 记录：

```bash
python3 <skill-iterate路径>/scripts/cc_iterate.py record <workspace路径> -s fail --notes "失败原因"
```

### Step 6: 循环或完成

- 所有测试通过，完成并输出变更摘要
- 仍有失败，回到 Step 3 继续
- 最多 5 轮迭代
- 用 `cc_iterate.py history` 查看完整迭代过程

## 迭代跟踪

用迭代跟踪器记录每轮变更，保留快照和 diff：

```bash
# 初始化 workspace（首次迭代前）
python3 $SKILL_DIR/scripts/cc_iterate.py init <目标skill路径>

# 每轮迭代后记录
python3 $SKILL_DIR/scripts/cc_iterate.py record <workspace> -n 1 -s pass/fail --notes "修改了什么"

# 查看历史
python3 $SKILL_DIR/scripts/cc_iterate.py history <workspace>

# 查看某轮 diff
python3 $SKILL_DIR/scripts/cc_iterate.py diff <workspace> -n 1
```

## Skill 编写规范

SKILL.md 必须有 YAML frontmatter，包含 `name`（kebab-case，最长 64 字符）和 `description`（最长 1024 字符，明确描述触发条件）。

目录结构：

```
skill-name/
├── SKILL.md          # 必需
├── scripts/          # 可执行脚本（可选）
├── references/       # 参考文档（可选）
├── assets/           # 模板资源（可选）
└── evals/
    └── evals.json    # 测试用例（推荐）
```

  → Inspect HEAD, SMASH skill files, and the earlier-finding guard changes
  • Run selftest_smash.sh and targeted negative/legitimate-output probes
  • Classify all 19 findings and report new defects concisely
exec
/bin/zsh -lc 'git rev-parse HEAD && git status --short && rg --files skills/smash | sort' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 167ms:
skills/smash/SKILL.md
skills/smash/examples/README.md
skills/smash/references/failure-modes.md
skills/smash/references/input-format.md
skills/smash/references/output-format.md
skills/smash/references/verification.md
skills/smash/scripts/__pycache__/check_conservation_smash.cpython-314.pyc
skills/smash/scripts/check_conservation_smash.py
skills/smash/scripts/install_smash.sh
skills/smash/scripts/run_smash.sh
skills/smash/scripts/selftest_smash.sh
skills/smash/scripts/verify_smash.sh

codex
The repository is currently at `04c202c6`, not `842cbabd5`. I’m checking whether the SMASH tree is identical to the requested commit before testing, so the verdict is tied to the right code.
exec
/bin/zsh -lc "git show -s --format='%H %ci %s' 842cbabd5 && git show -s --format='%H %ci %s' HEAD && git diff --stat 842cbabd5..HEAD -- skills/smash && git diff --name-status 842cbabd5..HEAD -- skills/smash && git status --porcelain=v1 --untracked-files=all -- skills/smash" in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
842cbabd5ad21a9ea22645fc8302a1cdb1a2849d 2026-07-23 16:17:36 +0800 SMASH: apply the adversarial pass, 19 defects, all fixed
04c202c694ea0a5053a9ebf6d62c566e59e5b8ca 2026-07-23 16:18:13 +0800 devlog: the SMASH baryon-number rule, wrong twice in the same way

exec
/bin/zsh -lc "wc -l skills/smash/SKILL.md skills/smash/scripts/* skills/smash/references/* skills/smash/examples/* && sed -n '1,260p' skills/smash/SKILL.md && sed -n '1,260p' skills/smash/scripts/verify_smash.sh && sed -n '1,260p' skills/smash/scripts/run_smash.sh" in /Users/jinlei/Desktop/code/FUSION
 exited 1 in 0ms:
wc: skills/smash/scripts/__pycache__: read: Is a directory
     144 skills/smash/SKILL.md
     233 skills/smash/scripts/check_conservation_smash.py
     235 skills/smash/scripts/install_smash.sh
     212 skills/smash/scripts/run_smash.sh
     305 skills/smash/scripts/selftest_smash.sh
     235 skills/smash/scripts/verify_smash.sh
     134 skills/smash/references/failure-modes.md
     114 skills/smash/references/input-format.md
      85 skills/smash/references/output-format.md
     184 skills/smash/references/verification.md
      62 skills/smash/examples/README.md
    1943 total

exec
/bin/zsh -lc "sed -n '1,220p' skills/smash/SKILL.md
sed -n '1,280p' skills/smash/scripts/verify_smash.sh
sed -n '1,250p' skills/smash/scripts/run_smash.sh
sed -n '1,280p' skills/smash/scripts/check_conservation_smash.py" in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
---
name: smash
description: >-
  Drive SMASH (Simulating Many Accelerated Strongly-interacting Hadrons), the hadronic transport approach of J. Weil et al. (Phys. Rev. C 94, 054905 (2016)), release SMASH-3.3. Solve the relativistic Boltzmann equation for a hadron gas: heavy-ion collisions from a few hundred MeV to collider energies, thermal boxes for equilibration and detailed-balance studies, expanding spheres, and replay of externally supplied particle lists. Produces per-particle OSCAR/binary output for multiplicities, spectra, flow, dilepton and photon rates, with optional mean-field potentials and string fragmentation through Pythia. Use for 跑SMASH, 强子输运, hadronic transport, heavy-ion collision, 重离子碰撞, Boltzmann transport, cascade, Au+Au, particle multiplicity, flow, freeze-out, OSCAR2013, afterburner, dilepton, box equilibration, kinetic theory.
---

# Driving SMASH

SMASH solves the relativistic Boltzmann equation for hadrons: it propagates
particles, samples their collisions and decays, and writes the resulting particle
lists. It is used both as a standalone transport model at low and intermediate
beam energies and as the hadronic afterburner behind a hydrodynamic stage at
collider energies.

C++17, CMake. Needs GSL, Eigen 3.x, and Pythia exactly 8.316.

## Prime rules (do not skip)

1. **Pin the random seed for anything you intend to check, and it must be
   NON-NEGATIVE.** SMASH treats ANY negative seed as "draw a fresh one", not just
   the `-1` every shipped configuration carries: `--seed -2` produced two
   different runs whose `config_used.yaml` still read `-2`. `run_smash.sh`
   refuses every negative seed unless you pass `--allow-random-seed`. With a pinned seed the output is byte-identical between
   two runs of the same build.
2. **Do not compare multiplicities across machines.** SMASH is Monte Carlo, and
   transport amplifies floating-point differences into different collision
   histories; measured, macOS and Linux differ by up to 25 per cent at the same
   seed. Anchor on baryon number and electric charge, checked per event, which
   are integers fixed by the initial nuclei: `scripts/check_conservation_smash.py`.
   They are a limited invariant, not proof that the physics is right.
3. **This is a TIER 1 skill: it reproduces SMASH's own 104-case test suite.**
   Two of those cases are non-deterministic by upstream construction
   (`potentials.cc` and `random.cc` seed themselves from `std::random_device`
   and then assert statistical quantities), so `verify_smash.sh` retries exactly
   those two by name, once, and treats every other failure as fatal. Do not
   widen that into "allow one failure". Two further files self-seed through
   `generate_63bit_seed()` and have not been seen to flake, so this skill does
   not claim the remaining cases are deterministic.
4. **The dependencies fail with messages that point away from the cause.** The
   Pythia URL in SMASH's own INSTALL.md 404s and the 404 page gets saved as a
   `.tgz`; Eigen 5 makes SMASH report "at least version 3.0 is required" when the
   problem is that your Eigen is too NEW. Read `references/failure-modes.md`
   before debugging a build by hand.
5. **`WARN Fpe : Failed to setup trap on pole error.` is harmless** and appears
   on every macOS run. Never grep logs case-insensitively for "error"; match
   SMASH's severity field.
6. **No em-dashes in any prose you write** (user's flat rule).

## Environment (auto-install)

`scripts/install_smash.sh` fetches and builds everything, then prints:

```
SMASH=<executable>
SMASH_ROOT=<repository root>
SMASH_BUILD=<build dir, where ctest runs>
SMASH_EIGEN3_ROOT=<Eigen 3.4 prefix>
SMASH_GSL_PREFIX=<GSL prefix>
SMASH_PYTHIA_PREFIX=<Pythia prefix>
```

The last three matter beyond the build: the `usage_of_SMASH_as_library` test
spawns a fresh cmake that inherits no cache variables, so those prefixes must
reach it through the environment. `verify_smash.sh` does that for you.

First run takes roughly 20 minutes, almost all of it compiling Pythia and SMASH,
and is cached afterwards. Needs `git`, `cmake` 3.16+, a C++17 compiler, `curl`
and `python3`. Overrides: `SMASH_ROOT_DIR`, `SMASH_PIN`, `SMASH_JOBS`,
`SMASH_GSL_PREFIX`, `SMASH_PYTHIA_URL`, `SMASH_EIGEN_VERSION`.

## Running

```bash
scripts/run_smash.sh --config <config.yaml> --outdir /tmp/run1 \
  --seed 20260723 --nevents 2 --end-time 20.0
```

Prints `RESULT_DIR=` and `RESULT_OSCAR=`. It copies the configuration it actually
used into the output directory, then asserts a zero exit, an OSCAR2013 header,
records with the column count that header declares, matched event-start and
event-end markers whose number equals `Nevents` (a run can stop early and still
exit 0), no NaN, and no `ERROR`-severity log line.

It also stages a shipped example's own `particles.txt` and `decaymodes.txt` when
they sit beside the configuration. SMASH does NOT pick those up implicitly, so
without `-p/-d` the box and sphere examples run against the default tables and
quietly compute something other than the example you asked for.

## Verifying

```bash
scripts/verify_smash.sh              # test suite + seeded anchor, about 8 min
scripts/verify_smash.sh --tests-only
scripts/selftest_smash.sh            # harness only, seconds, no build needed
```

## Writing an input

Field reference taken from the source: `references/input-format.md`. The shape:

```yaml
General:
    Modus:      Collider      # or Box, Sphere, List, ListBox
    Delta_Time: 0.1
    End_Time:   200.0
    Randomseed: 20260723      # PIN THIS
    Nevents:    1
Output:
    Particles:
        Format: ["Oscar2013"]
Modi:
    Collider:
        Projectile: {Particles: {2212: 79, 2112: 118}}   # Au197
        Target:     {Particles: {2212: 79, 2112: 118}}
        E_Kin: 1.23                                       # GeV per nucleon
```

Nuclei are given as PDG-code counts, so the expected baryon number and charge of
the whole run follow directly from them, which is what makes the conservation
anchor possible.

## Reading the output

`references/output-format.md`. `particle_lists.oscar` has twelve columns named on
its first line (parse that line, it changes with the requested content), events
delimited by `# event N ensemble E out COUNT` and
`# event N ensemble E end ...`. The `ensemble` field is part of the real grammar;
a pattern written without it matches nothing.

## Benchmark

| stage | what | result |
|---|---|---|
| test suite | SMASH's own 104 ctest cases | reproduced; 104/104 first attempt on Linux, with the two self-seeded cases retried once when they flake |
| anchor | seeded Au+Au, 2 events, E_kin 1.23 GeV/nucleon, 20 fm/c | baryon number 788 and charge 316, EXACT per event (394 and 158 each) |

The anchor's multiplicities (450 n, 336 p, 76 pi-, 65 pi0, 57 pi+, plus eta, K0,
Lambda, Sigma-) are recorded in `references/verification.md` as a same-build
reproducibility check only, never as a cross-platform reference.

## Failure modes

`references/failure-modes.md`, nine of them, starting with the dead Pythia URL
and the Eigen 5 message that says the opposite of the truth.
#!/bin/bash
# verify_smash.sh
#
# Reproduce SMASH's own test suite, then run a seeded physics anchor and check
# the conservation laws on it.
#
#   verify_smash.sh              both stages (about 8 minutes)
#   verify_smash.sh --tests-only
#   verify_smash.sh --anchor-only
#
# STAGE 1, the tier-1 evidence: SMASH ships 104 ctest cases and this reproduces
# them. Two of them are NOT deterministic, by upstream construction rather than
# by platform: src/tests/potentials.cc and src/tests/random.cc open with
#
#     TEST(set_random_seed) { std::random_device rd; random::set_seed(rd()); }
#
# and then assert statistical quantities to fixed tolerances, so they fail
# occasionally on a perfectly good build. Measured on macOS: `potentials` passed
# 4 of 5 consecutive standalone runs.
#
# The policy that follows is deliberately narrow. All 102 other tests must pass
# on the FIRST attempt; a single failure among them fails verification. Only
# those two named tests may be retried, once. A statistical fluke passes with a
# fresh seed; a genuinely broken build fails again. Do not widen this into
# "allow one failure", which is how a real regression walks through.
#
# STAGE 2: a seeded Au+Au run whose baryon number and electric charge are
# checked against the exact expectation. Those are conserved by the transport
# whatever the seed or platform, so they falsify a broken build in a way that
# comparing Monte Carlo multiplicities cannot.
#
# SMASH is GPL-3.0-or-later; see install_smash.sh.
set -euo pipefail

HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DO_TESTS=1
DO_ANCHOR=1
# The only tests permitted a retry, because they seed themselves from
# std::random_device. Keep this list minimal and justified.
FLAKY_TESTS="potentials random"
# 104 is the count for the pinned SMASH-3.3. An override is allowed for a
# deliberately different pin, but then the run is NOT tier-1 certifying and says
# so, because "reproduces the shipped suite" means the shipped suite.
EXPECTED_TESTS_PINNED=104
EXPECTED_TESTS="${SMASH_EXPECTED_TESTS:-$EXPECTED_TESTS_PINNED}"

log () { echo "verify_smash: $*" >&2; }
die () { log "$*"; exit 1; }

while [ $# -gt 0 ]; do
  case "$1" in
    --tests-only)  DO_ANCHOR=0; shift ;;
    --anchor-only) DO_TESTS=0; shift ;;
    -h|--help)     sed -n '2,30p' "$0"; exit 0 ;;
    *) die "unknown argument '$1'" ;;
  esac
done

if [ -n "${SMASH:-}" ] && [ -n "${SMASH_BUILD:-}" ] && [ -n "${SMASH_ROOT:-}" ]; then
  BIN="$SMASH"; BUILD="$SMASH_BUILD"; SRCROOT="$SMASH_ROOT"; EIGEN="${SMASH_EIGEN3_ROOT:-}"
  GSL="${SMASH_GSL_PREFIX:-}"; PYPFX="${SMASH_PYTHIA_PREFIX:-}"
  PRESET=1
else
  PRESET=0
  OUT="$("$HERE/install_smash.sh")" || die "install_smash.sh failed"
  BIN="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH=//p')"
  BUILD="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_BUILD=//p')"
  SRCROOT="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_ROOT=//p')"
  EIGEN="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_EIGEN3_ROOT=//p')"
  GSL="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_GSL_PREFIX=//p')"
  PYPFX="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_PYTHIA_PREFIX=//p')"
fi
[ -x "$BIN" ] || die "no usable SMASH executable"
[ -d "$BUILD" ] || die "no build directory at '$BUILD'"

# Pre-set SMASH/SMASH_BUILD/SMASH_ROOT used to skip every identity check, so a
# fake build directory next to the real binary certified tier 1. Whatever the
# route, the tree must be the pinned commit, the build must carry the
# installer's stamp, and the binary must be the one that stamp describes.
PIN="${SMASH_PIN:-d1a1c6cf0a0002ee064eec1b929b9a7c14b3d5bc}"
check_identity () {
  local head stamp
  [ -d "$SRCROOT/.git" ] || { log "'$SRCROOT' is not a git clone, so its commit cannot be established"; return 1; }
  head="$(cd "$SRCROOT" && git rev-parse HEAD 2>/dev/null || echo none)"
  [ "$head" = "$PIN" ] || { log "source tree is at $head, not the pinned $PIN"; return 1; }
  ( cd "$SRCROOT" && git diff --quiet HEAD 2>/dev/null ) || { log "source tree has uncommitted modifications"; return 1; }
  stamp="$BUILD/.fusion_build_stamp"
  [ -f "$stamp" ] || { log "no build stamp at $stamp; this build was not produced by install_smash.sh"; return 1; }
  local want got
  want="$(sed -n 2p "$stamp")"
  got="$(shasum -a 256 "$BIN" 2>/dev/null | cut -d' ' -f1 || echo none)"
  [ "$want" = "$got" ] || { log "the binary does not match the build stamp (stamped $want, found $got)"; return 1; }
  case "$BIN" in
    "$BUILD"/*) : ;;
    *) log "the binary '$BIN' does not live inside the build directory '$BUILD'"; return 1 ;;
  esac
  return 0
}
if ! check_identity; then
  die "refusing to certify: the build does not verifiably come from the pinned source (see above)"
fi
log "identity OK: pinned commit, clean tree, stamped binary"

FAILED=0

# The usage_of_SMASH_as_library test spawns a FRESH cmake for the library
# example, and that child inherits NONE of this build's -D cache variables, so
# every hint has to reach it through the ENVIRONMENT. Three were needed, each
# discovered by a different failure:
#   EIGEN3_ROOT     macOS, where the system Eigen is a 5.x it cannot parse
#   GSL hints       a cluster where GSL lives only inside a conda prefix
#   LD_LIBRARY_PATH the example BUILDS and then fails to RUN, because Pythia is
#                   installed under a custom prefix with no rpath
run_ctest () {
  ( cd "$BUILD" \
    && EIGEN3_ROOT="$EIGEN" \
       GSL_ROOT_DIR="$GSL" \
       PKG_CONFIG_PATH="${GSL:+$GSL/lib/pkgconfig:}${PKG_CONFIG_PATH:-}" \
       CMAKE_PREFIX_PATH="${GSL:+$GSL:}${CMAKE_PREFIX_PATH:-}" \
       LD_LIBRARY_PATH="${PYPFX:+$PYPFX/lib:}${GSL:+$GSL/lib:}${LD_LIBRARY_PATH:-}" \
       DYLD_LIBRARY_PATH="${PYPFX:+$PYPFX/lib:}${GSL:+$GSL/lib:}${DYLD_LIBRARY_PATH:-}" \
       ctest "$@" )
}

# ------------------------------------------------------------------ stage 1
if [ "$DO_TESTS" = "1" ]; then
  command -v ctest >/dev/null || die "ctest not found"
  LOG="$(mktemp)"
  log "running SMASH's own test suite (about 6 minutes)"
  set +e
  run_ctest -j"$( (command -v nproc >/dev/null && nproc) || sysctl -n hw.ncpu 2>/dev/null || echo 4 )" > "$LOG" 2>&1
  CTEST_RC=$?
  set -e

  TOTAL="$(sed -nE 's/.* tests passed, [0-9]+ tests failed out of ([0-9]+).*/\1/p' "$LOG" | tail -1)"
  NFAIL="$(sed -nE 's/.* tests passed, ([0-9]+) tests failed out of [0-9]+.*/\1/p' "$LOG" | tail -1)"
  [ -n "$TOTAL" ] || { log "could not parse a ctest summary; last lines:"; tail -10 "$LOG" >&2; rm -f "$LOG"; exit 1; }

  # The exit status is checked INDEPENDENTLY of the parsed text. A ctest that
  # printed a clean "104 of 104" summary and then exited nonzero used to be
  # accepted, because only the text was read.
  if [ "$CTEST_RC" -ne 0 ] && [ "${NFAIL:-0}" -eq 0 ]; then
    log "FAIL: ctest exited $CTEST_RC while reporting no failures; the summary and the status disagree"
    tail -10 "$LOG" >&2
    FAILED=1
  fi

  # An EXACT count, never "at least": a suite that silently skipped cases would
  # otherwise certify the build.
  if [ "$EXPECTED_TESTS" != "$EXPECTED_TESTS_PINNED" ]; then
    log "NOTE: SMASH_EXPECTED_TESTS=$EXPECTED_TESTS overrides the pinned $EXPECTED_TESTS_PINNED;"
    log "      this run is NOT a tier-1 certification of the pinned release"
  fi
  if [ "$TOTAL" != "$EXPECTED_TESTS" ]; then
    log "FAIL: ctest ran $TOTAL cases, expected exactly $EXPECTED_TESTS"
    log "      (set SMASH_EXPECTED_TESTS if you deliberately pinned a different SMASH version)"
    FAILED=1
  fi

  if [ "${NFAIL:-0}" -eq 0 ]; then
    log "test suite: $TOTAL of $TOTAL passed on the first attempt"
  else
    # Parse EVERY ctest failure status, not only "(Failed)": a case that timed
    # out or was not run is still a failure, and matching only "(Failed)" let a
    # real regression through while the one self-seeded flake was retried.
    FAILING="$(sed -nE 's/^[[:space:]]+[0-9]+ - ([A-Za-z0-9_.-]+) \(([A-Za-z ]+)\).*/\1/p' "$LOG" | sort -u)"
    NPARSED="$(printf '%s\n' "$FAILING" | grep -c . || true)"
    # The number of names parsed must equal the number ctest reported, or
    # something was missed and the retry logic below is reasoning about a
    # different set than the one that actually failed.
    if [ "${NPARSED:-0}" -ne "${NFAIL:-0}" ]; then
      log "FAIL: ctest reported $NFAIL failures but $NPARSED names could be parsed; refusing to guess"
      sed -nE 's/^[[:space:]]+[0-9]+ - .*/&/p' "$LOG" | head -10 >&2
      FAILED=1
      FAILING=""
    fi
    for t in $FAILING; do
      case " $FLAKY_TESTS " in
        *" $t "*)
          log "'$t' failed; it seeds itself from std::random_device, so retrying it once"
          set +e
          run_ctest -R "^$t\$" > "$LOG.retry" 2>&1
          RC=$?
          set -e
          # Exit status alone is not enough: `ctest -R` that matches NOTHING
          # prints "No tests were found!!!" and exits 0, which used to be read
          # as a clean retry. Require exactly one test selected and passed.
          RTOTAL="$(sed -nE 's/.* tests passed, [0-9]+ tests failed out of ([0-9]+).*/\1/p' "$LOG.retry" | tail -1)"
          RFAIL="$(sed -nE 's/.* tests passed, ([0-9]+) tests failed out of [0-9]+.*/\1/p' "$LOG.retry" | tail -1)"
          if [ "$RC" -eq 0 ] && [ "${RTOTAL:-0}" = "1" ] && [ "${RFAIL:-1}" = "0" ]; then
            log "'$t' passed on retry (1 of 1), which is what a statistical fluke does"
          else
            log "FAIL: retry of '$t' did not cleanly pass exactly one test (rc=$RC, total=${RTOTAL:-none}, failed=${RFAIL:-none})"
            tail -15 "$LOG.retry" >&2
            FAILED=1
          fi
          rm -f "$LOG.retry"
          ;;
        *)
          log "FAIL: '$t' failed, and it is not one of the self-seeded tests ($FLAKY_TESTS)"
          FAILED=1
          ;;
      esac
    done
  fi
  rm -f "$LOG"
fi

# ------------------------------------------------------------------ stage 2
if [ "$DO_ANCHOR" = "1" ]; then
  CFG="$SRCROOT/input/config.yaml"
  [ -s "$CFG" ] || die "the shipped collider config is missing from '$CFG'"
  # Au+Au: 2 events x 2 nuclei x 197 nucleons = 788 baryons, x 79 protons = 316 e.
  WORK="$(mktemp -d)"
  log "running the seeded Au+Au anchor (2 events, 20 fm/c)"
  if ! SMASH="$BIN" "$HERE/run_smash.sh" --config "$CFG" --outdir "$WORK/run" \
        --seed 20260723 --nevents 2 --end-time 20.0 >/dev/null; then
    log "FAIL: the anchor run did not complete"
    FAILED=1
  elif python3 "$HERE/check_conservation_smash.py" "$WORK/run/out/particle_lists.oscar" \
        --baryons 788 --charge 316; then
    log "anchor: conservation laws hold exactly"
  else
    log "FAIL: the anchor run violates a conservation law"
    FAILED=1
  fi
  rm -rf "$WORK"
fi

if [ "$FAILED" = "0" ]; then
  echo "VERIFY OK"
  exit 0
fi
echo "VERIFY FAILED"
exit 1
#!/bin/bash
# run_smash.sh
#
# Run a SMASH configuration in an isolated directory and assert the output is
# usable. Usage:
#
#   run_smash.sh --config <config.yaml> [--outdir <dir>] [--seed <int>]
#                [--nevents <int>] [--end-time <fm/c>] [--allow-random-seed]
#
# --config     a SMASH YAML configuration. Copied into the output directory, so
#              the run records the input it actually used.
# --outdir     defaults to a fresh mktemp -d. Must be new or empty.
# --seed       overwrite General:Randomseed. SMASH's shipped configs use -1,
#              which draws a fresh seed per run and makes the output
#              irreproducible. A benchmark MUST pin the seed; run_smash.sh
#              therefore refuses -1 unless --allow-random-seed is given.
# --nevents, --end-time   convenience overrides for a shorter run.
#
# Prints RESULT_DIR= and RESULT_OSCAR= on the last two lines.
#
# SMASH is GPL-3.0-or-later; see install_smash.sh.
set -euo pipefail

HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CONFIG=""; OUTDIR=""; SEED=""; NEVENTS=""; END_TIME=""; ALLOW_RANDOM=0
PARTICLES=""; DECAYMODES=""; AUTO_TABLES=1

log () { echo "run_smash: $*" >&2; }
die () { log "$*"; exit 1; }

while [ $# -gt 0 ]; do
  case "$1" in
    --config)   CONFIG="${2:-}"; shift 2 ;;
    --outdir)   OUTDIR="${2:-}"; shift 2 ;;
    --seed)     SEED="${2:-}"; shift 2 ;;
    --nevents)  NEVENTS="${2:-}"; shift 2 ;;
    --end-time) END_TIME="${2:-}"; shift 2 ;;
    --particles)  PARTICLES="${2:-}"; shift 2 ;;
    --decaymodes) DECAYMODES="${2:-}"; shift 2 ;;
    --no-auto-tables) AUTO_TABLES=0; shift ;;
    --allow-random-seed) ALLOW_RANDOM=1; shift ;;
    -h|--help)  sed -n '2,22p' "$0"; exit 0 ;;
    *) die "unknown argument '$1'" ;;
  esac
done

[ -n "$CONFIG" ] || die "--config is required"
[ -f "$CONFIG" ] || die "config '$CONFIG' does not exist"
CONFIG="$(cd "$(dirname "$CONFIG")" && pwd)/$(basename "$CONFIG")"
grep -q "General:" "$CONFIG" || die "config '$CONFIG' has no 'General:' block; is it a SMASH configuration?"

# Numeric arguments must be numeric before they are spliced into YAML.
# Full-match validation. The earlier character-class test accepted '--', '1-2'
# and '1..2', which are not numbers even though they contain only permitted
# characters.
is_int ()  { printf '%s' "$1" | grep -qE '^-?[0-9]+$'; }
is_uint () { printf '%s' "$1" | grep -qE '^[0-9]+$'; }
is_num ()  { printf '%s' "$1" | grep -qE '^[0-9]+(\.[0-9]+)?$'; }

if [ -n "$SEED" ]; then
  is_int "$SEED" || die "--seed must be an integer, got '$SEED'"
  # SMASH treats EVERY negative seed as "draw a random one", not just -1: two
  # runs with --seed -2 produced seeds 8409242248972502135 and
  # 4845130125537222390 and different output, while config_used.yaml still read
  # -2 and looked pinned.
  if [ "$SEED" -lt 0 ] && [ "$ALLOW_RANDOM" = "0" ]; then
    die "--seed $SEED is negative, and SMASH treats any negative seed as random, so the run would be irreproducible. Use a non-negative seed, or --allow-random-seed."
  fi
fi
if [ -n "$NEVENTS" ]; then
  is_uint "$NEVENTS" || die "--nevents must be a non-negative integer, got '$NEVENTS'"
  [ "$NEVENTS" -ge 1 ] || die "--nevents must be at least 1"
fi
if [ -n "$END_TIME" ]; then
  is_num "$END_TIME" || die "--end-time must be a non-negative number, got '$END_TIME'"
fi

if [ -n "${SMASH:-}" ]; then
  BIN="$SMASH"
else
  INSTALL_OUT="$("$HERE/install_smash.sh")" || die "install_smash.sh failed"
  BIN="$(printf '%s\n' "$INSTALL_OUT" | sed -n 's/^SMASH=//p')"
fi
[ -x "$BIN" ] || die "no usable SMASH executable (SMASH='${SMASH:-unset}')"

if [ -z "$OUTDIR" ]; then
  OUTDIR="$(mktemp -d)"
else
  if [ -e "$OUTDIR" ]; then
    [ -d "$OUTDIR" ] || die "outdir '$OUTDIR' exists and is not a directory"
    [ -n "$(ls -A "$OUTDIR" 2>/dev/null)" ] && die "outdir '$OUTDIR' is not empty; give a fresh directory"
  else
    mkdir -p "$OUTDIR"
  fi
fi
OUTDIR="$(cd "$OUTDIR" && pwd -P)"

WORKCFG="$OUTDIR/config_used.yaml"
cp "$CONFIG" "$WORKCFG"
apply () {  # apply <key> <value> to the YAML, in place, only if requested
  local key="$1" val="$2"
  [ -n "$val" ] || return 0
  grep -qE "^[[:space:]]*$key:" "$WORKCFG" || die "config has no '$key:' line to override"
  python3 - "$WORKCFG" "$key" "$val" <<'PY'
import re, sys
path, key, val = sys.argv[1], sys.argv[2], sys.argv[3]
text = open(path).read()
new, n = re.subn(rf'(?m)^(\s*{re.escape(key)}:\s*)\S+', rf'\g<1>{val}', text)
if n == 0:
    sys.exit(1)
open(path, 'w').write(new)
PY
}
apply Randomseed "$SEED"
apply Nevents "$NEVENTS"
apply End_Time "$END_TIME"

# Refuse an unpinned seed by default. SMASH's shipped configs carry -1, and a
# run made with it cannot be compared with anything, including itself.
# Read the seed that will ACTUALLY be used, from the config as it now stands,
# and judge it numerically. A literal test against "-1" missed every other
# negative value, which SMASH treats identically.
EFFECTIVE_SEED="$(grep -E "^[[:space:]]*Randomseed:" "$WORKCFG" | head -1 | sed -E 's/.*Randomseed:[[:space:]]*//' | tr -d '\r')"
if [ "$ALLOW_RANDOM" = "0" ]; then
  if ! is_int "$EFFECTIVE_SEED"; then
    die "the configuration's Randomseed is '$EFFECTIVE_SEED', which is not an integer; pin it with --seed"
  fi
  if [ "$EFFECTIVE_SEED" -lt 0 ]; then
    die "the configuration's Randomseed is $EFFECTIVE_SEED; SMASH treats ANY negative seed as random, so this run would be irreproducible. Pass --seed <non-negative>, or --allow-random-seed."
  fi
fi

# Several shipped examples (box, sphere, multi_particle_box) carry their OWN
# particles.txt and decaymodes.txt next to the configuration, and SMASH does not
# pick those up implicitly: without -p/-d it silently runs the DEFAULT tables, so
# the run succeeds and is simply not the example you asked for. Auto-detect them.
CFGDIR="$(dirname "$CONFIG")"
if [ "$AUTO_TABLES" = "1" ]; then
  [ -z "$PARTICLES" ]  && [ -f "$CFGDIR/particles.txt" ]   && PARTICLES="$CFGDIR/particles.txt"
  [ -z "$DECAYMODES" ] && [ -f "$CFGDIR/decaymodes.txt" ] && DECAYMODES="$CFGDIR/decaymodes.txt"
fi
EXTRA=()
if [ -n "$PARTICLES" ]; then
  [ -f "$PARTICLES" ] || die "--particles '$PARTICLES' does not exist"
  cp "$PARTICLES" "$OUTDIR/particles_used.txt"; EXTRA+=(-p "$PARTICLES")
  log "using particle table $PARTICLES"
fi
if [ -n "$DECAYMODES" ]; then
  [ -f "$DECAYMODES" ] || die "--decaymodes '$DECAYMODES' does not exist"
  cp "$DECAYMODES" "$OUTDIR/decaymodes_used.txt"; EXTRA+=(-d "$DECAYMODES")
  log "using decay table $DECAYMODES"
fi

log "running $(basename "$BIN") with seed $EFFECTIVE_SEED into $OUTDIR"
set +e
( cd "$CFGDIR" && "$BIN" -i "$WORKCFG" -o "$OUTDIR/out" ${EXTRA+"${EXTRA[@]}"} ) > "$OUTDIR/smash.log" 2> "$OUTDIR/stderr.txt"
STATUS=$?
set -e

if [ "$STATUS" -ne 0 ]; then
  log "SMASH exited with status $STATUS"
  tail -15 "$OUTDIR/stderr.txt" >&2 2>/dev/null
  tail -5 "$OUTDIR/smash.log" >&2 2>/dev/null
  exit 1
fi

OSCAR="$OUTDIR/out/particle_lists.oscar"
[ -s "$OSCAR" ] || die "no particle output at $OSCAR; the run produced nothing usable"

# Word-bounded: a bare substring search for "inf" would also match ordinary
# text, and the OSCAR header is text.
if grep -qiE "(^|[^a-z])(nan|inf|infinity)([^a-z]|$)" "$OSCAR"; then
  log "the particle output contains NaN or Inf"
  grep -niE "(^|[^a-z])(nan|inf|infinity)([^a-z]|$)" "$OSCAR" | head -3 >&2; exit 1
fi
# Match SMASH's log SEVERITY FIELD, not the word "error" anywhere in the text.
# SMASH lines look like "[15'04'57]  WARN   Fpe : Failed to setup trap on pole
# error." and a case-insensitive search for "error" flags that benign warning on
# every macOS run. Severity is the second field; aborts are matched literally.
ERROR_RE="^\[[^]]*\][[:space:]]+(ERROR|FATAL)\b|terminate called|Assertion .* failed"
if grep -qE "$ERROR_RE" "$OUTDIR/smash.log" "$OUTDIR/stderr.txt" 2>/dev/null; then
  log "the run logged an error:"; grep -E "$ERROR_RE" "$OUTDIR/smash.log" "$OUTDIR/stderr.txt" 2>/dev/null | head -5 >&2
  exit 1
fi

# Structural validation, not a line count. A stub that wrote a non-OSCAR header,
# two garbage records and two forged "# event ... end" comments used to be
# reported as a complete run.
head -1 "$OSCAR" | grep -qE '^#!OSCAR2013' \
  || die "the particle output does not start with an OSCAR2013 header"
NPART="$(grep -cvE '^#' "$OSCAR" || true)"
[ "${NPART:-0}" -gt 0 ] || die "the particle output has a header but no particles"

WANT_EVENTS="$(grep -E "^[[:space:]]*Nevents:" "$WORKCFG" | head -1 | sed -E 's/.*Nevents:[[:space:]]*//')"
# Match SMASH-3.3's real grammar, "# event N ensemble E out/end ...", and require
# the out and end blocks to pair up.
GOT_OUT="$(grep -cE '^# event [0-9]+ ensemble [0-9]+ out\b' "$OSCAR" || true)"
GOT_EVENTS="$(grep -cE '^# event [0-9]+ ensemble [0-9]+ end\b' "$OSCAR" || true)"
[ "${GOT_OUT:-0}" -eq "${GOT_EVENTS:-0}" ] \
  || die "the output has $GOT_OUT event-start and $GOT_EVENTS event-end markers; they must pair"
if [ -n "$WANT_EVENTS" ] && [ "${GOT_EVENTS:-0}" -ne "$WANT_EVENTS" ]; then
  die "requested $WANT_EVENTS events but the output completes $GOT_EVENTS of them; the run stopped early"
fi
# Every record must have the number of columns the header declares.
NCOL="$(head -1 "$OSCAR" | awk '{print NF-2}')"
BADCOL="$(awk -v n="$NCOL" '!/^#/ && NF != n' "$OSCAR" | wc -l | tr -d ' ')"
[ "${BADCOL:-0}" -eq 0 ] \
  || die "$BADCOL particle records do not have the $NCOL columns the header declares"

log "run complete: $GOT_EVENTS events, $NPART particle records"
echo "RESULT_DIR=$OUTDIR"
echo "RESULT_OSCAR=$OSCAR"
#!/usr/bin/env python3
"""Check exact conservation laws on a SMASH OSCAR2013 particle list.

Why this and not a multiplicity comparison. SMASH is a Monte Carlo transport
code: with a pinned seed it is bit-reproducible on one build, but the individual
multiplicities are not a portable reference and comparing them across builds
proves little. Baryon number and electric charge, by contrast, are conserved
EXACTLY by the transport, whatever the seed, the platform or the statistics. A
build that is subtly broken shows up here as an integer that does not match,
which no amount of statistical spread can excuse.

Conservation is checked PER EVENT, not only on the total. Two events with equal
and opposite violations sum to the right answer, so a total-only check can be
satisfied by output that is wrong twice over.

Usage:
  check_conservation_smash.py <particle_lists.oscar> --baryons N --charge Z
                              [--species-min 3]
--baryons and --charge are TOTALS over all events; they must divide evenly by
the number of events found, because every event starts from the same nuclei.
Exit 0 when the conservation laws hold, 1 otherwise.
"""
import argparse
import collections
import math
import re
import sys


def baryon_number(pdg):
    """Baryon number from a PDG code, transcribed from SMASH's own rule.

    `src/include/smash/pdgcode.h`, `PdgCode::baryon_number()`:

        if (is_nucleus())                       return A * antiparticle_sign();
        if (!is_hadron() || digits_.n_q1_ == 0) return 0;
        return antiparticle_sign();

    with `is_hadron() = (n_q3 != 0 && n_q2 != 0 && !is_nucleus())`.

    So a baryon is NOT "a four-digit code". It is any non-nuclear hadron whose
    first quark digit n_q1 is nonzero, which includes every excited state SMASH
    propagates: N(1440) is 12112, Lambda(1405) is 13122, and higher resonances
    run to six and seven digits. Judging by digit count silently gives all of
    them baryon number zero, and resonances are the bulk of a transport run's
    intermediate state, so that error turns an "exact" conservation check into a
    wrong one on any output taken before they have decayed.

    Digit layout for a non-nucleus, from the right:
        n_J (1) n_q3 (10) n_q2 (100) n_q1 (1000) n_L (1e4) n_R (1e5) n (1e6)
    Nuclei use +-10LZZZAAAI, ten digits, and their baryon number is A.
    """
    a = abs(pdg)
    sign = 1 if pdg > 0 else -1
    # Nucleus: +-10LZZZAAAI
    if a >= 1000000000:
        A = (a // 10) % 1000
        return sign * A
    n_q3 = (a // 10) % 10
    n_q2 = (a // 100) % 10
    n_q1 = (a // 1000) % 10
    if n_q3 == 0 or n_q2 == 0:      # not a hadron (lepton, photon, ...)
        return 0
    if n_q1 == 0:                   # meson
        return 0
    return sign



# Real SMASH-3.3 grammar, from src/oscaroutput.cc:
#   # event N ensemble E out COUNT
#   # event N ensemble E end 0 impact X scattering_projectile_target yes|no
# Counting any comment that contains " end" accepted a file with no event blocks
# at all, as long as some comment happened to contain that substring.
EVENT_OUT = re.compile(r'^#\s+event\s+(\d+)\s+ensemble\s+(\d+)\s+out\b(?:\s+(\d+))?')
EVENT_END = re.compile(r'^#\s+event\s+(\d+)\s+ensemble\s+(\d+)\s+end\b')


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument('oscar')
    ap.add_argument('--baryons', type=int, required=True,
                    help='expected TOTAL baryon number over all events (Au+Au: Nevents * 2 * 197)')
    ap.add_argument('--charge', type=int, required=True,
                    help='expected TOTAL electric charge in units of e (Au+Au: Nevents * 2 * 79)')
    ap.add_argument('--species-min', type=int, default=0,
                    help='minimum number of distinct species. Off by default: a species count is '
                         'weak evidence and was overstated as proof that inelastic physics happened.')
    args = ap.parse_args()

    for name, val in (('--baryons', args.baryons), ('--charge', args.charge)):
        if abs(val) > 10 ** 9:
            print(f"FAIL: {name}={val} is not a plausible expectation")
            return 1
    if args.species_min < 0:
        print(f"FAIL: --species-min cannot be negative, got {args.species_min}")
        return 1

    try:
        fh = open(args.oscar)
    except OSError as exc:
        print(f"FAIL: cannot read {args.oscar}: {exc}")
        return 1

    pdg_counts = collections.Counter()
    events = []          # (label, baryons, charge, n_records, declared)
    open_ev = None       # (label, baryons, charge, n_records, declared)
    bad_lines = 0
    stray = 0

    with fh:
        header = fh.readline()
        if not header.startswith('#!OSCAR2013'):
            print(f"FAIL: {args.oscar} does not start with an OSCAR2013 header: {header.strip()[:80]!r}")
            return 1
        # The header NAMES its columns and the set changes with the requested
        # content, so read the positions rather than assuming 9 and 11.
        cols = header.split()[2:]
        try:
            i_pdg = cols.index('pdg')
            i_chg = cols.index('charge')
        except ValueError:
            print(f"FAIL: the OSCAR header does not declare both 'pdg' and 'charge' columns: {cols}")
            return 1
        ncols = len(cols)

        for line in fh:
            if line.startswith('#'):
                m = EVENT_OUT.match(line)
                if m:
                    if open_ev is not None:
                        print(f"FAIL: event {m.group(1)}/{m.group(2)} starts while "
                              f"{open_ev[0]} is still open")
                        return 1
                    declared = int(m.group(3)) if m.group(3) else None
                    open_ev = [f"{m.group(1)}/{m.group(2)}", 0, 0, 0, declared]
                    continue
                m = EVENT_END.match(line)
                if m:
                    label = f"{m.group(1)}/{m.group(2)}"
                    if open_ev is None:
                        print(f"FAIL: event {label} ends without a matching 'out' line")
                        return 1
                    if open_ev[0] != label:
                        print(f"FAIL: event {label} ends while {open_ev[0]} is open")
                        return 1
                    events.append(tuple(open_ev))
                    open_ev = None
                continue

            fields = line.split()
            if len(fields) != ncols:
                bad_lines += 1
                continue
            try:
                for f in fields[:i_pdg]:
                    if not math.isfinite(float(f)):
                        print(f"FAIL: non-finite value in {args.oscar}: {line.strip()[:90]!r}")
                        return 1
                pdg = int(fields[i_pdg])
                charge = int(fields[i_chg])
            except ValueError:
                bad_lines += 1
                continue
            if open_ev is None:
                stray += 1
                continue
            pdg_counts[pdg] += 1
            open_ev[1] += baryon_number(pdg)
            open_ev[2] += charge
            open_ev[3] += 1

    if bad_lines:
        print(f"FAIL: {bad_lines} malformed particle records in {args.oscar}")
        return 1
    if stray:
        print(f"FAIL: {stray} particle records lie outside any event block")
        return 1
    if open_ev is not None:
        print(f"FAIL: event {open_ev[0]} was never closed; the run was truncated")
        return 1
    if not events:
        print(f"FAIL: {args.oscar} contains no complete event block")
        return 1

    n_ev = len(events)
    n_records = sum(e[3] for e in events)
    if n_records == 0:
        print(f"FAIL: {args.oscar} contains no particle records")
        return 1

    print(f"particles: {n_records} records, {len(pdg_counts)} distinct species, {n_ev} events")

    ok = True
    # The declared count on each 'out' line must match what followed it.
    for label, _, _, got, declared in events:
        if declared is not None and declared != got:
            print(f"FAIL: event {label} declares {declared} particles but {got} records follow")
            ok = False

    # Per event, not just on the total: equal and opposite violations in two
    # events would otherwise cancel into a clean-looking sum.
    if args.baryons % n_ev or args.charge % n_ev:
        print(f"FAIL: expectations {args.baryons}/{args.charge} do not divide evenly by {n_ev} events; "
              f"every event starts from the same nuclei, so they must")
        return 1
    b_per, q_per = args.baryons // n_ev, args.charge // n_ev
    for label, b, q, _, _ in events:
        if b != b_per:
            print(f"FAIL: event {label} has baryon number {b}, expected {b_per}")
            ok = False
        if q != q_per:
            print(f"FAIL: event {label} has charge {q}, expected {q_per}")
            ok = False
    if ok:
        print(f"  baryon number conserved EXACTLY in every event: {b_per} each, {args.baryons} total")
        print(f"  electric charge conserved EXACTLY in every event: {q_per} each, {args.charge} total")

    if args.species_min and len(pdg_counts) < args.species_min:
        print(f"FAIL: only {len(pdg_counts)} distinct species, expected at least {args.species_min}")
        ok = False

    print("  most abundant: " + ', '.join(f"{k}:{v}" for k, v in pdg_counts.most_common(6)))

    if ok:
        print("CONSERVATION OK")
        return 0
    print("CONSERVATION FAILED")
    return 1


if __name__ == '__main__':
    sys.exit(main())

exec
/bin/zsh -lc "sed -n '1,360p' skills/smash/scripts/selftest_smash.sh
sed -n '1,280p' skills/smash/scripts/install_smash.sh
sed -n '1,240p' skills/smash/references/output-format.md
sed -n '1,260p' skills/smash/references/verification.md
sed -n '1,220p' skills/smash/references/input-format.md
sed -n '1,220p' skills/smash/examples/README.md" in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
#!/bin/bash
# selftest_smash.sh
#
# Test the HARNESS, not the physics. Every guard gets a negative case that fails
# ONLY that guard, and each negative case asserts WHICH guard fired, because a
# test that fails for the wrong reason looks exactly like a test that passes.
# Runs in seconds and needs no SMASH build: the runs use a stub executable.
set -uo pipefail

HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
RUN="$HERE/run_smash.sh"
CC="$HERE/check_conservation_smash.py"
VERIFY="$HERE/verify_smash.sh"
PASS=0; FAIL=0
TMP="$(mktemp -d)"
trap 'rm -rf "$TMP"' EXIT

ok  () { PASS=$((PASS+1)); echo "  ok    $*"; }
bad () { FAIL=$((FAIL+1)); echo "  FAIL  $*"; }
expect_pass () { local d="$1"; shift; if "$@" >/dev/null 2>&1; then ok "$d"; else bad "$d (expected success, got failure)"; fi; }
expect_fail () { local d="$1"; shift; if "$@" >/dev/null 2>&1; then bad "$d (expected failure, got success)"; else ok "$d"; fi; }
expect_fail_with () {
  local d="$1" marker="$2"; shift 2
  local out; out="$("$@" 2>&1)" && { bad "$d (expected failure, got success)"; return; }
  case "$out" in
    *"$marker"*) ok "$d" ;;
    *) bad "$d (failed on the wrong guard: no '$marker' in the output)" ;;
  esac
}

# ------------------------------------------------------------------ fixtures
CFG="$TMP/config.yaml"
cat > "$CFG" <<'EOF'
General:
    Modus:          Collider
    Delta_Time:     0.1
    End_Time:       20.0
    Randomseed:     -1
    Nevents:        2
Modi:
    Collider:
        Projectile:
            Particles: {2212: 79, 2112: 118}
        Target:
            Particles: {2212: 79, 2112: 118}
        E_Kin: 1.23
EOF

# A minimal but VALID OSCAR2013 list: 2 events, baryon number 4, charge 2.
write_oscar () {   # write_oscar <path> [extra body lines on stdin]
  {
    echo '#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge'
    echo '# Units: fm fm fm fm GeV GeV GeV GeV GeV none none e'
    echo '# SMASH-3.3'
    echo '# event 0 ensemble 0 out 4'
    echo '  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1'
    echo '  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2112 1 0'
    echo '  20.0 0.1 0.2 0.3 0.138 0.3 0.1 0.0 0.0  211 2 1'
    echo '  20.0 0.1 0.2 0.3 0.138 0.3 0.1 0.0 0.0 -211 3 -1'
    echo '# event 0 ensemble 0 end 0 impact   4.000 scattering_projectile_target yes'
    echo '# event 1 ensemble 0 out 3'
    echo '  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1'
    echo '  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2112 1 0'
    echo '  20.0 0.1 0.2 0.3 0.138 0.3 0.1 0.0 0.0  111 2 0'
    echo '# event 1 ensemble 0 end 0 impact   4.000 scattering_projectile_target yes'
  } > "$1"
}
GOOD="$TMP/good.oscar"; write_oscar "$GOOD"
# baryons: 2212,2112 twice = 4;  charge: +1+0+1-1 +1+0+0 = +2

# A stub SMASH: writes a valid OSCAR list and a benign log, honouring -o.
write_stub () {
  local path="$TMP/$1" mode="$2"
  {
    echo '#!/bin/bash'
    echo "MODE=\"$mode\""
    cat <<'STUB'
OUT=""
while [ $# -gt 0 ]; do
  case "$1" in
    -o) OUT="$2"; shift 2 ;;
    -i) shift 2 ;;
    --version) echo "SMASH-3.3"; exit 0 ;;
    *) shift ;;
  esac
done
[ "$MODE" = "exitfail" ] && { echo "boom" >&2; exit 3; }
mkdir -p "$OUT"
# a benign macOS-style warning that must NOT be read as an error
printf "[15'04'57]  WARN         Fpe         : Failed to setup trap on pole error.\n"
STUB
    echo 'F="$OUT/particle_lists.oscar"'
    echo 'case "$MODE" in'
    echo '  nooutput) exit 0 ;;'
    echo '  headeronly) printf "#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge\n" > "$F"; exit 0 ;;'
    echo 'esac'
    echo 'cat > "$F" <<'"'"'OSC'"'"''
    cat "$GOOD"
    echo 'OSC'
    cat <<'STUB2'
case "$MODE" in
  nan)      sed -i.bak 's/0.938 1.0/0.938 nan/' "$F"; rm -f "$F.bak" ;;
  oneevent) sed -i.bak '/# event 1 ensemble 0/d' "$F"; rm -f "$F.bak" ;;
  realerror) printf "[15'04'57]  ERROR        Main        : something exploded\n" ;;
esac
exit 0
STUB2
  } > "$path"
  chmod +x "$path"
}

echo "run_smash.sh argument handling"
expect_fail_with "missing --config is rejected" "--config is required" "$RUN"
expect_fail_with "a nonexistent config is rejected" "does not exist" "$RUN" --config "$TMP/nope"
printf 'not: a smash config\n' > "$TMP/notcfg"
expect_fail_with "a file with no General: block is rejected" "no 'General:' block" "$RUN" --config "$TMP/notcfg"
expect_fail_with "an unknown argument is rejected" "unknown argument" "$RUN" --config "$CFG" --bogus
expect_fail_with "a non-integer --seed is rejected" "must be an integer" "$RUN" --config "$CFG" --seed 1.5
expect_fail_with "a non-numeric --end-time is rejected" "must be a non-negative number" "$RUN" --config "$CFG" --seed 1 --end-time abc

echo
echo "run_smash.sh seed policy"
write_stub stub_ok ok
SKY=""; export SMASH="$TMP/stub_ok"
expect_fail_with "Randomseed -1 is refused by default" "irreproducible" \
  "$RUN" --config "$CFG" --outdir "$TMP/w_seed"
expect_pass "--allow-random-seed accepts it deliberately" \
  "$RUN" --config "$CFG" --outdir "$TMP/w_seed2" --allow-random-seed
expect_pass "a pinned --seed runs (control)" \
  "$RUN" --config "$CFG" --outdir "$TMP/w_ok" --seed 12345
grep -q "Randomseed:     12345" "$TMP/w_ok/config_used.yaml" \
  && ok "the pinned seed was written into config_used.yaml" \
  || bad "the seed override did not reach the config"

echo
echo "run_smash.sh output guards (stub, one broken property each)"
for mode in exitfail nooutput headeronly nan oneevent realerror; do
  write_stub "stub_$mode" "$mode"
  case "$mode" in
    exitfail)  d="a nonzero exit status fails"; m="exited with status" ;;
    nooutput)  d="a missing particle list fails"; m="no particle output" ;;
    headeronly) d="a header with no particles fails"; m="no particles" ;;
    nan)       d="NaN in the particle list fails"; m="NaN or Inf" ;;
    oneevent)  d="fewer events than requested fails"; m="stopped early" ;;
    realerror) d="a real ERROR log line fails"; m="logged an error" ;;
  esac
  SMASH="$TMP/stub_$mode" expect_fail_with "$d" "$m" "$RUN" --config "$CFG" --outdir "$TMP/w_$mode" --seed 1
done
# The control above already proves the benign Fpe WARN does not trip the error
# guard, since every stub prints it and the ok stub passes.
grep -q "Fpe" "$TMP/w_ok/smash.log" \
  && ok "the passing control really does contain the benign 'Fpe ... error.' warning" \
  || bad "control lost the Fpe warning, so the error-guard test is not isolated"

echo
echo "check_conservation_smash.py"
expect_pass "a conserving list passes (control)" python3 "$CC" "$GOOD" --baryons 4 --charge 2
expect_fail_with "a wrong baryon expectation fails" "baryon number" python3 "$CC" "$GOOD" --baryons 8 --charge 2
expect_fail_with "a wrong charge expectation fails" "charge" python3 "$CC" "$GOOD" --baryons 4 --charge 6
sed 's/^#!OSCAR2013/#!SOMETHINGELSE/' "$GOOD" > "$TMP/badhdr.oscar"
expect_fail_with "a non-OSCAR2013 header fails" "does not start with an OSCAR2013 header" \
  python3 "$CC" "$TMP/badhdr.oscar" --baryons 4 --charge 2
sed 's/0.938 1.0 0.1 0.1 0.1 2212 0 1/0.938 nan 0.1 0.1 0.1 2212 0 1/' "$GOOD" > "$TMP/nan.oscar"
expect_fail_with "a non-finite kinematic value fails" "non-finite" python3 "$CC" "$TMP/nan.oscar" --baryons 4 --charge 2
grep -v '^ ' "$GOOD" > "$TMP/empty.oscar"
expect_fail_with "a list with no particle records fails" "no particle records" \
  python3 "$CC" "$TMP/empty.oscar" --baryons 4 --charge 2
sed -n '1,7p' "$GOOD" | grep -v 'ensemble 0 end' > "$TMP/noend.oscar"
expect_fail_with "an unclosed event block fails" "never closed" \
  python3 "$CC" "$TMP/noend.oscar" --baryons 2 --charge 1
sed 's/  20.0 0.1 0.2 0.3 0.138 0.3 0.1 0.0 0.0  211 2 1/  20.0 0.1 BAD/' "$GOOD" > "$TMP/mal.oscar"
expect_fail_with "a malformed record fails rather than being skipped" "malformed" \
  python3 "$CC" "$TMP/mal.oscar" --baryons 4 --charge 2
expect_fail_with "too few species fails when --species-min is given" "distinct species" \
  python3 "$CC" "$GOOD" --baryons 4 --charge 2 --species-min 99
expect_pass "--species-min is off by default (it was overstated as proof of physics)" \
  python3 "$CC" "$GOOD" --baryons 4 --charge 2

echo
echo "verify_smash.sh"
expect_fail_with "verify rejects an unknown argument" "unknown argument" "$VERIFY" --bogus
SMASH="$TMP/stub_ok" SMASH_BUILD="$TMP/no_such_build" SMASH_ROOT="$TMP" \
  expect_fail_with "verify fails when the build directory is absent" "no build directory" "$VERIFY" --tests-only
mkdir -p "$TMP/fakebuild"
SMASH="$TMP/stub_ok" SMASH_BUILD="$TMP/fakebuild" SMASH_ROOT="$TMP/no_such_root" \
  expect_fail_with "verify refuses a build it cannot trace to the pinned source" "not verifiably come from the pinned source" \
  "$VERIFY" --anchor-only
# The identity guard must also reject a REAL clone whose binary is not the
# stamped one, which is the attack that a fake build directory represented.
if [ -d "$HOME/.cache/fusion/smash/smash/.git" ]; then
  cp "$TMP/stub_ok" "$TMP/impostor"
  SMASH="$TMP/impostor" \
    SMASH_BUILD="$HOME/.cache/fusion/smash/smash/build" \
    SMASH_ROOT="$HOME/.cache/fusion/smash/smash" \
    expect_fail_with "an impostor binary beside a real build is rejected" "does not match the build stamp" \
    "$VERIFY" --tests-only
else
  ok "skipped the impostor-binary case (no local SMASH clone to test against)"
fi

echo
echo "guards added after the 2026-07-23 adversarial pass"
# --- negative seeds other than -1 (SMASH treats every negative seed as random)
for sd in -2 -999; do
  SMASH="$TMP/stub_ok" expect_fail_with "--seed $sd is refused (SMASH randomizes any negative seed)" "irreproducible" \
    "$RUN" --config "$CFG" --outdir "$TMP/w_neg$sd" --seed "$sd"
done
SMASH="$TMP/stub_ok" expect_pass "--seed 0 is accepted (non-negative)" \
  "$RUN" --config "$CFG" --outdir "$TMP/w_zero" --seed 0
for bad in -- 1-2 . 1..2 "1e3" "-5"; do
  SMASH="$TMP/stub_ok" expect_fail "a malformed --end-time '$bad' is rejected" \
    "$RUN" --config "$CFG" --outdir "$TMP/w_bad" --seed 1 --end-time "$bad"
done
SMASH="$TMP/stub_ok" expect_fail_with "--nevents 0 is rejected" "at least 1" \
  "$RUN" --config "$CFG" --outdir "$TMP/w_ev0" --seed 1 --nevents 0

# --- structural OSCAR validation: forged output must not pass
write_bad_stub () {   # $1 name, $2 the body written to particle_lists.oscar
  local path="$TMP/$1"
  { echo '#!/bin/bash'
    echo 'OUT=""; while [ $# -gt 0 ]; do case "$1" in -o) OUT="$2"; shift 2;; --version) echo SMASH-3.3; exit 0;; *) shift;; esac; done'
    echo 'mkdir -p "$OUT"'
    echo "cat > \"\$OUT/particle_lists.oscar\" <<'EOSC'"
    cat
    echo 'EOSC'
    echo 'exit 0'
  } > "$path"
  chmod +x "$path"
}
write_bad_stub stub_forged <<'EOF'
# not an oscar header at all
# event 0 ensemble 0 end 0
# event 1 ensemble 0 end 0
  garbage record here
EOF
SMASH="$TMP/stub_forged" expect_fail_with "a forged non-OSCAR header is rejected" "OSCAR2013 header" \
  "$RUN" --config "$CFG" --outdir "$TMP/w_forged" --seed 1
write_bad_stub stub_unpaired <<'EOF'
#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge
# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes
# event 1 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes
  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1
EOF
SMASH="$TMP/stub_unpaired" expect_fail_with "event-end markers with no matching starts are rejected" "must pair" \
  "$RUN" --config "$CFG" --outdir "$TMP/w_unpaired" --seed 1
write_bad_stub stub_cols <<'EOF'
#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge
# event 0 ensemble 0 out 1
  20.0 0.1 0.2 2212 1
# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes
# event 1 ensemble 0 out 1
  20.0 0.1 0.2 2212 1
# event 1 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes
EOF
SMASH="$TMP/stub_cols" expect_fail_with "records with the wrong column count are rejected" "columns the header declares" \
  "$RUN" --config "$CFG" --outdir "$TMP/w_cols" --seed 1

# --- conservation checker: resonances, per-event, strict grammar
python3 - <<'PYEOF' > "$TMP/reso.oscar"
print('#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge')
print('# Units: fm fm fm fm GeV GeV GeV GeV GeV none none e')
print('# event 0 ensemble 0 out 2')
print('  20.0 0.1 0.2 0.3 1.440 1.5 0.1 0.1 0.1 12112 0 0')   # N(1440)0, B=1
print('  20.0 0.1 0.2 0.3 1.405 1.5 0.1 0.1 0.1 13122 1 0')   # Lambda(1405), B=1
print('# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes')
PYEOF
expect_pass "five-digit baryon resonances are counted (B=2, Q=0)" \
  python3 "$CC" "$TMP/reso.oscar" --baryons 2 --charge 0
expect_fail_with "and a wrong expectation on them still fails" "baryon number" \
  python3 "$CC" "$TMP/reso.oscar" --baryons 0 --charge 0

# Per-event: two events whose violations cancel in the sum must NOT pass.
python3 - <<'PYEOF' > "$TMP/cancel.oscar"
print('#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge')
print('# event 0 ensemble 0 out 2')
print('  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1')
print('  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 1 1')   # event 0: B=2
print('# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes')
print('# event 1 ensemble 0 out 0')
print('# event 1 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes')  # event 1: B=0
PYEOF
expect_fail_with "equal-and-opposite per-event violations do not cancel" "event" \
  python3 "$CC" "$TMP/cancel.oscar" --baryons 2 --charge 2
# A comment merely containing " end" is not an event end.
python3 - <<'PYEOF' > "$TMP/fakeend.oscar"
print('#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge')
print('# this comment contains the word end but is not an event marker')
print('  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1')
PYEOF
expect_fail_with "a comment containing 'end' is not an event marker" "outside any event block" \
  python3 "$CC" "$TMP/fakeend.oscar" --baryons 1 --charge 1
# The declared per-event count must match the records that follow.
python3 - <<'PYEOF' > "$TMP/miscount.oscar"
print('#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge')
print('# event 0 ensemble 0 out 5')
print('  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1')
print('# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes')
PYEOF
expect_fail_with "a declared particle count that does not match is rejected" "declares 5" \
  python3 "$CC" "$TMP/miscount.oscar" --baryons 1 --charge 1

echo
echo "-------------------------------------------"
echo "selftest: $PASS passed, $FAIL failed"
[ "$FAIL" -eq 0 ] || exit 1
#!/bin/bash
# install_smash.sh
#
# Provision SMASH and its two awkward dependencies, then print
#   SMASH=<path to the smash executable>
#   SMASH_ROOT=<repository root>
#   SMASH_BUILD=<build directory, where ctest runs>
#   SMASH_EIGEN3_ROOT=<Eigen 3.4 prefix, see below: EXPORT THIS>
#   SMASH_GSL_PREFIX=<GSL prefix, also needed by the library-example sub-build>
#   SMASH_PYTHIA_PREFIX=<Pythia prefix; its lib/ must be on the loader path>
# on the last six lines. run_smash.sh and verify_smash.sh parse those.
#
# SMASH is the hadronic transport approach of J. Weil et al., Phys. Rev. C 94,
# 054905 (2016), DOI 10.1103/physrevc.94.054905; software release SMASH-3.3,
# DOI 10.5281/zenodo.3484711. GPL-3.0-or-later (LICENSE.md, and the Zenodo
# record's rightsList; GitHub reports NOASSERTION only because LICENSE.md also
# carries the BSD-3, CC0 and Unlicense terms of bundled third-party code).
#
# Two dependencies cost real time, and both fail in ways that point away from
# the cause. See references/failure-modes.md.
#
#  * Pythia must be EXACTLY 8.316 and is built from source. SMASH's own
#    INSTALL.md still gives the URL pythia.org/download/pythia83/..., which now
#    404s because Pythia moved everything under /releases/. The 404 body is a
#    3.7 KB HTML page, so a naive download writes that to pythia8316.tgz and the
#    first symptom is "tar: Unrecognized archive format". This script validates
#    that what it downloaded is really a gzip tarball before unpacking it.
#
#  * Eigen must be a 3.x. Homebrew now ships Eigen 5.0.1, which renamed
#    EIGEN_WORLD_VERSION, so SMASH 3.3's bundled FindEigen3.cmake cannot parse
#    the version and reports "at least version 3.0 is required", which reads as
#    "too old" when the truth is "too new". This script fetches the Eigen 3.4.0
#    headers (header-only, no build) rather than patching SMASH.
set -euo pipefail

ROOT_DIR="${SMASH_ROOT_DIR:-$HOME/.cache/fusion/smash}"
mkdir -p "$ROOT_DIR"
ROOT_CANON="$(cd "$ROOT_DIR" && pwd -P)"

REPO="${SMASH_REPO:-https://github.com/smash-transport/smash}"
PIN="${SMASH_PIN:-d1a1c6cf0a0002ee064eec1b929b9a7c14b3d5bc}"       # SMASH-3.3
PYTHIA_VERSION="${SMASH_PYTHIA_VERSION:-8316}"
PYTHIA_URL="${SMASH_PYTHIA_URL:-https://pythia.org/releases/pythia83/pythia${PYTHIA_VERSION}.tgz}"
EIGEN_VERSION="${SMASH_EIGEN_VERSION:-3.4.0}"
EIGEN_URL="${SMASH_EIGEN_URL:-https://gitlab.com/libeigen/eigen/-/archive/${EIGEN_VERSION}/eigen-${EIGEN_VERSION}.tar.gz}"

SRCROOT="$ROOT_DIR/smash"
BUILD="$SRCROOT/build"
BIN="$BUILD/smash"
PYTHIA_PREFIX="$ROOT_DIR/pythia-install"
EIGEN_ROOT="$ROOT_DIR/eigen-$EIGEN_VERSION"
STAMP="$BUILD/.fusion_build_stamp"
JOBS="${SMASH_JOBS:-$( (command -v nproc >/dev/null && nproc) || sysctl -n hw.ncpu 2>/dev/null || echo 4 )}"

log () { echo "install_smash: $*" >&2; }

safe_rmrf () {
  local target="$1" parent canon
  [ -n "$target" ] || { log "refusing to delete an empty path"; return 1; }
  case "$target" in /*) : ;; *) log "refusing to delete relative path '$target'"; return 1 ;; esac
  [ -e "$target" ] || return 0
  parent="$(cd "$(dirname "$target")" 2>/dev/null && pwd -P)" || { log "cannot resolve '$target'"; return 1; }
  canon="$parent/$(basename "$target")"
  case "$canon" in "/"|"$HOME"|"/usr"|"/etc"|"/var"|"/tmp") log "refusing to delete '$canon'"; return 1 ;; esac
  [ "${#canon}" -ge 12 ] || { log "refusing to delete short path '$canon'"; return 1; }
  case "$canon/" in "$ROOT_CANON"/*) : ;; *) log "refusing to delete '$canon' outside $ROOT_CANON"; return 1 ;; esac
  rm -rf "$canon"
}

# A download that silently produced an HTML error page is the trap this whole
# script exists to prevent, so check the magic bytes, not just the exit status.
fetch_tarball () {
  local url="$1" dest="$2" what="$3"
  log "downloading $what"
  curl -fsSL --retry 3 --max-time 900 -o "$dest" "$url" || {
    log "download of $what failed (HTTP error or timeout): $url"
    log "if this is Pythia, the upstream path moved once already; check pythia.org/releases/"
    rm -f "$dest"; return 1
  }
  # gzip magic is 1f 8b. An HTML 404 body starts with '<' and would otherwise
  # only surface later as "tar: Unrecognized archive format".
  local magic; magic="$(od -An -tx1 -N2 "$dest" | tr -d ' \n')"
  if [ "$magic" != "1f8b" ]; then
    log "$what did not download as a gzip archive (first bytes: $magic, size $(wc -c < "$dest") bytes)"
    head -c 120 "$dest" | tr -d '\0' >&2; echo >&2
    log "that is almost certainly an HTML error page saved under a .tgz name; the URL is wrong or moved"
    rm -f "$dest"; return 1
  fi
}

# ------------------------------------------------------------------- Eigen 3.4
if [ ! -d "$EIGEN_ROOT/Eigen" ]; then
  fetch_tarball "$EIGEN_URL" "$ROOT_DIR/eigen.tar.gz" "Eigen $EIGEN_VERSION headers" || exit 1
  ( cd "$ROOT_DIR" && tar xzf eigen.tar.gz ) || { log "unpacking Eigen failed"; exit 1; }
  rm -f "$ROOT_DIR/eigen.tar.gz"
fi
[ -f "$EIGEN_ROOT/Eigen/src/Core/util/Macros.h" ] || { log "Eigen headers missing from $EIGEN_ROOT"; exit 1; }
grep -q "define[[:space:]]\+EIGEN_WORLD_VERSION[[:space:]]\+3" "$EIGEN_ROOT/Eigen/src/Core/util/Macros.h" || {
  log "the Eigen at $EIGEN_ROOT is not a 3.x; SMASH 3.3 cannot parse Eigen 5's renamed version macros"
  exit 1
}

# --------------------------------------------------------------------- GSL
# SMASH's FindGSL uses pkg-config first. A machine with no system GSL (common on
# a cluster where everything lives in conda) then fails at configure time with a
# message about GSL rather than about where to find it, so look in the usual
# conda prefixes too and hand cmake an explicit root.
find_gsl_prefix () {
  local p
  if [ -n "${SMASH_GSL_PREFIX:-}" ]; then echo "$SMASH_GSL_PREFIX"; return 0; fi
  if command -v pkg-config >/dev/null 2>&1 && pkg-config --exists gsl 2>/dev/null; then
    p="$(pkg-config --variable=prefix gsl 2>/dev/null || true)"
    [ -n "$p" ] && [ -d "$p" ] && { echo "$p"; return 0; }
  fi
  for p in "${CONDA_PREFIX:-/nonexistent}" "$HOME"/miniforge3/envs/*/ "$HOME"/miniconda3/envs/*/ \
           /opt/homebrew /usr/local /usr; do
    p="${p%/}"
    if [ -f "$p/lib/libgsl.so" ] || [ -f "$p/lib/libgsl.dylib" ] || [ -f "$p/lib/libgsl.a" ]; then
      echo "$p"; return 0
    fi
  done
  return 1
}
GSL_PREFIX="$(find_gsl_prefix || true)"
if [ -n "$GSL_PREFIX" ]; then
  log "GSL prefix: $GSL_PREFIX"
else
  log "GSL not found. Install it (macOS: brew install gsl; Linux: conda install -c conda-forge gsl,"
  log "or the distribution's libgsl-dev) or set SMASH_GSL_PREFIX."
  exit 1
fi

# ------------------------------------------------------------------ Pythia
if [ ! -x "$PYTHIA_PREFIX/bin/pythia8-config" ]; then
  if [ ! -d "$ROOT_DIR/pythia$PYTHIA_VERSION" ]; then
    fetch_tarball "$PYTHIA_URL" "$ROOT_DIR/pythia$PYTHIA_VERSION.tgz" "Pythia $PYTHIA_VERSION" || exit 1
    ( cd "$ROOT_DIR" && tar xzf "pythia$PYTHIA_VERSION.tgz" ) || { log "unpacking Pythia failed"; exit 1; }
    rm -f "$ROOT_DIR/pythia$PYTHIA_VERSION.tgz"
  fi
  log "building Pythia $PYTHIA_VERSION (several minutes, once)"
  ( cd "$ROOT_DIR/pythia$PYTHIA_VERSION" \
    && ./configure --prefix="$PYTHIA_PREFIX" \
    && make -j"$JOBS" && make install ) > "$ROOT_DIR/pythia-build.log" 2>&1 || {
    log "Pythia build failed, last 15 lines of $ROOT_DIR/pythia-build.log:"
    tail -15 "$ROOT_DIR/pythia-build.log" >&2; exit 1; }
fi
[ -x "$PYTHIA_PREFIX/bin/pythia8-config" ] || { log "pythia8-config missing from $PYTHIA_PREFIX"; exit 1; }
PYTHIA_ACTUAL="$("$PYTHIA_PREFIX/bin/pythia8-config" --version 2>/dev/null || echo unknown)"
log "Pythia $PYTHIA_ACTUAL at $PYTHIA_PREFIX"

# -------------------------------------------------------------------- clone
if [ -d "$SRCROOT/.git" ]; then
  log "reusing clone at $SRCROOT"
elif [ -e "$SRCROOT" ]; then
  log "$SRCROOT exists but is not a git clone; refusing to delete a directory this script did not create"
  exit 1
else
  command -v git >/dev/null || { log "git not found"; exit 1; }
  TMPCLONE="$ROOT_DIR/.clone.$$"
  if [ -e "$TMPCLONE" ] && [ ! -f "$TMPCLONE/.fusion_tmpclone" ]; then
    log "refusing to reuse '$TMPCLONE': not one of this script's temporary clones"; exit 1
  fi
  safe_rmrf "$TMPCLONE"
  mkdir -p "$TMPCLONE" && : > "$TMPCLONE/.fusion_tmpclone"
  git clone -q "$REPO" "$TMPCLONE/repo" || { log "clone failed"; safe_rmrf "$TMPCLONE"; exit 1; }
  mv "$TMPCLONE/repo" "$TMPCLONE.repo" && safe_rmrf "$TMPCLONE" && mv "$TMPCLONE.repo" "$SRCROOT"
fi

( cd "$SRCROOT" && git diff --quiet HEAD 2>/dev/null ) || {
  log "the clone at $SRCROOT has uncommitted modifications; refusing to build from it"; exit 1; }
( cd "$SRCROOT" && git checkout -q "$PIN" 2>/dev/null ) || {
  log "cannot check out pinned commit $PIN (shallow clone, or upstream history moved)"
  log "set SMASH_PIN to re-pin deliberately"; exit 1; }
log "pinned at $PIN"

# ------------------------------------------------------------------- build
build_identity () {
  local head dirty cxx
  head="$(cd "$SRCROOT" 2>/dev/null && git rev-parse HEAD 2>/dev/null || echo nohead)"
  if (cd "$SRCROOT" 2>/dev/null && git diff --quiet HEAD 2>/dev/null); then dirty=clean; else dirty=DIRTY; fi
  cxx="$(command -v c++ 2>/dev/null || echo nocxx)|$(c++ --version 2>/dev/null | head -1 || echo nover)"
  echo "$head|$dirty|$cxx|$PYTHIA_ACTUAL|$EIGEN_VERSION|$GSL_PREFIX|$(uname -s)|$(uname -m)"
}
binary_digest () { shasum -a 256 "$BIN" 2>/dev/null | cut -d' ' -f1 || echo nodigest; }

fast_path_ok () {
  [ -x "$BIN" ] || return 1
  [ -f "$STAMP" ] || return 1
  [ "$(cd "$SRCROOT" && git rev-parse HEAD)" = "$PIN" ] || { log "clone HEAD is not the pin, rebuilding"; return 1; }
  [ "$(head -1 "$STAMP")" = "$(build_identity)" ] || { log "build identity changed, rebuilding"; return 1; }
  [ "$(sed -n 2p "$STAMP")" = "$(binary_digest)" ] || { log "the binary changed since it was stamped, rebuilding"; return 1; }
  return 0
}

if fast_path_ok; then
  log "found smash built from the pinned commit with a matching build identity, probing it"
else
  command -v cmake >/dev/null || { log "cmake not found (3.16 or newer required)"; exit 1; }
  mkdir -p "$BUILD"
  log "configuring"
  ( cd "$BUILD" && rm -f CMakeCache.txt && rm -rf CMakeFiles \
    && EIGEN3_ROOT="$EIGEN_ROOT" \
       PKG_CONFIG_PATH="$GSL_PREFIX/lib/pkgconfig${PKG_CONFIG_PATH:+:$PKG_CONFIG_PATH}" \
       CMAKE_PREFIX_PATH="$GSL_PREFIX${CMAKE_PREFIX_PATH:+:$CMAKE_PREFIX_PATH}" \
       cmake -DGSL_ROOT_DIR="$GSL_PREFIX" \
         -DPythia_CONFIG_EXECUTABLE="$PYTHIA_PREFIX/bin/pythia8-config" \
         -DEIGEN3_INCLUDE_DIR="$EIGEN_ROOT" .. ) > "$ROOT_DIR/cmake.log" 2>&1 || {
    log "cmake failed, last 15 lines of $ROOT_DIR/cmake.log:"; tail -15 "$ROOT_DIR/cmake.log" >&2; exit 1; }
  log "building with -j$JOBS (several minutes, once)"
  ( cd "$BUILD" && EIGEN3_ROOT="$EIGEN_ROOT" make -j"$JOBS" ) > "$ROOT_DIR/build.log" 2>&1 || {
    log "build failed, last 20 lines of $ROOT_DIR/build.log:"; tail -20 "$ROOT_DIR/build.log" >&2; exit 1; }
  [ -x "$BIN" ] || { log "build reported success but $BIN is missing"; exit 1; }
  { build_identity; binary_digest; } > "$STAMP"
fi

# ------------------------------------------------------------------- probe
# Content is the verdict AND the exit status must be zero. A crash after
# printing a plausible banner would otherwise pass.
probe_binary () {
  local out
  out="$("$BIN" --version 2>&1)" || { log "smash --version exited nonzero"; return 1; }
  case "$out" in
    SMASH-*) : ;;
    *) log "smash --version did not report a SMASH version: ${out%%$'\n'*}"; return 1 ;;
  esac
  log "probe: ${out%%$'\n'*}"
}
probe_binary || { log "the built executable failed its probe"; exit 1; }

echo "SMASH=$BIN"
echo "SMASH_ROOT=$SRCROOT"
echo "SMASH_BUILD=$BUILD"
echo "SMASH_EIGEN3_ROOT=$EIGEN_ROOT"
echo "SMASH_GSL_PREFIX=$GSL_PREFIX"
echo "SMASH_PYTHIA_PREFIX=$PYTHIA_PREFIX"
# SMASH output

Everything goes into the directory given with `-o`, which SMASH creates. It also
copies the configuration it actually used into that directory, so a run records
its own input.

## Files

| file | when | content |
|---|---|---|
| `particle_lists.oscar` | `Output: Particles: Format: ["Oscar2013"]` | the particle list, the main result |
| `collisions_custom.bin` | `Collisions:` with `Binary` format | every interaction, for reaction-by-reaction analysis |
| `collisions_oscar2013.bin` | `Collisions:` with `Oscar2013_bin` | the same, in binary OSCAR |
| `config.yaml` | always | the configuration as SMASH parsed it |
| `thermodynamics.dat`, `*.vtk` | on request | densities and fields on a grid |


SMASH logs to stdout; there is no configurable log FILE. `run_smash.sh` captures
that stream as `smash.log`, alongside `stderr.txt`, `config_used.yaml` and, when
a shipped example carries its own tables, `particles_used.txt` and
`decaymodes_used.txt`.

## OSCAR2013 particle lists

```
#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge
# Units: fm fm fm fm GeV GeV GeV GeV GeV none none e
# SMASH-3.3
# event 0 ensemble 0 out 497
      20.0   1.23  -4.56   7.89  0.938   1.02   0.11  -0.05   0.30  2212   17   1
...
# event 0 ensemble 0 end 0 impact   0.000 scattering_projectile_target yes
```

Twelve columns, in the order named on the first line: time, three positions,
mass, four-momentum, PDG code, particle ID within the event, and electric charge
in units of e. **The header line is the authority on the column order**, since it
changes with the output content requested; parse it rather than assuming.

Events are delimited by `# event N ensemble E out COUNT` and
`# event N ensemble E end 0 impact X scattering_projectile_target yes|no`, the
grammar in `src/oscaroutput.cc`. The `out` line DECLARES how many particles
follow, which is worth checking against the records that actually follow.
Counting matched pairs of those markers is how `run_smash.sh` checks that the run
wrote every event it was asked for instead of stopping early with a zero exit
status. Note the `ensemble` field: matching only `# event N out` misses the real
output entirely.

`Only_Final: No` writes an intermediate list at each `Output_Interval`, so the
same particle appears many times and multiplicities must be taken from the final
block only. `Only_Final: Yes` writes just the end of each event.

## What is reproducible, and what is not

SMASH is Monte Carlo. Three statements, kept apart on purpose:

1. **With a pinned `Randomseed`, the output is bit-reproducible on one build.**
   Measured: two runs of the same seeded Au+Au configuration produced a
   byte-identical `particle_lists.oscar`.
2. **Across builds it is not**, and should not be expected to be. Different
   compilers, libm implementations and Pythia builds reorder floating-point
   operations, and a transport code amplifies that into different collision
   histories.
3. **Conservation laws hold exactly regardless.** Baryon number and electric
   charge are integers fixed by the initial nuclei, so they are the same on
   every platform and for every seed. That is why the skill anchors on them
   rather than on multiplicities: see `scripts/check_conservation_smash.py`.

A consequence worth internalizing: a multiplicity from a 2-event run is not a
benchmark of anything. If you want a physics number to compare, run enough
events that the statistical error is smaller than the effect you care about, and
quote that error.

## Reading a run quickly

```bash
# final-state multiplicities by species
awk '!/^#/ {print $10}' particle_lists.oscar | sort -n | uniq -c | sort -rn | head

# number of events actually completed
grep -c '^# event .* end' particle_lists.oscar
```

PDG codes you will meet immediately: 2212 proton, 2112 neutron, 211/-211/111
charged and neutral pions, 321/-321/311 kaons, 3122 Lambda, 221 eta.
# SMASH verification

**Tier 1: this reproduces SMASH's own test suite.** Measured 2026-07-23 at
upstream commit `d1a1c6cf0a0002ee064eec1b929b9a7c14b3d5bc` (SMASH-3.3, released
2025-12-03), with **no source patches on either platform**.

## Identity

| | |
|---|---|
| paper | Weil et al., Phys. Rev. C **94**, 054905 (2016), `10.1103/physrevc.94.054905` (CrossRef-verified) |
| software | SMASH-3.3, `10.5281/zenodo.3484711` (DataCite-verified, 2025-12-03) |
| licence | GPL-3.0-or-later, confirmed twice: `LICENSE.md` and the Zenodo `rightsList` |

GitHub reports NOASSERTION for this repository only because `LICENSE.md` also
reproduces the BSD-3, CC0 and Unlicense terms of bundled third-party code. This
is the KSHELL lesson applied: read the licence text, not the detector's verdict.

## Builds compared

| | macOS | Linux |
|---|---|---|
| machine | Apple Silicon | heliumx, x86-64 |
| compiler | Apple clang (C++17) | g++ 13.3.0 |
| GSL | Homebrew 2.8 | conda-forge, inside a conda prefix |
| Eigen | 3.4.0 headers fetched by the installer | same |
| Pythia | 8.316, built from source | same |

Neither platform needed a patch. The differences are entirely in where the
dependencies live, which is what the installer's detection logic exists for.

## Stage 1: the shipped test suite

SMASH ships **104 ctest cases**. Both platforms run them; `verify_smash.sh`
requires the count to be exactly 104, never "at least", so a suite that silently
skipped cases cannot certify the build.

Two of the 104 are **non-deterministic by upstream construction**, not by
platform. `src/tests/potentials.cc` and `src/tests/random.cc` both open with

```cpp
TEST(set_random_seed) {
  std::random_device rd;
  int64_t seed = rd();
  random::set_seed(seed);
}
```

and then assert statistical quantities against fixed tolerances, for example
`COMPARE_ABSOLUTE_ERROR(P2[0].front().momentum().velocity().x1(), 0.6, 0.003)`.
A fresh seed on every run therefore produces an occasional failure on a
perfectly good build.

**Two more test files seed themselves and are not on the retry list.**
`scatteraction.cc` calls `random::generate_63bit_seed()` at three sites and
`dynamic_fluidization.cc` at one; that helper also draws from
`std::random_device`. Neither has been observed to fail, so neither is retried,
but this skill does NOT claim that the other 102 cases are deterministic. Four
files self-seed; two of them have been seen to flake.

Measured flake rate on macOS: **`potentials` passed 4 of 5 consecutive
standalone runs**. Across two full-suite runs it failed once and passed once.
On Linux the suite passed **104 of 104 on the first attempt**, which is what a
statistical failure looks like: present on one machine's run and absent on
another's, with no platform meaning to it at all.

The policy this justifies is deliberately narrow: every other case must pass
on the **first** attempt, and only those two names may be retried, **once**. A
statistical fluke passes with a fresh seed; a broken build fails twice. Widening
this to "allow one failure" would let a real regression through, which is the
hole an earlier skill's `Total >= N` check had.

## Stage 2: a seeded physics anchor

The shipped collider configuration, Au+Au at E_kin = 1.23 GeV per nucleon
(the HADES energy), shortened to 2 events and 20 fm/c, with `Randomseed` pinned
to 20260723. About 25 s.

**Reproducibility within one build is exact.** Two runs of the same seeded
configuration produced a byte-identical `particle_lists.oscar` (997 lines).

**The anchor itself is a conservation check, not a multiplicity comparison:**

| quantity | measured | expected | |
|---|---|---|---|
| baryon number | 788 | 2 events x 2 nuclei x 197 | EXACT |
| electric charge | 316 e | 2 events x 2 nuclei x 79 | EXACT |

Those are integers fixed by the initial nuclei, so they are identical on every
platform and for every seed. They are checked **per event**, not only on the
total, because equal and opposite violations in two events would otherwise
cancel into a clean-looking sum. Their scope is limited and stated as such: they
catch lost particles and corrupted bookkeeping, not a wrong cross section, and
the test suite is what covers the rest. Contrast the multiplicities from the same run:

```
n 450, p 336, pi- 76, pi0 65, pi+ 57, eta 2, K0 2, Lambda 1, Sigma- 1
```

Those are recorded **only** as a same-build reproducibility check. They are a
2-event Monte Carlo sample; they are not a reference, they carry no quoted
uncertainty, and comparing them against another build would prove nothing.

That is not an argument, it is a measurement. The identical seeded configuration
on Linux gave a DIFFERENT sample:

| | macOS | Linux |
|---|---|---|
| particle records | 990 | 978 |
| n / p | 450 / 336 | 442 / 344 |
| pi- / pi0 / pi+ | 76 / 65 / 57 | 70 / 71 / 43 |
| **baryon number** | **788** | **788** |
| **electric charge** | **316** | **316** |

Same seed, same configuration, same source, and the multiplicities move by up to
25 per cent while the two conservation laws are identical integers. Anchoring a
Monte Carlo transport code on multiplicities would therefore have produced a
skill that fails on every machine but the one it was built on. This is the whole
reason the anchor is a conservation law.

## The library test needs three environment hints, each found the hard way

`usage_of_SMASH_as_library` spawns a fresh cmake for the example project, and
that child inherits none of the main build's `-D` cache variables. It therefore
re-runs `find_package` for everything and fails on whatever the machine lacks.
Three failures appeared in sequence, each hidden behind the previous:

1. macOS: `Could NOT find Eigen3 (missing: EIGEN3_VERSION_OK)`, because it found
   the system Eigen 5.0.1 whose renamed macros SMASH 3.3 cannot parse. Fixed by
   exporting `EIGEN3_ROOT`.
2. Linux: `Could NOT find GSL (Required is at least version 2.0)`, because GSL
   lives only inside a conda prefix. Fixed by `GSL_ROOT_DIR`, `PKG_CONFIG_PATH`
   and `CMAKE_PREFIX_PATH`.
3. Linux again: `Failed to run SMASH library example`. The example now BUILT and
   then failed to load `libpythia8` from the custom prefix. Fixed by
   `LD_LIBRARY_PATH` (`DYLD_LIBRARY_PATH` on macOS).

Worth stating because the first fix moved the error rather than removing it, and
a skill that stopped after step 1 would have shipped a suite that fails 103/104
on any cluster.

## What the adversarial pass found

The first version of this skill FAILED its adversarial pass with 19 defects, 7
of them blockers, and every one was reproduced before being accepted. Two are
worth carrying forward because both looked like the skill's strong points:

1. **`--seed -2` did not pin anything.** SMASH treats ANY negative seed as
   "draw a fresh one", not only the `-1` in the shipped configs. Two runs with
   `--seed -2` used seeds 8409242248972502135 and 4845130125537222390 and
   produced different output, while `config_used.yaml` still read `-2` and
   looked pinned. The guard now judges the effective seed numerically.
2. **The baryon-number rule was still wrong after being "fixed" once.** Adding
   nuclear codes did not repair the underlying error, which was treating "four
   digits" as the definition of a baryon. Every excited state SMASH propagates
   breaks it: N(1440) is 12112, Lambda(1405) is 13122. It now transcribes
   `PdgCode::baryon_number()` from the source (a non-nuclear hadron whose n_q1
   digit is nonzero), checked against 24 cases including anti-resonances and
   hypertriton. The Au+Au anchor happens to be taken after the resonances have
   decayed, which is exactly why the error survived the first fix.

Both share a shape: a rule that held for the sample in front of me, generalized
without checking it against the code's own definition.

Other blockers were false-success paths: verify discarded ctest's exit status; a
mixed `(Failed)` plus `(Timeout)` slipped through a regex that matched only the
first; a retry that selected NO tests exited 0 and counted as a pass; pre-set
`SMASH`/`SMASH_BUILD` bypassed every identity check; `run_smash.sh` accepted a
forged OSCAR file; and shipped examples ran against the DEFAULT particle tables
because `-p/-d` were never passed, succeeding while computing the wrong thing.

Two factual corrections came out of it too: `potentials.cc` and `random.cc` are
not the only self-seeded tests, and the event grammar carries an `ensemble`
field this document originally omitted.

## Harness

`scripts/selftest_smash.sh`, 49 cases, no SMASH build required (the run tests use
a stub executable). Every guard has a negative case that fails only that guard,
and each asserts WHICH guard fired. The one that most needed it: SMASH prints
`WARN Fpe : Failed to setup trap on pole error.` on every macOS run, so a
case-insensitive search for "error" flags a healthy run. The passing control is
asserted to actually contain that warning line, so the error-guard test cannot
pass vacuously.
# SMASH input format

SMASH reads one YAML configuration, given with `-i`, and writes into the
directory given with `-o`. Everything below comes from the source
(`src/experiment.cc`, `src/include/smash/input_keys.h`, which is the
authoritative key list) and from the configurations the distribution ships in
`input/`, not from memory.

```
smash -i config.yaml -o output_dir
```

## The five modi

`General: Modus:` selects the physics setup, and `src/experiment.cc` accepts
exactly five values; anything else throws `InvalidModusRequest`:

| Modus | what it is | shipped example |
|---|---|---|
| `Collider` | two nuclei at an impact parameter, the heavy-ion case | `input/config.yaml` |
| `Box` | a periodic box of thermal matter, for equilibration studies | `input/box/config.yaml` |
| `Sphere` | an expanding thermalized sphere | `input/sphere/config.yaml` |
| `List` | replay externally supplied particles | `input/list/config.yaml` |
| `ListBox` | as `List`, but with box boundaries | |

Each modus reads its own sub-block under `Modi:`, and only that one.

## `General`

```yaml
General:
    Modus:          Collider
    Time_Step_Mode: Fixed
    Delta_Time:     0.1        # fm/c
    End_Time:       200.0      # fm/c
    Randomseed:     -1
    Nevents:        1
```

**`Randomseed: -1` means "draw a fresh seed from the system"**, which is what
every shipped config carries. A run made with it cannot be reproduced, compared
with a reference, or compared with itself. Pin an integer seed for anything you
intend to check. `run_smash.sh` refuses `-1` unless you pass
`--allow-random-seed`, precisely because the default is the irreproducible one.

`End_Time` dominates the run time: the shipped collider config uses 200 fm/c,
while 20 fm/c is enough for a smoke test and runs in about 25 s for 2 events.

## `Modi: Collider`

```yaml
Modi:
    Collider:
        Projectile:
            Particles: {2212: 79, 2112: 118}   # 79 protons, 118 neutrons = Au197
        Target:
            Particles: {2212: 79, 2112: 118}
        E_Kin: 1.23                            # GeV per nucleon, beam kinetic energy
        Fermi_Motion: "frozen"
```

Nuclei are given as PDG-code counts, not as a name and mass number: `2212` is the
proton and `2112` the neutron. This is worth stating because it is also what
makes the conservation check possible, since the expected baryon number and
charge follow directly from these counts (see `verification.md`).

The beam energy can be given as `E_Kin` (kinetic energy per nucleon in the
target frame), `E_Tot`, `P_Lab` or `Sqrtsnn`; give exactly one.

## `Output`

```yaml
Output:
    Output_Interval: 10.0
    Particles:
        Format: ["Oscar2013"]
        Only_Final: No
```

`Format` values are content-specific and case-sensitive, and the spelling is not
what you would guess: it is `Root`, not `ROOT`. Depending on the output block,
SMASH accepts `Oscar2013`, `Oscar2013_bin`, `Oscar1999`, `Binary`, `Root`,
`VTK`, `ASCII`, `HepMC_asciiv3`, `HepMC_treeroot`, `For_vHLLE`, `Lattice_ASCII`
and `Lattice_Binary`; the ROOT, HepMC and Rivet ones exist only if those optional
libraries were found at configure time. Read
`src/include/smash/input_keys.h` for the per-block list rather than assuming one
global set.
`Only_Final: Yes` writes just the final state, which is what you want unless you
are studying the time evolution. Content of the file: see `output-format.md`.

## `Collision_Term`

```yaml
Collision_Term:
    Collision_Criterion: "Covariant"
```

The main physics switch. Others in the shipped examples include `Strings`,
`Two_to_One`, `Included_2to2`, `Multi_Particle_Reactions` and the potentials
block; `input/potentials/config.yaml` is the worked example for mean-field
potentials.

## Overriding keys from the command line

SMASH accepts `-c "Key: value"` to override configuration entries, and
`run_smash.sh` instead rewrites the copied YAML so the output directory keeps a
`config_used.yaml` recording exactly what ran. Prefer that: a run whose input you
cannot reconstruct is not a measurement.

## Particle and decay data

`particles.txt` and `decaymodes.txt` next to a configuration override the
built-in tables. The box and sphere examples ship their own copies, which is why
their multiplicities differ from a collider run using the defaults.
# SMASH examples

No configuration is vendored here. `scripts/install_smash.sh` clones SMASH, and
its own `input/` directory holds the worked examples; `$SMASH_ROOT` points at it.

```bash
out="$(scripts/install_smash.sh)"
SMASH="$(printf '%s\n' "$out" | sed -n 's/^SMASH=//p')"
SMASH_ROOT="$(printf '%s\n' "$out" | sed -n 's/^SMASH_ROOT=//p')"
export SMASH SMASH_ROOT
```

Parse those lines rather than `eval`ing the output: they are filesystem paths
that the environment can influence.

## 1. Au+Au, the anchor

`$SMASH_ROOT/input/config.yaml` is the shipped collider setup: Au+Au at
E_kin = 1.23 GeV per nucleon, the HADES energy. Shortened to 2 events and
20 fm/c it runs in about 25 s.

```bash
scripts/run_smash.sh --config "$SMASH_ROOT/input/config.yaml" \
  --outdir /tmp/auau --seed 20260723 --nevents 2 --end-time 20.0

python3 scripts/check_conservation_smash.py /tmp/auau/out/particle_lists.oscar \
  --baryons 788 --charge 316
```

788 = 2 events x 2 nuclei x 197 nucleons, 316 = 2 x 2 x 79 protons. Both must be
exact. If you change `--nevents` or the nuclei, recompute them; the script will
not guess for you, on purpose.

At the full 200 fm/c the same case takes minutes rather than seconds, and that is
what you want for physics, not for a check.

## 2. Thermal box

`$SMASH_ROOT/input/box/config.yaml` fills a periodic box with thermal
multiplicities at T = 0.15 GeV and lets it equilibrate. This is the setup for
detailed-balance and equilibration studies, and it ships its own `particles.txt`
and `decaymodes.txt`, so its species content differs from a collider run.

Note that the conservation anchor above does NOT transfer: a box created from
thermal multiplicities has whatever baryon number the sampling gave it, so read
it from the first event rather than computing it from a projectile and target.

## 3. The others

`sphere` (expanding thermalized sphere), `potentials` (mean-field potentials),
`dileptons` and `photons` (rare-particle output with their own weighting),
`list` (replay an external particle list, which is how SMASH is used as an
afterburner), `deformed_nucleus` and `custom_nucleus` (initial-state geometry).
Each directory carries the configuration and any auxiliary tables it needs.

## Writing your own

Start from `input/config.yaml`, pin `Randomseed`, and change one thing at a time.
The two mistakes that cost the most time are leaving `Randomseed: -1` in a run
you later want to compare with something, and reading multiplicities from a run
with `Only_Final: No`, where every particle appears once per output interval and
the totals are therefore meaningless unless you select the final block.

exec
/bin/zsh -lc 'bash skills/smash/scripts/selftest_smash.sh' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 4169ms:
  ok    Randomseed -1 is refused by default
  ok    --allow-random-seed accepts it deliberately
  ok    a pinned --seed runs (control)
  ok    the pinned seed was written into config_used.yaml

run_smash.sh output guards (stub, one broken property each)
  ok    a nonzero exit status fails
  ok    a missing particle list fails
  ok    a header with no particles fails
  ok    NaN in the particle list fails
  ok    fewer events than requested fails
  ok    a real ERROR log line fails
  ok    the passing control really does contain the benign 'Fpe ... error.' warning

check_conservation_smash.py
  ok    a conserving list passes (control)
  ok    a wrong baryon expectation fails
  ok    a wrong charge expectation fails
  ok    a non-OSCAR2013 header fails
  ok    a non-finite kinematic value fails
  ok    a list with no particle records fails
  ok    an unclosed event block fails
  ok    a malformed record fails rather than being skipped
  ok    too few species fails when --species-min is given
  ok    --species-min is off by default (it was overstated as proof of physics)

verify_smash.sh
  ok    verify rejects an unknown argument
  ok    verify fails when the build directory is absent
  ok    verify refuses a build it cannot trace to the pinned source
  ok    an impostor binary beside a real build is rejected

guards added after the 2026-07-23 adversarial pass
  ok    --seed -2 is refused (SMASH randomizes any negative seed)
  ok    --seed -999 is refused (SMASH randomizes any negative seed)
  ok    --seed 0 is accepted (non-negative)
  ok    a malformed --end-time '--' is rejected
  ok    a malformed --end-time '1-2' is rejected
  ok    a malformed --end-time '.' is rejected
  ok    a malformed --end-time '1..2' is rejected
  ok    a malformed --end-time '1e3' is rejected
  ok    a malformed --end-time '-5' is rejected
  ok    --nevents 0 is rejected
  ok    a forged non-OSCAR header is rejected
  ok    event-end markers with no matching starts are rejected
  ok    records with the wrong column count are rejected
  ok    five-digit baryon resonances are counted (B=2, Q=0)
  ok    and a wrong expectation on them still fails
  ok    equal-and-opposite per-event violations do not cancel
  ok    a comment containing 'end' is not an event marker
  ok    a declared particle count that does not match is rejected

-------------------------------------------
selftest: 49 passed, 0 failed

exec
/bin/zsh -lc 'git diff --unified=5 842cbabd5''^..842cbabd5 -- skills/smash' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
diff --git a/skills/smash/SKILL.md b/skills/smash/SKILL.md
index b03d7c33b..8d3c9f143 100644
--- a/skills/smash/SKILL.md
+++ b/skills/smash/SKILL.md
@@ -14,26 +14,30 @@ collider energies.
 
 C++17, CMake. Needs GSL, Eigen 3.x, and Pythia exactly 8.316.
 
 ## Prime rules (do not skip)
 
-1. **Pin the random seed for anything you intend to check.** Every shipped
-   configuration carries `Randomseed: -1`, which draws a fresh seed and makes the
-   run unreproducible. `scripts/run_smash.sh` refuses `-1` unless you pass
-   `--allow-random-seed`. With a pinned seed the output is byte-identical between
+1. **Pin the random seed for anything you intend to check, and it must be
+   NON-NEGATIVE.** SMASH treats ANY negative seed as "draw a fresh one", not just
+   the `-1` every shipped configuration carries: `--seed -2` produced two
+   different runs whose `config_used.yaml` still read `-2`. `run_smash.sh`
+   refuses every negative seed unless you pass `--allow-random-seed`. With a pinned seed the output is byte-identical between
    two runs of the same build.
 2. **Do not compare multiplicities across machines.** SMASH is Monte Carlo, and
    transport amplifies floating-point differences into different collision
-   histories. Anchor on baryon number and electric charge, which are integers
-   fixed by the initial nuclei and identical on every platform and every seed:
-   `scripts/check_conservation_smash.py`.
+   histories; measured, macOS and Linux differ by up to 25 per cent at the same
+   seed. Anchor on baryon number and electric charge, checked per event, which
+   are integers fixed by the initial nuclei: `scripts/check_conservation_smash.py`.
+   They are a limited invariant, not proof that the physics is right.
 3. **This is a TIER 1 skill: it reproduces SMASH's own 104-case test suite.**
    Two of those cases are non-deterministic by upstream construction
    (`potentials.cc` and `random.cc` seed themselves from `std::random_device`
    and then assert statistical quantities), so `verify_smash.sh` retries exactly
    those two by name, once, and treats every other failure as fatal. Do not
-   widen that into "allow one failure".
+   widen that into "allow one failure". Two further files self-seed through
+   `generate_63bit_seed()` and have not been seen to flake, so this skill does
+   not claim the remaining cases are deterministic.
 4. **The dependencies fail with messages that point away from the cause.** The
    Pythia URL in SMASH's own INSTALL.md 404s and the 404 page gets saved as a
    `.tgz`; Eigen 5 makes SMASH report "at least version 3.0 is required" when the
    problem is that your Eigen is too NEW. Read `references/failure-modes.md`
    before debugging a build by hand.
@@ -70,13 +74,19 @@ and `python3`. Overrides: `SMASH_ROOT_DIR`, `SMASH_PIN`, `SMASH_JOBS`,
 scripts/run_smash.sh --config <config.yaml> --outdir /tmp/run1 \
   --seed 20260723 --nevents 2 --end-time 20.0
 ```
 
 Prints `RESULT_DIR=` and `RESULT_OSCAR=`. It copies the configuration it actually
-used into the output directory, then asserts a zero exit, a non-empty particle
-list, no NaN, no `ERROR`-severity log line, and that the number of completed
-events matches `Nevents` (a run can stop early and still exit 0).
+used into the output directory, then asserts a zero exit, an OSCAR2013 header,
+records with the column count that header declares, matched event-start and
+event-end markers whose number equals `Nevents` (a run can stop early and still
+exit 0), no NaN, and no `ERROR`-severity log line.
+
+It also stages a shipped example's own `particles.txt` and `decaymodes.txt` when
+they sit beside the configuration. SMASH does NOT pick those up implicitly, so
+without `-p/-d` the box and sphere examples run against the default tables and
+quietly compute something other than the example you asked for.
 
 ## Verifying
 
 ```bash
 scripts/verify_smash.sh              # test suite + seeded anchor, about 8 min
@@ -111,18 +121,20 @@ anchor possible.
 
 ## Reading the output
 
 `references/output-format.md`. `particle_lists.oscar` has twelve columns named on
 its first line (parse that line, it changes with the requested content), events
-delimited by `# event N out` and `# event N end`.
+delimited by `# event N ensemble E out COUNT` and
+`# event N ensemble E end ...`. The `ensemble` field is part of the real grammar;
+a pattern written without it matches nothing.
 
 ## Benchmark
 
 | stage | what | result |
 |---|---|---|
-| test suite | SMASH's own 104 ctest cases | reproduced, with the two self-seeded cases retried once |
-| anchor | seeded Au+Au, 2 events, E_kin 1.23 GeV/nucleon, 20 fm/c | baryon number 788 and charge 316, both EXACT |
+| test suite | SMASH's own 104 ctest cases | reproduced; 104/104 first attempt on Linux, with the two self-seeded cases retried once when they flake |
+| anchor | seeded Au+Au, 2 events, E_kin 1.23 GeV/nucleon, 20 fm/c | baryon number 788 and charge 316, EXACT per event (394 and 158 each) |
 
 The anchor's multiplicities (450 n, 336 p, 76 pi-, 65 pi0, 57 pi+, plus eta, K0,
 Lambda, Sigma-) are recorded in `references/verification.md` as a same-build
 reproducibility check only, never as a cross-platform reference.
 
diff --git a/skills/smash/references/failure-modes.md b/skills/smash/references/failure-modes.md
index b2776d7f1..8897ec59b 100644
--- a/skills/smash/references/failure-modes.md
+++ b/skills/smash/references/failure-modes.md
@@ -70,22 +70,29 @@ hints moves the error rather than removing it.
 Every shipped config carries `Randomseed: -1`, meaning "draw a fresh seed". A run
 made with it cannot be compared with a reference, with another machine, or with
 itself. `run_smash.sh` refuses `-1` unless `--allow-random-seed` is given. With a
 pinned seed the output is byte-identical between two runs of the same build.
 
-## 5. Two of the shipped unit tests fail at random, by construction
+## 5. Four test files seed themselves; two of them are observed flakes
 
 `src/tests/potentials.cc` and `src/tests/random.cc` open with
 
 ```cpp
 TEST(set_random_seed) { std::random_device rd; random::set_seed(rd()); }
 ```
 
 and then assert statistical quantities against fixed tolerances, so they fail
 occasionally on a correct build. Measured: `potentials` passed 4 of 5
-consecutive standalone runs on macOS. Only 2 of 82 test files do this; the rest
-are deterministic.
+consecutive standalone runs on macOS.
+
+**Those two are the observed flakes, not the complete list of self-seeded
+tests.** `src/tests/scatteraction.cc` (three call sites) and
+`src/tests/dynamic_fluidization.cc` (one) also seed themselves, through
+`random::generate_63bit_seed()`, which draws from `std::random_device` as well.
+Neither has been seen to fail, so they are not on the retry list, but the claim
+"the other 102 are deterministic" is false and is not made. Audit this set before
+extending the retry policy.
 
 `verify_smash.sh` therefore retries **only those two by name, once**, and treats
 any other failure as fatal. Do not relax this into "allow one failure": that is
 exactly how a real regression gets through.
 
@@ -96,22 +103,30 @@ startup. It is a warning, not an error, and every run prints it. Beware when
 writing log checks: a case-insensitive search for "error" matches this line, so
 match SMASH's severity FIELD (`^[time] ERROR`) instead. `run_smash.sh` does.
 
 ## 7. A run can exit 0 having written fewer events than requested
 
-Check the number of `# event N end` markers against `Nevents` rather than
-trusting the exit status. `run_smash.sh` does this and fails when they disagree.
+Check matched `# event N ensemble E out` / `... end` pairs against `Nevents`
+rather than trusting the exit status. `run_smash.sh` does this and fails when
+they disagree. Note the `ensemble` field: a pattern written as `# event N end`
+matches nothing in real SMASH-3.3 output.
 
 ## 8. Comparing multiplicities across machines is not a test
 
 SMASH is Monte Carlo, and a transport code amplifies any floating-point
 difference into different collision histories. The seeded output is bit-identical
 within one build and should NOT be expected to match another build. Anchor on
 baryon number and electric charge, which are integers fixed by the initial
 nuclei and identical on every platform, for every seed. See
 `scripts/check_conservation_smash.py`.
 
+Be honest about what that buys, though. B and Q are a **limited invariant**, not
+a general broken-build detector: most cross-section, collision-ordering, spectra,
+flow and timing regressions preserve both. They catch a build that has lost
+particles or corrupted its bookkeeping, and the test suite catches the rest. Do
+not present a passing conservation check as evidence that the physics is right.
+
 ## 9. Licensing
 
 GPL-3.0-or-later, confirmed twice: `LICENSE.md` in the repository and the
 `rightsList` of the Zenodo record for SMASH-3.3. GitHub's detector reports
 NOASSERTION only because `LICENSE.md` additionally reproduces the BSD-3, CC0 and
diff --git a/skills/smash/references/input-format.md b/skills/smash/references/input-format.md
index 76e580e27..cabf50fcc 100644
--- a/skills/smash/references/input-format.md
+++ b/skills/smash/references/input-format.md
@@ -75,12 +75,18 @@ Output:
     Particles:
         Format: ["Oscar2013"]
         Only_Final: No
 ```
 
-`Format` chooses among `Oscar2013`, `Oscar1999`, `Binary`, `ROOT`, `VTK` and
-`HepMC` (the last two need optional libraries found at configure time).
+`Format` values are content-specific and case-sensitive, and the spelling is not
+what you would guess: it is `Root`, not `ROOT`. Depending on the output block,
+SMASH accepts `Oscar2013`, `Oscar2013_bin`, `Oscar1999`, `Binary`, `Root`,
+`VTK`, `ASCII`, `HepMC_asciiv3`, `HepMC_treeroot`, `For_vHLLE`, `Lattice_ASCII`
+and `Lattice_Binary`; the ROOT, HepMC and Rivet ones exist only if those optional
+libraries were found at configure time. Read
+`src/include/smash/input_keys.h` for the per-block list rather than assuming one
+global set.
 `Only_Final: Yes` writes just the final state, which is what you want unless you
 are studying the time evolution. Content of the file: see `output-format.md`.
 
 ## `Collision_Term`
 
diff --git a/skills/smash/references/output-format.md b/skills/smash/references/output-format.md
index ace89a782..e86be061b 100644
--- a/skills/smash/references/output-format.md
+++ b/skills/smash/references/output-format.md
@@ -7,39 +7,46 @@ its own input.
 ## Files
 
 | file | when | content |
 |---|---|---|
 | `particle_lists.oscar` | `Output: Particles: Format: ["Oscar2013"]` | the particle list, the main result |
-| `collisions_binary.bin` | `Collisions:` with `Binary` format | every interaction, for reaction-by-reaction analysis |
+| `collisions_custom.bin` | `Collisions:` with `Binary` format | every interaction, for reaction-by-reaction analysis |
+| `collisions_oscar2013.bin` | `Collisions:` with `Oscar2013_bin` | the same, in binary OSCAR |
 | `config.yaml` | always | the configuration as SMASH parsed it |
 | `thermodynamics.dat`, `*.vtk` | on request | densities and fields on a grid |
-| `SMASH.log` | if configured | the run log; otherwise it goes to stdout |
 
-`run_smash.sh` additionally keeps `smash.log`, `stderr.txt` and
-`config_used.yaml` beside the output directory.
+
+SMASH logs to stdout; there is no configurable log FILE. `run_smash.sh` captures
+that stream as `smash.log`, alongside `stderr.txt`, `config_used.yaml` and, when
+a shipped example carries its own tables, `particles_used.txt` and
+`decaymodes_used.txt`.
 
 ## OSCAR2013 particle lists
 
 ```
 #!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge
 # Units: fm fm fm fm GeV GeV GeV GeV GeV none none e
 # SMASH-3.3
-# event 0 out
+# event 0 ensemble 0 out 497
       20.0   1.23  -4.56   7.89  0.938   1.02   0.11  -0.05   0.30  2212   17   1
 ...
-# event 0 end 0 impact   4.321 empty no
+# event 0 ensemble 0 end 0 impact   0.000 scattering_projectile_target yes
 ```
 
 Twelve columns, in the order named on the first line: time, three positions,
 mass, four-momentum, PDG code, particle ID within the event, and electric charge
 in units of e. **The header line is the authority on the column order**, since it
 changes with the output content requested; parse it rather than assuming.
 
-Events are delimited by `# event N out` and `# event N end`. The `end` line
-carries the impact parameter and an `empty` flag. Counting `# event .* end`
-markers is how `run_smash.sh` checks that the run wrote every event it was asked
-for instead of stopping early with a zero exit status.
+Events are delimited by `# event N ensemble E out COUNT` and
+`# event N ensemble E end 0 impact X scattering_projectile_target yes|no`, the
+grammar in `src/oscaroutput.cc`. The `out` line DECLARES how many particles
+follow, which is worth checking against the records that actually follow.
+Counting matched pairs of those markers is how `run_smash.sh` checks that the run
+wrote every event it was asked for instead of stopping early with a zero exit
+status. Note the `ensemble` field: matching only `# event N out` misses the real
+output entirely.
 
 `Only_Final: No` writes an intermediate list at each `Output_Interval`, so the
 same particle appears many times and multiplicities must be taken from the final
 block only. `Only_Final: Yes` writes just the end of each event.
 
diff --git a/skills/smash/references/verification.md b/skills/smash/references/verification.md
index 7d3149ca8..ec4c03963 100644
--- a/skills/smash/references/verification.md
+++ b/skills/smash/references/verification.md
@@ -47,19 +47,26 @@ TEST(set_random_seed) {
 ```
 
 and then assert statistical quantities against fixed tolerances, for example
 `COMPARE_ABSOLUTE_ERROR(P2[0].front().momentum().velocity().x1(), 0.6, 0.003)`.
 A fresh seed on every run therefore produces an occasional failure on a
-perfectly good build. Only 2 of the 82 test source files do this.
+perfectly good build.
+
+**Two more test files seed themselves and are not on the retry list.**
+`scatteraction.cc` calls `random::generate_63bit_seed()` at three sites and
+`dynamic_fluidization.cc` at one; that helper also draws from
+`std::random_device`. Neither has been observed to fail, so neither is retried,
+but this skill does NOT claim that the other 102 cases are deterministic. Four
+files self-seed; two of them have been seen to flake.
 
 Measured flake rate on macOS: **`potentials` passed 4 of 5 consecutive
 standalone runs**. Across two full-suite runs it failed once and passed once.
 On Linux the suite passed **104 of 104 on the first attempt**, which is what a
 statistical failure looks like: present on one machine's run and absent on
 another's, with no platform meaning to it at all.
 
-The policy this justifies is deliberately narrow: the other 102 cases must pass
+The policy this justifies is deliberately narrow: every other case must pass
 on the **first** attempt, and only those two names may be retried, **once**. A
 statistical fluke passes with a fresh seed; a broken build fails twice. Widening
 this to "allow one failure" would let a real regression through, which is the
 hole an earlier skill's `Total >= N` check had.
 
@@ -78,12 +85,15 @@ configuration produced a byte-identical `particle_lists.oscar` (997 lines).
 |---|---|---|---|
 | baryon number | 788 | 2 events x 2 nuclei x 197 | EXACT |
 | electric charge | 316 e | 2 events x 2 nuclei x 79 | EXACT |
 
 Those are integers fixed by the initial nuclei, so they are identical on every
-platform and for every seed, and a build that is subtly broken cannot satisfy
-them by luck. Contrast the multiplicities from the same run:
+platform and for every seed. They are checked **per event**, not only on the
+total, because equal and opposite violations in two events would otherwise
+cancel into a clean-looking sum. Their scope is limited and stated as such: they
+catch lost particles and corrupted bookkeeping, not a wrong cross section, and
+the test suite is what covers the rest. Contrast the multiplicities from the same run:
 
 ```
 n 450, p 336, pi- 76, pi0 65, pi+ 57, eta 2, K0 2, Lambda 1, Sigma- 1
 ```
 
@@ -127,13 +137,47 @@ Three failures appeared in sequence, each hidden behind the previous:
 
 Worth stating because the first fix moved the error rather than removing it, and
 a skill that stopped after step 1 would have shipped a suite that fails 103/104
 on any cluster.
 
+## What the adversarial pass found
+
+The first version of this skill FAILED its adversarial pass with 19 defects, 7
+of them blockers, and every one was reproduced before being accepted. Two are
+worth carrying forward because both looked like the skill's strong points:
+
+1. **`--seed -2` did not pin anything.** SMASH treats ANY negative seed as
+   "draw a fresh one", not only the `-1` in the shipped configs. Two runs with
+   `--seed -2` used seeds 8409242248972502135 and 4845130125537222390 and
+   produced different output, while `config_used.yaml` still read `-2` and
+   looked pinned. The guard now judges the effective seed numerically.
+2. **The baryon-number rule was still wrong after being "fixed" once.** Adding
+   nuclear codes did not repair the underlying error, which was treating "four
+   digits" as the definition of a baryon. Every excited state SMASH propagates
+   breaks it: N(1440) is 12112, Lambda(1405) is 13122. It now transcribes
+   `PdgCode::baryon_number()` from the source (a non-nuclear hadron whose n_q1
+   digit is nonzero), checked against 24 cases including anti-resonances and
+   hypertriton. The Au+Au anchor happens to be taken after the resonances have
+   decayed, which is exactly why the error survived the first fix.
+
+Both share a shape: a rule that held for the sample in front of me, generalized
+without checking it against the code's own definition.
+
+Other blockers were false-success paths: verify discarded ctest's exit status; a
+mixed `(Failed)` plus `(Timeout)` slipped through a regex that matched only the
+first; a retry that selected NO tests exited 0 and counted as a pass; pre-set
+`SMASH`/`SMASH_BUILD` bypassed every identity check; `run_smash.sh` accepted a
+forged OSCAR file; and shipped examples ran against the DEFAULT particle tables
+because `-p/-d` were never passed, succeeding while computing the wrong thing.
+
+Two factual corrections came out of it too: `potentials.cc` and `random.cc` are
+not the only self-seeded tests, and the event grammar carries an `ensemble`
+field this document originally omitted.
+
 ## Harness
 
-`scripts/selftest_smash.sh`, 29 cases, no SMASH build required (the run tests use
+`scripts/selftest_smash.sh`, 49 cases, no SMASH build required (the run tests use
 a stub executable). Every guard has a negative case that fails only that guard,
 and each asserts WHICH guard fired. The one that most needed it: SMASH prints
 `WARN Fpe : Failed to setup trap on pole error.` on every macOS run, so a
 case-insensitive search for "error" flags a healthy run. The passing control is
 asserted to actually contain that warning line, so the error-guard test cannot
diff --git a/skills/smash/scripts/__pycache__/check_conservation_smash.cpython-314.pyc b/skills/smash/scripts/__pycache__/check_conservation_smash.cpython-314.pyc
index 77fd2fc04..d8bb12335 100644
Binary files a/skills/smash/scripts/__pycache__/check_conservation_smash.cpython-314.pyc and b/skills/smash/scripts/__pycache__/check_conservation_smash.cpython-314.pyc differ
diff --git a/skills/smash/scripts/check_conservation_smash.py b/skills/smash/scripts/check_conservation_smash.py
index 763c5621d..95e6f521b 100755
--- a/skills/smash/scripts/check_conservation_smash.py
+++ b/skills/smash/scripts/check_conservation_smash.py
@@ -7,142 +7,222 @@ multiplicities are not a portable reference and comparing them across builds
 proves little. Baryon number and electric charge, by contrast, are conserved
 EXACTLY by the transport, whatever the seed, the platform or the statistics. A
 build that is subtly broken shows up here as an integer that does not match,
 which no amount of statistical spread can excuse.
 
+Conservation is checked PER EVENT, not only on the total. Two events with equal
+and opposite violations sum to the right answer, so a total-only check can be
+satisfied by output that is wrong twice over.
+
 Usage:
   check_conservation_smash.py <particle_lists.oscar> --baryons N --charge Z
                               [--species-min 3]
+--baryons and --charge are TOTALS over all events; they must divide evenly by
+the number of events found, because every event starts from the same nuclei.
 Exit 0 when the conservation laws hold, 1 otherwise.
 """
 import argparse
 import collections
 import math
+import re
 import sys
 
 
 def baryon_number(pdg):
-    """Baryon number from a PDG code.
-
-    Two cases, and getting only the first right is a real bug rather than an
-    edge case: SMASH's particle table contains light NUCLEI (deuteron
-    1000010020, triton 1000010030, He3 1000020030, hypertriton 1010010030) and
-    the `light_nuclei` configuration produces them.
-
-      * Ordinary baryons have 4-digit codes, |code| in [1000, 10000), B = +-1.
-      * Nuclei use the PDG nuclear code +-10LZZZAAAI, a 10-digit number whose
-        baryon number is A, read from digits 7 to 9. A deuteron counted as a
-        4-digit baryon, or ignored, costs 2 units of baryon number and turns an
-        exact conservation check into a wrong one.
+    """Baryon number from a PDG code, transcribed from SMASH's own rule.
+
+    `src/include/smash/pdgcode.h`, `PdgCode::baryon_number()`:
+
+        if (is_nucleus())                       return A * antiparticle_sign();
+        if (!is_hadron() || digits_.n_q1_ == 0) return 0;
+        return antiparticle_sign();
+
+    with `is_hadron() = (n_q3 != 0 && n_q2 != 0 && !is_nucleus())`.
+
+    So a baryon is NOT "a four-digit code". It is any non-nuclear hadron whose
+    first quark digit n_q1 is nonzero, which includes every excited state SMASH
+    propagates: N(1440) is 12112, Lambda(1405) is 13122, and higher resonances
+    run to six and seven digits. Judging by digit count silently gives all of
+    them baryon number zero, and resonances are the bulk of a transport run's
+    intermediate state, so that error turns an "exact" conservation check into a
+    wrong one on any output taken before they have decayed.
+
+    Digit layout for a non-nucleus, from the right:
+        n_J (1) n_q3 (10) n_q2 (100) n_q1 (1000) n_L (1e4) n_R (1e5) n (1e6)
+    Nuclei use +-10LZZZAAAI, ten digits, and their baryon number is A.
     """
     a = abs(pdg)
     sign = 1 if pdg > 0 else -1
-    if 1000 <= a < 10000:
-        return sign
-    if 1000000000 <= a < 10000000000:      # +-10LZZZAAAI
+    # Nucleus: +-10LZZZAAAI
+    if a >= 1000000000:
         A = (a // 10) % 1000
         return sign * A
-    return 0
+    n_q3 = (a // 10) % 10
+    n_q2 = (a // 100) % 10
+    n_q1 = (a // 1000) % 10
+    if n_q3 == 0 or n_q2 == 0:      # not a hadron (lepton, photon, ...)
+        return 0
+    if n_q1 == 0:                   # meson
+        return 0
+    return sign
+
+
+
+# Real SMASH-3.3 grammar, from src/oscaroutput.cc:
+#   # event N ensemble E out COUNT
+#   # event N ensemble E end 0 impact X scattering_projectile_target yes|no
+# Counting any comment that contains " end" accepted a file with no event blocks
+# at all, as long as some comment happened to contain that substring.
+EVENT_OUT = re.compile(r'^#\s+event\s+(\d+)\s+ensemble\s+(\d+)\s+out\b(?:\s+(\d+))?')
+EVENT_END = re.compile(r'^#\s+event\s+(\d+)\s+ensemble\s+(\d+)\s+end\b')
 
 
 def main():
     ap = argparse.ArgumentParser()
     ap.add_argument('oscar')
     ap.add_argument('--baryons', type=int, required=True,
-                    help='expected total baryon number, summed over all events '
-                         '(for Au+Au that is Nevents * 2 * 197)')
+                    help='expected TOTAL baryon number over all events (Au+Au: Nevents * 2 * 197)')
     ap.add_argument('--charge', type=int, required=True,
-                    help='expected total electric charge in units of e '
-                         '(for Au+Au that is Nevents * 2 * 79)')
-    ap.add_argument('--species-min', type=int, default=3,
-                    help='minimum number of distinct species expected; a transport run that '
-                         'produced only nucleons did not actually do any physics')
+                    help='expected TOTAL electric charge in units of e (Au+Au: Nevents * 2 * 79)')
+    ap.add_argument('--species-min', type=int, default=0,
+                    help='minimum number of distinct species. Off by default: a species count is '
+                         'weak evidence and was overstated as proof that inelastic physics happened.')
     args = ap.parse_args()
 
     for name, val in (('--baryons', args.baryons), ('--charge', args.charge)):
         if abs(val) > 10 ** 9:
             print(f"FAIL: {name}={val} is not a plausible expectation")
             return 1
-    if args.species_min < 1:
-        print(f"FAIL: --species-min must be at least 1, got {args.species_min}")
+    if args.species_min < 0:
+        print(f"FAIL: --species-min cannot be negative, got {args.species_min}")
         return 1
 
-    pdg_counts = collections.Counter()
-    total_charge = 0
-    total_baryons = 0
-    n_records = 0
-    events_ended = 0
-    bad_lines = 0
-
     try:
         fh = open(args.oscar)
     except OSError as exc:
         print(f"FAIL: cannot read {args.oscar}: {exc}")
         return 1
 
+    pdg_counts = collections.Counter()
+    events = []          # (label, baryons, charge, n_records, declared)
+    open_ev = None       # (label, baryons, charge, n_records, declared)
+    bad_lines = 0
+    stray = 0
+
     with fh:
         header = fh.readline()
         if not header.startswith('#!OSCAR2013'):
             print(f"FAIL: {args.oscar} does not start with an OSCAR2013 header: {header.strip()[:80]!r}")
             return 1
+        # The header NAMES its columns and the set changes with the requested
+        # content, so read the positions rather than assuming 9 and 11.
+        cols = header.split()[2:]
+        try:
+            i_pdg = cols.index('pdg')
+            i_chg = cols.index('charge')
+        except ValueError:
+            print(f"FAIL: the OSCAR header does not declare both 'pdg' and 'charge' columns: {cols}")
+            return 1
+        ncols = len(cols)
+
         for line in fh:
             if line.startswith('#'):
-                if ' end' in line:
-                    events_ended += 1
+                m = EVENT_OUT.match(line)
+                if m:
+                    if open_ev is not None:
+                        print(f"FAIL: event {m.group(1)}/{m.group(2)} starts while "
+                              f"{open_ev[0]} is still open")
+                        return 1
+                    declared = int(m.group(3)) if m.group(3) else None
+                    open_ev = [f"{m.group(1)}/{m.group(2)}", 0, 0, 0, declared]
+                    continue
+                m = EVENT_END.match(line)
+                if m:
+                    label = f"{m.group(1)}/{m.group(2)}"
+                    if open_ev is None:
+                        print(f"FAIL: event {label} ends without a matching 'out' line")
+                        return 1
+                    if open_ev[0] != label:
+                        print(f"FAIL: event {label} ends while {open_ev[0]} is open")
+                        return 1
+                    events.append(tuple(open_ev))
+                    open_ev = None
                 continue
+
             fields = line.split()
-            # t x y z mass p0 px py pz pdg ID charge
-            if len(fields) < 12:
+            if len(fields) != ncols:
                 bad_lines += 1
                 continue
             try:
-                for f in fields[:9]:
+                for f in fields[:i_pdg]:
                     if not math.isfinite(float(f)):
-                        print(f"FAIL: non-finite kinematic value in {args.oscar}: {line.strip()[:90]!r}")
+                        print(f"FAIL: non-finite value in {args.oscar}: {line.strip()[:90]!r}")
                         return 1
-                pdg = int(fields[9])
-                charge = int(fields[11])
+                pdg = int(fields[i_pdg])
+                charge = int(fields[i_chg])
             except ValueError:
                 bad_lines += 1
                 continue
+            if open_ev is None:
+                stray += 1
+                continue
             pdg_counts[pdg] += 1
-            total_charge += charge
-            total_baryons += baryon_number(pdg)
-            n_records += 1
+            open_ev[1] += baryon_number(pdg)
+            open_ev[2] += charge
+            open_ev[3] += 1
 
     if bad_lines:
         print(f"FAIL: {bad_lines} malformed particle records in {args.oscar}")
         return 1
-    if n_records == 0:
-        print(f"FAIL: {args.oscar} contains no particle records")
+    if stray:
+        print(f"FAIL: {stray} particle records lie outside any event block")
+        return 1
+    if open_ev is not None:
+        print(f"FAIL: event {open_ev[0]} was never closed; the run was truncated")
         return 1
-    if events_ended == 0:
-        print(f"FAIL: {args.oscar} contains no event-end marker; the run was truncated")
+    if not events:
+        print(f"FAIL: {args.oscar} contains no complete event block")
         return 1
 
-    ok = True
-    print(f"particles: {n_records} records, {len(pdg_counts)} distinct species, {events_ended} events")
+    n_ev = len(events)
+    n_records = sum(e[3] for e in events)
+    if n_records == 0:
+        print(f"FAIL: {args.oscar} contains no particle records")
+        return 1
 
-    if total_baryons != args.baryons:
-        print(f"FAIL: baryon number {total_baryons} != expected {args.baryons}")
-        ok = False
-    else:
-        print(f"  baryon number conserved EXACTLY: {total_baryons}")
+    print(f"particles: {n_records} records, {len(pdg_counts)} distinct species, {n_ev} events")
 
-    if total_charge != args.charge:
-        print(f"FAIL: total charge {total_charge} != expected {args.charge}")
-        ok = False
-    else:
-        print(f"  electric charge conserved EXACTLY: {total_charge}")
+    ok = True
+    # The declared count on each 'out' line must match what followed it.
+    for label, _, _, got, declared in events:
+        if declared is not None and declared != got:
+            print(f"FAIL: event {label} declares {declared} particles but {got} records follow")
+            ok = False
+
+    # Per event, not just on the total: equal and opposite violations in two
+    # events would otherwise cancel into a clean-looking sum.
+    if args.baryons % n_ev or args.charge % n_ev:
+        print(f"FAIL: expectations {args.baryons}/{args.charge} do not divide evenly by {n_ev} events; "
+              f"every event starts from the same nuclei, so they must")
+        return 1
+    b_per, q_per = args.baryons // n_ev, args.charge // n_ev
+    for label, b, q, _, _ in events:
+        if b != b_per:
+            print(f"FAIL: event {label} has baryon number {b}, expected {b_per}")
+            ok = False
+        if q != q_per:
+            print(f"FAIL: event {label} has charge {q}, expected {q_per}")
+            ok = False
+    if ok:
+        print(f"  baryon number conserved EXACTLY in every event: {b_per} each, {args.baryons} total")
+        print(f"  electric charge conserved EXACTLY in every event: {q_per} each, {args.charge} total")
 
-    if len(pdg_counts) < args.species_min:
-        print(f"FAIL: only {len(pdg_counts)} distinct species, expected at least {args.species_min}; "
-              f"no inelastic physics happened")
+    if args.species_min and len(pdg_counts) < args.species_min:
+        print(f"FAIL: only {len(pdg_counts)} distinct species, expected at least {args.species_min}")
         ok = False
 
-    top = ', '.join(f"{k}:{v}" for k, v in pdg_counts.most_common(6))
-    print(f"  most abundant: {top}")
+    print("  most abundant: " + ', '.join(f"{k}:{v}" for k, v in pdg_counts.most_common(6)))
 
     if ok:
         print("CONSERVATION OK")
         return 0
     print("CONSERVATION FAILED")
diff --git a/skills/smash/scripts/run_smash.sh b/skills/smash/scripts/run_smash.sh
index 342d3ef4b..7c4bca27f 100755
--- a/skills/smash/scripts/run_smash.sh
+++ b/skills/smash/scripts/run_smash.sh
@@ -21,10 +21,11 @@
 # SMASH is GPL-3.0-or-later; see install_smash.sh.
 set -euo pipefail
 
 HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 CONFIG=""; OUTDIR=""; SEED=""; NEVENTS=""; END_TIME=""; ALLOW_RANDOM=0
+PARTICLES=""; DECAYMODES=""; AUTO_TABLES=1
 
 log () { echo "run_smash: $*" >&2; }
 die () { log "$*"; exit 1; }
 
 while [ $# -gt 0 ]; do
@@ -32,10 +33,13 @@ while [ $# -gt 0 ]; do
     --config)   CONFIG="${2:-}"; shift 2 ;;
     --outdir)   OUTDIR="${2:-}"; shift 2 ;;
     --seed)     SEED="${2:-}"; shift 2 ;;
     --nevents)  NEVENTS="${2:-}"; shift 2 ;;
     --end-time) END_TIME="${2:-}"; shift 2 ;;
+    --particles)  PARTICLES="${2:-}"; shift 2 ;;
+    --decaymodes) DECAYMODES="${2:-}"; shift 2 ;;
+    --no-auto-tables) AUTO_TABLES=0; shift ;;
     --allow-random-seed) ALLOW_RANDOM=1; shift ;;
     -h|--help)  sed -n '2,22p' "$0"; exit 0 ;;
     *) die "unknown argument '$1'" ;;
   esac
 done
@@ -44,17 +48,33 @@ done
 [ -f "$CONFIG" ] || die "config '$CONFIG' does not exist"
 CONFIG="$(cd "$(dirname "$CONFIG")" && pwd)/$(basename "$CONFIG")"
 grep -q "General:" "$CONFIG" || die "config '$CONFIG' has no 'General:' block; is it a SMASH configuration?"
 
 # Numeric arguments must be numeric before they are spliced into YAML.
-for pair in "seed:$SEED" "nevents:$NEVENTS"; do
-  name="${pair%%:*}"; val="${pair#*:}"
-  [ -z "$val" ] && continue
-  case "$val" in ''|*[!0-9-]*) die "--$name must be an integer, got '$val'" ;; esac
-done
+# Full-match validation. The earlier character-class test accepted '--', '1-2'
+# and '1..2', which are not numbers even though they contain only permitted
+# characters.
+is_int ()  { printf '%s' "$1" | grep -qE '^-?[0-9]+$'; }
+is_uint () { printf '%s' "$1" | grep -qE '^[0-9]+$'; }
+is_num ()  { printf '%s' "$1" | grep -qE '^[0-9]+(\.[0-9]+)?$'; }
+
+if [ -n "$SEED" ]; then
+  is_int "$SEED" || die "--seed must be an integer, got '$SEED'"
+  # SMASH treats EVERY negative seed as "draw a random one", not just -1: two
+  # runs with --seed -2 produced seeds 8409242248972502135 and
+  # 4845130125537222390 and different output, while config_used.yaml still read
+  # -2 and looked pinned.
+  if [ "$SEED" -lt 0 ] && [ "$ALLOW_RANDOM" = "0" ]; then
+    die "--seed $SEED is negative, and SMASH treats any negative seed as random, so the run would be irreproducible. Use a non-negative seed, or --allow-random-seed."
+  fi
+fi
+if [ -n "$NEVENTS" ]; then
+  is_uint "$NEVENTS" || die "--nevents must be a non-negative integer, got '$NEVENTS'"
+  [ "$NEVENTS" -ge 1 ] || die "--nevents must be at least 1"
+fi
 if [ -n "$END_TIME" ]; then
-  case "$END_TIME" in ''|*[!0-9.]*) die "--end-time must be a number, got '$END_TIME'" ;; esac
+  is_num "$END_TIME" || die "--end-time must be a non-negative number, got '$END_TIME'"
 fi
 
 if [ -n "${SMASH:-}" ]; then
   BIN="$SMASH"
 else
@@ -95,18 +115,47 @@ apply Randomseed "$SEED"
 apply Nevents "$NEVENTS"
 apply End_Time "$END_TIME"
 
 # Refuse an unpinned seed by default. SMASH's shipped configs carry -1, and a
 # run made with it cannot be compared with anything, including itself.
-EFFECTIVE_SEED="$(grep -E "^[[:space:]]*Randomseed:" "$WORKCFG" | head -1 | sed -E 's/.*Randomseed:[[:space:]]*//')"
-if [ "$EFFECTIVE_SEED" = "-1" ] && [ "$ALLOW_RANDOM" = "0" ]; then
-  die "Randomseed is -1, so this run would be irreproducible. Pass --seed <int>, or --allow-random-seed if that is what you want."
+# Read the seed that will ACTUALLY be used, from the config as it now stands,
+# and judge it numerically. A literal test against "-1" missed every other
+# negative value, which SMASH treats identically.
+EFFECTIVE_SEED="$(grep -E "^[[:space:]]*Randomseed:" "$WORKCFG" | head -1 | sed -E 's/.*Randomseed:[[:space:]]*//' | tr -d '\r')"
+if [ "$ALLOW_RANDOM" = "0" ]; then
+  if ! is_int "$EFFECTIVE_SEED"; then
+    die "the configuration's Randomseed is '$EFFECTIVE_SEED', which is not an integer; pin it with --seed"
+  fi
+  if [ "$EFFECTIVE_SEED" -lt 0 ]; then
+    die "the configuration's Randomseed is $EFFECTIVE_SEED; SMASH treats ANY negative seed as random, so this run would be irreproducible. Pass --seed <non-negative>, or --allow-random-seed."
+  fi
+fi
+
+# Several shipped examples (box, sphere, multi_particle_box) carry their OWN
+# particles.txt and decaymodes.txt next to the configuration, and SMASH does not
+# pick those up implicitly: without -p/-d it silently runs the DEFAULT tables, so
+# the run succeeds and is simply not the example you asked for. Auto-detect them.
+CFGDIR="$(dirname "$CONFIG")"
+if [ "$AUTO_TABLES" = "1" ]; then
+  [ -z "$PARTICLES" ]  && [ -f "$CFGDIR/particles.txt" ]   && PARTICLES="$CFGDIR/particles.txt"
+  [ -z "$DECAYMODES" ] && [ -f "$CFGDIR/decaymodes.txt" ] && DECAYMODES="$CFGDIR/decaymodes.txt"
+fi
+EXTRA=()
+if [ -n "$PARTICLES" ]; then
+  [ -f "$PARTICLES" ] || die "--particles '$PARTICLES' does not exist"
+  cp "$PARTICLES" "$OUTDIR/particles_used.txt"; EXTRA+=(-p "$PARTICLES")
+  log "using particle table $PARTICLES"
+fi
+if [ -n "$DECAYMODES" ]; then
+  [ -f "$DECAYMODES" ] || die "--decaymodes '$DECAYMODES' does not exist"
+  cp "$DECAYMODES" "$OUTDIR/decaymodes_used.txt"; EXTRA+=(-d "$DECAYMODES")
+  log "using decay table $DECAYMODES"
 fi
 
 log "running $(basename "$BIN") with seed $EFFECTIVE_SEED into $OUTDIR"
 set +e
-"$BIN" -i "$WORKCFG" -o "$OUTDIR/out" > "$OUTDIR/smash.log" 2> "$OUTDIR/stderr.txt"
+( cd "$CFGDIR" && "$BIN" -i "$WORKCFG" -o "$OUTDIR/out" ${EXTRA+"${EXTRA[@]}"} ) > "$OUTDIR/smash.log" 2> "$OUTDIR/stderr.txt"
 STATUS=$?
 set -e
 
 if [ "$STATUS" -ne 0 ]; then
   log "SMASH exited with status $STATUS"
@@ -132,19 +181,32 @@ ERROR_RE="^\[[^]]*\][[:space:]]+(ERROR|FATAL)\b|terminate called|Assertion .* fa
 if grep -qE "$ERROR_RE" "$OUTDIR/smash.log" "$OUTDIR/stderr.txt" 2>/dev/null; then
   log "the run logged an error:"; grep -E "$ERROR_RE" "$OUTDIR/smash.log" "$OUTDIR/stderr.txt" 2>/dev/null | head -5 >&2
   exit 1
 fi
 
+# Structural validation, not a line count. A stub that wrote a non-OSCAR header,
+# two garbage records and two forged "# event ... end" comments used to be
+# reported as a complete run.
+head -1 "$OSCAR" | grep -qE '^#!OSCAR2013' \
+  || die "the particle output does not start with an OSCAR2013 header"
 NPART="$(grep -cvE '^#' "$OSCAR" || true)"
 [ "${NPART:-0}" -gt 0 ] || die "the particle output has a header but no particles"
 
-# The number of events actually written must match what was asked for, or the
-# run stopped early while still exiting 0.
 WANT_EVENTS="$(grep -E "^[[:space:]]*Nevents:" "$WORKCFG" | head -1 | sed -E 's/.*Nevents:[[:space:]]*//')"
-GOT_EVENTS="$(grep -cE '^# event .* end' "$OSCAR" || true)"
+# Match SMASH-3.3's real grammar, "# event N ensemble E out/end ...", and require
+# the out and end blocks to pair up.
+GOT_OUT="$(grep -cE '^# event [0-9]+ ensemble [0-9]+ out\b' "$OSCAR" || true)"
+GOT_EVENTS="$(grep -cE '^# event [0-9]+ ensemble [0-9]+ end\b' "$OSCAR" || true)"
+[ "${GOT_OUT:-0}" -eq "${GOT_EVENTS:-0}" ] \
+  || die "the output has $GOT_OUT event-start and $GOT_EVENTS event-end markers; they must pair"
 if [ -n "$WANT_EVENTS" ] && [ "${GOT_EVENTS:-0}" -ne "$WANT_EVENTS" ]; then
-  die "requested $WANT_EVENTS events but the output ends $GOT_EVENTS of them; the run stopped early"
+  die "requested $WANT_EVENTS events but the output completes $GOT_EVENTS of them; the run stopped early"
 fi
+# Every record must have the number of columns the header declares.
+NCOL="$(head -1 "$OSCAR" | awk '{print NF-2}')"
+BADCOL="$(awk -v n="$NCOL" '!/^#/ && NF != n' "$OSCAR" | wc -l | tr -d ' ')"
+[ "${BADCOL:-0}" -eq 0 ] \
+  || die "$BADCOL particle records do not have the $NCOL columns the header declares"
 
 log "run complete: $GOT_EVENTS events, $NPART particle records"
 echo "RESULT_DIR=$OUTDIR"
 echo "RESULT_OSCAR=$OSCAR"
diff --git a/skills/smash/scripts/selftest_smash.sh b/skills/smash/scripts/selftest_smash.sh
index 6df6dccec..a88c5bd13 100755
--- a/skills/smash/scripts/selftest_smash.sh
+++ b/skills/smash/scripts/selftest_smash.sh
@@ -50,21 +50,21 @@ EOF
 write_oscar () {   # write_oscar <path> [extra body lines on stdin]
   {
     echo '#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge'
     echo '# Units: fm fm fm fm GeV GeV GeV GeV GeV none none e'
     echo '# SMASH-3.3'
-    echo '# event 0 out'
+    echo '# event 0 ensemble 0 out 4'
     echo '  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1'
     echo '  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2112 1 0'
     echo '  20.0 0.1 0.2 0.3 0.138 0.3 0.1 0.0 0.0  211 2 1'
     echo '  20.0 0.1 0.2 0.3 0.138 0.3 0.1 0.0 0.0 -211 3 -1'
-    echo '# event 0 end 0 impact 4.0 empty no'
-    echo '# event 1 out'
+    echo '# event 0 ensemble 0 end 0 impact   4.000 scattering_projectile_target yes'
+    echo '# event 1 ensemble 0 out 3'
     echo '  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1'
     echo '  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2112 1 0'
     echo '  20.0 0.1 0.2 0.3 0.138 0.3 0.1 0.0 0.0  111 2 0'
-    echo '# event 1 end 0 impact 4.0 empty no'
+    echo '# event 1 ensemble 0 end 0 impact   4.000 scattering_projectile_target yes'
   } > "$1"
 }
 GOOD="$TMP/good.oscar"; write_oscar "$GOOD"
 # baryons: 2212,2112 twice = 4;  charge: +1+0+1-1 +1+0+0 = +2
 
@@ -98,11 +98,11 @@ STUB
     cat "$GOOD"
     echo 'OSC'
     cat <<'STUB2'
 case "$MODE" in
   nan)      sed -i.bak 's/0.938 1.0/0.938 nan/' "$F"; rm -f "$F.bak" ;;
-  oneevent) sed -i.bak '/# event 1 end/d' "$F"; rm -f "$F.bak" ;;
+  oneevent) sed -i.bak '/# event 1 ensemble 0/d' "$F"; rm -f "$F.bak" ;;
   realerror) printf "[15'04'57]  ERROR        Main        : something exploded\n" ;;
 esac
 exit 0
 STUB2
   } > "$path"
@@ -114,11 +114,11 @@ expect_fail_with "missing --config is rejected" "--config is required" "$RUN"
 expect_fail_with "a nonexistent config is rejected" "does not exist" "$RUN" --config "$TMP/nope"
 printf 'not: a smash config\n' > "$TMP/notcfg"
 expect_fail_with "a file with no General: block is rejected" "no 'General:' block" "$RUN" --config "$TMP/notcfg"
 expect_fail_with "an unknown argument is rejected" "unknown argument" "$RUN" --config "$CFG" --bogus
 expect_fail_with "a non-integer --seed is rejected" "must be an integer" "$RUN" --config "$CFG" --seed 1.5
-expect_fail_with "a non-numeric --end-time is rejected" "must be a number" "$RUN" --config "$CFG" --end-time abc
+expect_fail_with "a non-numeric --end-time is rejected" "must be a non-negative number" "$RUN" --config "$CFG" --seed 1 --end-time abc
 
 echo
 echo "run_smash.sh seed policy"
 write_stub stub_ok ok
 SKY=""; export SMASH="$TMP/stub_ok"
@@ -153,38 +153,153 @@ grep -q "Fpe" "$TMP/w_ok/smash.log" \
   || bad "control lost the Fpe warning, so the error-guard test is not isolated"
 
 echo
 echo "check_conservation_smash.py"
 expect_pass "a conserving list passes (control)" python3 "$CC" "$GOOD" --baryons 4 --charge 2
-expect_fail_with "a wrong baryon expectation fails" "baryon number" python3 "$CC" "$GOOD" --baryons 5 --charge 2
-expect_fail_with "a wrong charge expectation fails" "total charge" python3 "$CC" "$GOOD" --baryons 4 --charge 3
+expect_fail_with "a wrong baryon expectation fails" "baryon number" python3 "$CC" "$GOOD" --baryons 8 --charge 2
+expect_fail_with "a wrong charge expectation fails" "charge" python3 "$CC" "$GOOD" --baryons 4 --charge 6
 sed 's/^#!OSCAR2013/#!SOMETHINGELSE/' "$GOOD" > "$TMP/badhdr.oscar"
 expect_fail_with "a non-OSCAR2013 header fails" "does not start with an OSCAR2013 header" \
   python3 "$CC" "$TMP/badhdr.oscar" --baryons 4 --charge 2
 sed 's/0.938 1.0 0.1 0.1 0.1 2212 0 1/0.938 nan 0.1 0.1 0.1 2212 0 1/' "$GOOD" > "$TMP/nan.oscar"
 expect_fail_with "a non-finite kinematic value fails" "non-finite" python3 "$CC" "$TMP/nan.oscar" --baryons 4 --charge 2
 grep -v '^ ' "$GOOD" > "$TMP/empty.oscar"
 expect_fail_with "a list with no particle records fails" "no particle records" \
   python3 "$CC" "$TMP/empty.oscar" --baryons 4 --charge 2
-grep -v 'end' "$GOOD" > "$TMP/noend.oscar"
-expect_fail_with "a truncated list with no event-end marker fails" "no event-end marker" \
-  python3 "$CC" "$TMP/noend.oscar" --baryons 4 --charge 2
+sed -n '1,7p' "$GOOD" | grep -v 'ensemble 0 end' > "$TMP/noend.oscar"
+expect_fail_with "an unclosed event block fails" "never closed" \
+  python3 "$CC" "$TMP/noend.oscar" --baryons 2 --charge 1
 sed 's/  20.0 0.1 0.2 0.3 0.138 0.3 0.1 0.0 0.0  211 2 1/  20.0 0.1 BAD/' "$GOOD" > "$TMP/mal.oscar"
 expect_fail_with "a malformed record fails rather than being skipped" "malformed" \
   python3 "$CC" "$TMP/mal.oscar" --baryons 4 --charge 2
-expect_fail_with "too few species fails" "distinct species" \
+expect_fail_with "too few species fails when --species-min is given" "distinct species" \
   python3 "$CC" "$GOOD" --baryons 4 --charge 2 --species-min 99
+expect_pass "--species-min is off by default (it was overstated as proof of physics)" \
+  python3 "$CC" "$GOOD" --baryons 4 --charge 2
 
 echo
 echo "verify_smash.sh"
 expect_fail_with "verify rejects an unknown argument" "unknown argument" "$VERIFY" --bogus
 SMASH="$TMP/stub_ok" SMASH_BUILD="$TMP/no_such_build" SMASH_ROOT="$TMP" \
   expect_fail_with "verify fails when the build directory is absent" "no build directory" "$VERIFY" --tests-only
 mkdir -p "$TMP/fakebuild"
 SMASH="$TMP/stub_ok" SMASH_BUILD="$TMP/fakebuild" SMASH_ROOT="$TMP/no_such_root" \
-  expect_fail_with "verify fails when the shipped config is absent" "shipped collider config is missing" \
+  expect_fail_with "verify refuses a build it cannot trace to the pinned source" "not verifiably come from the pinned source" \
   "$VERIFY" --anchor-only
+# The identity guard must also reject a REAL clone whose binary is not the
+# stamped one, which is the attack that a fake build directory represented.
+if [ -d "$HOME/.cache/fusion/smash/smash/.git" ]; then
+  cp "$TMP/stub_ok" "$TMP/impostor"
+  SMASH="$TMP/impostor" \
+    SMASH_BUILD="$HOME/.cache/fusion/smash/smash/build" \
+    SMASH_ROOT="$HOME/.cache/fusion/smash/smash" \
+    expect_fail_with "an impostor binary beside a real build is rejected" "does not match the build stamp" \
+    "$VERIFY" --tests-only
+else
+  ok "skipped the impostor-binary case (no local SMASH clone to test against)"
+fi
+
+echo
+echo "guards added after the 2026-07-23 adversarial pass"
+# --- negative seeds other than -1 (SMASH treats every negative seed as random)
+for sd in -2 -999; do
+  SMASH="$TMP/stub_ok" expect_fail_with "--seed $sd is refused (SMASH randomizes any negative seed)" "irreproducible" \
+    "$RUN" --config "$CFG" --outdir "$TMP/w_neg$sd" --seed "$sd"
+done
+SMASH="$TMP/stub_ok" expect_pass "--seed 0 is accepted (non-negative)" \
+  "$RUN" --config "$CFG" --outdir "$TMP/w_zero" --seed 0
+for bad in -- 1-2 . 1..2 "1e3" "-5"; do
+  SMASH="$TMP/stub_ok" expect_fail "a malformed --end-time '$bad' is rejected" \
+    "$RUN" --config "$CFG" --outdir "$TMP/w_bad" --seed 1 --end-time "$bad"
+done
+SMASH="$TMP/stub_ok" expect_fail_with "--nevents 0 is rejected" "at least 1" \
+  "$RUN" --config "$CFG" --outdir "$TMP/w_ev0" --seed 1 --nevents 0
+
+# --- structural OSCAR validation: forged output must not pass
+write_bad_stub () {   # $1 name, $2 the body written to particle_lists.oscar
+  local path="$TMP/$1"
+  { echo '#!/bin/bash'
+    echo 'OUT=""; while [ $# -gt 0 ]; do case "$1" in -o) OUT="$2"; shift 2;; --version) echo SMASH-3.3; exit 0;; *) shift;; esac; done'
+    echo 'mkdir -p "$OUT"'
+    echo "cat > \"\$OUT/particle_lists.oscar\" <<'EOSC'"
+    cat
+    echo 'EOSC'
+    echo 'exit 0'
+  } > "$path"
+  chmod +x "$path"
+}
+write_bad_stub stub_forged <<'EOF'
+# not an oscar header at all
+# event 0 ensemble 0 end 0
+# event 1 ensemble 0 end 0
+  garbage record here
+EOF
+SMASH="$TMP/stub_forged" expect_fail_with "a forged non-OSCAR header is rejected" "OSCAR2013 header" \
+  "$RUN" --config "$CFG" --outdir "$TMP/w_forged" --seed 1
+write_bad_stub stub_unpaired <<'EOF'
+#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge
+# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes
+# event 1 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes
+  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1
+EOF
+SMASH="$TMP/stub_unpaired" expect_fail_with "event-end markers with no matching starts are rejected" "must pair" \
+  "$RUN" --config "$CFG" --outdir "$TMP/w_unpaired" --seed 1
+write_bad_stub stub_cols <<'EOF'
+#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge
+# event 0 ensemble 0 out 1
+  20.0 0.1 0.2 2212 1
+# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes
+# event 1 ensemble 0 out 1
+  20.0 0.1 0.2 2212 1
+# event 1 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes
+EOF
+SMASH="$TMP/stub_cols" expect_fail_with "records with the wrong column count are rejected" "columns the header declares" \
+  "$RUN" --config "$CFG" --outdir "$TMP/w_cols" --seed 1
+
+# --- conservation checker: resonances, per-event, strict grammar
+python3 - <<'PYEOF' > "$TMP/reso.oscar"
+print('#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge')
+print('# Units: fm fm fm fm GeV GeV GeV GeV GeV none none e')
+print('# event 0 ensemble 0 out 2')
+print('  20.0 0.1 0.2 0.3 1.440 1.5 0.1 0.1 0.1 12112 0 0')   # N(1440)0, B=1
+print('  20.0 0.1 0.2 0.3 1.405 1.5 0.1 0.1 0.1 13122 1 0')   # Lambda(1405), B=1
+print('# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes')
+PYEOF
+expect_pass "five-digit baryon resonances are counted (B=2, Q=0)" \
+  python3 "$CC" "$TMP/reso.oscar" --baryons 2 --charge 0
+expect_fail_with "and a wrong expectation on them still fails" "baryon number" \
+  python3 "$CC" "$TMP/reso.oscar" --baryons 0 --charge 0
+
+# Per-event: two events whose violations cancel in the sum must NOT pass.
+python3 - <<'PYEOF' > "$TMP/cancel.oscar"
+print('#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge')
+print('# event 0 ensemble 0 out 2')
+print('  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1')
+print('  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 1 1')   # event 0: B=2
+print('# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes')
+print('# event 1 ensemble 0 out 0')
+print('# event 1 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes')  # event 1: B=0
+PYEOF
+expect_fail_with "equal-and-opposite per-event violations do not cancel" "event" \
+  python3 "$CC" "$TMP/cancel.oscar" --baryons 2 --charge 2
+# A comment merely containing " end" is not an event end.
+python3 - <<'PYEOF' > "$TMP/fakeend.oscar"
+print('#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge')
+print('# this comment contains the word end but is not an event marker')
+print('  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1')
+PYEOF
+expect_fail_with "a comment containing 'end' is not an event marker" "outside any event block" \
+  python3 "$CC" "$TMP/fakeend.oscar" --baryons 1 --charge 1
+# The declared per-event count must match the records that follow.
+python3 - <<'PYEOF' > "$TMP/miscount.oscar"
+print('#!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge')
+print('# event 0 ensemble 0 out 5')
+print('  20.0 0.1 0.2 0.3 0.938 1.0 0.1 0.1 0.1 2212 0 1')
+print('# event 0 ensemble 0 end 0 impact 1.0 scattering_projectile_target yes')
+PYEOF
+expect_fail_with "a declared particle count that does not match is rejected" "declares 5" \
+  python3 "$CC" "$TMP/miscount.oscar" --baryons 1 --charge 1
 
 echo
 echo "-------------------------------------------"
 echo "selftest: $PASS passed, $FAIL failed"
 [ "$FAIL" -eq 0 ] || exit 1
diff --git a/skills/smash/scripts/verify_smash.sh b/skills/smash/scripts/verify_smash.sh
index 559fc4aa1..e87fe2faa 100755
--- a/skills/smash/scripts/verify_smash.sh
+++ b/skills/smash/scripts/verify_smash.sh
@@ -36,11 +36,15 @@ HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 DO_TESTS=1
 DO_ANCHOR=1
 # The only tests permitted a retry, because they seed themselves from
 # std::random_device. Keep this list minimal and justified.
 FLAKY_TESTS="potentials random"
-EXPECTED_TESTS="${SMASH_EXPECTED_TESTS:-104}"
+# 104 is the count for the pinned SMASH-3.3. An override is allowed for a
+# deliberately different pin, but then the run is NOT tier-1 certifying and says
+# so, because "reproduces the shipped suite" means the shipped suite.
+EXPECTED_TESTS_PINNED=104
+EXPECTED_TESTS="${SMASH_EXPECTED_TESTS:-$EXPECTED_TESTS_PINNED}"
 
 log () { echo "verify_smash: $*" >&2; }
 die () { log "$*"; exit 1; }
 
 while [ $# -gt 0 ]; do
@@ -53,11 +57,13 @@ while [ $# -gt 0 ]; do
 done
 
 if [ -n "${SMASH:-}" ] && [ -n "${SMASH_BUILD:-}" ] && [ -n "${SMASH_ROOT:-}" ]; then
   BIN="$SMASH"; BUILD="$SMASH_BUILD"; SRCROOT="$SMASH_ROOT"; EIGEN="${SMASH_EIGEN3_ROOT:-}"
   GSL="${SMASH_GSL_PREFIX:-}"; PYPFX="${SMASH_PYTHIA_PREFIX:-}"
+  PRESET=1
 else
+  PRESET=0
   OUT="$("$HERE/install_smash.sh")" || die "install_smash.sh failed"
   BIN="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH=//p')"
   BUILD="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_BUILD=//p')"
   SRCROOT="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_ROOT=//p')"
   EIGEN="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_EIGEN3_ROOT=//p')"
@@ -65,10 +71,38 @@ else
   PYPFX="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_PYTHIA_PREFIX=//p')"
 fi
 [ -x "$BIN" ] || die "no usable SMASH executable"
 [ -d "$BUILD" ] || die "no build directory at '$BUILD'"
 
+# Pre-set SMASH/SMASH_BUILD/SMASH_ROOT used to skip every identity check, so a
+# fake build directory next to the real binary certified tier 1. Whatever the
+# route, the tree must be the pinned commit, the build must carry the
+# installer's stamp, and the binary must be the one that stamp describes.
+PIN="${SMASH_PIN:-d1a1c6cf0a0002ee064eec1b929b9a7c14b3d5bc}"
+check_identity () {
+  local head stamp
+  [ -d "$SRCROOT/.git" ] || { log "'$SRCROOT' is not a git clone, so its commit cannot be established"; return 1; }
+  head="$(cd "$SRCROOT" && git rev-parse HEAD 2>/dev/null || echo none)"
+  [ "$head" = "$PIN" ] || { log "source tree is at $head, not the pinned $PIN"; return 1; }
+  ( cd "$SRCROOT" && git diff --quiet HEAD 2>/dev/null ) || { log "source tree has uncommitted modifications"; return 1; }
+  stamp="$BUILD/.fusion_build_stamp"
+  [ -f "$stamp" ] || { log "no build stamp at $stamp; this build was not produced by install_smash.sh"; return 1; }
+  local want got
+  want="$(sed -n 2p "$stamp")"
+  got="$(shasum -a 256 "$BIN" 2>/dev/null | cut -d' ' -f1 || echo none)"
+  [ "$want" = "$got" ] || { log "the binary does not match the build stamp (stamped $want, found $got)"; return 1; }
+  case "$BIN" in
+    "$BUILD"/*) : ;;
+    *) log "the binary '$BIN' does not live inside the build directory '$BUILD'"; return 1 ;;
+  esac
+  return 0
+}
+if ! check_identity; then
+  die "refusing to certify: the build does not verifiably come from the pinned source (see above)"
+fi
+log "identity OK: pinned commit, clean tree, stamped binary"
+
 FAILED=0
 
 # The usage_of_SMASH_as_library test spawns a FRESH cmake for the library
 # example, and that child inherits NONE of this build's -D cache variables, so
 # every hint has to reach it through the ENVIRONMENT. Three were needed, each
@@ -93,41 +127,72 @@ if [ "$DO_TESTS" = "1" ]; then
   command -v ctest >/dev/null || die "ctest not found"
   LOG="$(mktemp)"
   log "running SMASH's own test suite (about 6 minutes)"
   set +e
   run_ctest -j"$( (command -v nproc >/dev/null && nproc) || sysctl -n hw.ncpu 2>/dev/null || echo 4 )" > "$LOG" 2>&1
+  CTEST_RC=$?
   set -e
 
   TOTAL="$(sed -nE 's/.* tests passed, [0-9]+ tests failed out of ([0-9]+).*/\1/p' "$LOG" | tail -1)"
   NFAIL="$(sed -nE 's/.* tests passed, ([0-9]+) tests failed out of [0-9]+.*/\1/p' "$LOG" | tail -1)"
   [ -n "$TOTAL" ] || { log "could not parse a ctest summary; last lines:"; tail -10 "$LOG" >&2; rm -f "$LOG"; exit 1; }
 
+  # The exit status is checked INDEPENDENTLY of the parsed text. A ctest that
+  # printed a clean "104 of 104" summary and then exited nonzero used to be
+  # accepted, because only the text was read.
+  if [ "$CTEST_RC" -ne 0 ] && [ "${NFAIL:-0}" -eq 0 ]; then
+    log "FAIL: ctest exited $CTEST_RC while reporting no failures; the summary and the status disagree"
+    tail -10 "$LOG" >&2
+    FAILED=1
+  fi
+
   # An EXACT count, never "at least": a suite that silently skipped cases would
   # otherwise certify the build.
+  if [ "$EXPECTED_TESTS" != "$EXPECTED_TESTS_PINNED" ]; then
+    log "NOTE: SMASH_EXPECTED_TESTS=$EXPECTED_TESTS overrides the pinned $EXPECTED_TESTS_PINNED;"
+    log "      this run is NOT a tier-1 certification of the pinned release"
+  fi
   if [ "$TOTAL" != "$EXPECTED_TESTS" ]; then
     log "FAIL: ctest ran $TOTAL cases, expected exactly $EXPECTED_TESTS"
     log "      (set SMASH_EXPECTED_TESTS if you deliberately pinned a different SMASH version)"
     FAILED=1
   fi
 
   if [ "${NFAIL:-0}" -eq 0 ]; then
     log "test suite: $TOTAL of $TOTAL passed on the first attempt"
   else
-    FAILING="$(sed -nE 's/^[[:space:]]+[0-9]+ - ([A-Za-z0-9_]+) \(Failed\).*/\1/p' "$LOG" | sort -u)"
-    [ -n "$FAILING" ] || { log "FAIL: $NFAIL tests failed but their names could not be parsed"; tail -10 "$LOG" >&2; FAILED=1; FAILING=""; }
+    # Parse EVERY ctest failure status, not only "(Failed)": a case that timed
+    # out or was not run is still a failure, and matching only "(Failed)" let a
+    # real regression through while the one self-seeded flake was retried.
+    FAILING="$(sed -nE 's/^[[:space:]]+[0-9]+ - ([A-Za-z0-9_.-]+) \(([A-Za-z ]+)\).*/\1/p' "$LOG" | sort -u)"
+    NPARSED="$(printf '%s\n' "$FAILING" | grep -c . || true)"
+    # The number of names parsed must equal the number ctest reported, or
+    # something was missed and the retry logic below is reasoning about a
+    # different set than the one that actually failed.
+    if [ "${NPARSED:-0}" -ne "${NFAIL:-0}" ]; then
+      log "FAIL: ctest reported $NFAIL failures but $NPARSED names could be parsed; refusing to guess"
+      sed -nE 's/^[[:space:]]+[0-9]+ - .*/&/p' "$LOG" | head -10 >&2
+      FAILED=1
+      FAILING=""
+    fi
     for t in $FAILING; do
       case " $FLAKY_TESTS " in
         *" $t "*)
           log "'$t' failed; it seeds itself from std::random_device, so retrying it once"
           set +e
           run_ctest -R "^$t\$" > "$LOG.retry" 2>&1
           RC=$?
           set -e
-          if [ "$RC" -eq 0 ]; then
-            log "'$t' passed on retry, which is what a statistical fluke does"
+          # Exit status alone is not enough: `ctest -R` that matches NOTHING
+          # prints "No tests were found!!!" and exits 0, which used to be read
+          # as a clean retry. Require exactly one test selected and passed.
+          RTOTAL="$(sed -nE 's/.* tests passed, [0-9]+ tests failed out of ([0-9]+).*/\1/p' "$LOG.retry" | tail -1)"
+          RFAIL="$(sed -nE 's/.* tests passed, ([0-9]+) tests failed out of [0-9]+.*/\1/p' "$LOG.retry" | tail -1)"
+          if [ "$RC" -eq 0 ] && [ "${RTOTAL:-0}" = "1" ] && [ "${RFAIL:-1}" = "0" ]; then
+            log "'$t' passed on retry (1 of 1), which is what a statistical fluke does"
           else
-            log "FAIL: '$t' failed twice, so this is not statistical"
+            log "FAIL: retry of '$t' did not cleanly pass exactly one test (rc=$RC, total=${RTOTAL:-none}, failed=${RFAIL:-none})"
             tail -15 "$LOG.retry" >&2
             FAILED=1
           fi
           rm -f "$LOG.retry"
           ;;

exec
/bin/zsh -lc "nl -ba skills/smash/scripts/install_smash.sh | sed -n '120,255p'
nl -ba skills/smash/scripts/verify_smash.sh | sed -n '35,245p'
printf 'STAMP\\n'; sed -n '1,20p' \""'$HOME/.cache/fusion/smash/smash/build/.fusion_build_stamp" 2>/dev/null || true
printf '"'TREE\\n'; git -C \""'$HOME/.cache/fusion/smash/smash" status --short 2>/dev/null || true
printf '"'HEAD\\n'; git -C \""'$HOME/.cache/fusion/smash/smash" rev-parse HEAD 2>/dev/null || true
printf '"'BIN\\n'; shasum -a 256 \""'$HOME/.cache/fusion/smash/smash/build/smash" 2>/dev/null || true' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
   120	    fi
   121	  done
   122	  return 1
   123	}
   124	GSL_PREFIX="$(find_gsl_prefix || true)"
   125	if [ -n "$GSL_PREFIX" ]; then
   126	  log "GSL prefix: $GSL_PREFIX"
   127	else
   128	  log "GSL not found. Install it (macOS: brew install gsl; Linux: conda install -c conda-forge gsl,"
   129	  log "or the distribution's libgsl-dev) or set SMASH_GSL_PREFIX."
   130	  exit 1
   131	fi
   132	
   133	# ------------------------------------------------------------------ Pythia
   134	if [ ! -x "$PYTHIA_PREFIX/bin/pythia8-config" ]; then
   135	  if [ ! -d "$ROOT_DIR/pythia$PYTHIA_VERSION" ]; then
   136	    fetch_tarball "$PYTHIA_URL" "$ROOT_DIR/pythia$PYTHIA_VERSION.tgz" "Pythia $PYTHIA_VERSION" || exit 1
   137	    ( cd "$ROOT_DIR" && tar xzf "pythia$PYTHIA_VERSION.tgz" ) || { log "unpacking Pythia failed"; exit 1; }
   138	    rm -f "$ROOT_DIR/pythia$PYTHIA_VERSION.tgz"
   139	  fi
   140	  log "building Pythia $PYTHIA_VERSION (several minutes, once)"
   141	  ( cd "$ROOT_DIR/pythia$PYTHIA_VERSION" \
   142	    && ./configure --prefix="$PYTHIA_PREFIX" \
   143	    && make -j"$JOBS" && make install ) > "$ROOT_DIR/pythia-build.log" 2>&1 || {
   144	    log "Pythia build failed, last 15 lines of $ROOT_DIR/pythia-build.log:"
   145	    tail -15 "$ROOT_DIR/pythia-build.log" >&2; exit 1; }
   146	fi
   147	[ -x "$PYTHIA_PREFIX/bin/pythia8-config" ] || { log "pythia8-config missing from $PYTHIA_PREFIX"; exit 1; }
   148	PYTHIA_ACTUAL="$("$PYTHIA_PREFIX/bin/pythia8-config" --version 2>/dev/null || echo unknown)"
   149	log "Pythia $PYTHIA_ACTUAL at $PYTHIA_PREFIX"
   150	
   151	# -------------------------------------------------------------------- clone
   152	if [ -d "$SRCROOT/.git" ]; then
   153	  log "reusing clone at $SRCROOT"
   154	elif [ -e "$SRCROOT" ]; then
   155	  log "$SRCROOT exists but is not a git clone; refusing to delete a directory this script did not create"
   156	  exit 1
   157	else
   158	  command -v git >/dev/null || { log "git not found"; exit 1; }
   159	  TMPCLONE="$ROOT_DIR/.clone.$$"
   160	  if [ -e "$TMPCLONE" ] && [ ! -f "$TMPCLONE/.fusion_tmpclone" ]; then
   161	    log "refusing to reuse '$TMPCLONE': not one of this script's temporary clones"; exit 1
   162	  fi
   163	  safe_rmrf "$TMPCLONE"
   164	  mkdir -p "$TMPCLONE" && : > "$TMPCLONE/.fusion_tmpclone"
   165	  git clone -q "$REPO" "$TMPCLONE/repo" || { log "clone failed"; safe_rmrf "$TMPCLONE"; exit 1; }
   166	  mv "$TMPCLONE/repo" "$TMPCLONE.repo" && safe_rmrf "$TMPCLONE" && mv "$TMPCLONE.repo" "$SRCROOT"
   167	fi
   168	
   169	( cd "$SRCROOT" && git diff --quiet HEAD 2>/dev/null ) || {
   170	  log "the clone at $SRCROOT has uncommitted modifications; refusing to build from it"; exit 1; }
   171	( cd "$SRCROOT" && git checkout -q "$PIN" 2>/dev/null ) || {
   172	  log "cannot check out pinned commit $PIN (shallow clone, or upstream history moved)"
   173	  log "set SMASH_PIN to re-pin deliberately"; exit 1; }
   174	log "pinned at $PIN"
   175	
   176	# ------------------------------------------------------------------- build
   177	build_identity () {
   178	  local head dirty cxx
   179	  head="$(cd "$SRCROOT" 2>/dev/null && git rev-parse HEAD 2>/dev/null || echo nohead)"
   180	  if (cd "$SRCROOT" 2>/dev/null && git diff --quiet HEAD 2>/dev/null); then dirty=clean; else dirty=DIRTY; fi
   181	  cxx="$(command -v c++ 2>/dev/null || echo nocxx)|$(c++ --version 2>/dev/null | head -1 || echo nover)"
   182	  echo "$head|$dirty|$cxx|$PYTHIA_ACTUAL|$EIGEN_VERSION|$GSL_PREFIX|$(uname -s)|$(uname -m)"
   183	}
   184	binary_digest () { shasum -a 256 "$BIN" 2>/dev/null | cut -d' ' -f1 || echo nodigest; }
   185	
   186	fast_path_ok () {
   187	  [ -x "$BIN" ] || return 1
   188	  [ -f "$STAMP" ] || return 1
   189	  [ "$(cd "$SRCROOT" && git rev-parse HEAD)" = "$PIN" ] || { log "clone HEAD is not the pin, rebuilding"; return 1; }
   190	  [ "$(head -1 "$STAMP")" = "$(build_identity)" ] || { log "build identity changed, rebuilding"; return 1; }
   191	  [ "$(sed -n 2p "$STAMP")" = "$(binary_digest)" ] || { log "the binary changed since it was stamped, rebuilding"; return 1; }
   192	  return 0
   193	}
   194	
   195	if fast_path_ok; then
   196	  log "found smash built from the pinned commit with a matching build identity, probing it"
   197	else
   198	  command -v cmake >/dev/null || { log "cmake not found (3.16 or newer required)"; exit 1; }
   199	  mkdir -p "$BUILD"
   200	  log "configuring"
   201	  ( cd "$BUILD" && rm -f CMakeCache.txt && rm -rf CMakeFiles \
   202	    && EIGEN3_ROOT="$EIGEN_ROOT" \
   203	       PKG_CONFIG_PATH="$GSL_PREFIX/lib/pkgconfig${PKG_CONFIG_PATH:+:$PKG_CONFIG_PATH}" \
   204	       CMAKE_PREFIX_PATH="$GSL_PREFIX${CMAKE_PREFIX_PATH:+:$CMAKE_PREFIX_PATH}" \
   205	       cmake -DGSL_ROOT_DIR="$GSL_PREFIX" \
   206	         -DPythia_CONFIG_EXECUTABLE="$PYTHIA_PREFIX/bin/pythia8-config" \
   207	         -DEIGEN3_INCLUDE_DIR="$EIGEN_ROOT" .. ) > "$ROOT_DIR/cmake.log" 2>&1 || {
   208	    log "cmake failed, last 15 lines of $ROOT_DIR/cmake.log:"; tail -15 "$ROOT_DIR/cmake.log" >&2; exit 1; }
   209	  log "building with -j$JOBS (several minutes, once)"
   210	  ( cd "$BUILD" && EIGEN3_ROOT="$EIGEN_ROOT" make -j"$JOBS" ) > "$ROOT_DIR/build.log" 2>&1 || {
   211	    log "build failed, last 20 lines of $ROOT_DIR/build.log:"; tail -20 "$ROOT_DIR/build.log" >&2; exit 1; }
   212	  [ -x "$BIN" ] || { log "build reported success but $BIN is missing"; exit 1; }
   213	  { build_identity; binary_digest; } > "$STAMP"
   214	fi
   215	
   216	# ------------------------------------------------------------------- probe
   217	# Content is the verdict AND the exit status must be zero. A crash after
   218	# printing a plausible banner would otherwise pass.
   219	probe_binary () {
   220	  local out
   221	  out="$("$BIN" --version 2>&1)" || { log "smash --version exited nonzero"; return 1; }
   222	  case "$out" in
   223	    SMASH-*) : ;;
   224	    *) log "smash --version did not report a SMASH version: ${out%%$'\n'*}"; return 1 ;;
   225	  esac
   226	  log "probe: ${out%%$'\n'*}"
   227	}
   228	probe_binary || { log "the built executable failed its probe"; exit 1; }
   229	
   230	echo "SMASH=$BIN"
   231	echo "SMASH_ROOT=$SRCROOT"
   232	echo "SMASH_BUILD=$BUILD"
   233	echo "SMASH_EIGEN3_ROOT=$EIGEN_ROOT"
   234	echo "SMASH_GSL_PREFIX=$GSL_PREFIX"
   235	echo "SMASH_PYTHIA_PREFIX=$PYTHIA_PREFIX"
    35	HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    36	DO_TESTS=1
    37	DO_ANCHOR=1
    38	# The only tests permitted a retry, because they seed themselves from
    39	# std::random_device. Keep this list minimal and justified.
    40	FLAKY_TESTS="potentials random"
    41	# 104 is the count for the pinned SMASH-3.3. An override is allowed for a
    42	# deliberately different pin, but then the run is NOT tier-1 certifying and says
    43	# so, because "reproduces the shipped suite" means the shipped suite.
    44	EXPECTED_TESTS_PINNED=104
    45	EXPECTED_TESTS="${SMASH_EXPECTED_TESTS:-$EXPECTED_TESTS_PINNED}"
    46	
    47	log () { echo "verify_smash: $*" >&2; }
    48	die () { log "$*"; exit 1; }
    49	
    50	while [ $# -gt 0 ]; do
    51	  case "$1" in
    52	    --tests-only)  DO_ANCHOR=0; shift ;;
    53	    --anchor-only) DO_TESTS=0; shift ;;
    54	    -h|--help)     sed -n '2,30p' "$0"; exit 0 ;;
    55	    *) die "unknown argument '$1'" ;;
    56	  esac
    57	done
    58	
    59	if [ -n "${SMASH:-}" ] && [ -n "${SMASH_BUILD:-}" ] && [ -n "${SMASH_ROOT:-}" ]; then
    60	  BIN="$SMASH"; BUILD="$SMASH_BUILD"; SRCROOT="$SMASH_ROOT"; EIGEN="${SMASH_EIGEN3_ROOT:-}"
    61	  GSL="${SMASH_GSL_PREFIX:-}"; PYPFX="${SMASH_PYTHIA_PREFIX:-}"
    62	  PRESET=1
    63	else
    64	  PRESET=0
    65	  OUT="$("$HERE/install_smash.sh")" || die "install_smash.sh failed"
    66	  BIN="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH=//p')"
    67	  BUILD="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_BUILD=//p')"
    68	  SRCROOT="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_ROOT=//p')"
    69	  EIGEN="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_EIGEN3_ROOT=//p')"
    70	  GSL="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_GSL_PREFIX=//p')"
    71	  PYPFX="$(printf '%s\n' "$OUT" | sed -n 's/^SMASH_PYTHIA_PREFIX=//p')"
    72	fi
    73	[ -x "$BIN" ] || die "no usable SMASH executable"
    74	[ -d "$BUILD" ] || die "no build directory at '$BUILD'"
    75	
    76	# Pre-set SMASH/SMASH_BUILD/SMASH_ROOT used to skip every identity check, so a
    77	# fake build directory next to the real binary certified tier 1. Whatever the
    78	# route, the tree must be the pinned commit, the build must carry the
    79	# installer's stamp, and the binary must be the one that stamp describes.
    80	PIN="${SMASH_PIN:-d1a1c6cf0a0002ee064eec1b929b9a7c14b3d5bc}"
    81	check_identity () {
    82	  local head stamp
    83	  [ -d "$SRCROOT/.git" ] || { log "'$SRCROOT' is not a git clone, so its commit cannot be established"; return 1; }
    84	  head="$(cd "$SRCROOT" && git rev-parse HEAD 2>/dev/null || echo none)"
    85	  [ "$head" = "$PIN" ] || { log "source tree is at $head, not the pinned $PIN"; return 1; }
    86	  ( cd "$SRCROOT" && git diff --quiet HEAD 2>/dev/null ) || { log "source tree has uncommitted modifications"; return 1; }
    87	  stamp="$BUILD/.fusion_build_stamp"
    88	  [ -f "$stamp" ] || { log "no build stamp at $stamp; this build was not produced by install_smash.sh"; return 1; }
    89	  local want got
    90	  want="$(sed -n 2p "$stamp")"
    91	  got="$(shasum -a 256 "$BIN" 2>/dev/null | cut -d' ' -f1 || echo none)"
    92	  [ "$want" = "$got" ] || { log "the binary does not match the build stamp (stamped $want, found $got)"; return 1; }
    93	  case "$BIN" in
    94	    "$BUILD"/*) : ;;
    95	    *) log "the binary '$BIN' does not live inside the build directory '$BUILD'"; return 1 ;;
    96	  esac
    97	  return 0
    98	}
    99	if ! check_identity; then
   100	  die "refusing to certify: the build does not verifiably come from the pinned source (see above)"
   101	fi
   102	log "identity OK: pinned commit, clean tree, stamped binary"
   103	
   104	FAILED=0
   105	
   106	# The usage_of_SMASH_as_library test spawns a FRESH cmake for the library
   107	# example, and that child inherits NONE of this build's -D cache variables, so
   108	# every hint has to reach it through the ENVIRONMENT. Three were needed, each
   109	# discovered by a different failure:
   110	#   EIGEN3_ROOT     macOS, where the system Eigen is a 5.x it cannot parse
   111	#   GSL hints       a cluster where GSL lives only inside a conda prefix
   112	#   LD_LIBRARY_PATH the example BUILDS and then fails to RUN, because Pythia is
   113	#                   installed under a custom prefix with no rpath
   114	run_ctest () {
   115	  ( cd "$BUILD" \
   116	    && EIGEN3_ROOT="$EIGEN" \
   117	       GSL_ROOT_DIR="$GSL" \
   118	       PKG_CONFIG_PATH="${GSL:+$GSL/lib/pkgconfig:}${PKG_CONFIG_PATH:-}" \
   119	       CMAKE_PREFIX_PATH="${GSL:+$GSL:}${CMAKE_PREFIX_PATH:-}" \
   120	       LD_LIBRARY_PATH="${PYPFX:+$PYPFX/lib:}${GSL:+$GSL/lib:}${LD_LIBRARY_PATH:-}" \
   121	       DYLD_LIBRARY_PATH="${PYPFX:+$PYPFX/lib:}${GSL:+$GSL/lib:}${DYLD_LIBRARY_PATH:-}" \
   122	       ctest "$@" )
   123	}
   124	
   125	# ------------------------------------------------------------------ stage 1
   126	if [ "$DO_TESTS" = "1" ]; then
   127	  command -v ctest >/dev/null || die "ctest not found"
   128	  LOG="$(mktemp)"
   129	  log "running SMASH's own test suite (about 6 minutes)"
   130	  set +e
   131	  run_ctest -j"$( (command -v nproc >/dev/null && nproc) || sysctl -n hw.ncpu 2>/dev/null || echo 4 )" > "$LOG" 2>&1
   132	  CTEST_RC=$?
   133	  set -e
   134	
   135	  TOTAL="$(sed -nE 's/.* tests passed, [0-9]+ tests failed out of ([0-9]+).*/\1/p' "$LOG" | tail -1)"
   136	  NFAIL="$(sed -nE 's/.* tests passed, ([0-9]+) tests failed out of [0-9]+.*/\1/p' "$LOG" | tail -1)"
   137	  [ -n "$TOTAL" ] || { log "could not parse a ctest summary; last lines:"; tail -10 "$LOG" >&2; rm -f "$LOG"; exit 1; }
   138	
   139	  # The exit status is checked INDEPENDENTLY of the parsed text. A ctest that
   140	  # printed a clean "104 of 104" summary and then exited nonzero used to be
   141	  # accepted, because only the text was read.
   142	  if [ "$CTEST_RC" -ne 0 ] && [ "${NFAIL:-0}" -eq 0 ]; then
   143	    log "FAIL: ctest exited $CTEST_RC while reporting no failures; the summary and the status disagree"
   144	    tail -10 "$LOG" >&2
   145	    FAILED=1
   146	  fi
   147	
   148	  # An EXACT count, never "at least": a suite that silently skipped cases would
   149	  # otherwise certify the build.
   150	  if [ "$EXPECTED_TESTS" != "$EXPECTED_TESTS_PINNED" ]; then
   151	    log "NOTE: SMASH_EXPECTED_TESTS=$EXPECTED_TESTS overrides the pinned $EXPECTED_TESTS_PINNED;"
   152	    log "      this run is NOT a tier-1 certification of the pinned release"
   153	  fi
   154	  if [ "$TOTAL" != "$EXPECTED_TESTS" ]; then
   155	    log "FAIL: ctest ran $TOTAL cases, expected exactly $EXPECTED_TESTS"
   156	    log "      (set SMASH_EXPECTED_TESTS if you deliberately pinned a different SMASH version)"
   157	    FAILED=1
   158	  fi
   159	
   160	  if [ "${NFAIL:-0}" -eq 0 ]; then
   161	    log "test suite: $TOTAL of $TOTAL passed on the first attempt"
   162	  else
   163	    # Parse EVERY ctest failure status, not only "(Failed)": a case that timed
   164	    # out or was not run is still a failure, and matching only "(Failed)" let a
   165	    # real regression through while the one self-seeded flake was retried.
   166	    FAILING="$(sed -nE 's/^[[:space:]]+[0-9]+ - ([A-Za-z0-9_.-]+) \(([A-Za-z ]+)\).*/\1/p' "$LOG" | sort -u)"
   167	    NPARSED="$(printf '%s\n' "$FAILING" | grep -c . || true)"
   168	    # The number of names parsed must equal the number ctest reported, or
   169	    # something was missed and the retry logic below is reasoning about a
   170	    # different set than the one that actually failed.
   171	    if [ "${NPARSED:-0}" -ne "${NFAIL:-0}" ]; then
   172	      log "FAIL: ctest reported $NFAIL failures but $NPARSED names could be parsed; refusing to guess"
   173	      sed -nE 's/^[[:space:]]+[0-9]+ - .*/&/p' "$LOG" | head -10 >&2
   174	      FAILED=1
   175	      FAILING=""
   176	    fi
   177	    for t in $FAILING; do
   178	      case " $FLAKY_TESTS " in
   179	        *" $t "*)
   180	          log "'$t' failed; it seeds itself from std::random_device, so retrying it once"
   181	          set +e
   182	          run_ctest -R "^$t\$" > "$LOG.retry" 2>&1
   183	          RC=$?
   184	          set -e
   185	          # Exit status alone is not enough: `ctest -R` that matches NOTHING
   186	          # prints "No tests were found!!!" and exits 0, which used to be read
   187	          # as a clean retry. Require exactly one test selected and passed.
   188	          RTOTAL="$(sed -nE 's/.* tests passed, [0-9]+ tests failed out of ([0-9]+).*/\1/p' "$LOG.retry" | tail -1)"
   189	          RFAIL="$(sed -nE 's/.* tests passed, ([0-9]+) tests failed out of [0-9]+.*/\1/p' "$LOG.retry" | tail -1)"
   190	          if [ "$RC" -eq 0 ] && [ "${RTOTAL:-0}" = "1" ] && [ "${RFAIL:-1}" = "0" ]; then
   191	            log "'$t' passed on retry (1 of 1), which is what a statistical fluke does"
   192	          else
   193	            log "FAIL: retry of '$t' did not cleanly pass exactly one test (rc=$RC, total=${RTOTAL:-none}, failed=${RFAIL:-none})"
   194	            tail -15 "$LOG.retry" >&2
   195	            FAILED=1
   196	          fi
   197	          rm -f "$LOG.retry"
   198	          ;;
   199	        *)
   200	          log "FAIL: '$t' failed, and it is not one of the self-seeded tests ($FLAKY_TESTS)"
   201	          FAILED=1
   202	          ;;
   203	      esac
   204	    done
   205	  fi
   206	  rm -f "$LOG"
   207	fi
   208	
   209	# ------------------------------------------------------------------ stage 2
   210	if [ "$DO_ANCHOR" = "1" ]; then
   211	  CFG="$SRCROOT/input/config.yaml"
   212	  [ -s "$CFG" ] || die "the shipped collider config is missing from '$CFG'"
   213	  # Au+Au: 2 events x 2 nuclei x 197 nucleons = 788 baryons, x 79 protons = 316 e.
   214	  WORK="$(mktemp -d)"
   215	  log "running the seeded Au+Au anchor (2 events, 20 fm/c)"
   216	  if ! SMASH="$BIN" "$HERE/run_smash.sh" --config "$CFG" --outdir "$WORK/run" \
   217	        --seed 20260723 --nevents 2 --end-time 20.0 >/dev/null; then
   218	    log "FAIL: the anchor run did not complete"
   219	    FAILED=1
   220	  elif python3 "$HERE/check_conservation_smash.py" "$WORK/run/out/particle_lists.oscar" \
   221	        --baryons 788 --charge 316; then
   222	    log "anchor: conservation laws hold exactly"
   223	  else
   224	    log "FAIL: the anchor run violates a conservation law"
   225	    FAILED=1
   226	  fi
   227	  rm -rf "$WORK"
   228	fi
   229	
   230	if [ "$FAILED" = "0" ]; then
   231	  echo "VERIFY OK"
   232	  exit 0
   233	fi
   234	echo "VERIFY FAILED"
   235	exit 1
STAMP
d1a1c6cf0a0002ee064eec1b929b9a7c14b3d5bc|clean|/usr/bin/c++|Apple clang version 21.0.0 (clang-2100.1.1.101)|8.316|3.4.0|/opt/homebrew/Cellar/gsl/2.8|Darwin|arm64
aa165dea2f36409e86ddeb60e633e0cac47068dc5602557d9c51e2283836307c
TREE
HEAD
d1a1c6cf0a0002ee064eec1b929b9a7c14b3d5bc
BIN
bd0101d7328e52ea1a29b8637018950883732edfb3eefff35be305b8d1dd63bb  /Users/jinlei/.cache/fusion/smash/smash/build/smash

exec
/bin/zsh -lc "stat -f '%N %Sm %z' -t '%Y-%m-%d %H:%M:%S' \""'$HOME/.cache/fusion/smash/smash/build/.fusion_build_stamp" "$HOME/.cache/fusion/smash/smash/build/smash"; strings "$HOME/.cache/fusion/smash/smash/build/smash" | rg -m2 '"'SMASH-3\\.3|SMASH-' || true; \""'$HOME/.cache/fusion/smash/smash/build/smash" --version | head -3; bash skills/smash/scripts/verify_smash.sh --anchor-only' in /Users/jinlei/Desktop/code/FUSION
 exited 1 in 0ms:
/Users/jinlei/.cache/fusion/smash/smash/build/.fusion_build_stamp 2026-07-23 16:10:18 227
/Users/jinlei/.cache/fusion/smash/smash/build/smash 2026-07-23 16:10:39 18069272
SMASH-3.3
SMASH-3.3
Branch   : HEAD
System   : Darwin-25.5.0
install_smash: GSL prefix: /opt/homebrew/Cellar/gsl/2.8
install_smash: Pythia 8.316 at /Users/jinlei/.cache/fusion/smash/pythia-install
install_smash: reusing clone at /Users/jinlei/.cache/fusion/smash/smash
install_smash: cannot check out pinned commit d1a1c6cf0a0002ee064eec1b929b9a7c14b3d5bc (shallow clone, or upstream history moved)
install_smash: set SMASH_PIN to re-pin deliberately
verify_smash: install_smash.sh failed

exec
/bin/zsh -lc 'SMASH="$HOME/.cache/fusion/smash/smash/build/smash" SMASH_BUILD="$HOME/.cache/fusion/smash/smash/build" SMASH_ROOT="$HOME/.cache/fusion/smash/smash" bash skills/smash/scripts/verify_smash.sh --anchor-only' in /Users/jinlei/Desktop/code/FUSION
 exited 1 in 0ms:
verify_smash: the binary does not match the build stamp (stamped aa165dea2f36409e86ddeb60e633e0cac47068dc5602557d9c51e2283836307c, found bd0101d7328e52ea1a29b8637018950883732edfb3eefff35be305b8d1dd63bb)
verify_smash: refusing to certify: the build does not verifiably come from the pinned source (see above)

codex
The supplied cached build exposes a real regression immediately: its source is clean and pinned, but `verify_smash.sh` rejects it because the installer’s stamp hash is stale. The stamp predates the binary by 21 seconds. I’m tracing whether the test suite legitimately relinks `build/smash`, which would make the new identity check reject a normal verified build.
exec
/bin/zsh -lc "sed -n '360,430p' \""'$HOME/.cache/fusion/smash/smash/src/CMakeLists.txt"
sed -n '"'525,560p' \""'$HOME/.cache/fusion/smash/smash/src/tests/CMakeLists.txt"
rg -n "always|version|git|smash.cc|add_executable'"\\(smash|POST_BUILD|PRE_BUILD\" \""'$HOME/.cache/fusion/smash/smash/src/CMakeLists.txt" "$HOME/.cache/fusion/smash/smash/CMakeLists.txt" | head -120
find "$HOME/.cache/fusion/smash/smash/build" -maxdepth 2 -type f -newer "$HOME/.cache/fusion/smash/smash/build/.fusion_build_stamp" -print | head -100' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:

    if(HAVE_SANITIZER)
        option(USE_SANITIZER
               "Use available address and undefined behavior sanitizers for test code." ON)
    else()
        set(USE_SANITIZER FALSE)
    endif()

    # define BUILD_TESTS for smash_static in order to enable small code changes for the unit tests.
    # It is an unfortunate side effect that the sanitzer build now also compiles to slightly
    # different code. To fix this we'd have to compile all source files a third time... not sure we
    # prefer that.
    if(USE_SANITIZER)
        set_target_properties(smash_static
                              PROPERTIES COMPILE_FLAGS "${SANITIZER_FLAG} -DBUILD_TESTS"
                                         LINK_FLAGS ${SANITIZER_FLAG})

        add_executable(smash_sanitizer smash.cc)
        set_target_properties(smash_sanitizer
                              PROPERTIES COMPILE_FLAGS "${SANITIZER_FLAG} -DBUILD_TESTS"
                                         LINK_FLAGS ${SANITIZER_FLAG})
        target_link_libraries(smash_sanitizer smash_static ${SMASH_LIBRARIES})
    else()
        set_target_properties(smash_static PROPERTIES COMPILE_FLAGS "-DBUILD_TESTS")
    endif()

    add_subdirectory(tests)
endif()

# Since at the top level the dependency of the install target on all target was disabled, it is
# needed to now build the smash library and exectuable target here before installing them
install(CODE "execute_process(COMMAND ${CMAKE_COMMAND}
                                      --build ${PROJECT_BINARY_DIR}
                                      --target smash_shared smash)")
install(TARGETS smash_shared LIBRARY DESTINATION "lib/${SMASH_INSTALLATION_SUBFOLDER}")
install(FILES ${generated_headers} DESTINATION "include/${SMASH_INSTALLATION_SUBFOLDER}/smash")
# Note that the absent trailing slash in the DIRECTORY argument is crucial to create an additional
# "smash" folder at destination!
install(DIRECTORY include/smash DESTINATION "include/${SMASH_INSTALLATION_SUBFOLDER}"
        PATTERN "*.h.in" EXCLUDE)
install(DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/include/smash"
        DESTINATION "include/${SMASH_INSTALLATION_SUBFOLDER}")
# Copy SMASH executable to installation bin folder adding a suffix to allow different versions to
# coexist. A symbolic link called 'smash' is also created, pointing to the last installed version.
# Due to this mechanism, we do not use "install(TARGETS ...)", as it does not offer a renaming
# mechanism.
install(PROGRAMS $<TARGET_FILE_NAME:smash> DESTINATION bin RENAME "${SMASH_INSTALLATION_SUBFOLDER}")
install(CODE "execute_process(COMMAND ${CMAKE_COMMAND} -E create_symlink
                                      ${SMASH_INSTALLATION_SUBFOLDER}
                                      smash
                              WORKING_DIRECTORY ${CMAKE_INSTALL_PREFIX}/bin)")
                                  "${PROJECT_BINARY_DIR}/test_output/memcheck_box" -f
                          DEPENDS smash
                          COMMENT "Executing test memcheck_box."
                          VERBATIM)
        add_custom_target(memcheck_sphere
                          COMMAND ${CTEST_MEMORYCHECK_COMMAND} ${CTEST_MEMORYCHECK_COMMAND_OPTIONS}
                                  ${PROJECT_BINARY_DIR}/smash -c
                                  "General: {End_Time: 5.0, Delta_Time: 0.1}" -i
                                  ${PROJECT_SOURCE_DIR}/input/sphere/config.yaml -o
                                  "${PROJECT_BINARY_DIR}/test_output/memcheck_sphere" -f
                          DEPENDS smash
                          COMMENT "Executing test memcheck_sphere."
                          VERBATIM)
    endif()
else()
    message(STATUS "Valgrind not found. Memcheck tests are disabled!")
endif()

add_test(NAME usage_of_SMASH_as_library
         COMMAND bash run_examples_for_testing.bash "${PROJECT_SOURCE_DIR}"
                 "${Pythia_CONFIG_EXECUTABLE}"
         WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}/examples/using_SMASH_as_library)

set_tests_properties(usage_of_SMASH_as_library PROPERTIES ENVIRONMENT
                                                          SMASH_BUILD_DIR=${PROJECT_BINARY_DIR})

if(USE_SANITIZER)
    smash_add_runtest(sanitizer_box smash_sanitizer smash_sanitizer
                      -e 5.0
                      -m Box
                      -c "Modi: {Box: {Initial_Condition: thermal momenta}}"
                      -c "Modi: {Box: {Length: 10.0}}"
                      -c "Modi: {Box: {Temperature: 0.2}}"
                      -c "Modi: {Box: {Start_Time: 0.0}}"
                      -c "Modi: {Box: {Init_Multiplicities: {211: 100}}}"
                      -c "Modi: {Box: {Init_Multiplicities: {-211: 100}}}"
/Users/jinlei/.cache/fusion/smash/smash/CMakeLists.txt:10:# Minimum cmake version this is tested on
/Users/jinlei/.cache/fusion/smash/smash/CMakeLists.txt:13:# The name, version and language of our project
/Users/jinlei/.cache/fusion/smash/smash/CMakeLists.txt:63:# VERSION option. Here we check if .git information is available and increase verbosity
/Users/jinlei/.cache/fusion/smash/smash/CMakeLists.txt:65:git_describe(SMASH_VERSION_VERBOSE)
/Users/jinlei/.cache/fusion/smash/smash/CMakeLists.txt:95:        message(FATAL_ERROR " At least version ${MINIMUM_CXX_COMPILER_VERSION}"
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:18:        "${CMAKE_CXX_FLAGS} -W -Wall -Wextra -Wmissing-declarations -Wpointer-arith -Wshadow -Wuninitialized -Wfloat-conversion -Winit-self -Wundef -Wcast-align -Wformat=2 -Wold-style-cast -Werror=switch"
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:24:        # Needed for workaround about C++17 compatibility (bug in some GNU versions)
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:31:            # controlled by a flag whose default unfortunately varies among compilers and versions
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:33:            # to "on" also for older versions. GNU default is "fast", but this seems to be similar
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:50:    # message("git found: ${GIT_EXECUTABLE} in version ${GIT_VERSION_STRING}")
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:52:    set(GIT_DIR "${GIT_PARENT_DIR}/.git")
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:60:    # message("git branch: ${GIT_BRANCH}")
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:70:    find_package(ROOT QUIET) # Min version not handled here but after to better inform user
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:73:        message(STATUS "Candidate ROOT version ${ROOT_VERSION} found, "
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:92:    find_package(HepMC3 QUIET) # Min version not handled here but after to better inform user
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:95:        message(STATUS "Candidate HepMC version ${HepMC3_VERSION} found, "
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:121:    find_package(Rivet QUIET) # Min version not handled here but after to better inform user
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:124:        message(STATUS "Candidate Rivet version ${RIVET_VERSION} found, "
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:129:            message(ATTENTION "Rivet version larger than 3.1.x found. "
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:149:    # https://github.com/smash-transport/smash/issues/38#issuecomment-1033239643
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:174:# For GNU and Clang compilers, link to C++ filesystem library if compiler version requires it
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:295:add_executable(smash smash.cc $<TARGET_OBJECTS:objlib>)
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:342:    #   infinity/NaN values, or conversions to unrepresentable values
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:377:        add_executable(smash_sanitizer smash.cc)
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:402:# Copy SMASH executable to installation bin folder adding a suffix to allow different versions to
/Users/jinlei/.cache/fusion/smash/smash/src/CMakeLists.txt:403:# coexist. A symbolic link called 'smash' is also created, pointing to the last installed version.
/Users/jinlei/.cache/fusion/smash/smash/build/compile_commands.json
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeFiles/CMakeRuleHashes.txt
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeFiles/cmake.check_cache
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeFiles/Makefile.cmake
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeFiles/cmake.verify_globs
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeFiles/InstallScripts.json
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeFiles/progress.marks
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeFiles/TargetDirectories.txt
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeFiles/Makefile2
/Users/jinlei/.cache/fusion/smash/smash/build/Makefile
/Users/jinlei/.cache/fusion/smash/smash/build/cmake_install.cmake
/Users/jinlei/.cache/fusion/smash/smash/build/CTestTestfile.cmake
/Users/jinlei/.cache/fusion/smash/smash/build/smash
/Users/jinlei/.cache/fusion/smash/smash/build/doc/cmake_install.cmake
/Users/jinlei/.cache/fusion/smash/smash/build/doc/CTestTestfile.cmake
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeCache.txt
/Users/jinlei/.cache/fusion/smash/smash/build/3rdparty/cmake_install.cmake
/Users/jinlei/.cache/fusion/smash/smash/build/install_manifest.txt
/Users/jinlei/.cache/fusion/smash/smash/build/src/libsmash.dylib
/Users/jinlei/.cache/fusion/smash/smash/build/src/cmake_install.cmake
/Users/jinlei/.cache/fusion/smash/smash/build/src/CTestTestfile.cmake

exec
/bin/zsh -lc "sed -n '1,260p' \""'$HOME/.cache/fusion/smash/smash/examples/using_SMASH_as_library/run_examples_for_testing.bash"
stat -f '"'%N %Sm' -t '%H:%M:%S' \""'$HOME/.cache/fusion/smash/smash/build/CMakeCache.txt" "$HOME/.cache/fusion/smash/smash/build/src/libsmash.dylib" "$HOME/.cache/fusion/smash/smash/build/smash" "$HOME/.cache/fusion/smash/smash/build/.fusion_build_stamp"' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
#!/usr/bin/env bash

########################################################
#
#    Copyright (c) 2015,2019-2020,2022
#      SMASH Team
#
#    GNU General Public License (GPLv3 or later)
#
########################################################

# Stricter bash mode
set -euo pipefail

if [[ $# -ne 1 ]] && [[ $# -ne 2 ]]; then
  echo "Script requires 1 or 2 arguments. Usage: $0 SMASH_DIR [PYTHIA_CONFIG_PATH]"
  echo "PYTHIA_CONFIG_PATH needs to be given if Pythia is not installed system wide."
  exit 1
fi

SMASH_DIR_VAR=$1
PYTHIA_CONFIG_PATH=$2
EXAMPLE_DIR=${SMASH_DIR_VAR}/examples/using_SMASH_as_library

if [[ "${PYTHIA_CONFIG_PATH}" = '' ]]; then
    PYTHIA_OPTION_FOR_CMAKE=''
else
    PYTHIA_OPTION_FOR_CMAKE="-DPythia_CONFIG_EXECUTABLE=${PYTHIA_CONFIG_PATH}"
fi

# Few sanity checks. CMake sets SMASH_BUILD_DIR when adding the test that runs this script.
if [[ ! -d "${EXAMPLE_DIR}" ]]; then
  echo "Path to SMASH codebase invalid. Check the arguments passed to the script."
  exit 1
elif [[ ! -d "${SMASH_BUILD_DIR-}" ]]; then
  echo "Environment variable SMASH_BUILD_DIR unset or not pointing to a valid directory."
  exit 1
fi

function do_clean_up()
{
  printf 'Removing folders:\n  %s\n  %s\n' "${EXAMPLE_DIR}/build" "${SMASH_INSTALL_DIR}"
  rm -rf "${EXAMPLE_DIR}/build" "${SMASH_INSTALL_DIR}"
}

function fail_and_rm_build() {
  echo "$*" >&2
  do_clean_up
  exit 1
}

# SMASH needs to be installed before using it as a library. We do this in a temporary folder.
# However, we need to run CMake again in the build folder to be sure to set the correct installation
# prefix. This should work both if SMASH was not set up and even if it was already built.
export SMASH_INSTALL_DIR="$(mktemp -d)"
cd "${SMASH_BUILD_DIR}"
cmake -DCMAKE_INSTALL_PREFIX="${SMASH_INSTALL_DIR}"\
      "${PYTHIA_OPTION_FOR_CMAKE}" "${SMASH_DIR_VAR}" || fail_and_rm_build 'Failed to set up SMASH'
make -j$(nproc) install || fail_and_rm_build 'Failed to install SMASH'

# Now set up "external" CMake project and build it using SMASH as library
cd "${EXAMPLE_DIR}"
mkdir -p build && cd build
# Assume the default compiler is fine or that `CC` and `CXX` environment variables are set
cmake "${PYTHIA_OPTION_FOR_CMAKE}" .. || fail_and_rm_build 'Failed to setup cmake for SMASH library examples'
make -j$(nproc) || fail_and_rm_build 'Failed to build SMASH library examples'

./example || fail_and_rm_build 'Failed to run SMASH library example'
./example_rate_equations || fail_and_rm_build 'Failed to run SMASH rate equation library example'
./example_smash_wrapper || fail_and_rm_build 'Failed to execute SMASH wrapper library example'

do_clean_up
/Users/jinlei/.cache/fusion/smash/smash/build/CMakeCache.txt 16:10:19
/Users/jinlei/.cache/fusion/smash/smash/build/src/libsmash.dylib 16:10:33
/Users/jinlei/.cache/fusion/smash/smash/build/smash 16:10:39
/Users/jinlei/.cache/fusion/smash/smash/build/.fusion_build_stamp 16:10:18

exec
/bin/zsh -lc "rg -n \"baryon_number\\(|is_hadron\\(|is_nucleus\\(|antiparticle_sign|n_q1_|n_q2_|n_q3_\" \""'$HOME/.cache/fusion/smash/smash/src/include/smash/pdgcode.h" "$HOME/.cache/fusion/smash/smash/src/pdgcode.cc" | head -200
sed -n '"'155,250p' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/pdgcode.h"
sed -n '"'480,610p' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/pdgcode.h"' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:39:  if (is_nucleus()) {
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:45:        return (2 * Nn + Np + NL) * antiparticle_sign();
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:47:        return (Nn + 2 * Np + NL) * antiparticle_sign();
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:49:        return NL * antiparticle_sign();
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:56:  if (!is_hadron() || (digits_.n_q1_ != quark && digits_.n_q2_ != quark &&
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:57:                       digits_.n_q3_ != quark)) {
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:61:  if (baryon_number() != 0) {
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:63:    return antiparticle_sign() *
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:64:           ((digits_.n_q1_ == quark) + (digits_.n_q2_ == quark) +
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:65:            (digits_.n_q3_ == quark));
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:71:  if (digits_.n_q3_ == quark && digits_.n_q2_ == quark) {
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:77:  int otherquark = (digits_.n_q2_ == quark) ? digits_.n_q3_ : digits_.n_q2_;
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:81:    return ((quark % 2 == 0) ? 1 : -1) * antiparticle_sign();
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:85:  return ((otherquark % 2 == 0) ? -1 : 1) * antiparticle_sign();
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:98:    if (baryon_number() == 1) {
/Users/jinlei/.cache/fusion/smash/smash/src/pdgcode.cc:101:    if (baryon_number() == -1) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:62: *                                           other_particle.baryon_number());
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:261:    if (digits_.n_q1_ > 6) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:264:    if (digits_.n_q2_ > 6) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:267:    if (digits_.n_q3_ > 6) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:286:    if (is_hadron()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:287:      if (baryon_number() == 0) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:319:  inline std::int32_t code() const { return antiparticle_sign() * ucode(); }
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:361:  inline bool is_nucleus() const {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:367:  inline bool is_hadron() const {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:368:    return (digits_.n_q3_ != 0 && digits_.n_q2_ != 0 && !is_nucleus());
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:373:    return (digits_.n_q1_ == 0 && digits_.n_q2_ == 0 && digits_.n_q3_ == 1 &&
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:374:            !is_nucleus());
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:384:    return is_meson() && digits_.n_q2_ == 4 && digits_.n_q3_ == 4;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:388:  inline int baryon_number() const {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:389:    if (is_nucleus()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:390:      return static_cast<int>(nucleus_.A_) * antiparticle_sign();
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:392:    if (!is_hadron() || digits_.n_q1_ == 0) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:395:    return antiparticle_sign();
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:398:  inline bool is_baryon() const { return is_hadron() && digits_.n_q1_ != 0; }
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:401:  inline bool is_meson() const { return is_hadron() && digits_.n_q1_ == 0; }
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:435:  inline bool is_hyperon() const { return is_hadron() && digits_.n_q1_ == 3; }
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:439:    return is_hyperon() && digits_.n_q2_ == 3 && digits_.n_q3_ == 3;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:444:    return is_hyperon() && digits_.n_q2_ == 3 && digits_.n_q3_ != 3;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:449:    return is_hyperon() && digits_.n_q2_ == 1 && digits_.n_q3_ == 2;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:454:    return is_hyperon() && digits_.n_q2_ != 3 && !is_Lambda();
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:490:    return is_nucleus() && nucleus_.A_ == 2 && nucleus_.Z_ == 1 &&
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:496:    return is_nucleus() && nucleus_.A_ == 3 && nucleus_.Z_ == 1 &&
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:505:    if (is_nucleus()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:508:    if (is_hadron()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:509:      return (baryon_number() != 0) || (digits_.n_q2_ != digits_.n_q3_);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:511:      return digits_.n_q3_ == 1;  // leptons!
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:539:      if (digits_.n_q3_ == 3 && digits_.n_q2_ == 3) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:564:      if (digits_.n_q3_ == 4 && digits_.n_q2_ == 4) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:589:      if (digits_.n_q3_ == 5 && digits_.n_q2_ == 5) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:636:    if (is_hadron() || is_nucleus()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:653:    if (digits_.n_q3_ == 1) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:654:      return -1 * (digits_.n_J_ % 2) * antiparticle_sign();
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:661:      return antiparticle_sign();
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:677:    if (is_nucleus()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:698:    if (is_hadron()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:709:    return digits_.n_q3_;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:713:    if (is_hadron() && digits_.n_J_ > 0) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:719:  inline int antiparticle_sign() const {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:724:    if (!is_hadron() || is_nucleus()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:740:    std::array<int, 3> result = {static_cast<int>(digits_.n_q1_),
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:741:                                 static_cast<int>(digits_.n_q2_),
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:742:                                 static_cast<int>(digits_.n_q3_)};
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:743:    if (is_hadron()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:745:      if (digits_.n_q1_ != 0 && digits_.antiparticle_) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:751:      if (digits_.n_q1_ == 0) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:753:        if (digits_.n_q2_ == digits_.n_q3_) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:765:        if (digits_.n_q2_ != digits_.n_q3_ && digits_.n_q2_ % 2 == 1) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:838:    if (is_nucleus()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:840:      return antiparticle_sign() *
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:850:    return antiparticle_sign() *
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:851:           (n_J_2 + digits_.n_q3_ * 10 + digits_.n_q2_ * 100 +
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:852:            digits_.n_q1_ * 1000 + digits_.n_L_ * 10000 +
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:858:    if (!is_nucleus()) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:879:    return (is_nucleus() && !nucleus_.antiparticle_) ? nucleus_.Z_ : 0;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:883:    return (is_nucleus() && !nucleus_.antiparticle_)
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:889:    return (is_nucleus() && !nucleus_.antiparticle_) ? nucleus_.n_Lambda_ : 0;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:893:    return (is_nucleus() && nucleus_.antiparticle_) ? nucleus_.Z_ : 0;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:897:    return (is_nucleus() && nucleus_.antiparticle_)
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:903:    return (is_nucleus() && nucleus_.antiparticle_) ? nucleus_.n_Lambda_ : 0;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:906:  int nucleus_A() const { return is_nucleus() ? nucleus_.A_ : 0; }
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:924:      std::uint32_t n_q3_ : 4;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:926:      std::uint32_t n_q2_ : 4;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:928:      std::uint32_t n_q1_ : 4;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:945:      std::uint32_t n_q1_ : 4;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:946:      std::uint32_t n_q2_ : 4;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:947:      std::uint32_t n_q3_ : 4;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1051:    digits_.n_ = digits_.n_R_ = digits_.n_L_ = digits_.n_q1_ = digits_.n_q2_ =
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1052:        digits_.n_q3_ = digits_.n_J_ = digits_.is_nucleus_ = 0;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1112:    // 4th from last is n_q1_.
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1114:      digits_.n_q1_ = get_digit_from_char(codestring[c++]);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1115:      if (digits_.n_q1_ > 6) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1119:    // 3rd from last is n_q2_.
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1121:      digits_.n_q2_ = get_digit_from_char(codestring[c++]);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1122:      if (digits_.n_q2_ > 6) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1126:    // Next to last is n_q3_.
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1128:      digits_.n_q3_ = get_digit_from_char(codestring[c++]);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:1129:      if (digits_.n_q3_ > 6) {
  explicit PdgCode(const std::uint32_t abscode) : dump_(0x0) {
    // use the first bit for the antiparticle_ boolean.
    digits_.antiparticle_ = ((abscode & 0x80000000u) != 0);
    set_fields(abscode);
  }

  /**
   * The creation of \c PdgCode instances for nuclei that have a 10-digits code
   * cannot be done using the integer constructors above, since a 10-digits
   * hexadecimal number like 0x1000020030 exceeds the int32_t capacity. Nuclei
   * instances can in principle either be created with the string constructor or
   * via the \c PdgCode::from_decimal() static member, but offering a uniform
   * interface for all cases is definitely user-friendly.
   *
   * Since the string constructor works, we delegate here the construction to
   * it. In order to execute the needed code to built the string in the member
   * initializer list, we use a common lambda idiom (IIFE) that consists in
   * immediately invoking a lambda function. Using \c std::invoke makes it more
   * explicit than using \c () after the lambda braces.
   *
   * \note
   * One might wonder why a function template has been used instead of e.g.
   * adding a constructor taking a \c int64_t argument. The reason is to
   * facilitate the class usage. Having a fixed type would have required the
   * users of this class to \b exactly match the argument type (and for
   * \c int64_t there is no standard literal suffix), otherwise the call would
   * have been ambiguous and compilation would have failed. Said differently, we
   * would like
   * \code
   * PdgCode deuteron(0x1000010020);
   * \endcode
   * to work and not oblige the user to write
   * \code
   * PdgCode deuteron(INT64_C(0x1000010020));
   * \endcode
   * just because the constructor takes an \c int64_t number.
   * The idea here is to offer a better matching to the compiler when the
   * constructor is called with an integer type with size larger than 4 bytes.
   * Type-traits are used to limit the template instantiation to reasonable
   * cases, only.
   *
   * \warning
   * In C++, it is more common to enable constructors template via a non-type
   * template parameter, i.e.
   * \code
   * template <typename T,
   * template <typename T,
   *           typename std::enable_if_t<
   *               std::is_integral_v<T> && 4 < sizeof(T), bool
   *           > = true>
   * PdgCode(T codenumber) // ...
   * \endcode
   * but this breaks Doxygen documentation, because apparently multiple "nested"
   * template parameters are not able to be correctly handled (possibly because
   * of the additional less-than symbol). This limitation has been detected
   * using Doxygen 1.9.X and might be solved in future versions.
   *
   * \param[in] codenumber The hexadecimal PDG code
   * \tparam T The type of the PDG code, irrelevant for the user
   *           and deduced by the compiler
   */
  template <typename T>
  PdgCode(T codenumber,  // NOLINT(runtime/explicit)
          typename std::enable_if_t<std::is_integral_v<T> && 4 < sizeof(T),
                                    bool> = true)
      : PdgCode{std::invoke([&codenumber]() {
          std::stringstream stream;
          char sign = '+';
          if (codenumber < 0) {
            sign = '-';
            codenumber = -codenumber;
          }
          stream << sign << std::hex << codenumber;
          return stream.str();
        })} {}

  /****************************************************************************
   *                                                                          *
   * test function and export functions                                       *
   *                                                                          *
   ****************************************************************************/

  /**
   * Checks the integer for invalid hex digits.
   *
   * Usually all digits are at least <= 9. The n_q digits are even <= 6
   * (because there are only six quarks). The only exception is n_J, where
   * we allow f = 15, which is the largest hexadecimal digit.
   * If one of the hex digits is not also a valid decimal digit,
   * something possibly went wrong - maybe some user of this class forgot
   * to prefix the input with '0x' and thus passed 221 instead of 0x221.
   * \return a bitmask indicating the offending digits. In the above
   * example, 221 = 0xd3, the second-to-last-digit is the offending one,
   * to the return value is 0b10 = 0x2 = 2.
   */
  inline int test_code() const {
  }

  /// \return whether this is a rho meson (rho+/rho0/rho-)
  inline bool is_rho() const {
    const auto c = code();
    return (c == pdg::rho_z) || (c == pdg::rho_p) || (c == pdg::rho_m);
  }

  /// \return whether this is (anti-)deuteron
  inline bool is_deuteron() const {
    return is_nucleus() && nucleus_.A_ == 2 && nucleus_.Z_ == 1 &&
           nucleus_.n_Lambda_ == 0 && nucleus_.I_ == 0;
  }

  /// \return whether this is (anti-)triton
  inline bool is_triton() const {
    return is_nucleus() && nucleus_.A_ == 3 && nucleus_.Z_ == 1 &&
           nucleus_.n_Lambda_ == 0 && nucleus_.I_ == 0;
  }

  /**
   * \return whether a particle has a distinct antiparticle
   * (or whether it is its own antiparticle).
   */
  bool has_antiparticle() const {
    if (is_nucleus()) {
      return true;
    }
    if (is_hadron()) {
      return (baryon_number() != 0) || (digits_.n_q2_ != digits_.n_q3_);
    } else {
      return digits_.n_q3_ == 1;  // leptons!
    }
  }

  /**
   * \return twice the isospin-3 component \f$I_3\f$.
   *
   * This is calculated from the sum of net_quark_number of up and down.
   */
  inline int isospin3() const {
    /* net_quark_number(2) is the number of u quarks,
     * net_quark_number(1) is the number of d quarks. */
    return net_quark_number(2) - net_quark_number(1);
  }

  /**
   * \return the fraction number of strange quarks
   *         (strange + anti-strange) / total
   *
   * This is useful for the AQM cross-section scaling, and needs to
   * be positive definite.
   */
  inline double frac_strange() const {
    if (is_baryon()) {
      return std::abs(strangeness()) / 3.;
    } else if (is_meson()) {
      /* The quarkonium state has 0 net strangeness
       * but there are actually 2 strange quarks out of 2 total */
      if (digits_.n_q3_ == 3 && digits_.n_q2_ == 3) {
        return 1.;
      } else {
        return std::abs(strangeness()) / 2.;
      }
    } else {
      /* If not baryon or meson, this should be 0, as AQM does not
       * extend to non-hadrons */
      return 0.;
    }
  }

  /**
   * \return the fraction number of charm quarks
   *         (charm + anti-charm) / total
   *
   * This is useful for the AQM cross-section scaling, and needs to
   * be positive definite.
   */
  inline double frac_charm() const {
    if (is_baryon()) {
      return std::abs(charmness()) / 3.;
    } else if (is_meson()) {
      /* The charmonium state has 0 net charmness
       * but there are actually 2 charm quarks out of 2 total */
      if (digits_.n_q3_ == 4 && digits_.n_q2_ == 4) {
        return 1.;
      } else {
        return std::abs(charmness()) / 2.;
      }
    } else {
      /* If not baryon or meson, this should be 0, as AQM does not
       * extend to non-hadrons */
      return 0.;
    }
  }

  /**
   * \return the fraction number of bottom quarks
   *         (bottom + anti-bottom) / total
   *
   * This is useful for the AQM cross-section scaling, and needs to
   * be positive definite.
   */
  inline double frac_bottom() const {
    if (is_baryon()) {
      return std::abs(bottomness()) / 3.;
    } else if (is_meson()) {
      /* The bottomonium state has 0 net bottomness
       * but there are actually 2 bottom quarks out of 2 total */
      if (digits_.n_q3_ == 5 && digits_.n_q2_ == 5) {
        return 1.;
      } else {
        return std::abs(bottomness()) / 2.;
      }
    } else {
      /* If not baryon or meson, this should be 0, as AQM does not
       * extend to non-hadrons */
      return 0.;
    }
  }

  /// \return whether the hadron contains a charm or bottom quark
  inline bool is_heavy_flavor() const {
    return (frac_charm() != 0) || (frac_bottom() != 0);
  }

  /**
   * \return the net number of \f$\bar s\f$ quarks.
   *
   * For particles with one strange quark, -1 is returned.
   */

exec
/bin/zsh -lc 'rg -n "24 cases|hypertriton|12112|13122|baryon_number" skills/smash "$HOME/.cache/fusion/smash/smash/src/tests/pdgcode.cc" | head -200
sed -n '"'360,410p' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/pdgcode.h"
sed -n '"'715,735p' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/pdgcode.h"
sed -n '"'1020,1140p' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/pdgcode.h"' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:263:TEST(baryon_number) {
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:264:  COMPARE(electron.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:265:  COMPARE(antimu.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:266:  COMPARE(photon.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:267:  COMPARE(pion.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:268:  COMPARE(K0.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:269:  COMPARE(K0L.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:270:  COMPARE(K0S.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:271:  COMPARE(Kminus.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:272:  COMPARE(dminus.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:273:  COMPARE(bnulls.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:274:  COMPARE(bPcbar.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:275:  COMPARE(eta_pr.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:276:  COMPARE(j_psi.baryon_number(), 0);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:277:  COMPARE(proton.baryon_number(), 1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:278:  COMPARE(antidelta.baryon_number(), -1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:279:  COMPARE(sigma.baryon_number(), 1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:280:  COMPARE(lambda.baryon_number(), 1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:281:  COMPARE(antixi.baryon_number(), -1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:282:  COMPARE(omega_bar.baryon_number(), -1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:283:  COMPARE(lambda_c.baryon_number(), 1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:284:  COMPARE(sigma_c_bar.baryon_number(), -1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:285:  COMPARE(xi_c.baryon_number(), 1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:286:  COMPARE(omega_c_bar.baryon_number(), -1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:287:  COMPARE(xi_cc_bar.baryon_number(), -1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:288:  COMPARE(omega_bc.baryon_number(), 1);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:289:  COMPARE(deuteron.baryon_number(), 2);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:290:  COMPARE(antideutron.baryon_number(), -2);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:291:  COMPARE(antitriton.baryon_number(), -3);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:292:  COMPARE(he3.baryon_number(), 3);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:293:  COMPARE(He4.baryon_number(), 4);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:294:  COMPARE(H3L.baryon_number(), 3);
skills/smash/scripts/check_conservation_smash.py:30:def baryon_number(pdg):
skills/smash/scripts/check_conservation_smash.py:33:    `src/include/smash/pdgcode.h`, `PdgCode::baryon_number()`:
skills/smash/scripts/check_conservation_smash.py:43:    propagates: N(1440) is 12112, Lambda(1405) is 13122, and higher resonances
skills/smash/scripts/check_conservation_smash.py:169:            open_ev[1] += baryon_number(pdg)
skills/smash/scripts/selftest_smash.sh:263:print('  20.0 0.1 0.2 0.3 1.440 1.5 0.1 0.1 0.1 12112 0 0')   # N(1440)0, B=1
skills/smash/scripts/selftest_smash.sh:264:print('  20.0 0.1 0.2 0.3 1.405 1.5 0.1 0.1 0.1 13122 1 0')   # Lambda(1405), B=1
skills/smash/references/verification.md:156:   breaks it: N(1440) is 12112, Lambda(1405) is 13122. It now transcribes
skills/smash/references/verification.md:157:   `PdgCode::baryon_number()` from the source (a non-nuclear hadron whose n_q1
skills/smash/references/verification.md:158:   digit is nonzero), checked against 24 cases including anti-resonances and
skills/smash/references/verification.md:159:   hypertriton. The Au+Au anchor happens to be taken after the resonances have
  /// \return true if this is a nucleus, false otherwise
  inline bool is_nucleus() const {
    assert(digits_.is_nucleus_ == nucleus_.is_nucleus_);
    return nucleus_.is_nucleus_;
  }

  /// \return true if this is a baryon, antibaryon or meson.
  inline bool is_hadron() const {
    return (digits_.n_q3_ != 0 && digits_.n_q2_ != 0 && !is_nucleus());
  }

  /// \return true if this is a lepton.
  inline bool is_lepton() const {
    return (digits_.n_q1_ == 0 && digits_.n_q2_ == 0 && digits_.n_q3_ == 1 &&
            !is_nucleus());
  }

  /// \return true if this is a neutrino.
  inline bool is_neutrino() const {
    return (is_lepton() && digits_.n_J_ % 2 == 0);
  }

  /// \return whether this is a charmonia
  inline bool is_charmonia() const {
    return is_meson() && digits_.n_q2_ == 4 && digits_.n_q3_ == 4;
  }

  /// \return the baryon number of the particle.
  inline int baryon_number() const {
    if (is_nucleus()) {
      return static_cast<int>(nucleus_.A_) * antiparticle_sign();
    }
    if (!is_hadron() || digits_.n_q1_ == 0) {
      return 0;
    }
    return antiparticle_sign();
  }
  /// \return whether this PDG code identifies a baryon.
  inline bool is_baryon() const { return is_hadron() && digits_.n_q1_ != 0; }

  /// \return whether this PDG code identifies a meson.
  inline bool is_meson() const { return is_hadron() && digits_.n_q1_ == 0; }

  /// \return whether this is a nucleon/anti-nucleon (p, n, -p, -n)
  inline bool is_nucleon() const {
    const auto abs_code = std::abs(code());
    return (abs_code == pdg::p || abs_code == pdg::n);
  }

  /// \return whether this is a proton/anti-proton
  inline bool is_proton() const {
    }
    return spin() + 1;
  }
  /// \return -1 for antiparticles and +1 for particles.
  inline int antiparticle_sign() const {
    return (digits_.antiparticle_ ? -1 : +1);
  }
  /// \return an integer with only the quark numbers set.
  inline std::int32_t quarks() const {
    if (!is_hadron() || is_nucleus()) {
      return 0;
    }
    return chunks_.quarks_;
  }

  /**
   * The return is always an array of three numbers, which are pdgcodes
   * of quarks: 1 - d, 2 - u, 3 - s, 4 - c, 5 - b. Antiquarks get a negative
   * sign. For mesons the first number in array is always 0.
   * There is a difficulty with mesons that are a superposition, for example
   * \f$ \pi^0 = \frac{1}{\sqrt{2}}(u \bar{u} + d \bar{d}) \f$. Currently for
    if (97 <= inp && inp <= 102) {
      return inp - 97 + 10;
    }
    throw InvalidPdgCode("PdgCode: Invalid character " + std::string(&inp, 1) +
                         " found.\n");
  }

  /**
   * Set the PDG code from the given string.
   * This supports hexdecimal digits. If the last digit is not enough to
   * represent the spin, a digit can be added at the beginning which will be
   * added to the total spin.
   * \param[in] codestring string which is translated into PdgCode
   *
   * \throw InvalidPdgCode if the input string is empty
   * \throw InvalidPdgCode
   * if it is a nucleus whose PDG code does not begin with 10
   * \throw InvalidPdgCode
   * if it is not a nucleus while number of digits is more than 8
   * \throw InvalidPdgCode
   * if the 1st quark field is not any of d, u, s, c, b and t quarks
   * \throw InvalidPdgCode
   * if the 2nd quark field is not any of d, u, s, c, b and t quarks
   * \throw InvalidPdgCode
   * if the 3rd quark field is not any of d, u, s, c, b and t quarks
   * \throw InvalidPdgCode
   * if there is nothing else but sign
   */
  inline void set_from_string(const std::string& codestring) {
    dump_ = 0;
    // Implicit with the above: digits_.antiparticle_ = false;
    digits_.n_ = digits_.n_R_ = digits_.n_L_ = digits_.n_q1_ = digits_.n_q2_ =
        digits_.n_q3_ = digits_.n_J_ = digits_.is_nucleus_ = 0;
    size_t length = codestring.size();
    if (length < 1) {
      throw InvalidPdgCode("Empty string does not contain PDG Code\n");
    }
    int c = 0;
    /* Look at current character; if it is a + or minus sign, read it
     * and advance to next char. */
    if (codestring[c] == '-') {
      digits_.antiparticle_ = true;
      ++c;
    } else if (codestring[c] == '+') {
      digits_.antiparticle_ = false;
      ++c;
    }
    // Save if the first character was a sign:
    unsigned int sign = c;

    // Nucleus
    if (length == 10 + sign) {
      nucleus_.is_nucleus_ = true;
      if (codestring[c] != '1' || codestring[c + 1] != '0') {
        throw InvalidPdgCode("Pdg code of nucleus \"" + codestring +
                             "\" should start with 10\n");
      }
      c += 2;
      // ±10LZZZAAAI is the standard for nuclei
      std::array<int, 8> digits;
      for (int i = 0; i < 8; i++) {
        digits[i] = get_digit_from_char(codestring[c + i]);
      }
      nucleus_.n_Lambda_ = digits[0];
      nucleus_.Z_ = 100 * digits[1] + 10 * digits[2] + digits[3];
      nucleus_.A_ = 100 * digits[4] + 10 * digits[5] + digits[6];
      nucleus_.I_ = digits[7];
      return;
    }

    // Codestring shouldn't be longer than 8 + sign, except for nuclei
    if (length > 8 + sign) {
      throw InvalidPdgCode("String \"" + codestring +
                           "\" too long for PDG Code\n");
    }
    /* Please note that in what follows, we actually need c++, not ++c.
     * first digit is used for n_J if the last digit is not enough. */
    if (length > 7 + sign) {
      digits_.n_J_ += get_digit_from_char(codestring[c++]);
    }
    // Codestring has 7 digits? 7th from last goes in n_.
    if (length > 6 + sign) {
      digits_.n_ = get_digit_from_char(codestring[c++]);
    }
    // It has 6 or 7 digits? 6th from last is n_R_.
    if (length > 5 + sign) {
      digits_.n_R_ = get_digit_from_char(codestring[c++]);
    }
    // 5th from last is n_L_.
    if (length > 4 + sign) {
      digits_.n_L_ = get_digit_from_char(codestring[c++]);
    }
    // 4th from last is n_q1_.
    if (length > 3 + sign) {
      digits_.n_q1_ = get_digit_from_char(codestring[c++]);
      if (digits_.n_q1_ > 6) {
        throw InvalidPdgCode("Invalid PDG code " + codestring + " (n_q1>6)");
      }
    }
    // 3rd from last is n_q2_.
    if (length > 2 + sign) {
      digits_.n_q2_ = get_digit_from_char(codestring[c++]);
      if (digits_.n_q2_ > 6) {
        throw InvalidPdgCode("Invalid PDG code " + codestring + " (n_q2>6)");
      }
    }
    // Next to last is n_q3_.
    if (length > 1 + sign) {
      digits_.n_q3_ = get_digit_from_char(codestring[c++]);
      if (digits_.n_q3_ > 6) {
        throw InvalidPdgCode("Invalid PDG code " + codestring + " (n_q3>6)");
      }
    }
    // Last digit is the spin degeneracy.
    if (length > sign) {
      digits_.n_J_ += get_digit_from_char(codestring[c++]);
    } else {
      throw InvalidPdgCode(
          "String \"" + codestring +
          "\" only consists of a sign, that is no valid PDG Code\n");
    }

exec
/bin/zsh -lc "sed -n '1,130p' \""'$HOME/.cache/fusion/smash/smash/src/tests/pdgcode.cc"
sed -n '"'250,305p' \""'$HOME/.cache/fusion/smash/smash/src/tests/pdgcode.cc"
rg -n "constexpr.*(electron|antimu|photon|pion|K0|dminus|bnulls|bPcbar|eta_pr|j_psi|proton|antidelta|sigma|lambda|antixi|omega_bar|lambda_c|sigma_c_bar|xi_c|omega_c_bar|xi_cc_bar|omega_bc|deuteron|antideutron|antitriton|he3|He4|H3L)" "$HOME/.cache/fusion/smash/smash/src/tests/pdgcode.cc" "$HOME/.cache/fusion/smash/smash/src/include/smash/pdgcode.h" | head -200' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
/*
 *
 *    Copyright (c) 2014-2023,2025
 *      SMASH Team
 *
 *    GNU General Public License (GPLv3 or later)
 *
 */

#include "vir/test.h"  // This include has to be first

#include "smash/pdgcode.h"

#include "setup.h"
#include "smash/pdgcode_constants.h"

using namespace smash;

// mesons:
PdgCode pinull(0x111);
PdgCode pion(0x211);
PdgCode piminus(-0x211);
PdgCode eta(0x221);
PdgCode rhominus(-0x213);
PdgCode omega(0x223);
PdgCode phi(0x333);
PdgCode K0(0x311);
PdgCode Kplus(0x321);
PdgCode Kminus(-0x321);
PdgCode K0L(0x130);
PdgCode K0S(0x310);
PdgCode dminus(-0x411);
PdgCode bnulls(0x531);
PdgCode bPcbar(-0x541);
PdgCode eta_pr(0x331);
PdgCode j_psi(0x443);
// baryons:
PdgCode neutron(0x2112);
PdgCode proton(0x2212);
PdgCode antiproton(-0x2212);
PdgCode delta(0x2224);
PdgCode nstar(0x202112);       // N*(1440)^+
PdgCode antidelta(-0x122224);  // Δ(1700)
PdgCode sigmanull(0x3212);
PdgCode sigma(0x3222);
PdgCode lambda(0x3122);
PdgCode antixi(-0x103312);  // Anti-Ξ(1820)
PdgCode omega_bar(-0x3334);
PdgCode lambda_c(0x4122);
PdgCode sigma_c_bar(-0x4114);
PdgCode xi_c(0x4322);
PdgCode omega_c_bar(-0x4332);
PdgCode xi_cc_bar(-0x4422);
PdgCode omega_bc(0x5432);
PdgCode omega_bb(0x5532);
PdgCode deuteron("1000010020");
PdgCode antideutron("-1000010020");
PdgCode triton("1000010030");
PdgCode antitriton("-1000010030");
PdgCode he3("1000020030");
PdgCode antihe3("-1000020030");
PdgCode He4("1000020040");
PdgCode H3L("1010010030");
PdgCode antiH3L("-1010010030");
// non-hadrons:
// leptons
PdgCode electron(0x11);
PdgCode antimu(-0x13);
// bosons
PdgCode photon(0x22);
PdgCode higgs(0x25);

TEST(create_nuclei_using_hexadecimal_codes) {
  VERIFY(deuteron == PdgCode{0x1000010020});
  VERIFY(antideutron == PdgCode{-0x1000010020});
  VERIFY(triton == PdgCode{0x1000010030});
  VERIFY(antitriton == PdgCode{-0x1000010030});
  VERIFY(he3 == PdgCode{0x1000020030});
  VERIFY(antihe3 == PdgCode{-0x1000020030});
  VERIFY(He4 == PdgCode{0x1000020040});
  VERIFY(H3L == PdgCode{0x1010010030});
  VERIFY(antiH3L == PdgCode{-0x1010010030});
}

TEST(write_codes) {
  std::printf("######################### Non-Hadrons:\n");
  std::printf("e^-:       %8s %8x 0x%08x\n", electron.string().c_str(),
              electron.code(), electron.dump());
  std::printf("μ^+:       %8s %8x 0x%08x\n", antimu.string().c_str(),
              antimu.code(), antimu.dump());
  std::printf("γ:         %8s %8x 0x%08x\n", photon.string().c_str(),
              photon.code(), photon.dump());
  std::printf("############################## Mesons:\n");
  std::printf("π^+:       %8s %8x 0x%08x\n", pion.string().c_str(), pion.code(),
              pion.dump());
  std::printf("K^0:       %8s %8x 0x%08x\n", K0.string().c_str(), K0.code(),
              K0.dump());
  std::printf("K^0L:      %8s %8x 0x%08x\n", K0L.string().c_str(), K0L.code(),
              K0L.dump());
  std::printf("K^0S:      %8s %8x 0x%08x\n", K0S.string().c_str(), K0S.code(),
              K0S.dump());
  std::printf("K^-:       %8s %8x 0x%08x\n", Kminus.string().c_str(),
              Kminus.code(), Kminus.dump());
  std::printf("D^-:       %8s %8x 0x%08x\n", dminus.string().c_str(),
              dminus.code(), dminus.dump());
  std::printf("B^0_s:     %8s %8x 0x%08x\n", bnulls.string().c_str(),
              bnulls.code(), bnulls.dump());
  std::printf("bar B^+_c: %8s %8x 0x%08x\n", bPcbar.string().c_str(),
              bPcbar.code(), bPcbar.dump());
  std::printf("η^-:       %8s %8x 0x%08x\n", eta_pr.string().c_str(),
              eta_pr.code(), eta_pr.dump());
  std::printf("J/Ψ:       %8s %8x 0x%08x\n", j_psi.string().c_str(),
              j_psi.code(), j_psi.dump());
  std::printf("############################# Baryons:\n");
  std::printf("p:         %8s %8x 0x%08x\n", proton.string().c_str(),
              proton.code(), proton.dump());
  std::printf("bar Δ(1700)%8s %8x 0x%08x\n", antidelta.string().c_str(),
              antidelta.code(), antidelta.dump());
  std::printf("Σ:         %8s %8x 0x%08x\n", sigma.string().c_str(),
              sigma.code(), sigma.dump());
  std::printf("Λ:         %8s %8x 0x%08x\n", lambda.string().c_str(),
              lambda.code(), lambda.dump());
  std::printf("bar Ξ(1820)%8s %8x 0x%08x\n", antixi.string().c_str(),
              antixi.code(), antixi.dump());
  std::printf("bar Ω:     %8s %8x 0x%08x\n", omega_bar.string().c_str(),
              omega_bar.code(), omega_bar.dump());
  std::printf("Λ_c:       %8s %8x 0x%08x\n", lambda_c.string().c_str(),
              lambda_c.code(), lambda_c.dump());
  std::printf("bar Σ_c:   %8s %8x 0x%08x\n", sigma_c_bar.string().c_str(),
              sigma_c_bar.code(), sigma_c_bar.dump());
  VERIFY(antiproton.is_baryon());
  VERIFY(!antimu.is_baryon());
}

TEST(dilepton) {
  VERIFY(is_dilepton(0x11, -0x11));
  VERIFY(is_dilepton(-0x11, 0x11));
  VERIFY(is_dilepton(0x13, -0x13));
  VERIFY(!is_dilepton(0x11, 0x11));
  VERIFY(!is_dilepton(-0x13, -0x13));
  VERIFY(!is_dilepton(0x211, -0x211));
}

TEST(baryon_number) {
  COMPARE(electron.baryon_number(), 0);
  COMPARE(antimu.baryon_number(), 0);
  COMPARE(photon.baryon_number(), 0);
  COMPARE(pion.baryon_number(), 0);
  COMPARE(K0.baryon_number(), 0);
  COMPARE(K0L.baryon_number(), 0);
  COMPARE(K0S.baryon_number(), 0);
  COMPARE(Kminus.baryon_number(), 0);
  COMPARE(dminus.baryon_number(), 0);
  COMPARE(bnulls.baryon_number(), 0);
  COMPARE(bPcbar.baryon_number(), 0);
  COMPARE(eta_pr.baryon_number(), 0);
  COMPARE(j_psi.baryon_number(), 0);
  COMPARE(proton.baryon_number(), 1);
  COMPARE(antidelta.baryon_number(), -1);
  COMPARE(sigma.baryon_number(), 1);
  COMPARE(lambda.baryon_number(), 1);
  COMPARE(antixi.baryon_number(), -1);
  COMPARE(omega_bar.baryon_number(), -1);
  COMPARE(lambda_c.baryon_number(), 1);
  COMPARE(sigma_c_bar.baryon_number(), -1);
  COMPARE(xi_c.baryon_number(), 1);
  COMPARE(omega_c_bar.baryon_number(), -1);
  COMPARE(xi_cc_bar.baryon_number(), -1);
  COMPARE(omega_bc.baryon_number(), 1);
  COMPARE(deuteron.baryon_number(), 2);
  COMPARE(antideutron.baryon_number(), -2);
  COMPARE(antitriton.baryon_number(), -3);
  COMPARE(he3.baryon_number(), 3);
  COMPARE(He4.baryon_number(), 4);
  COMPARE(H3L.baryon_number(), 3);
}
TEST(isospin3) {
  COMPARE(electron.isospin3(), 0);
  COMPARE(antimu.isospin3(), 0);
  COMPARE(photon.isospin3(), 0);
  COMPARE(pion.isospin3(), +2);
  COMPARE(K0.isospin3(), -1);
  COMPARE(Kminus.isospin3(), -1);
  COMPARE(dminus.isospin3(), -1);
  COMPARE(bnulls.isospin3(), 0);
  COMPARE(bPcbar.isospin3(), 0);

exec
/bin/zsh -lc "sed -n '815,860p' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/pdgcode.h"
rg -n "particle_lists.*pdg|pdg.*output|'"\\.pdgcode\\(\\)\\.code|pdgcode\\.code\" \""'$HOME/.cache/fusion/smash/smash/src/oscaroutput.cc" "$HOME/.cache/fusion/smash/smash/src" | head -100' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
    return code() == -rhs.code();
  }

  /// istream >> PdgCode assigns the PDG Code from an istream.
  friend std::istream& operator>>(std::istream& is, PdgCode& code);

  /**
   * PdgCode 0x0 is guaranteed not to be valid by the PDG standard, but
   * it passes all tests here, so we can use it to show some code is not
   * yet set.
   */
  static PdgCode invalid() { return PdgCode(0x0); }

  /**
   * \return an integer with decimal representation of the code.
   * If the spin is too large for the last digit, an additional digit at the
   * beginning will be used, so that the sum of the first and the last digit is
   * the spin.
   * This is used for binary and ROOT output.
   *
   * \throw InvalidPdgCode if the spin degeneracy is larger than 9
   */
  int32_t get_decimal() const {
    if (is_nucleus()) {
      // ±10LZZZAAAI
      return antiparticle_sign() *
             (nucleus_.I_ + 10 * nucleus_.A_ + 10000 * nucleus_.Z_ +
              10000000 * nucleus_.n_Lambda_ + 1000000000);
    }
    int n_J_1 = 0;
    int n_J_2 = digits_.n_J_;
    if (n_J_2 > 9) {
      n_J_1 = n_J_2 - 9;
      n_J_2 = 9;
    }
    return antiparticle_sign() *
           (n_J_2 + digits_.n_q3_ * 10 + digits_.n_q2_ * 100 +
            digits_.n_q1_ * 1000 + digits_.n_L_ * 10000 +
            digits_.n_R_ * 100000 + digits_.n_ * 1000000 + n_J_1 * 10000000);
  }

  /// Remove all excitation, except spin. Sign and quark content remains.
  void deexcite() {
    if (!is_nucleus()) {
      chunks_.excitation_ = 0;
    } else {
/Users/jinlei/.cache/fusion/smash/smash/src/oscaroutput.cc:300: * #!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge
/Users/jinlei/.cache/fusion/smash/smash/src/oscaroutput.cc:849: * #!ASCII particle_lists p0 pz pdg charge
/Users/jinlei/.cache/fusion/smash/smash/src/oscaroutput.cc:300: * #!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge
/Users/jinlei/.cache/fusion/smash/smash/src/oscaroutput.cc:849: * #!ASCII particle_lists p0 pz pdg charge
/Users/jinlei/.cache/fusion/smash/smash/src/parametrizations.cc:693:        std::make_pair(pack(a.pdgcode().code(), b.pdgcode().code()),
/Users/jinlei/.cache/fusion/smash/smash/src/parametrizations.cc:694:                       pack(c.pdgcode().code(), d.pdgcode().code()));
/Users/jinlei/.cache/fusion/smash/smash/src/parametrizations.cc:762:      pack(a.pdgcode().code() * flip, b.pdgcode().code() * flip),
/Users/jinlei/.cache/fusion/smash/smash/src/parametrizations.cc:763:      pack(c.pdgcode().code() * flip, d.pdgcode().code() * flip));
/Users/jinlei/.cache/fusion/smash/smash/src/crosssections.cc:552:  const auto pdg_nucleon = type_nucleon.pdgcode().code();
/Users/jinlei/.cache/fusion/smash/smash/src/crosssections.cc:553:  const auto pdg_pion = type_pion.pdgcode().code();
/Users/jinlei/.cache/fusion/smash/smash/src/crosssections.cc:1287:  const auto pdg_nucleon = type_nucleon.pdgcode().code();
/Users/jinlei/.cache/fusion/smash/smash/src/crosssections.cc:1288:  const auto pdg_kaon = type_kaon.pdgcode().code();
/Users/jinlei/.cache/fusion/smash/smash/src/crosssections.cc:1795:  const auto pdg_delta = type_delta.pdgcode().code();
/Users/jinlei/.cache/fusion/smash/smash/src/crosssections.cc:1796:  const auto pdg_kaon = type_kaon.pdgcode().code();
/Users/jinlei/.cache/fusion/smash/smash/src/crosssections.cc:1946:  const auto pdg_hyperon = type_hyperon.pdgcode().code();
/Users/jinlei/.cache/fusion/smash/smash/src/crosssections.cc:1947:  const auto pdg_pion = type_pion.pdgcode().code();
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/hepmcoutput.h:31: * are combined into single intial particles with a nuclear pdg code. The output

exec
/bin/zsh -lc "sed -n '285,340p' \""'$HOME/.cache/fusion/smash/smash/src/oscaroutput.cc"
rg -n "get_decimal'"\\(\\)\" \""'$HOME/.cache/fusion/smash/smash/src" | head -100' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
 * **Unless IC output is enabled, the Particle output always provides the
 * current particle list at a specific time.** See \ref
 * doxypage_output_initial_conditions for details about the particles IC output.
 * Even though they are compatible, we do not recommend using Oscar1999 for the
 * Initial_Conditions output.
 * \n
 *
 * \anchor oscar2013_format
 * <h2> Oscar2013 </h2>
 *
 * Oscar2013 is an ASCII (text) human-readable output following the OSCAR 2013
 * standard. The format specifics are the following:\n
 * \n
 * **File header**
 * \code
 * #!OSCAR2013 particle_lists t x y z mass p0 px py pz pdg ID charge
 * # Units: fm fm fm fm GeV GeV GeV GeV GeV none none e
 * # SMASH_version
 * \endcode
 * The header consists of 3 lines starting with '#'. They contain the following
 * information:
 * -# Output version (OSCAR2013) and the type of output (particle_lists),
 * followed by the substructure of the particle lines.
 * -# Units of the quantities in the particle lines
 * -# SMASH version
 *
 * **File header for extended output**
 *
 * If desired, the OSCAR2013 output can be extended
 * by additional particle properties. This requires enabling the extended
 * output in the configuration file, see the \key Extended switch in
 * \ref input_output_content_specific_ "content-specific output options" for
 * further details. The header of the extended OSCAR output is structured
 * identically to the non-extended version, but simply contains more columns
 * because of the additional entries:
 * <div class="fragment">
 * <div class="line"><span class="preprocessor">#!OSCAR2013 particle_lists
 *   t x y z mass p0 px py pz pdg
 *   ID charge ncoll form_time xsecfac proc_id_origin proc_type_origin
 *   time_last_coll pdg_mother1 pdg_mother2 baryon_number strangeness</span>
 * </div>
 * <div class="line"><span class="preprocessor">\# Units: fm fm fm fm GeV GeV
 * GeV GeV GeV none none e none fm none none none fm none none none none</span>
 * </div> <div class="line"><span class="preprocessor">\# SMASH_version</span>
 * </div>
 * </div>
 *
 * **Block header**
 *
 * The OSCAR2013 format is based on a block structure. The beginning of a new
 * block is marked by either the start of a new event or a new intermediate
 * output (at the next timestep).
 *
 * Output block header for a new event:
 * \code
 * # event ev_num ensemble ens_num in Nparticles
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcinterface.cc:89:            "Particle of PID=" + std::to_string(data.pdgcode().get_decimal()) +
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcinterface.cc:242:  auto h = make_gen(p.pdgcode().get_decimal(), status, p.momentum(),
/Users/jinlei/.cache/fusion/smash/smash/src/smash.cc:557:                    ptype->pdgcode().get_decimal(),
/Users/jinlei/.cache/fusion/smash/smash/src/smash.cc:568:                        ptype->pdgcode().get_decimal(), 2, decay->weight(),
/Users/jinlei/.cache/fusion/smash/smash/src/smash.cc:569:                        ptypes[0]->pdgcode().get_decimal(),
/Users/jinlei/.cache/fusion/smash/smash/src/smash.cc:570:                        ptypes[1]->pdgcode().get_decimal(), 0, 0, 0);
/Users/jinlei/.cache/fusion/smash/smash/src/smash.cc:574:                      ptype->pdgcode().get_decimal(), 1, 1.0,
/Users/jinlei/.cache/fusion/smash/smash/src/smash.cc:575:                      ptype->pdgcode().get_decimal(), 0, 0, 0, 0);
/Users/jinlei/.cache/fusion/smash/smash/src/stringprocess.cc:132:    int pdgid = ptype.pdgcode().get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/stringprocess.cc:2627:    const int pdgid = ptype.pdgcode().get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/stringprocess.cc:2736:    const int pdgid = ptype.pdgcode().get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/stringprocess.cc:3280:  return pdg_mapped.get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/rootoutput.cc:528:      pdgcode_[i] = p.pdgcode().get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/rootoutput.cc:549:        pdg_mother1_[i] = h.p1.get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/rootoutput.cc:550:        pdg_mother2_[i] = h.p2.get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/rootoutput.cc:587:      pdgcode_[i] = p.pdgcode().get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/rootoutput.cc:608:        pdg_mother1_[i] = h.p1.get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/rootoutput.cc:609:        pdg_mother2_[i] = h.p2.get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/particletype.h:262:    return is_nucleus() && std::abs(pdgcode_.get_decimal()) == 1000010021;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:324:    ss << get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/pdgcode.h:837:  int32_t get_decimal() const {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputformatter.h:251:          return this->converter_.as_integer(in.pdgcode().get_decimal());
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputformatter.h:290:          return this->converter_.as_integer(in.get_history().p1.get_decimal());
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputformatter.h:294:          return this->converter_.as_integer(in.get_history().p2.get_decimal());
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:184:  COMPARE(electron.get_decimal(), 11);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:185:  COMPARE(antimu.get_decimal(), -13);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:186:  COMPARE(photon.get_decimal(), 22);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:187:  COMPARE(pion.get_decimal(), 211);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:188:  COMPARE(K0.get_decimal(), 311);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:189:  COMPARE(proton.get_decimal(), 2212);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:190:  COMPARE(antidelta.get_decimal(), -122224);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:191:  COMPARE(lambda.get_decimal(), 3122);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:192:  COMPARE(antixi.get_decimal(), -103312);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:193:  COMPARE(deuteron.get_decimal(), 1000010020);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:194:  COMPARE(antideutron.get_decimal(), -1000010020);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:195:  COMPARE(He4.get_decimal(), 1000020040);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:196:  COMPARE(H3L.get_decimal(), 1010010030);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:203:  COMPARE(lambda_2350.get_decimal(), 19903129);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:789:    int dec = t.pdgcode().get_decimal();
/Users/jinlei/.cache/fusion/smash/smash/src/tests/pdgcode.cc:790:    COMPARE(dec, PdgCode::from_decimal(dec).get_decimal());
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:88:  return (p.id() == id) && (p.pdgcode().get_decimal() == pdgcode) &&
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:117:  COMPARE(p1pdg, h.p1.get_decimal());
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:118:  COMPARE(p2pdg, h.p2.get_decimal());

exec
/bin/zsh -lc "sed -n '330,460p' \""'$HOME/.cache/fusion/smash/smash/src/oscaroutput.cc"
rg -n "event .* ensemble|EventLabel|block.*in|Only_Final|only_final|intermediate" "$HOME/.cache/fusion/smash/smash/src/oscaroutput.cc" | head -100' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
 * </div>
 *
 * **Block header**
 *
 * The OSCAR2013 format is based on a block structure. The beginning of a new
 * block is marked by either the start of a new event or a new intermediate
 * output (at the next timestep).
 *
 * Output block header for a new event:
 * \code
 * # event ev_num ensemble ens_num in Nparticles
 * \endcode
 * Where
 * \li \key ev_num: Event number
 * \li \key ens_num: Ensemble number
 * \li \key Nparticles: Number of particles initialized at the beginning of
 * the event
 *
 * Note that `event`, `ensemble` and `in` are no variables, but words that are
 * printed in the header.
 *
 * Output block header for an intermediate output:
 * \code
 * # event ev_num ensemble ens_num out Nparticles
 * \endcode
 * Where
 * \li \key ev_num: Event number
 * \li \key ens_num: Ensemble number
 * \li \key Nparticles: Number of particles at the end of the timestep
 *
 * Note that `event`, `ensemble` and `out` are no variables, but words that are
 * printed in the header.
 *
 * **Particle line**
 *
 * The particle lines are formatted as follows:
 * \code
 * t x y z mass p0 px py pz pdg ID charge
 * \endcode
 *
 * where
 * \li \key t, \key x, \key y, \key z: Space-time coordinates
 * \li \key mass: Rest-mass
 * \li \key p0, \key px, \key py, \key pz: Energy and 3-momentum
 * \li \key pdg: PDG code of the particle (see http://pdg.lbl.gov/).
 * It contains all quantum numbers and uniquely identifies its type
 * \li \key ID: Particle identifier in terms of an integer, it is
 * unique for each particle in the event
 * \li \key charge: the electric charge of the particle in units of the
 * elementary charge e
 *
 * For the **extended output** the particle line contains
 *
 * <div class="fragment">
 * <div class="line"><span class="preprocessor">t x y z mass p0 px py pz pdg
 * ID charge ncoll form_time xsecfac proc_id_origin proc_type_origin
 * time_last_coll pdg_mother1 pdg_mother2 baryon_number strangeness</span></div>
 * </div>
 *
 * \anchor extended_output_format_
 * The additional particle properties available in the extended output format
 * are:
 * \li \key ncoll: Number of collisions the particle has undergone
 * \li \key form_time: Formation time of the particle
 * \li \key xsecfac: Cross section scaling factor (if the particles are
 *     not yet fully formed at the time of interaction, the cross section for
 *     the underlying process is scaled down by the cross section scaling
 *     factor)
 * \li \key proc_id_origin: ID of the process of the particle's last interaction
 * \li \key proc_type_origin: Type of the last process the particle has
 *     undergone. The possible process types are listed in
 *     \ref doxypage_output_process_types
 * \li \key time_last_coll: time of the particle's last interaction (except wall
 *     crossings)
 * \li \key pdg_mother1: PDG code of the 1st mother particle (0 in case the
 *     particle is sampled in a thermal bubble. It is not updated by elastic
 *     scatterings)
 * \li \key pdg_mother2: PDG code of the 2nd mother particle (0 in case the
 *     particle results from the decay of a resonance or the appearance of a
 *     thermal bubble. In the former case, \key pdg_mother1 is the PDG code of
 *     this resonance. It is not updated by elastic scatterings)
 * \li \key baryon_number: Baryon number of the particle. 1 for baryons, -1 for
 *     anti-baryons and 0 for mesons
 * \li \key strangeness: Strangeness of the particle
 *
 * The mother particles are also set in case of an elastic scattering process.
 *
 * **Event end line**\n
 * The end of an event is indicated by the following line:
 * \code
 * # event ev_num ensemble ens_num end 0 impact impact_parameter empty yes_or_no
 * \endcode
 * Where
 * \li \key ev_num: Event number
 * \li \key ens_num: Ensemble number
 * \li \key impact_parameter: Impact parameter of the collision in case of a
 *          collider setup, 0.0 otherwise
 * \li \key yes_or_no: "no" if there was an interaction between the projectile
 * and the target, "yes" otherwise. For non-collider setups, this is always
 * "no"
 *
 * Note that `event`, `end`, `impact` and `empty` are no variables, but words
 * that are printed in the header.
 *
 * <h2> Oscar1999 </h2>
 *
 * Oscar1999 is an ASCII (text) human-readable output following the OSCAR 1999
 * standard. The format specifics are the following:
 *
 * **File header**
 * \code
 * # OSC1999A
 * # final_id_p_x
 * # smash <version>
 * # Block format:
 * # nin nout event_number ensemble_number
 * # id pdg 0 px py pz p0 mass x y z t
 * # End of event: 0 0 event_number ensemble_number impact_parameter
 * #
 * \endcode
 * The header consists of 8 lines starting with '#', of which the last one
 * is basically empty.
 * They contain the following information:
 * -# The specific OSCAR1999 version the formatting follows - OSCAR1999A
 * -# The substructure of each particle line: (id - momentum - coordinates)
 * -# The SMASH version with which the oputput was generated
 * -# - 7. Info on the block structure
 *
 * **Block header**
 *
 * Each output block starts with a line indicating the numbers of ingoing and
42:   * arbitrary number of output blocks and special event end lines that mark the
137:                                                  const EventLabel &event_label,
144:      std::fprintf(file_.get(), "# event %i ensemble %i in %zu\n",
149:       * First block of an event: initial = 0, final = number of particles
159:      std::fprintf(file_.get(), "# event %i ensemble %i start\n",
171:                                                const EventLabel &event_label,
177:      std::fprintf(file_.get(), "# event %i ensemble %i out %zu\n",
186:                   "# event %i ensemble %i end 0 impact %7.3f "
191:      std::fprintf(file_.get(), "# event %i ensemble %i end\n",
196:     * Last block of an event: initial = number of particles, final = 0
254:void OscarOutput<Format, Contents>::at_intermediate_time(
256:    const DensityParameters &, const EventLabel &event_label,
261:      std::fprintf(file_.get(), "# event %i ensemble %i out %zu\n",
334: * The OSCAR2013 format is based on a block structure. The beginning of a new
335: * block is marked by either the start of a new event or a new intermediate
340: * # event ev_num ensemble ens_num in Nparticles
351: * Output block header for an intermediate output:
353: * # event ev_num ensemble ens_num out Nparticles
420: * # event ev_num ensemble ens_num end 0 impact impact_parameter empty yes_or_no
460: * Each output block starts with a line indicating the numbers of ingoing and
461: * outgoing particles as well the numbers of the event and ensemble.
472: * (Nparticles, 0) for intermediate and final timesteps. Nparticles is the
614: * corresponds to one interaction. Each block starts with a line formatted
662: * Each output block starts with a line of the following format:
839:         Only_Final: IfNotEmpty
930:    if (out_par.part_only_final == OutputOnlyFinal::Yes) {
934:    } else if (out_par.part_only_final == OutputOnlyFinal::IfNotEmpty) {
938:    } else {  // out_par.part_only_final == OutputOnlyFinal::No

exec
/bin/zsh -lc "sed -n '110,215p' \""'$HOME/.cache/fusion/smash/smash/src/oscaroutput.cc"
sed -n '"'240,275p' \""'$HOME/.cache/fusion/smash/smash/src/oscaroutput.cc"
sed -n '"'900,950p' \""'$HOME/.cache/fusion/smash/smash/src/oscaroutput.cc"
rg -n "particle_output.*at_eventstart|at_eventstart'"\\(|at_eventend\\(|at_intermediate_time\\(\" \""'$HOME/.cache/fusion/smash/smash/src" | head -150' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:

    std::fprintf(file_.get(), "# %s\n# %s\n# %s\n", format_name.c_str(),
                 oscar_name.c_str(), SMASH_VERSION);
    std::fprintf(file_.get(), "# Block format:\n");
    if (oscar_name == "full_event_history") {
      std::fprintf(file_.get(),
                   "# nin nout density tot_weight part_weight proc_type\n");
    } else {
      std::fprintf(file_.get(), "# nin nout event_number ensemble_number\n");
    }
    std::fprintf(file_.get(), "# %s", formatter_.quantities_line().c_str());
    std::fprintf(
        file_.get(),
        "# End of event: 0 0 event_number ensemble_number impact_parameter\n");
    std::fprintf(file_.get(), "#\n");
  }
}

template <OscarOutputFormat Format, int Contents>
inline void OscarOutput<Format, Contents>::write(const Particles &particles) {
  write_in_chunk<ToASCII>(
      particles, formatter_,
      [this](const ToASCII::type &buf) { this->write(buf); });
}

template <OscarOutputFormat Format, int Contents>
void OscarOutput<Format, Contents>::at_eventstart(const Particles &particles,
                                                  const EventLabel &event_label,
                                                  const EventInfo &) {
  // We do not want the inital particle list or number to be printed in case of
  // IC output
  if (Contents & OscarAtEventstart && !(Contents & OscarParticlesIC)) {
    if (Format == ASCII || Format == OscarFormat2013 ||
        Format == OscarFormat2013Extended) {
      std::fprintf(file_.get(), "# event %i ensemble %i in %zu\n",
                   event_label.event_number, event_label.ensemble_number,
                   particles.size());
    } else {
      /* OSCAR line prefix : initial particles; final particles; event id
       * First block of an event: initial = 0, final = number of particles
       */
      const size_t zero = 0;
      std::fprintf(file_.get(), "%zu %zu %i %i\n", zero, particles.size(),
                   event_label.event_number, event_label.ensemble_number);
    }
    write(particles);
  } else if (Contents & OscarParticlesIC) {
    if (Format == ASCII || Format == OscarFormat2013 ||
        Format == OscarFormat2013Extended) {
      std::fprintf(file_.get(), "# event %i ensemble %i start\n",
                   event_label.event_number, event_label.ensemble_number);
    } else if (Format == OscarFormat1999) {
      const size_t zero = 0;
      std::fprintf(file_.get(), "%zu %zu %i %i\n", zero, zero,
                   event_label.event_number, event_label.ensemble_number);
    }
  }
}

template <OscarOutputFormat Format, int Contents>
void OscarOutput<Format, Contents>::at_eventend(const Particles &particles,
                                                const EventLabel &event_label,
                                                const EventInfo &event) {
  if (Format == ASCII || Format == OscarFormat2013 ||
      Format == OscarFormat2013Extended) {
    if (Contents & OscarParticlesAtEventend ||
        (Contents & OscarParticlesAtEventendIfNotEmpty && !event.empty_event)) {
      std::fprintf(file_.get(), "# event %i ensemble %i out %zu\n",
                   event_label.event_number, event_label.ensemble_number,
                   particles.size());
      write(particles);
    }
    // Comment end of an event
    if (!(Contents & OscarParticlesIC)) {
      const char *empty_event_str = event.empty_event ? "no" : "yes";
      std::fprintf(file_.get(),
                   "# event %i ensemble %i end 0 impact %7.3f "
                   "scattering_projectile_target %s\n",
                   event_label.event_number, event_label.ensemble_number,
                   event.impact_parameter, empty_event_str);
    } else {
      std::fprintf(file_.get(), "# event %i ensemble %i end\n",
                   event_label.event_number, event_label.ensemble_number);
    }
  } else {
    /* OSCAR line prefix : initial particles; final particles; event id
     * Last block of an event: initial = number of particles, final = 0
     * Block ends with null interaction. */
    const size_t zero = 0;
    if (Contents & OscarParticlesAtEventend ||
        (Contents & OscarParticlesAtEventendIfNotEmpty && !event.empty_event)) {
      std::fprintf(file_.get(), "%zu %zu %i %i\n", particles.size(), zero,
                   event_label.event_number, event_label.ensemble_number);
      write(particles);
    }
    // Null interaction marks the end of an event
    std::fprintf(file_.get(), "%zu %zu %i %i %7.3f\n", zero, zero,
                 event_label.event_number, event_label.ensemble_number,
                 event.impact_parameter);
  }
  // Flush to disk
  std::fflush(file_.get());
}

template <OscarOutputFormat Format, int Contents>
void OscarOutput<Format, Contents>::at_interaction(const Action &action,
    for (const auto &p : action.incoming_particles()) {
      write_particledata(p);
    }
    for (const auto &p : action.outgoing_particles()) {
      write_particledata(p);
    }
  } else if (Contents & OscarParticlesIC) {
    for (const auto &p : action.incoming_particles()) {
      write_particledata(p);
    }
  }
}

template <OscarOutputFormat Format, int Contents>
void OscarOutput<Format, Contents>::at_intermediate_time(
    const Particles &particles, const std::unique_ptr<Clock> &,
    const DensityParameters &, const EventLabel &event_label,
    const EventInfo &) {
  if (Contents & OscarTimesteps) {
    if (Format == ASCII || Format == OscarFormat2013 ||
        Format == OscarFormat2013Extended) {
      std::fprintf(file_.get(), "# event %i ensemble %i out %zu\n",
                   event_label.event_number, event_label.ensemble_number,
                   particles.size());
    } else {
      const size_t zero = 0;
      std::fprintf(file_.get(), "%zu %zu %i %i\n", particles.size(), zero,
                   event_label.event_number, event_label.ensemble_number);
    }
    write(particles);
  }
}

/*!\Userguide
 * \page doxypage_output_oscar_particles
 * The OSCAR particles format follows the general block structure of the
          path, name);
    } else if (modern_format && !extended_format) {
      return std::make_unique<OscarOutput<OscarFormat2013, Contents>>(path,
                                                                      name);
    } else if (!modern_format && !extended_format) {
      return std::make_unique<OscarOutput<OscarFormat1999, Contents>>(path,
                                                                      name);
    } else {
      // Only remaining possibility: (!modern_format && extended_format)
      logg[LOutput].warn() << "There is no extended Oscar1999 format, creating "
                              "a regular Oscar1999 output instead.";
      return std::make_unique<OscarOutput<OscarFormat1999, Contents>>(path,
                                                                      name);
    }
  }
}
}  // unnamed namespace

std::unique_ptr<OutputInterface> create_oscar_output(
    const std::string &format, const std::string &content,
    const std::filesystem::path &path, const OutputParameters &out_par) {
  if (format != "Oscar2013" && format != "Oscar1999" && format != "ASCII") {
    throw std::invalid_argument("Creating Oscar output: unknown format");
  }
  const bool modern_format = (format == "Oscar2013");
  const bool custom_format = (format == "ASCII");
  const auto &quantities = custom_format ? out_par.quantities.at(content)
                                         : std::vector<std::string>{};

  if (content == "Particles") {
    if (out_par.part_only_final == OutputOnlyFinal::Yes) {
      return create_selected_format<OscarParticlesAtEventend>(
          path, "particle_lists", modern_format, out_par.part_extended,
          custom_format, quantities);
    } else if (out_par.part_only_final == OutputOnlyFinal::IfNotEmpty) {
      return create_selected_format<OscarParticlesAtEventendIfNotEmpty>(
          path, "particle_lists", modern_format, out_par.part_extended,
          custom_format, quantities);
    } else {  // out_par.part_only_final == OutputOnlyFinal::No
      return create_selected_format<OscarTimesteps | OscarAtEventstart |
                                    OscarParticlesAtEventend>(
          path, "particle_lists", modern_format, out_par.part_extended,
          custom_format, quantities);
    }
  } else if (content == "Collisions") {
    if (out_par.coll_printstartend) {
      return create_selected_format<OscarInteractions | OscarAtEventstart |
                                    OscarParticlesAtEventend>(
          path, "full_event_history", modern_format, out_par.coll_extended,
          custom_format, quantities);
    } else {
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamicoutput.cc:172:void ThermodynamicOutput::at_eventstart(
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamicoutput.cc:177:void ThermodynamicOutput::at_eventend(
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamicoutput.cc:182:void ThermodynamicOutput::at_intermediate_time(
/Users/jinlei/.cache/fusion/smash/smash/src/vtkoutput.cc:65:void VtkOutput::at_eventstart(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/vtkoutput.cc:83:void VtkOutput::at_eventend(const Particles & /*particles*/,
/Users/jinlei/.cache/fusion/smash/smash/src/vtkoutput.cc:87:void VtkOutput::at_intermediate_time(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcinterface.cc:57:void HepMcInterface::at_eventstart(const Particles& particles,
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcinterface.cc:170:void HepMcInterface::at_eventend(const Particles& particles, const EventLabel&,
/Users/jinlei/.cache/fusion/smash/smash/src/oscaroutput.cc:136:void OscarOutput<Format, Contents>::at_eventstart(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/oscaroutput.cc:170:void OscarOutput<Format, Contents>::at_eventend(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/oscaroutput.cc:254:void OscarOutput<Format, Contents>::at_intermediate_time(
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:270:void BinaryOutputCollisions::at_eventstart(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:283:void BinaryOutputCollisions::at_eventend(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:332:void BinaryOutputParticles::at_eventstart(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:345:void BinaryOutputParticles::at_eventend(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:370:void BinaryOutputParticles::at_intermediate_time(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:391:void BinaryOutputInitialConditions::at_eventstart(const Particles &,
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:395:void BinaryOutputInitialConditions::at_eventend(
/Users/jinlei/.cache/fusion/smash/smash/src/rootoutput.cc:400:void RootOutput::at_eventstart(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/rootoutput.cc:424:void RootOutput::at_intermediate_time(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/rootoutput.cc:443:void RootOutput::at_eventend(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamiclatticeoutput.cc:246:void ThermodynamicLatticeOutput::at_eventstart(
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamiclatticeoutput.cc:333:void ThermodynamicLatticeOutput::at_eventstart(
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamiclatticeoutput.cc:458:void ThermodynamicLatticeOutput::at_eventend(const ThermodynamicQuantity tq) {
/Users/jinlei/.cache/fusion/smash/smash/src/icoutput.cc:118:void ICOutput::at_eventstart(const Particles &, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/icoutput.cc:128:void ICOutput::at_eventend([[maybe_unused]] const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/icoutput.cc:139:void ICOutput::at_intermediate_time(const Particles &,
/Users/jinlei/.cache/fusion/smash/smash/src/rivetoutput.cc:113:void RivetOutput::at_eventend(const Particles& particles,
/Users/jinlei/.cache/fusion/smash/smash/src/rivetoutput.cc:116:  HepMcInterface::at_eventend(particles, event_label, event);
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcoutput.cc:281:void HepMcOutput::at_eventend(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcoutput.cc:284:  HepMcInterface::at_eventend(particles, event_label, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar2013output.cc:132:    osc2013full->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar2013output.cc:135:    osc2013full->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar2013output.cc:256:    osc2013final->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar2013output.cc:261:    osc2013final->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar2013output.cc:340:    osc2013full->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar2013output.cc:344:    osc2013full->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar2013output.cc:461:    osc2013full->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar2013output.cc:464:    osc2013full->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/icoutput.cc:79:    IC_output->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/icoutput.cc:85:    IC_output->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/thermodynamicoutput.h:58:  void at_eventstart(const std::vector<Particles> &ensembles,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/thermodynamicoutput.h:67:  void at_eventend(const std::vector<Particles> &ensembles,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/thermodynamicoutput.h:81:  void at_intermediate_time(const std::vector<Particles> &ensembles,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/thermodynamiclatticeoutput.h:73:  void at_eventstart(
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/thermodynamiclatticeoutput.h:89:  void at_eventstart(
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/thermodynamiclatticeoutput.h:99:  void at_eventend(const ThermodynamicQuantity tq) override;
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/hepmcoutput.h:61:  void at_eventend(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2194:      output->at_eventstart(ensembles_[i_ens], {event_, i_ens}, event_info);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2197:    output->at_eventstart(ensembles_, event_);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2203:            output->at_eventstart(event_, ThermodynamicQuantity::EckartDensity,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2207:            output->at_eventstart(event_, ThermodynamicQuantity::EckartDensity,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2213:            output->at_eventstart(event_, ThermodynamicQuantity::EckartDensity,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2219:        output->at_eventstart(event_, ThermodynamicQuantity::Tmn,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2223:        output->at_eventstart(event_, ThermodynamicQuantity::TmnLandau,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2227:        output->at_eventstart(event_, ThermodynamicQuantity::LandauVelocity,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2231:        output->at_eventstart(event_, ThermodynamicQuantity::j_QBS,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2834:        output->at_intermediate_time(ensembles_[i_ens], parameters_.outputclock,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:2840:      output->at_intermediate_time(ensembles_, parameters_.outputclock,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:3185:      output->at_eventend(ensembles_[i_ens], {event_, i_ens}, event_info);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:3188:    output->at_eventend(ensembles_, event_);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:3193:        output->at_eventend(ThermodynamicQuantity::EckartDensity);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:3197:      output->at_eventend(ThermodynamicQuantity::Tmn);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:3200:      output->at_eventend(ThermodynamicQuantity::TmnLandau);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:3203:      output->at_eventend(ThermodynamicQuantity::LandauVelocity);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:3206:      output->at_eventend(ThermodynamicQuantity::j_QBS);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/vtkoutput.h:51:  void at_eventstart(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/vtkoutput.h:63:  void at_eventend(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/vtkoutput.h:75:  void at_intermediate_time(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar1999output.cc:102:    oscfull->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar1999output.cc:107:    oscfull->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar1999output.cc:233:    oscfinal->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar1999output.cc:240:    oscfinal->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar1999output.cc:329:    oscfinal->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/oscar1999output.cc:336:    oscfinal->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/icoutput.h:49:  void at_eventstart(const Particles &, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/icoutput.h:58:  void at_eventend(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/icoutput.h:64:  void at_intermediate_time(const Particles &, const std::unique_ptr<Clock> &,
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:227:    bin_output->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:232:    bin_output->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:314:    bin_output->at_eventstart(*particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:320:    bin_output->at_intermediate_time(*particles, nullptr, dens_par, event_id,
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:324:    bin_output->at_eventend(*particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:413:    bin_output->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:418:    bin_output->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:511:    bin_output->at_eventend(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:587:    bin_output->at_eventstart(*particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:593:    bin_output->at_intermediate_time(*particles, nullptr, dens_par, event_id,
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:597:    bin_output->at_eventend(*particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/oscaroutput.h:86:  void at_eventstart(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/oscaroutput.h:95:  void at_eventend(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/oscaroutput.h:115:  void at_intermediate_time(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/hepmcinterface.h:106:  void at_eventstart(const Particles& particles, const EventLabel& event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/hepmcinterface.h:124:  void at_eventend(const Particles& particles, const EventLabel& event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/tests/vtkoutput.cc:68:  vtkop->at_eventstart(particles, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/vtkoutput.cc:75:  vtkop->at_intermediate_time(particles, nullptr, dens_par, event_id, event);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/binaryoutput.h:171:  void at_eventstart(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/binaryoutput.h:181:  void at_eventend(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/binaryoutput.h:232:  void at_eventstart(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/binaryoutput.h:241:  void at_eventend(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/binaryoutput.h:252:  void at_intermediate_time(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/binaryoutput.h:293:  void at_eventstart(const Particles &, const EventLabel &,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/binaryoutput.h:302:  void at_eventend(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/tests/listmodus.cc:59:    osc2013final->at_eventend(particles, {event, 0}, default_event_info);
/Users/jinlei/.cache/fusion/smash/smash/src/tests/listmodus.cc:132:    oscar_output->at_eventend(particles, {event, 0}, default_event_info);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/rivetoutput.h:62:  void at_eventend(const Particles& particles, const EventLabel& event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/rootoutput.h:67:  void at_eventstart(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/rootoutput.h:76:  void at_eventend(const Particles &particles, const EventLabel &event_label,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/rootoutput.h:87:  void at_intermediate_time(const Particles &particles,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputinterface.h:131:  virtual void at_eventstart(const Particles &, const EventLabel &,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputinterface.h:138:  virtual void at_eventstart(const std::vector<Particles> &, int) {}
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputinterface.h:144:  virtual void at_eventstart(const int, const ThermodynamicQuantity,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputinterface.h:152:  virtual void at_eventstart(const int, const ThermodynamicQuantity,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputinterface.h:160:  virtual void at_eventend(const ThermodynamicQuantity) {}
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputinterface.h:166:  virtual void at_eventend(const Particles &, const EventLabel &,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputinterface.h:172:  virtual void at_eventend(const std::vector<Particles> &, const int) {}
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputinterface.h:182:  virtual void at_intermediate_time(const Particles &,
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputinterface.h:189:  virtual void at_intermediate_time(const std::vector<Particles> &,

exec
/bin/zsh -lc 'rg -n "N_ensembles|Nevents|ensembles" "$HOME/.cache/fusion/smash/smash/input" "$HOME/.cache/fusion/smash/smash/src/include/smash/input_keys.h" | head -200' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:78:  /// Subsection for the minimum-nonempty-ensembles mechanism
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:303:       Nevents: 20
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:321: * number of ensembles in which an interaction took place. Using this option
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:323: * events will be calculated until the desired number of non-empty ensembles
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:324: * is generated. If the <tt>\ref key_gen_nevents_ "Nevents"</tt> key is not
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:328: * Without parallel ensembles (`Ensembles: 1`) the number of ensembles is equal
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:837: * \warning A large enough number of <tt>\ref key_gen_ensembles_
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:840: * needed to compute the potentials. We recommend minimal values of 20 ensembles
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1173:   * \required_key{key_gen_nevents_,Nevents,int}
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1178:   * of ensembles containing interactions is requested, see
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1184:  inline static const Key<int> gen_nevents{InputSections::general + "Nevents",
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1202:   * \required_key{key_gen_mnee_maximum_ensembles_,Maximum_Ensembles_Run,int}
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1204:   * Maximum number of ensembles run. This number serves as a safeguard
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1208:   * \see_key{key_gen_mnee_maximum_ensembles_}
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1217:   * The number of desired non-empty ensembles.\n
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1298:   * \optional_key{key_gen_ensembles_,Ensembles,int,1}
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1300:   * Number of parallel ensembles in the simulation.
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1309:   * over all ensembles (within a given event). This process can be also viewed
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1311:   * ensembles combined, where each particle contributes to the local charge
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1312:   * with a weight of 1/n_ensembles. Such technique is called the *parallel
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1317:   * the parallel ensembles technique is computationally faster than the full
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1321:   * \see_key{key_gen_ensembles_}
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:1323:  inline static const Key<int> gen_ensembles{
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:6089:      std::cref(gen_ensembles),
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:6473:* ensembles. Here, the maximum number of ensembles run is 2000. The calculation
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:6474:* will continue until either this number of ensembles is reached or 1000
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:6475:* ensembles contain interactions. Note that an event consists of 20 ensembles.
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:6476:* The 20 ensembles run in parallel, so the number of non-empty ensembles in the
/Users/jinlei/.cache/fusion/smash/smash/input/custom_nucleus/config.yaml:10:  Nevents:        1
/Users/jinlei/.cache/fusion/smash/smash/input/dileptons/config.yaml:10:    Nevents:        1
/Users/jinlei/.cache/fusion/smash/smash/input/stochastic_box/config.yaml:10:    Nevents:       1
/Users/jinlei/.cache/fusion/smash/smash/input/sphere/config.yaml:10:    Nevents:       1
/Users/jinlei/.cache/fusion/smash/smash/input/deformed_nucleus/config.yaml:10:    Nevents:       1
/Users/jinlei/.cache/fusion/smash/smash/input/box/config.yaml:10:    Nevents:       1
/Users/jinlei/.cache/fusion/smash/smash/input/config.yaml:10:    Nevents:        1
/Users/jinlei/.cache/fusion/smash/smash/input/potentials/config.yaml:11:    Nevents:       1
/Users/jinlei/.cache/fusion/smash/smash/input/multi_particle_box/config.yaml:9:    Nevents:       1
/Users/jinlei/.cache/fusion/smash/smash/input/list/config.yaml:10:    Nevents:       10
/Users/jinlei/.cache/fusion/smash/smash/input/photons/config.yaml:10:    Nevents:       1
/Users/jinlei/.cache/fusion/smash/smash/input/list/config_box.yaml:9:    Nevents:       3

exec
/bin/zsh -lc "sed -n '1160,1330p' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/input_keys.h"
rg -n "Ensembles:" "$HOME/.cache/fusion/smash/smash/input" | head -100' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
   * - `"List"` &rarr; For given external particle list. See
   *   \ref doxypage_input_conf_modi_list "here" for further information.
   * - `"ListBox"` &rarr; For given external particle list in the Box. See
   *   \ref doxypage_input_conf_modi_listbox "here" for further information.
   */
  /**
   * \see_key{key_gen_modus_}
   */
  inline static const Key<std::string> gen_modus{
      InputSections::general + "Modus", {"0.50"}};

  /*!\Userguide
   * \page doxypage_input_conf_general
   * \required_key{key_gen_nevents_,Nevents,int}
   *
   * Number of events to calculate.
   *
   * This key may be omitted on constraint that a minimum number
   * of ensembles containing interactions is requested, see
   * \ref doxypage_input_conf_general_mne.
   */
  /**
   * \see_key{key_gen_nevents_}
   */
  inline static const Key<int> gen_nevents{InputSections::general + "Nevents",
                                           {"0.50"}};

  /*!\Userguide
   * \page doxypage_input_conf_general
   * \required_key{key_gen_randomseed_,Randomseed,64bits-int}
   *
   * Initial seed for the random number generator. If this is negative, the
   * seed will be randomly generated by the operating system.
   */
  /**
   * \see_key{key_gen_randomseed_}
   */
  inline static const Key<int64_t> gen_randomseed{
      InputSections::general + "Randomseed", {"0.50"}};

  /*!\Userguide
   * \page doxypage_input_conf_general_mne
   * \required_key{key_gen_mnee_maximum_ensembles_,Maximum_Ensembles_Run,int}
   *
   * Maximum number of ensembles run. This number serves as a safeguard
   * against SMASH unexpectedly running for a long time.
   */
  /**
   * \see_key{key_gen_mnee_maximum_ensembles_}
   */
  inline static const Key<int> gen_minNonEmptyEnsembles_maximumEnsembles{
      InputSections::g_minEnsembles + "Maximum_Ensembles_Run", {"2.2"}};

  /*!\Userguide
   * \page doxypage_input_conf_general_mne
   * \required_key{key_gen_mnee_number_,Number,int}
   *
   * The number of desired non-empty ensembles.\n
   */
  /**
   * \see_key{key_gen_mnee_number_}
   */
  inline static const Key<int> gen_minNonEmptyEnsembles_number{
      InputSections::g_minEnsembles + "Number", {"1.3"}};

  /*!\Userguide
   * \page doxypage_input_conf_general
   * <hr>
   * <h3> Optional keys </h3>
   */

  /*!\Userguide
   * \page doxypage_input_conf_general
   * \optional_key_no_line{key_gen_delta_time_,Delta_Time,double,1.0}
   *
   * Fixed time step \unit{in fm} at which the collision-finding grid is
   * recreated, and, if potentials are on, momenta are updated according to the
   * equations of motion. The collision-finding grid finds all the collisions
   * from time t_{beginning_of_timestep} until time t_{beginning_of_timestep} +
   * `Delta_Time`, and puts them into a vector. The collisions are then sorted
   * in order of occurrence, and particles are propagated from collision to
   * collision. After each performed collision, additional collisions are found
   * for outgoing particles and merged into the sorted vector.
   *
   * If potentials are on, the `Delta_Time` should be small enough, typically
   * around 0.1 fm. However, if potentials are off, it can be arbitrarily
   * large. In this case it only influences the runtime, but not physics.
   * If `Time_Step_Mode = "None"` is chosen, then the user-provided value of
   * `Delta_Time` is ignored and `Delta_Time` is set to the `End_Time`.
   */
  /**
   * \see_key{key_gen_delta_time_}
   */
  inline static const Key<double> gen_deltaTime{
      InputSections::general + "Delta_Time", 1.0, {"0.50"}};

  /*!\Userguide
   * \page doxypage_input_conf_general
   * \optional_key{key_gen_derivatives_mode_,Derivatives_Mode,string,"Covariant
   * Gaussian"}
   *
   * The mode of calculating the gradients, for example gradients of baryon
   * current. Currently SMASH supports two derivatives modes:
   *  - <tt>"Covariant Gaussian"</tt> and
   *  - <tt>"Finite difference"</tt>.
   *
   * Covariant Gaussian derivatives can be used when Covariant Gaussian smearing
   * is used; they are Lorentz covariant, but they do not calculate the time
   * derivative of the current properly. The `"Finite difference"` mode requires
   * using the lattice, and the derivatives are calculated based on finite
   * differences of a given quantity at adjacent lattice nodes; this mode is
   * numerically more efficient.
   */
  /**
   * \see_key{key_gen_derivatives_mode_}
   */
  inline static const Key<DerivativesMode> gen_derivativesMode{
      InputSections::general + "Derivatives_Mode",
      DerivativesMode::CovariantGaussian,
      {"2.1"}};

  /*!\Userguide
   * \page doxypage_input_conf_general
   * \optional_key{key_gen_discrete_weight_,Discrete_Weight,double,1./3}
   *
   * Parameter for Discrete smearing: Weight given to particle density at the
   * the center node; cannot be smaller than 1./7 (the boundary case of 1./7
   * results in an even distribution of particle's density over the center node
   * and 6 neighboring nodes).
   */
  /**
   * \see_key{key_gen_discrete_weight_}
   */
  inline static const Key<double> gen_smearingDiscreteWeight{
      InputSections::general + "Discrete_Weight", 1. / 3, {"2.1"}};

  /*!\Userguide
   * \page doxypage_input_conf_general
   * \optional_key{key_gen_ensembles_,Ensembles,int,1}
   *
   * Number of parallel ensembles in the simulation.
   *
   * An ensemble is an instance of the system, and without mean-field potentials
   * it is practically equivalent to a completely separate and uncorrelated
   * event. Each ensemble is an independent simulation: initialization,
   * collisions, decays, box wall crossings, and propagation of particles is
   * performed independently within each ensemble.
   *
   * However, the densities and mean-field potentials are computed as averages
   * over all ensembles (within a given event). This process can be also viewed
   * as calculating densities and mean-fields by summing over particles in all
   * ensembles combined, where each particle contributes to the local charge
   * with a weight of 1/n_ensembles. Such technique is called the *parallel
   * ensemble* technique. It increases the statistics necessary for a precise
   * density calculation without increasing the number of collisions, which is
   * not the case in the *full ensemble* method (see <tt>\ref
   * key_gen_testparticles_ "Testparticles"</tt> description). Because of this,
   * the parallel ensembles technique is computationally faster than the full
   * ensemble technique.
   */
  /**
   * \see_key{key_gen_ensembles_}
   */
  inline static const Key<int> gen_ensembles{
      InputSections::general + "Ensembles", 1, {"2.1"}};

  /*!\Userguide
   * \page doxypage_input_conf_general
   * \optional_key{key_gen_expansion_rate_,Expansion_Rate,double,0.1}
   *
   * Corresponds to the speed of expansion of the universe in non-Minkowski
/Users/jinlei/.cache/fusion/smash/smash/input/potentials/config.yaml:13:    Ensembles: 20

exec
/bin/zsh -lc 'for f in "$HOME/.cache/fusion/smash/smash/input"/config.yaml "$HOME/.cache/fusion/smash/smash/input"/{box,sphere,potentials,multi_particle_box,list}/config*.yaml; do echo "FILE $f"; rg -n "Modus:|Nevents:|Ensembles:|Output_Interval:|Particles:|Only_Final:|Format:" "$f"; done' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
FILE /Users/jinlei/.cache/fusion/smash/smash/input/config.yaml
5:    Modus:          Collider
10:    Nevents:        1
13:    Output_Interval: 10.0
14:    Particles:
15:        Format:          ["Oscar2013"]
20:            Particles: {2212: 79, 2112: 118} #Gold197
22:            Particles: {2212: 79, 2112: 118} #Gold197
FILE /Users/jinlei/.cache/fusion/smash/smash/input/box/config.yaml
5:    Modus:         Box
10:    Nevents:       1
13:    Output_Interval: 20.0
14:    Particles:
15:        Format:          ["Oscar2013"]
16:        Only_Final:      No
FILE /Users/jinlei/.cache/fusion/smash/smash/input/sphere/config.yaml
5:    Modus:         Sphere
10:    Nevents:       1
13:    Output_Interval: 20.0
14:    Particles:
15:        Format:          ["Oscar2013"]
FILE /Users/jinlei/.cache/fusion/smash/smash/input/potentials/config.yaml
6:    Modus:         Collider
11:    Nevents:       1
13:    Ensembles: 20
17:    Output_Interval: 5.0
18:    Particles:
19:        Format:          ["Oscar2013"]
24:            Particles: {2212: 29, 2112: 35} #Cu64
26:            Particles: {2212: 29, 2112: 35} #Cu64
FILE /Users/jinlei/.cache/fusion/smash/smash/input/multi_particle_box/config.yaml
5:    Modus:         Box
9:    Nevents:       1
12:    Output_Interval:  1.0
14:        Format:  ["Oscar2013_bin"]
16:    Particles:
17:        Format:  ["Oscar2013_bin"]
18:        Only_Final:         No
FILE /Users/jinlei/.cache/fusion/smash/smash/input/list/config.yaml
5:    Modus:         List
10:    Nevents:       10
13:    Output_Interval: 10.0
14:    Particles:
15:        Format:          ["Oscar2013"]
FILE /Users/jinlei/.cache/fusion/smash/smash/input/list/config_box.yaml
5:    Modus:         ListBox
9:    Nevents:       3
12:    Output_Interval:  1.0
13:    Particles:
14:        Format:  ["Oscar2013"]
15:        Only_Final: No

exec
/bin/zsh -lc 'audit_box_dir="$(mktemp -d /private/tmp/smash-box-audit.XXXXXX)"
SMASH="$HOME/.cache/fusion/smash/smash/build/smash" bash skills/smash/scripts/run_smash.sh --config "$HOME/.cache/fusion/smash/smash/input/box/config.yaml" --outdir "$audit_box_dir" --seed 7 --nevents 1 --end-time 21.0
rc=$?
printf '"'RC=%s OUT=%s\\n' \""'$rc" "$audit_box_dir"
if [ -f "$audit_box_dir/out/particle_lists.oscar" ]; then
  rg -n '"'"'^# event '"' \""'$audit_box_dir/out/particle_lists.oscar" | head -30
fi
ls -l "$audit_box_dir" | sed -n '"'1,30p'
exit \""'$rc"' in /Users/jinlei/Desktop/code/FUSION
 exited 1 in 37293ms:
run_smash: using particle table /Users/jinlei/.cache/fusion/smash/smash/input/box/particles.txt
run_smash: using decay table /Users/jinlei/.cache/fusion/smash/smash/input/box/decaymodes.txt
run_smash: running smash with seed 7 into /private/tmp/smash-box-audit.0MZV9B
run_smash: the output has 2 event-start and 1 event-end markers; they must pair
RC=1 OUT=/private/tmp/smash-box-audit.0MZV9B
4:# event 0 ensemble 0 in 247
252:# event 0 ensemble 0 out 255
508:# event 0 ensemble 0 out 259
768:# event 0 ensemble 0 end 0 impact  -1.000 scattering_projectile_target no
total 136
-rw-r--r--@   1 jinlei  wheel   1205 Jul 23 16:21 config_used.yaml
-rw-r--r--@   1 jinlei  wheel  23021 Jul 23 16:21 decaymodes_used.txt
drwxr-xr-x@   4 jinlei  wheel    128 Jul 23 16:22 out
-rw-r--r--@   1 jinlei  wheel  12373 Jul 23 16:21 particles_used.txt
-rw-r--r--@   1 jinlei  wheel  13620 Jul 23 16:22 smash.log
-rw-r--r--@   1 jinlei  wheel   6218 Jul 23 16:22 stderr.txt
drwxr-xr-x@ 248 jinlei  wheel   7936 Jul 23 16:22 tabulations

exec
/bin/zsh -lc 'audit_box_dir=/private/tmp/smash-box-audit.0MZV9B
ls -la "$audit_box_dir" "$audit_box_dir/out" 2>&1 | sed -n '"'1,80p'
printf 'markers\\n'
rg -n '"'^# event '"' \""'$audit_box_dir/out/particle_lists.oscar" 2>/dev/null | head -40
printf '"'logs\\n'
tail -20 \""'$audit_box_dir/stderr.txt" 2>/dev/null
tail -20 "$audit_box_dir/smash.log" 2>/dev/null' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
/private/tmp/smash-box-audit.0MZV9B:
total 136
drwx------@   9 jinlei  wheel    288 Jul 23 16:21 .
drwxrwxrwt  317 root    wheel  10144 Jul 23 16:22 ..
-rw-r--r--@   1 jinlei  wheel   1205 Jul 23 16:21 config_used.yaml
-rw-r--r--@   1 jinlei  wheel  23021 Jul 23 16:21 decaymodes_used.txt
drwxr-xr-x@   4 jinlei  wheel    128 Jul 23 16:22 out
-rw-r--r--@   1 jinlei  wheel  12373 Jul 23 16:21 particles_used.txt
-rw-r--r--@   1 jinlei  wheel  13620 Jul 23 16:22 smash.log
-rw-r--r--@   1 jinlei  wheel   6218 Jul 23 16:22 stderr.txt
drwxr-xr-x@ 248 jinlei  wheel   7936 Jul 23 16:22 tabulations

/private/tmp/smash-box-audit.0MZV9B/out:
total 344
drwxr-xr-x@ 4 jinlei  wheel    128 Jul 23 16:22 .
drwx------@ 9 jinlei  wheel    288 Jul 23 16:21 ..
-rw-r--r--@ 1 jinlei  wheel  37371 Jul 23 16:21 config.yaml
-rw-r--r--@ 1 jinlei  wheel  72156 Jul 23 16:22 particle_lists.oscar
markers
4:# event 0 ensemble 0 in 247
252:# event 0 ensemble 0 out 255
508:# event 0 ensemble 0 out 259
768:# event 0 ensemble 0 end 0 impact  -1.000 scattering_projectile_target no
logs
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'22'21]  WARN         Fpe         : Failed to setup traps on 7
[16'21'46]  INFO      Experiment     : Using 1 parallel ensembles.
[16'21'46]  INFO     FindScatter     : Evaluating total cross sections from partial processes.
[16'21'46]  INFO      Experiment     : Added output Particles of format Oscar2013
[16'21'46]  INFO      Experiment     : Smearing type: Covariant Gaussian
[16'21'46]  INFO         Main        : Tabulating cross section integrals...
[16'21'46]  INFO         Main        : Tabulations path: "/private/tmp/smash-box-audit.0MZV9B/tabulations"
Caching tabulation to "NN(1440).bin"Caching tabulation to "πN(1440).bin"Caching tabulation to "KN(1440).bin"Caching tabulation to "ΔN(1440).bin"Caching tabulation to "NN(1520).bin"Caching tabulation to "πN(1520).bin"Caching tabulation to "KN(1520).bin"Caching tabulation to "ΔN(1520).bin"Caching tabulation to "NN(1535).bin"Caching tabulation to "πN(1535).bin"Caching tabulation to "KN(1535).bin"Caching tabulation to "ΔN(1535).bin"Caching tabulation to "NN(1650).bin"Caching tabulation to "πN(1650).bin"Caching tabulation to "KN(1650).bin"Caching tabulation to "ΔN(1650).bin"Caching tabulation to "NN(1675).bin"Caching tabulation to "πN(1675).bin"Caching tabulation to "KN(1675).bin"Caching tabulation to "ΔN(1675).bin"Caching tabulation to "NN(1680).bin"Caching tabulation to "πN(1680).bin"Caching tabulation to "KN(1680).bin"Caching tabulation to "ΔN(1680).bin"Caching tabulation to "NN(1700).bin"Caching tabulation to "πN(1700).bin"Caching tabulation to "KN(1700).bin"Caching tabulation to "ΔN(1700).bin"Caching tabulation to "NN(1710).bin"Caching tabulation to "πN(1710).bin"Caching tabulation to "KN(1710).bin"Caching tabulation to "ΔN(1710).bin"Caching tabulation to "NN(1720).bin"Caching tabulation to "πN(1720).bin"Caching tabulation to "KN(1720).bin"Caching tabulation to "ΔN(1720).bin"Caching tabulation to "NN(1875).bin"Caching tabulation to "πN(1875).bin"Caching tabulation to "KN(1875).bin"Caching tabulation to "ΔN(1875).bin"Caching tabulation to "NN(1880).bin"Caching tabulation to "πN(1880).bin"Caching tabulation to "KN(1880).bin"Caching tabulation to "ΔN(1880).bin"Caching tabulation to "NN(1895).bin"Caching tabulation to "πN(1895).bin"Caching tabulation to "KN(1895).bin"Caching tabulation to "ΔN(1895).bin"Caching tabulation to "NN(1900).bin"Caching tabulation to "πN(1900).bin"Caching tabulation to "KN(1900).bin"Caching tabulation to "ΔN(1900).bin"Caching tabulation to "NN(1990).bin"Caching tabulation to "πN(1990).bin"Caching tabulation to "KN(1990).bin"Caching tabulation to "ΔN(1990).bin"Caching tabulation to "NN(2060).bin"Caching tabulation to "πN(2060).bin"Caching tabulation to "KN(2060).bin"Caching tabulation to "ΔN(2060).bin"Caching tabulation to "NN(2080).bin"Caching tabulation to "πN(2080).bin"Caching tabulation to "KN(2080).bin"Caching tabulation to "ΔN(2080).bin"Caching tabulation to "NN(2100).bin"Caching tabulation to "πN(2100).bin"Caching tabulation to "KN(2100).bin"Caching tabulation to "ΔN(2100).bin"Caching tabulation to "NN(2120).bin"Caching tabulation to "πN(2120).bin"Caching tabulation to "KN(2120).bin"Caching tabulation to "ΔN(2120).bin"Caching tabulation to "NN(2190).bin"Caching tabulation to "πN(2190).bin"Caching tabulation to "KN(2190).bin"Caching tabulation to "ΔN(2190).bin"Caching tabulation to "NN(2220).bin"Caching tabulation to "πN(2220).bin"Caching tabulation to "KN(2220).bin"Caching tabulation to "ΔN(2220).bin"Caching tabulation to "NN(2250).bin"Caching tabulation to "πN(2250).bin"Caching tabulation to "KN(2250).bin"Caching tabulation to "ΔN(2250).bin"Caching tabulation to "NΔ.bin"Caching tabulation to "πΔ.bin"Caching tabulation to "KΔ.bin"Caching tabulation to "ΔΔ.bin"Caching tabulation to "NΔ(1620).bin"Caching tabulation to "πΔ(1620).bin"Caching tabulation to "KΔ(1620).bin"Caching tabulation to "ΔΔ(1620).bin"Caching tabulation to "NΔ(1700).bin"Caching tabulation to "πΔ(1700).bin"Caching tabulation to "KΔ(1700).bin"Caching tabulation to "ΔΔ(1700).bin"Caching tabulation to "NΔ(1900).bin"Caching tabulation to "πΔ(1900).bin"Caching tabulation to "KΔ(1900).bin"Caching tabulation to "ΔΔ(1900).bin"Caching tabulation to "NΔ(1905).bin"Caching tabulation to "πΔ(1905).bin"Caching tabulation to "KΔ(1905).bin"Caching tabulation to "ΔΔ(1905).bin"Caching tabulation to "NΔ(1910).bin"Caching tabulation to "πΔ(1910).bin"Caching tabulation to "KΔ(1910).bin"Caching tabulation to "ΔΔ(1910).bin"Caching tabulation to "NΔ(1920).bin"Caching tabulation to "πΔ(1920).bin"Caching tabulation to "KΔ(1920).bin"Caching tabulation to "ΔΔ(1920).bin"Caching tabulation to "NΔ(1930).bin"Caching tabulation to "πΔ(1930).bin"Caching tabulation to "KΔ(1930).bin"Caching tabulation to "ΔΔ(1930).bin"Caching tabulation to "NΔ(1950).bin"Caching tabulation to "πΔ(1950).bin"Caching tabulation to "KΔ(1950).bin"Caching tabulation to "ΔΔ(1950).bin"Caching tabulation to "NΛ(1405).bin"Caching tabulation to "πΛ(1405).bin"Caching tabulation to "KΛ(1405).bin"Caching tabulation to "ΔΛ(1405).bin"Caching tabulation to "NΛ(1520).bin"Caching tabulation to "πΛ(1520).bin"Caching tabulation to "KΛ(1520).bin"Caching tabulation to "ΔΛ(1520).bin"Caching tabulation to "NΛ(1600).bin"Caching tabulation to "πΛ(1600).bin"Caching tabulation to "KΛ(1600).bin"Caching tabulation to "ΔΛ(1600).bin"Caching tabulation to "NΛ(1670).bin"Caching tabulation to "πΛ(1670).bin"Caching tabulation to "KΛ(1670).bin"Caching tabulation to "ΔΛ(1670).bin"Caching tabulation to "NΛ(1690).bin"Caching tabulation to "πΛ(1690).bin"Caching tabulation to "KΛ(1690).bin"Caching tabulation to "ΔΛ(1690).bin"Caching tabulation to "NΛ(1800).bin"Caching tabulation to "πΛ(1800).bin"Caching tabulation to "KΛ(1800).bin"Caching tabulation to "ΔΛ(1800).bin"Caching tabulation to "NΛ(1810).bin"Caching tabulation to "πΛ(1810).bin"Caching tabulation to "KΛ(1810).bin"Caching tabulation to "ΔΛ(1810).bin"Caching tabulation to "NΛ(1820).bin"Caching tabulation to "πΛ(1820).bin"Caching tabulation to "KΛ(1820).bin"Caching tabulation to "ΔΛ(1820).bin"Caching tabulation to "NΛ(1830).bin"Caching tabulation to "πΛ(1830).bin"Caching tabulation to "KΛ(1830).bin"Caching tabulation to "ΔΛ(1830).bin"Caching tabulation to "NΛ(1890).bin"Caching tabulation to "πΛ(1890).bin"Caching tabulation to "KΛ(1890).bin"Caching tabulation to "ΔΛ(1890).bin"Caching tabulation to "NΛ(2100).bin"Caching tabulation to "πΛ(2100).bin"Caching tabulation to "KΛ(2100).bin"Caching tabulation to "ΔΛ(2100).bin"Caching tabulation to "NΛ(2110).bin"Caching tabulation to "πΛ(2110).bin"Caching tabulation to "KΛ(2110).bin"Caching tabulation to "ΔΛ(2110).bin"Caching tabulation to "NΛ(2350).bin"Caching tabulation to "πΛ(2350).bin"Caching tabulation to "KΛ(2350).bin"Caching tabulation to "ΔΛ(2350).bin"Caching tabulation to "NΛc(2595).bin"Caching tabulation to "πΛc(2595).bin"Caching tabulation to "KΛc(2595).bin"Caching tabulation to "ΔΛc(2595).bin"Caching tabulation to "NΞ(1530).bin"Caching tabulation to "πΞ(1530).bin"Caching tabulation to "KΞ(1530).bin"Caching tabulation to "ΔΞ(1530).bin"Caching tabulation to "NΞ(1690).bin"Caching tabulation to "πΞ(1690).bin"Caching tabulation to "KΞ(1690).bin"Caching tabulation to "ΔΞ(1690).bin"Caching tabulation to "NΞ(1820).bin"Caching tabulation to "πΞ(1820).bin"Caching tabulation to "KΞ(1820).bin"Caching tabulation to "ΔΞ(1820).bin"Caching tabulation to "NΞ(1950).bin"Caching tabulation to "πΞ(1950).bin"Caching tabulation to "KΞ(1950).bin"Caching tabulation to "ΔΞ(1950).bin"Caching tabulation to "NΞ(2030).bin"Caching tabulation to "πΞ(2030).bin"Caching tabulation to "KΞ(2030).bin"Caching tabulation to "ΔΞ(2030).bin"Caching tabulation to "NΣ(1385).bin"Caching tabulation to "πΣ(1385).bin"Caching tabulation to "KΣ(1385).bin"Caching tabulation to "ΔΣ(1385).bin"Caching tabulation to "NΣ(1660).bin"Caching tabulation to "πΣ(1660).bin"Caching tabulation to "KΣ(1660).bin"Caching tabulation to "ΔΣ(1660).bin"Caching tabulation to "NΣ(1670).bin"Caching tabulation to "πΣ(1670).bin"Caching tabulation to "KΣ(1670).bin"Caching tabulation to "ΔΣ(1670).bin"Caching tabulation to "NΣ(1750).bin"Caching tabulation to "πΣ(1750).bin"Caching tabulation to "KΣ(1750).bin"Caching tabulation to "ΔΣ(1750).bin"Caching tabulation to "NΣ(1775).bin"Caching tabulation to "πΣ(1775).bin"Caching tabulation to "KΣ(1775).bin"Caching tabulation to "ΔΣ(1775).bin"Caching tabulation to "NΣ(1915).bin"Caching tabulation to "πΣ(1915).bin"Caching tabulation to "KΣ(1915).bin"Caching tabulation to "ΔΣ(1915).bin"Caching tabulation to "NΣ(1940).bin"Caching tabulation to "πΣ(1940).bin"Caching tabulation to "KΣ(1940).bin"Caching tabulation to "ΔΣ(1940).bin"Caching tabulation to "NΣ(2030).bin"Caching tabulation to "πΣ(2030).bin"Caching tabulation to "KΣ(2030).bin"Caching tabulation to "ΔΣ(2030).bin"Caching tabulation to "NΣ(2250).bin"Caching tabulation to "πΣ(2250).bin"Caching tabulation to "KΣ(2250).bin"Caching tabulation to "ΔΣ(2250).bin"Caching tabulation to "NΣc(2455).bin"Caching tabulation to "πΣc(2455).bin"Caching tabulation to "KΣc(2455).bin"Caching tabulation to "ΔΣc(2455).bin"Caching tabulation to "NΣc(2520).bin"Caching tabulation to "πΣc(2520).bin"Caching tabulation to "KΣc(2520).bin"Caching tabulation to "ΔΣc(2520).bin"Caching tabulation to "NΩ(2250).bin"Caching tabulation to "πΩ(2250).bin"Caching tabulation to "KΩ(2250).bin"Caching tabulation to "ΔΩ(2250).bin"Caching tabulation to "ρρ.bin"Caching tabulation to "ρh₁(1170).bin"[16'22'05]  INFO         Main        : Event 0
[16'22'05]  INFO      Experiment     : random number seed: 7
[16'22'21]  INFO         Box         : Initial hadron gas baryon density -3.42629e-06
[16'22'21]  INFO         Box         : Initial hadron gas strange density -1.19103e-05
[16'22'21]  INFO         Box         : Initial hadron gas charge density -1.92233e-06
[16'22'21]  INFO      Experiment     : -----------------------------------------------------------------------------------------------------------------
[16'22'21]  INFO      Experiment     : Time[fm]   Ekin[GeV]   E_MF[GeV]  ETotal[GeV]  ETot/N[GeV]  D(ETot/N)[GeV] Scatt&Decays  Particles     Comp.Time
[16'22'21]  INFO      Experiment     : -----------------------------------------------------------------------------------------------------------------
[16'22'21]  INFO      Experiment     :    0.000     217.108       0.000      217.108     0.878980      0.000000              0        247    34.327 [s]
[16'22'23]  INFO      Experiment     :   20.000     217.108       0.000      217.108     0.851404      0.000000           2394        255    37.060 [s]
[16'22'23]  INFO      Experiment     :   21.000     217.108       0.000      217.108     0.838255      0.000000            130        259    37.076 [s]
[16'22'23]  INFO      Experiment     : -----------------------------------------------------------------------------------------------------------------
[16'22'23]  INFO      Experiment     : Time real: 37.0759 [s]
[16'22'23]  INFO      Experiment     : Final interaction number: 2524

exec
/bin/zsh -lc "python3 skills/smash/scripts/check_conservation_smash.py /private/tmp/smash-box-audit.0MZV9B/out/particle_lists.oscar --baryons 0 --charge 0; printf 'RC=%s\\n' \""'$?"' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
FAIL: event 0/0 starts while 0/0 is still open
RC=1

exec
/bin/zsh -lc 'for d in "$HOME/.cache/fusion/smash/smash/input"/*; do [ -d "$d" ] || continue; ncfg=$(find "$d" -maxdepth 1 -name '"'*.yaml' | wc -l | tr -d ' '); hasp="'$([ -f "$d/particles.txt" ] && echo yes || echo no); hasd=$([ -f "$d/decaymodes.txt" ] && echo yes || echo no); if [ "$ncfg" -gt 0 ]; then printf '"'%s configs=%s particles=%s decays=%s\\n' \""'$(basename "$d")" "$ncfg" "$hasp" "$hasd"; fi; done
find "$HOME/.cache/fusion/smash/smash/input/list" -maxdepth 1 -type f -print | sort' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
box configs=1 particles=yes decays=yes
custom_nucleus configs=1 particles=no decays=no
deformed_nucleus configs=1 particles=no decays=no
dileptons configs=1 particles=no decays=yes
list configs=2 particles=no decays=no
multi_particle_box configs=1 particles=yes decays=yes
photons configs=1 particles=yes decays=yes
potentials configs=1 particles=no decays=no
sphere configs=1 particles=no decays=no
stochastic_box configs=1 particles=no decays=no
/Users/jinlei/.cache/fusion/smash/smash/input/list/box_list0
/Users/jinlei/.cache/fusion/smash/smash/input/list/config.yaml
/Users/jinlei/.cache/fusion/smash/smash/input/list/config_box.yaml
/Users/jinlei/.cache/fusion/smash/smash/input/list/example_list0

exec
/bin/zsh -lc "python3 - <<'PY'
import importlib.util
p='skills/smash/scripts/check_conservation_smash.py'
s=importlib.util.spec_from_file_location('cc',p); m=importlib.util.module_from_spec(s); s.loader.exec_module(m)
cases={11:0,-13:0,22:0,211:0,311:0,130:0,310:0,-321:0,-411:0,531:0,-541:0,331:0,443:0,2212:1,-122224:-1,3222:1,3122:1,-103312:-1,-3334:-1,4122:1,-4114:-1,4322:1,-4332:-1,-4422:-1,5432:1,12112:1,-12112:-1,202112:1,19903129:1,1000010020:2,-1000010020:-2,-1000010030:-3,1000020030:3,1000020040:4,1010010030:3}
bad=[]
for code,want in cases.items():
    got=m.baryon_number(code)
    if got"'!=want: bad.append((code,got,want))
print(f'"'{len(cases)} cases, bad={bad}')
PY" in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
35 cases, bad=[]

exec
/bin/zsh -lc "rg -n 'Oscar2013_bin|Oscar1999|HepMC_asciiv3|HepMC_treeroot|For_vHLLE|Lattice_ASCII|Lattice_Binary|Rivet|Root|VTK|ASCII|Binary' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/input_keys.h" | sed -n '"'1,260p'" in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
155:  /// Subsection for the output Rivet content
156:  inline static const Section o_rivet = InputSections::output + "Rivet";
157:  /// Subsection for the output Rivet weights information
755: * required if the \ref doxypage_output_vtk_lattice "Thermodynamic VTK Output",
1743:   * \optional_key{key_log_rootsolver_,RootSolver,string,$\{\ref
1752:      InputSections::logging + "RootSolver", DefaultType::Dependent, {"3.1"}};
5041:   * &rArr; Ignored with `Oscar1999`, `ASCII`, `Binary`, `VTK`, `HepMC_asciiv3`
5042:   * and `HepMC_treeroot` formats.
5057:   * &rArr; If using the `ASCII` or `Binary` format, a non-empty list must be
5059:   * specified without including `ASCII` or `Binary` as format.
5074:   * &rArr; Ignored with `VTK`, `HepMC_asciiv3` and `HepMC_treeroot` formats.
5093:   * &rArr; Format `VTK` not available
5097:   * &rArr; Ignored with `Oscar1999`, `ASCII`, `Binary`, `HepMC_asciiv3` and
5098:   * `HepMC_treeroot` formats.
5114:   * &rArr; If using the `ASCII` or `Binary` format, a non-empty list must be
5116:   * specified without including `ASCII` or `Binary` as format.
5131:   * &rArr; Ignored with `Root`, `HepMC_asciiv3` and `HepMC_treeroot` formats.
5145:   * &rArr; Only `ASCII` and `Binary` formats.
5149:   * &rArr; Ignored with `Oscar1999`, `ASCII` and `Binary` formats.
5165:   * &rArr; If using the `ASCII` or `Binary` format, a non-empty list must be
5167:   * specified without including `ASCII` or `Binary` as format.
5182:   * &rArr; Only `ASCII` and `Binary` formats.
5186:   * &rArr; Ignored with `Oscar1999`, `ASCII` and `Binary` formats.
5202:   * &rArr; If using the `ASCII` or `Binary` format, a non-empty list must be
5204:   * specified without including `ASCII` or `Binary` as format.
5219:   * &rArr; Only `ASCII`, `Binary` and `Root`.
5223:   * &rArr; Ignored with `Oscar1999`, `ASCII`, `Binary` and `Root` formats.
5239:   * &rArr; If using the `ASCII` or `Binary` format, a non-empty list must be
5241:   * specified without including `ASCII` or `Binary` as format.
5316:   * <h3> &diams; Rivet </h3>
5321:   *       omitted, Rivet default behavior will be used.
5327:   * Rivet analysis.
5354:   * Ask Rivet to not validate beams before running analyses. This is needed if
5369:   * Specifies log levels for various parts of Rivet, including analyses. Each
5385:   * This key specifies the directories that Rivet will search for analyses
5399:   * Specify data files to read into Rivet (e.g., centrality calibrations) at
5462:   * Ask Rivet not to do multi-weight processing.
5504:   * &rArr; Only `VTK` format.
5507:   * key_output_content_format_ "Format"</tt> key which accept `["VTK"]` value
5517:   * -# on the spatial lattice to VTK output;
5518:   * -# on the spatial lattice to ASCII output;
5519:   * -# at a given point to ASCII output;
5520:   * -# averaged over all particles to ASCII output.
6606:         Format: ["Oscar2013_bin"]
6956:* (\f$-1<y<1\f$). In addition, the extended OSCAR2013 and "For_vHLLE" outputs
6962:        Format: ["For_vHLLE","Oscar2013"]

exec
/bin/zsh -lc "rg -n 'HepMC_asciiv3|HepMC_treeroot|For_vHLLE|Lattice_ASCII|Lattice_Binary|Oscar2013_bin|\"Root\"|\"Rivet\"' \""'$HOME/.cache/fusion/smash/smash/src" | head -250' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
/Users/jinlei/.cache/fusion/smash/smash/src/rivetoutput.cc:152:  if (fname.rfind("Rivet", 0) != 0) {
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:442:  if (format == "Oscar2013_bin") {
/Users/jinlei/.cache/fusion/smash/smash/src/icoutput.cc:23: * The "For_vHLLE" initial conditions output **SMASH_IC_For_vHLLE.dat** contains
/Users/jinlei/.cache/fusion/smash/smash/src/icoutput.cc:33: * The "For_vHLLE" initial conditions output is formatted as follows:
/Users/jinlei/.cache/fusion/smash/smash/src/icoutput.cc:105:      file_{path / "SMASH_IC_For_vHLLE.dat", "w"},
/Users/jinlei/.cache/fusion/smash/smash/src/icoutput.cc:107:      formatter_{OutputDefaultQuantities::ic_For_vHLLE} {
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcoutput.cc:30: * - _HepMC_asciiv3_ plain human readable ASCII format
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcoutput.cc:31: * - _HepMC_treeroot_ ROOT Tree binary format, readable by ROOT
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcoutput.cc:76: * available in the output, depending on whether the HepMC_asciiv3 or
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcoutput.cc:77: * HepMC_treeroot output options are specified under \key %Particles or
/Users/jinlei/.cache/fusion/smash/smash/src/hepmcoutput.cc:110: * Here there is an example of the first lines of the HepMC_asciiv3 output, in
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamiclatticeoutput.cc:43: * - ASCII format (option "Lattice_ASCII")
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamiclatticeoutput.cc:44: * - Binary format (option "Lattice_Binary")
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamiclatticeoutput.cc:51:     Format: ["Lattice_ASCII","Lattice_Binary"]
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamiclatticeoutput.cc:63: * Lattice_ASCII and Lattice_Binary formats, but, of course, the details are a
/Users/jinlei/.cache/fusion/smash/smash/src/thermodynamiclatticeoutput.cc:80: * In "Lattice_ASCII" the data entries are separated by 1 space and each line of
/Users/jinlei/.cache/fusion/smash/smash/src/experiment.cc:58: * The particles output is generated in "Oscar1999", VTK and "Root" format,
/Users/jinlei/.cache/fusion/smash/smash/src/experiment.cc:67:         Format: ["Oscar1999", "VTK", "Root"]
/Users/jinlei/.cache/fusion/smash/smash/src/experiment.cc:83:         Format: ["Oscar2013", "Oscar2013_bin"]
/Users/jinlei/.cache/fusion/smash/smash/src/experiment.cc:107: * "Modi: Collider: Initial_Conditions: Type"</tt>. The "For_vHLLE" output
/Users/jinlei/.cache/fusion/smash/smash/src/experiment.cc:115:         Format: ["For_vHLLE", "Oscar2013", "Oscar2013_bin", "Root"]
/Users/jinlei/.cache/fusion/smash/smash/src/experiment.cc:118: * The HepMC_asciiv3 and/or HepMC_treeroot ouputs are enabled by specifying
/Users/jinlei/.cache/fusion/smash/smash/src/experiment.cc:124:         Format: ["HepMC_asciiv3","HepMC_treeroot"]
/Users/jinlei/.cache/fusion/smash/smash/src/experiment.cc:126:         Format: ["HepMC_asciiv3","HepMC_treeroot"]
/Users/jinlei/.cache/fusion/smash/smash/src/tests/functional/ic_for_hybrid.py:41:        "Output: {Initial_Conditions: {Format: [For_vHLLE, Oscar2013_bin, Oscar2013]}}"
/Users/jinlei/.cache/fusion/smash/smash/src/tests/functional/ic_for_hybrid.py:57:ascii_file = output_directory + '/SMASH_IC_For_vHLLE.dat'
/Users/jinlei/.cache/fusion/smash/smash/src/tests/functional/ic_for_hybrid.py:80:# px and py are given with default precision in "For_vHLLE", but with %.9 in OSCAR.
/Users/jinlei/.cache/fusion/smash/smash/src/tests/CMakeLists.txt:452:                      -c "Output: {Particles: {Format: [Root, HepMC_treeroot, HepMC_asciiv3]}}"
/Users/jinlei/.cache/fusion/smash/smash/src/tests/CMakeLists.txt:453:                      -c "Output: {Collisions: {Format: [Root, HepMC_treeroot, HepMC_asciiv3]}}"
/Users/jinlei/.cache/fusion/smash/smash/src/tests/icoutput.cc:66:      testoutputpath / "SMASH_IC_For_vHLLE.dat";
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:156:  inline static const Section o_rivet = InputSections::output + "Rivet";
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:5041:   * &rArr; Ignored with `Oscar1999`, `ASCII`, `Binary`, `VTK`, `HepMC_asciiv3`
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:5042:   * and `HepMC_treeroot` formats.
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:5074:   * &rArr; Ignored with `VTK`, `HepMC_asciiv3` and `HepMC_treeroot` formats.
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:5097:   * &rArr; Ignored with `Oscar1999`, `ASCII`, `Binary`, `HepMC_asciiv3` and
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:5098:   * `HepMC_treeroot` formats.
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:5131:   * &rArr; Ignored with `Root`, `HepMC_asciiv3` and `HepMC_treeroot` formats.
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:6606:         Format: ["Oscar2013_bin"]
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:6956:* (\f$-1<y<1\f$). In addition, the extended OSCAR2013 and "For_vHLLE" outputs
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/input_keys.h:6962:        Format: ["For_vHLLE","Oscar2013"]
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:743:    if ((format == "HepMC") || (format == "HepMC_asciiv3") ||
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:744:        (format == "HepMC_treeroot")) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:747:    if (content == "Rivet") {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:748:      abort_because_of("Rivet");
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:762:  } else if (format == "Root") {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:775:  } else if ((format == "Binary" || format == "Oscar2013_bin") &&
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:794:             (format == "Lattice_ASCII" || format == "Lattice_Binary")) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:797:        output_path, content, out_par, format == "Lattice_ASCII",
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:798:        format == "Lattice_Binary"));
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:803:  } else if (content == "Initial_Conditions" && format == "For_vHLLE") {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:810:        std::make_unique<ICOutput>(output_path, "SMASH_IC_For_vHLLE", out_par));
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:811:  } else if ((format == "HepMC") || (format == "HepMC_asciiv3") ||
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:812:             (format == "HepMC_treeroot")) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:815:      if ((format == "HepMC") || (format == "HepMC_asciiv3")) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:818:      } else if (format == "HepMC_treeroot") {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:824:            "Requested HepMC_treeroot output not available, "
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:829:      if ((format == "HepMC") || (format == "HepMC_asciiv3")) {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:832:      } else if (format == "HepMC_treeroot") {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:838:            "Requested HepMC_treeroot output not available, "
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:855:  } else if (content == "Rivet") {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:1251:   *      - using \b "Lattice_ASCII", the \ref doxypage_output_thermodyn_lattice
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:1265:   * - \b "Oscar2013_bin" - alias for the \b "Binary" format with a predefined
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:1267:   * - \b "For_vHLLE" - an alias for the \b "ASCII" format exclusive to the
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:1272:   * - \b "Root" - binary output in the format used by
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:1287:   * - \b "HepMC_asciiv3", \b "HepMC_treeroot" - HepMC3 human-readble asciiv3 or
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:1370:   * \n The output is available in Oscar1999, Oscar2013, ASCII, Oscar2013_bin
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:1371:   * and ROOT format, as well as in an additional "For_vHLLE" format. The latter
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:1507:      const bool oscar2013_bin_requested = formats_contains("Oscar2013_bin");
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/experiment.h:1537:               "same as those of the requested \"Oscar2013_bin\"\nformat for "
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:222:    auto bin_output = create_binary_output("Oscar2013_bin", "Collisions",
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:308:    auto bin_output = create_binary_output("Oscar2013_bin", "Particles",
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:408:    auto bin_output = create_binary_output("Oscar2013_bin", "Collisions",
/Users/jinlei/.cache/fusion/smash/smash/src/tests/binaryoutput.cc:500:        "Oscar2013_bin", "Initial_Conditions", testoutputpath, output_par);
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/outputparameters.h:336:  inline static const std::vector<std::string> ic_For_vHLLE = {
/Users/jinlei/.cache/fusion/smash/smash/src/include/smash/icoutput.h:28: * codes ("For_vHLLE"). Formatted such that it can be directly processed by

exec
/bin/zsh -lc "sed -n '1230,1315p' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/experiment.h"
sed -n '"'720,870p' \""'$HOME/.cache/fusion/smash/smash/src/include/smash/experiment.h"' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
   *
   * ---
   *
   * \section list_of_output_formats Output formats
   *
   * Every output content can be printed out in several formats:
   *
   * - \b "ASCII" - a human-readable text-format table of values.
   *   - For\n
   *     &emsp;&emsp;`"Particles"` (\ref doxypage_output_oscar_particles),\n
   *     &emsp;&emsp;`"Collisions"`, `"Dileptons"`, and `"Photons"` (\ref
   *     doxypage_output_oscar_collisions)\n contents, it uses the \ref
   *     doxypage_output_oscar "OSCAR block structure".\n In these cases it is
   *     possible to customize the quantities to be printed into the output file
   *     (\ref doxypage_output_ascii).
   *   - For `"Initial_Conditions"` content the output has \ref
   *     doxypage_output_initial_conditions "a fixed block structure".
   *   - For `"Thermodynamics"` content the information stored in the output
   *     file depends on few input keys. Furthermore,
   *      - using \b "ASCII" as format, the \ref doxypage_output_thermodyn
   *        "standard thermodynamics output" is produced;
   *      - using \b "Lattice_ASCII", the \ref doxypage_output_thermodyn_lattice
   *        "quantities on a lattice" are printed out.
   * - \b "Binary" - a binary, not human-readable list of values.
   *   - The \ref doxypage_output_binary "binary output" is faster to read and
   *     write than text outputs and all floating point numbers are printed with
   *     their full precision.
   *   - For `"Particles"`, `"Collisions"`, `"Dileptons"`, `"Photons"`, and
   *     `"Initial_Conditions"` contents, it is a binary version equivalent to
   *     the corresponding ASCII output.\n Also for binary format it is possible
   *     to customize the quantities to be printed into the file.
   *   - For the other contents the corresponding documentation pages about the
   *     ASCII format contain further information.
   * - \b "Oscar1999", \b "Oscar2013" - aliases for the \b "ASCII" format with a
   *   predefined set of quantities.
   * - \b "Oscar2013_bin" - alias for the \b "Binary" format with a predefined
   *   set of quantities.
   * - \b "For_vHLLE" - an alias for the \b "ASCII" format exclusive to the
   *   `"Initial_Conditions"` output content, which produces a file compatible
   *   with the vHLLE hydrodynamic evolution code (see \ref
   *   doxypage_output_initial_conditions). This is only available for
   *   <tt>\ref key_MC_IC_type_ "Constant_Tau"</tt> fluidizations.
   * - \b "Root" - binary output in the format used by
   *   <a href="http://root.cern.ch">the ROOT software</a>
   *   - Even faster to read and write, requires less disk space
   *   - Format description: \ref doxypage_output_root
   * - \b "VTK" - text output suitable for an easy visualization using
   *   third-party software
   *   - There are many different programs that can open a VTK file, although
   *     their functionality varies.
   *   - This output can be for example visualized with
   *     <a href="http://paraview.org/">Paraview</a>. Alternatives are e.g.
   *     <a href=https://docs.enthought.com/mayavi/mayavi/data.html>Mayavi</a>
   *     or <a
   *     href=https://reference.wolfram.com/language/ref/format/VTK.html>Mathematica</a>.
   *   - For "Particles" content \ref doxypage_output_vtk
   *   - For "Thermodynamics" content \ref doxypage_output_vtk_lattice
   * - \b "HepMC_asciiv3", \b "HepMC_treeroot" - HepMC3 human-readble asciiv3 or
   *   Tree ROOT format see \ref doxypage_output_hepmc for details
   * - \b "YODA", \b "YODA-full" - compact ASCII text format used by the
   *   Rivet output, see \ref doxypage_output_rivet for details
   *
   * \note Output of coordinates for the "Collisions" content in
   *       the periodic box has a feature:
   *       \ref doxypage_output_collisions_box_modus
   */

  /*!\Userguide
   * \page doxypage_output_dileptons
   * The existence of a dilepton subsection in the collision term section of the
   * configuration file enables the dilepton production. In addition, the
   * dilepton output also needs to be enabled in the output section and dilepton
   * decays have to be uncommented in the used decaymodes.txt file. The output
   * file named \a Dileptons (followed by the appropriate suffix) is generated
   * when SMASH is executed. It's format is identical to the collision output
   * (see \ref doxypage_output_oscar_collisions), it does however only contain
   * information about the dilepton decays. \n Further, the block headers differ
   * from the usual collision output: <div class="fragment"> <div class="line">
   * <span class="preprocessor">
   *  \# interaction in nin out nout rho density weight shining_weight partial
   *  part_weight type proc_type </span></div>
   * </div>
   * where \li \key nin: Number of ingoing
   * particles (initial state particles) \li \key nout: Number of outgoing
   * particles (finalstate particles) \li \key density: Density at the
   * interaction point \li \key shining_weight: Shining weight of the

/// Creates a verbose textual description of the setup of the Experiment.
template <typename Modus>
std::ostream &operator<<(std::ostream &out, const Experiment<Modus> &e) {
  out << "End time: " << e.end_time_ << " fm\n";
  out << e.modus_;
  return out;
}

template <typename Modus>
void Experiment<Modus>::create_output(const std::string &format,
                                      const std::string &content,
                                      const std::filesystem::path &output_path,
                                      const OutputParameters &out_par) {
  // Disable output which do not properly work with multiple ensembles
  if (ensembles_.size() > 1) {
    auto abort_because_of = [](const std::string &s) {
      throw std::invalid_argument(
          s + " output is not available with multiple parallel ensembles.");
    };
    if (content == "Initial_Conditions") {
      abort_because_of("Initial_Conditions");
    }
    if ((format == "HepMC") || (format == "HepMC_asciiv3") ||
        (format == "HepMC_treeroot")) {
      abort_because_of("HepMC");
    }
    if (content == "Rivet") {
      abort_because_of("Rivet");
    }
    if (content == "Collisions") {
      logg[LExperiment].warn(
          "Information coming from different ensembles in 'Collisions' output "
          "is not distinguishable.\nSuch an output with multiple parallel "
          "ensembles should only be used if later in the data analysis\nit is "
          "not necessary to trace back which data belongs to which ensemble.");
    }
  }

  if (format == "VTK" && content == "Particles") {
    outputs_.emplace_back(
        std::make_unique<VtkOutput>(output_path, content, out_par));
  } else if (format == "Root") {
#ifdef SMASH_USE_ROOT
    if (content == "Initial_Conditions") {
      outputs_.emplace_back(
          std::make_unique<RootOutput>(output_path, "SMASH_IC", out_par));
    } else {
      outputs_.emplace_back(
          std::make_unique<RootOutput>(output_path, content, out_par));
    }
#else
    logg[LExperiment].error(
        "Root output requested, but Root support not compiled in");
#endif
  } else if ((format == "Binary" || format == "Oscar2013_bin") &&
             (content == "Collisions" || content == "Particles" ||
              content == "Dileptons" || content == "Photons" ||
              content == "Initial_Conditions")) {
    outputs_.emplace_back(
        create_binary_output(format, content, output_path, out_par));
  } else if (format == "Oscar1999" || format == "Oscar2013") {
    outputs_.emplace_back(
        create_oscar_output(format, content, output_path, out_par));
  } else if (format == "ASCII" &&
             (content == "Particles" || content == "Collisions" ||
              content == "Dileptons" || content == "Photons" ||
              content == "Initial_Conditions")) {
    outputs_.emplace_back(
        create_oscar_output(format, content, output_path, out_par));
  } else if (content == "Thermodynamics" && format == "ASCII") {
    outputs_.emplace_back(
        std::make_unique<ThermodynamicOutput>(output_path, content, out_par));
  } else if (content == "Thermodynamics" &&
             (format == "Lattice_ASCII" || format == "Lattice_Binary")) {
    printout_full_lattice_any_td_ = true;
    outputs_.emplace_back(std::make_unique<ThermodynamicLatticeOutput>(
        output_path, content, out_par, format == "Lattice_ASCII",
        format == "Lattice_Binary"));
  } else if (content == "Thermodynamics" && format == "VTK") {
    printout_lattice_td_ = true;
    outputs_.emplace_back(
        std::make_unique<VtkOutput>(output_path, content, out_par));
  } else if (content == "Initial_Conditions" && format == "For_vHLLE") {
    if (IC_dynamic_) {
      throw std::invalid_argument(
          "Dynamic initial conditions are only available in Oscar2013 and "
          "Binary formats.");
    }
    outputs_.emplace_back(
        std::make_unique<ICOutput>(output_path, "SMASH_IC_For_vHLLE", out_par));
  } else if ((format == "HepMC") || (format == "HepMC_asciiv3") ||
             (format == "HepMC_treeroot")) {
#ifdef SMASH_USE_HEPMC
    if (content == "Particles") {
      if ((format == "HepMC") || (format == "HepMC_asciiv3")) {
        outputs_.emplace_back(std::make_unique<HepMcOutput>(
            output_path, "SMASH_HepMC_particles", false, "asciiv3"));
      } else if (format == "HepMC_treeroot") {
#ifdef SMASH_USE_HEPMC_ROOTIO
        outputs_.emplace_back(std::make_unique<HepMcOutput>(
            output_path, "SMASH_HepMC_particles", false, "root"));
#else
        logg[LExperiment].error(
            "Requested HepMC_treeroot output not available, "
            "ROOT or HepMC3_ROOTIO missing or not found by cmake.");
#endif
      }
    } else if (content == "Collisions") {
      if ((format == "HepMC") || (format == "HepMC_asciiv3")) {
        outputs_.emplace_back(std::make_unique<HepMcOutput>(
            output_path, "SMASH_HepMC_collisions", true, "asciiv3"));
      } else if (format == "HepMC_treeroot") {
#ifdef SMASH_USE_HEPMC_ROOTIO
        outputs_.emplace_back(std::make_unique<HepMcOutput>(
            output_path, "SMASH_HepMC_collisions", true, "root"));
#else
        logg[LExperiment].error(
            "Requested HepMC_treeroot output not available, "
            "ROOT or HepMC3_ROOTIO missing or not found by cmake.");
#endif
      }
    } else {
      logg[LExperiment].error(
          "HepMC only available for Particles and "
          "Collisions content. Requested for " +
          content + ".");
    }
#else
    logg[LExperiment].error(
        "HepMC output requested, but HepMC support not compiled in");
#endif
  } else if (content == "Coulomb" && format == "VTK") {
    outputs_.emplace_back(
        std::make_unique<VtkOutput>(output_path, "Fields", out_par));
  } else if (content == "Rivet") {
#ifdef SMASH_USE_RIVET
    // flag to ensure that the Rivet format has not been already assigned
    static bool rivet_format_already_selected = false;
    // if the next check is true, then we are trying to assign the format twice
    if (rivet_format_already_selected) {
      logg[LExperiment].warn(
          "Rivet output format can only be one, either YODA or YODA-full. "
          "Only your first valid choice will be used.");
      return;
    }
    if (format == "YODA") {
      outputs_.emplace_back(std::make_unique<RivetOutput>(
          output_path, "SMASH_Rivet", false, out_par.rivet_parameters));
      rivet_format_already_selected = true;
    } else if (format == "YODA-full") {

exec
/bin/zsh -lc "rg -n 'collisions_custom|collisions_oscar2013|SMASH\\.log|log file|filename' \""'$HOME/.cache/fusion/smash/smash/src/binaryoutput.cc" "$HOME/.cache/fusion/smash/smash/src/include/smash/outputparameters.h" "$HOME/.cache/fusion/smash/smash/src/logging.cc" | head -100
find /private/tmp/smash-box-audit.0MZV9B/out -maxdepth 1 -type f -print' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:26:static auto get_binary_filename(const std::string &content,
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:28:  std::string filename = content;
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:30:    std::transform(filename.begin(), filename.end(), filename.begin(),
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:35:    filename = "SMASH_IC";
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:38:        "Unknown content to get the binary output filename.");
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:41:    filename += "_oscar2013";
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:43:    filename += "_oscar2013_extended";
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:45:    filename += "_custom";
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:47:  return filename + ".bin";
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:182: *  \li \c collisions_custom.bin &rarr; this is the default;
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:183: *  \li \c collisions_oscar2013.bin &rarr;
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:185: *  \li \c collisions_oscar2013_extended.bin &rarr;
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:266:    : BinaryOutputBase(path / get_binary_filename(name, quantities), "wb", name,
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:328:    : BinaryOutputBase(path / get_binary_filename(name, quantities), "wb", name,
/Users/jinlei/.cache/fusion/smash/smash/src/binaryoutput.cc:388:    : BinaryOutputBase(path / get_binary_filename(name, quantities), "wb", name,
/private/tmp/smash-box-audit.0MZV9B/out/particle_lists.oscar
/private/tmp/smash-box-audit.0MZV9B/out/config.yaml

exec
/bin/zsh -lc "for x in 9223372036854775807 9223372036854775808 999999999999999999999999999 -9223372036854775809; do bash -c 'x=\""'$1"; if [ "$x" -lt 0 ]; then echo "$x negative"; else echo "$x nonnegative"; fi'"' _ \""'$x"; echo rc=$?; done' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
9223372036854775807 nonnegative
rc=0
_: line 0: [: 9223372036854775808: integer expression expected
9223372036854775808 nonnegative
rc=0
_: line 0: [: 999999999999999999999999999: integer expression expected
999999999999999999999999999 nonnegative
rc=0
_: line 0: [: -9223372036854775809: integer expression expected
-9223372036854775809 nonnegative
rc=0

exec
/bin/zsh -lc "rg -n \"file\\((GLOB|GLOB_RECURSE)|aux_source_directory|CONFIGURE_DEPENDS\" \""'$HOME/.cache/fusion/smash/smash" --glob '"'CMakeLists.txt' --glob '*.cmake' | head -120" in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
/Users/jinlei/.cache/fusion/smash/smash/src/tests/CMakeLists.txt:125:        file(GLOB CPPLINT_FILES ${PROJECT_SOURCE_DIR}/src/include/smash/*.h
/Users/jinlei/.cache/fusion/smash/smash/src/tests/CMakeLists.txt:157:        file(GLOB_RECURSE CPPLINT_FILES_FULL ${PROJECT_SOURCE_DIR}/src/include/smash/*.h
/Users/jinlei/.cache/fusion/smash/smash/src/tests/CMakeLists.txt:236:    file(GLOB CLANGTIDY_FILES ${PROJECT_SOURCE_DIR}/src/*.cc)
/Users/jinlei/.cache/fusion/smash/smash/cmake/FindSMASH.cmake:104:        file(GLOB smash_folders
/Users/jinlei/.cache/fusion/smash/smash/3rdparty/yaml-cpp-0.8.0/CMakeLists.txt:59:  list(INSERT contrib-pattern 0 CONFIGURE_DEPENDS)
/Users/jinlei/.cache/fusion/smash/smash/3rdparty/yaml-cpp-0.8.0/CMakeLists.txt:60:  list(INSERT src-pattern 0 CONFIGURE_DEPENDS)
/Users/jinlei/.cache/fusion/smash/smash/3rdparty/yaml-cpp-0.8.0/CMakeLists.txt:63:file(GLOB yaml-cpp-contrib-sources ${contrib-pattern})
/Users/jinlei/.cache/fusion/smash/smash/3rdparty/yaml-cpp-0.8.0/CMakeLists.txt:64:file(GLOB yaml-cpp-sources ${src-pattern})

exec
/bin/zsh -lc 'python3 /Users/jinlei/Desktop/claude_skills/skills/skill-iterate/scripts/cc_skill_test.py skills/smash' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:

[1m=== Memento-Skills Verification Report ===[0m

[1m[smash][0m [92mPASS[0m
  [92mPASS[0m [structure] SKILL.md exists
  [92mPASS[0m [structure] Name: smash
  [92mPASS[0m [structure] Description: Drive SMASH (Simulating Many Accelerated Strongly-interacting Hadrons), the hadr...
  [92mPASS[0m [structure] SKILL.md body: 138 lines
  [92mPASS[0m [scripts] scripts/check_conservation_smash.py: syntax OK
  [93mWARN[0m [references] Referenced file not found: config_used.yaml
  [92mPASS[0m [description] Description contains trigger context
  [93mWARN[0m [description] Description is 104 words - may be too verbose
  [92mPASS[0m [style] Has no-dash output rule for generated content
  [92mPASS[0m [academic] Academic skill [smash]: rag
  [93mWARN[0m [academic] RAG skill lacks API endpoint configuration
  [93mWARN[0m [academic] RAG skill lacks workspace definitions
  [93mWARN[0m [evals] No evals/evals.json - skill has no test cases
  Summary: 8/13 passed, 0 failed, 5 warnings

[1mOverall: 1/1 skills passed[0m

exec
/bin/zsh -lc 'shasum -a 256 "$HOME/.cache/fusion/smash/smash/input/box/particles.txt" /private/tmp/smash-box-audit.0MZV9B/particles_used.txt "$HOME/.cache/fusion/smash/smash/input/box/decaymodes.txt" /private/tmp/smash-box-audit.0MZV9B/decaymodes_used.txt
rg -n '"'"'^'"\\s*(Only_Final|Nevents|Ensembles|Output_Interval):' /private/tmp/smash-box-audit.0MZV9B/config_used.yaml" in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
80168208c64a6a5aa6ec2e608b4d27951a3ffea2dd45479f688f3b4707ba1fe8  /Users/jinlei/.cache/fusion/smash/smash/input/box/particles.txt
80168208c64a6a5aa6ec2e608b4d27951a3ffea2dd45479f688f3b4707ba1fe8  /private/tmp/smash-box-audit.0MZV9B/particles_used.txt
c81223a9f44aa2cae4617fe51fcd566490ae687e0c0a0461d5578b4eeddc4abc  /Users/jinlei/.cache/fusion/smash/smash/input/box/decaymodes.txt
c81223a9f44aa2cae4617fe51fcd566490ae687e0c0a0461d5578b4eeddc4abc  /private/tmp/smash-box-audit.0MZV9B/decaymodes_used.txt
10:    Nevents:       1
13:    Output_Interval: 20.0
16:        Only_Final:      No

exec
/bin/zsh -lc 'rg -n "ctest|Timeout|No tests were found|SMASH_EXPECTED_TESTS|auto-tables|particles_used|decaymodes_used|Only_Final|ensemble [1-9]| out [0-9]+" skills/smash/scripts/selftest_smash.sh' in /Users/jinlei/Desktop/code/FUSION
 succeeded in 0ms:
55:    echo '# event 0 ensemble 0 out 4'
61:    echo '# event 1 ensemble 0 out 3'
248:# event 0 ensemble 0 out 1
251:# event 1 ensemble 0 out 1
262:print('# event 0 ensemble 0 out 2')
275:print('# event 0 ensemble 0 out 2')
279:print('# event 1 ensemble 0 out 0')
295:print('# event 0 ensemble 0 out 5')

Completed the supplied 49-case self-test, source-level checks against SMASH 3.3, a 35-code baryon cross-check, and a real Box-mode output probe. No repository files were changed.
  ✓ Inspect HEAD, SMASH skill files, and the earlier-finding guard changes
  ✓ Run selftest_smash.sh and targeted negative/legitimate-output probes
  ✓ Classify all 19 findings and report new defects concisely
codex
Re-verification fails overall: 11 FIXED, 7 PARTIAL, 1 NOT FIXED. The SMASH tree at current HEAD `04c202c6` is identical to requested `842cbabd5`.

`selftest_smash.sh`: **49 passed, 0 failed**. Repository files were untouched.

## Part A

| # | Verdict | Evidence |
|---|---|---|
| 1 | FIXED | Self-test rejects `-2` and `-999`; seed `0` passes. |
| 2 | FIXED | Initial `ctest` status is captured and a nonzero status with zero reported failures is fatal. |
| 3 | FIXED | All parenthesized failure types are parsed; parsed count must equal `NFAIL`, otherwise verification fails closed. |
| 4 | FIXED | Retry requires exit 0, exactly one selected test, and zero failures. |
| 5 | PARTIAL | Preset variables no longer bypass all checks, but the new identity check is spoofable and rejects a legitimate cached build. See Part B. |
| 6 | PARTIAL | A non-104 override prints “NOT tier-1”, but can still finish with `VERIFY OK` and exit 0 when the reported total matches the override. |
| 7 | PARTIAL | Bad header, unequal marker counts, and wrong column counts fail. Declared block counts, marker labels, and record placement remain unchecked. |
| 8 | FIXED | Rewritten rule matches SMASH source on 35 tested PDG codes, including five to eight-digit resonances, anti-resonances, nuclei, and hypertriton. |
| 9 | FIXED | An arbitrary comment containing `end` is rejected by the checker. |
| 10 | FIXED | Cancelling violations across two final-state events fail per-event conservation. |
| 11 | FIXED | Real Box run auto-selected the correct tables; staged files were SHA-256 identical to the source tables. |
| 12 | NOT FIXED | [run_smash.sh](/Users/jinlei/Desktop/code/FUSION/skills/smash/scripts/run_smash.sh:169) still hard-requires `out/particle_lists.oscar`; binary-only and Root configurations fail. |
| 13 | PARTIAL | Self-test grew to 49 cases, but does not exercise the initial-ctest-status, mixed-status, no-tests retry, expected-count override, or auto-table guards. |
| 14 | FIXED | Documentation now identifies `scatteraction.cc` and `dynamic_fluidization.cc` in addition to `potentials.cc` and `random.cc`. |
| 15 | PARTIAL | `Root` and most formats are corrected, but accepted `HepMC`, `YODA`, and `YODA-full` remain omitted. |
| 16 | FIXED | Documentation now uses `collisions_custom.bin` and `collisions_oscar2013.bin`, and correctly says SMASH has no `SMASH.log`; the wrapper captures stdout. |
| 17 | PARTIAL | `ensemble` and modern `out/end` grammar are corrected, but the legitimate `in` block and multiple `out` blocks remain omitted. |
| 18 | PARTIAL | Earlier garbage forms are rejected, but valid numeric spellings such as `.5`, `5.`, and `1e-3` are rejected, while out-of-range seed integers bypass the sign comparison. |
| 19 | FIXED | `--species-min` defaults off, and B/Q are explicitly described as limited bookkeeping invariants rather than physics validation. |

## Part B

1. **BLOCKER: identity checking both breaks legitimate builds and is spoofable.**  
   The supplied clean, pinned build is rejected: stamp hash `aa165d...`, binary hash `bd0101...`. The stamp timestamp is 16:10:18 and the binary is 16:10:39. SMASH’s `usage_of_SMASH_as_library` test reruns CMake and `make install`, legitimately relinking `build/smash` after the installer stamped it. A second verification therefore fails before testing.  
   Spoofing is also straightforward: [verify_smash.sh](/Users/jinlei/Desktop/code/FUSION/skills/smash/scripts/verify_smash.sh:81) ignores stamp line 1, does not bind `BUILD` to `SRCROOT` through `CMakeCache.txt`, and trusts a user-writable hash on line 2. Any executable placed textually below `BUILD`, with its own hash written into the stamp, passes identity against an unrelated clean source clone.

2. **BLOCKER: real `Only_Final: No` output is unsupported.**  
   A real Box run produced:
   `in 247`, `out 255`, `out 259`, `end`.  
   `run_smash.sh` rejects it because two `out` blocks do not equal one `end`. [check_conservation_smash.py](/Users/jinlei/Desktop/code/FUSION/skills/smash/scripts/check_conservation_smash.py:124) ignores `in`, treats the next `out` as opening an event, then fails on the second `out` with “event starts while still open”. Even a run with only one final `out` leaves all `in` particles as stray records.

3. **HIGH: parallel ensembles are rejected.**  
   SMASH writes one `out/end` pair per ensemble, but the wrapper compares the number of end markers directly with `Nevents`. For `Nevents: 1, Ensembles: 20`, a legitimate 20-block output is reported as 20 events instead of one event with 20 ensembles.

4. **HIGH: the “strict” structure check remains forgeable and rejects valid empty output.**  
   It compares only total `out` and `end` counts, not matching `(event, ensemble)` labels. It ignores each `out COUNT`, allows records outside blocks, and checks only the `#!OSCAR2013` prefix rather than `particle_lists`. Conversely, a legitimate zero-particle event is rejected by the unconditional `NPART > 0` requirement.

5. **MEDIUM: table auto-detection worked for the shipped Box case, but can silently choose unrelated tables.**  
   Selection is based solely on same-directory filenames, not configuration intent. Explicit relative `--particles` or `--decaymodes` paths are worse: they are validated before execution, then SMASH runs after `cd "$CFGDIR"`, so those relative paths can resolve somewhere different. Documentation also claims the sphere example has local tables, but the SMASH 3.3 sphere directory has none.

6. **MEDIUM: ctest certification semantics remain ambiguous.**  
   `SMASH_EXPECTED_TESTS=103` can turn a 103-case run into exit-0 `VERIFY OK`; only an stderr note says it is not tier 1. Also, a nonzero initial ctest status is ignored whenever `NFAIL > 0`, even if that status includes an additional ctest-level error beyond the retried flake.

7. **Baryon rewrite: no defect found for valid SMASH PDG output.**  
   The implementation agrees with `PdgCode::baryon_number()` for all 35 cases tested, including `12112`, `202112`, `19903129`, anti-resonances, deuteron, anti-triton, He-4, and hypertriton. The self-test itself covers only two resonances, not the broader set claimed in the documentation.
tokens used
345,090
Re-verification fails overall: 11 FIXED, 7 PARTIAL, 1 NOT FIXED. The SMASH tree at current HEAD `04c202c6` is identical to requested `842cbabd5`.

`selftest_smash.sh`: **49 passed, 0 failed**. Repository files were untouched.

## Part A

| # | Verdict | Evidence |
|---|---|---|
| 1 | FIXED | Self-test rejects `-2` and `-999`; seed `0` passes. |
| 2 | FIXED | Initial `ctest` status is captured and a nonzero status with zero reported failures is fatal. |
| 3 | FIXED | All parenthesized failure types are parsed; parsed count must equal `NFAIL`, otherwise verification fails closed. |
| 4 | FIXED | Retry requires exit 0, exactly one selected test, and zero failures. |
| 5 | PARTIAL | Preset variables no longer bypass all checks, but the new identity check is spoofable and rejects a legitimate cached build. See Part B. |
| 6 | PARTIAL | A non-104 override prints “NOT tier-1”, but can still finish with `VERIFY OK` and exit 0 when the reported total matches the override. |
| 7 | PARTIAL | Bad header, unequal marker counts, and wrong column counts fail. Declared block counts, marker labels, and record placement remain unchecked. |
| 8 | FIXED | Rewritten rule matches SMASH source on 35 tested PDG codes, including five to eight-digit resonances, anti-resonances, nuclei, and hypertriton. |
| 9 | FIXED | An arbitrary comment containing `end` is rejected by the checker. |
| 10 | FIXED | Cancelling violations across two final-state events fail per-event conservation. |
| 11 | FIXED | Real Box run auto-selected the correct tables; staged files were SHA-256 identical to the source tables. |
| 12 | NOT FIXED | [run_smash.sh](/Users/jinlei/Desktop/code/FUSION/skills/smash/scripts/run_smash.sh:169) still hard-requires `out/particle_lists.oscar`; binary-only and Root configurations fail. |
| 13 | PARTIAL | Self-test grew to 49 cases, but does not exercise the initial-ctest-status, mixed-status, no-tests retry, expected-count override, or auto-table guards. |
| 14 | FIXED | Documentation now identifies `scatteraction.cc` and `dynamic_fluidization.cc` in addition to `potentials.cc` and `random.cc`. |
| 15 | PARTIAL | `Root` and most formats are corrected, but accepted `HepMC`, `YODA`, and `YODA-full` remain omitted. |
| 16 | FIXED | Documentation now uses `collisions_custom.bin` and `collisions_oscar2013.bin`, and correctly says SMASH has no `SMASH.log`; the wrapper captures stdout. |
| 17 | PARTIAL | `ensemble` and modern `out/end` grammar are corrected, but the legitimate `in` block and multiple `out` blocks remain omitted. |
| 18 | PARTIAL | Earlier garbage forms are rejected, but valid numeric spellings such as `.5`, `5.`, and `1e-3` are rejected, while out-of-range seed integers bypass the sign comparison. |
| 19 | FIXED | `--species-min` defaults off, and B/Q are explicitly described as limited bookkeeping invariants rather than physics validation. |

## Part B

1. **BLOCKER: identity checking both breaks legitimate builds and is spoofable.**  
   The supplied clean, pinned build is rejected: stamp hash `aa165d...`, binary hash `bd0101...`. The stamp timestamp is 16:10:18 and the binary is 16:10:39. SMASH’s `usage_of_SMASH_as_library` test reruns CMake and `make install`, legitimately relinking `build/smash` after the installer stamped it. A second verification therefore fails before testing.  
   Spoofing is also straightforward: [verify_smash.sh](/Users/jinlei/Desktop/code/FUSION/skills/smash/scripts/verify_smash.sh:81) ignores stamp line 1, does not bind `BUILD` to `SRCROOT` through `CMakeCache.txt`, and trusts a user-writable hash on line 2. Any executable placed textually below `BUILD`, with its own hash written into the stamp, passes identity against an unrelated clean source clone.

2. **BLOCKER: real `Only_Final: No` output is unsupported.**  
   A real Box run produced:
   `in 247`, `out 255`, `out 259`, `end`.  
   `run_smash.sh` rejects it because two `out` blocks do not equal one `end`. [check_conservation_smash.py](/Users/jinlei/Desktop/code/FUSION/skills/smash/scripts/check_conservation_smash.py:124) ignores `in`, treats the next `out` as opening an event, then fails on the second `out` with “event starts while still open”. Even a run with only one final `out` leaves all `in` particles as stray records.

3. **HIGH: parallel ensembles are rejected.**  
   SMASH writes one `out/end` pair per ensemble, but the wrapper compares the number of end markers directly with `Nevents`. For `Nevents: 1, Ensembles: 20`, a legitimate 20-block output is reported as 20 events instead of one event with 20 ensembles.

4. **HIGH: the “strict” structure check remains forgeable and rejects valid empty output.**  
   It compares only total `out` and `end` counts, not matching `(event, ensemble)` labels. It ignores each `out COUNT`, allows records outside blocks, and checks only the `#!OSCAR2013` prefix rather than `particle_lists`. Conversely, a legitimate zero-particle event is rejected by the unconditional `NPART > 0` requirement.

5. **MEDIUM: table auto-detection worked for the shipped Box case, but can silently choose unrelated tables.**  
   Selection is based solely on same-directory filenames, not configuration intent. Explicit relative `--particles` or `--decaymodes` paths are worse: they are validated before execution, then SMASH runs after `cd "$CFGDIR"`, so those relative paths can resolve somewhere different. Documentation also claims the sphere example has local tables, but the SMASH 3.3 sphere directory has none.

6. **MEDIUM: ctest certification semantics remain ambiguous.**  
   `SMASH_EXPECTED_TESTS=103` can turn a 103-case run into exit-0 `VERIFY OK`; only an stderr note says it is not tier 1. Also, a nonzero initial ctest status is ignored whenever `NFAIL > 0`, even if that status includes an additional ctest-level error beyond the retried flake.

7. **Baryon rewrite: no defect found for valid SMASH PDG output.**  
   The implementation agrees with `PdgCode::baryon_number()` for all 35 cases tested, including `12112`, `202112`, `19903129`, anti-resonances, deuteron, anti-triton, He-4, and hypertriton. The self-test itself covers only two resonances, not the broader set claimed in the documentation.
